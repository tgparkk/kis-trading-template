# -*- coding: utf-8 -*-
"""★ 태쏘 「후보 선정 알파 검정」 — 동결 상수 (게이트 C 의 해시 대상).

================================================================================
이 파일의 지위
================================================================================

**게이트 C 의 해시 대상 = 이 파일 전체 바이트의 sha256 이다.**

정확히는 `tests/test_frozen_constants.py::normalized_bytes()` 가 정의하는
정규화 바이트열(줄바꿈을 LF 로 통일한 UTF-8 바이트)의 sha256 이며, 그 함수가
정규화 규약의 SSOT 다. 실행 시 이 값을 **동결 커밋의 값과 대조**하고,
불일치하면 중단한다 (`PREREG.md` §6-C).

🔴 **이 파일 밖에 결과영향 상수를 두지 말 것.**
   `tests/test_frozen_constants.py` 가 `backtest/tasso_labels/` 트리 전역을
   AST 로 훑어 **시드 리터럴과 정규식 리터럴을 차단**한다. 본실행 코드
   (`run_selection.py` 등)에서 상수를 인라인하면 그 테스트가 FAIL 한다.
   값이 필요하면 **여기서 import** 할 것.

🔴 **모듈 레벨 `UPPER_SNAKE_CASE` 상수만 둔다.** 함수·클래스·실행 코드·import
   금지 — 순수 데이터 모듈이다. (이 규약도 위 테스트가 강제한다.)

🔴 **인코딩은 UTF-8, BOM 없음.** BOM 이 붙으면 `ast.parse` 가 SyntaxError 를 내고,
   그러면 검사기가 **조용히 0건**이 되어 무력화된다 (`test_adj_factor_no_arithmetic.py`
   가 실측한 사고 — `p5_nhb_optimization.py`). 테스트가 BOM 부재를 단언한다.

================================================================================
왜 파일 단위 해시인가 — 열거식 해시가 무엇을 놓쳤는가 (C6)
================================================================================

`PREREG.md` 2판 §6-C 는 해시 대상을 *"§5.2 의 층 정의 **및** §2.3-(c) 정규식 원본"*
으로 **열거**했다. `CLOSING.md` §5-C6 이 잡은 결함:

  | 누락 | 왜 문제인가 |
  |---|---|
  | **난수 시드 숫자** | §4.1·§4.3 이 *"시드 고정"* 이라고만 적고 **숫자를 안 적었다**.
  |                    | 대조군 추출 결과가 바뀌는데 해시가 안 잡는다 |
  | **`N` 파서 정규식** | §2.3-(f)·§4.1-(d) 가 파싱 결과(276 성공/52 실패)만 싣고
  |                    | **파서 자체는 문서에 없다**. T3/T4 풀 정의 전체가 여기 달렸다 |

⚠️ 이것은 §13.2-1 이 **스스로 세운 규칙**(*정규식을 손으로 옮겨 적으면 조용히 다른
것을 검사한다*)이 **같은 문서 안에서 재발**한 것이다 — 유형 판정 정규식은 붙여넣고
해시에 넣었는데, 룩백 파서는 그러지 않았다.

⇒ 교정은 **열거를 더 길게 쓰는 것이 아니라 열거를 그만두는 것**이다.
   같은 클래스의 선례가 이 저장소에 있다 — 커밋 `6c4cffc` 에서 `adj_factor` 오용
   가드 3본이 전부 열거식이라 **셋 다 같은 파일을 놓쳤고**, 트리 전역 AST 검사
   (`RoboTrader_template/tests/test_adj_factor_no_arithmetic.py`)로 교체해서 잡았다.
   이 파일 + 그 테스트는 그 설계를 승계한 것이다.

================================================================================
🔴 미해결 — 동결 차단 조건
================================================================================

아래 플래그가 `False` 인 동안 **이 상수 집합은 동결될 수 없다.**
값이 채워져 있다는 것과 값이 확인됐다는 것은 다른 명제다
(§13.2-6: *기본값 대입은 「채워졌다」가 아니라 「틀린 것으로 채워졌다」일 수 있다*).

  - 🔴 ① `COST_SOURCE_VERIFIED = False` — `PREREG.md` §0-d / §3.3.
        연도별 거래세율의 **원출처 미확인**. 절대 판정의 통과선이 이 값 위에 선다.
  - ✅ ② `N_LOOKBACK_PARSER_RESOLVED = True` — `CLOSING.md` §5-C6 누락 ②.
        원본을 찾아 실었고, `labels_v4.csv` 재실행으로 276/52 를 재현해 확인했다.
  - 🔴 ③ `D12_RESOLVED = False` — `PREREG_v3.md` §12.4 **D-12** (3판 C7 이 만든 결정).
        `C-B-matched-TV` 의 **매칭 방식**(격자 vs 캘리퍼)과 `ε` 가 미확정이다.
        🔴 §12.1 이 *"D-12 가 닫히기 전에는 PASS 조건 ③ 의 충족 가능성이 미확정"* 이라고
        적은 그 결정이며, 5분위 격자로 가면 게이트 B ⑥ 에서 **차단되어 ③ 이 불성립**한다.

🔑 **왜 이 목록에 D-12 를 넣었는가 — 이 목록 자신이 열거식 가드였다.**
   3판은 §13.2-12 로 *「열거식 가드는 반드시 빠뜨린다」* 를 규칙으로 세웠는데,
   **바로 그 판본의 동결 차단 목록이 D-12 를 빠뜨렸다**(①② 둘뿐이었다).
   그리고 하필 빠진 것이 **C7 이 새로 만든 결정**이었다.
   ⇒ §13.2-17(*자기가 세운 규칙으로 자기 문서를 훑어라*)의 **다섯 번째 사례**다.

`MATCH_R4_SATISFIED = False` 는 동결 차단 조건이 **아니다** — 시총 축 arm 을 조건부로
막는 스위치이며, False 인 채로 동결하는 것이 3판의 설계다(§4).
⚠️ 위 「차단 조건이 아니다」를 **`MATCH_R4_SATISFIED` 에 대해서만** 읽을 것.
   `D12_RESOLVED` 는 같은 bool 이지만 **차단 조건이다.**

⚠️ **2판에 근거 수치가 없어 v3 에서 처음 정한 값**은 추측이 아니라 「새로 정한 것」이므로
   관리자 확정 전까지 잠정으로 읽을 것:
   - `MASTER_SEED` · `SEED_CB` · `SEED_CB_MATCHED_TV` · `SEED_CA` · `SEED_SHUFFLE`
     (+ `ARM_BASE_SEEDS` / `ARM_SEED_OFFSETS` / `_K_MULTIPLIER` / `_DERIVATION_RULE`)
     — 값 자체에 근거가 없고, **있을 수도 없다**(시드는 임의여야 한다). 고정만이 요건.
   - `MATCH_VAR_AVAILABILITY_MIN = 0.95`
     — 값에 근거는 없으나 **결정이 이 값에 둔감함을 실측했다**(둔감 구간 폭 0.82).
       §4 주석 참조. ⚠️ 둔감성은 **현 데이터의 성질**이지 값의 정당화가 아니다.

================================================================================
🔧 3판에서 «뒤집힌» 절 — 2판을 인용해 되돌리지 말 것
================================================================================

  - **§4 매칭 축**: 2판의 `(log 시총, log 거래대금)` 5×5 **25셀** → 3판은
    **`log 거래대금` 단독 5분위 5셀**. `market_cap` 이 처치군 **82.51%** 에서
    결측이기 때문이다(C1). 셀 확장 축도 시총 → **거래대금**으로 뒤집혔다.
  - **§6 게이트 B ⑤⑥**: 2판은 둘을 같은 근거로 묶어 면제했으나, 3판은 분리한다 —
    ⑥ 은 「누출 우려로 안 하는 것」, ⑤ 는 「데이터가 없어 못 하는 것」(C4).
  - **§6 게이트 A**: 통과 기준에서 「셔플 95분위 vs 관측 Δ」가 **삭제**되고
    보고 항목으로 이동했다(C2). 남은 기준도 `평균|Δ|`(산포) → **`|평균 Δ|`(중심)** 으로
    재정의됐다(C2-b) — **표기가 거의 같고 뜻이 정반대**다. D-16.
  - 🔴 **§6 게이트 B ⑥**: 문턱이 「중앙값 **비** ∈ [0.90,1.10]」 → **로그차**
    `|Δ log tv| ≤ ln(1.10)` 로 재선언됐다(C7). 2판 해석은 **판별력 0**이라 폐기다. D-19.
  - **§5.1 연도 층**: 「필수 부수 판정」 → **부수 판정**으로 **강등**됐다.
    2판이 근거로 든 연도별 「손실률」 42.4/30.6/22.9/12.8% 가 **상폐율이 아니었다**
    (진짜 상폐 2.9/3.7/0.2/0.0%). ⇒ `STRATA["year"]["status"]` 를 2판 값으로 되돌리지 말 것.

================================================================================
출처 표기 규약
================================================================================

각 상수 위 주석의 `출처:` 는 **원본이 실재하는 위치**다.
`출처: (신규)` 는 2판에 값이 없어 **v3 에서 처음 확정한 것**이며, 그 사실 자체가
C6 이 지적한 누락 항목이라는 뜻이다.
"""

# ==============================================================================
# 1. 난수 시드
# ==============================================================================
# 🔴 2판은 §4.1·§4.3 에 "시드 고정" 이라고만 적고 **숫자를 안 적었다** (C6 누락 ①).
#    v3 에서 숫자를 확정한다.
# 출처: (신규) — CLOSING.md §5-C6 이 요구한 항목
MASTER_SEED = 20260803

# 🔴 **arm 별 기저 시드는 「명시적 상수」다 — 산술로 유도하지 않는다.**
#    `PREREG_v3.md` §6-C 계약이 요구하는 형태이며, 문서 §4.1·§4.3·§6-A 가
#    `frozen_constants.SEED_CB` · `SEED_CA` · `SEED_SHUFFLE` 을 **이름으로 지목**한다.
#
# ⚠️ **왜 「명시 상수 + 파생 규칙」의 이중 구조인가.**
#    `(k, rep)` 조합까지 전부 명시 상수로 두면 조합 폭발이라(창 3종 × 셔플 1,000회)
#    스트림 분기는 파생이 옳다. 그러나 **파생의 «기점»이 산술이면** 오프셋 표 한 줄만
#    바뀌어도 arm 전체의 난수열이 조용히 움직인다.
#    ⇒ **기점은 상수로 못 박고, 그 아래 스트림만 명시된 규칙으로 분기**한다.
#
# 🔑 **값은 `MASTER_SEED + ARM_SEED_OFFSETS[arm]` 과 같은 수로 잡았다** — 아래
#    `test_arm_seed_offsets_are_stable_across_rename` 가 그 항등식을 단언한다.
#    이유는 개명 규칙과 같다: **시드 체계가 흔들리면 「같은 설계인데 다른 대조군」이
#    되고, 그 사실은 산출물에 흔적을 안 남긴다.**
# 출처: (신규) — PREREG_v3.md §6-C 계약 · §4.1 · §4.3 · §6-A 가 이름으로 참조
#
# 🔧 **2026-08-04 교정으로 «세» 값이 움직였다** (간격 1,000 → 10,000,000).
#    ⚠️ **네 값이 아니다.** `SEED_CB` 는 `MASTER_SEED` 와 `ARM_SEED_OFFSETS["C-B-raw"] = 0`
#    이 둘 다 불변이므로 **원리적으로 움직일 수 없고**(항등식 `base == MASTER_SEED + offset`),
#    실제로 20260803 그대로다. 움직인 것은 `SEED_CB_MATCHED_TV`·`SEED_CA`·
#    `SEED_SHUFFLE` 셋이다. (아래 항등식 `base == MASTER_SEED + offset` 을 테스트가 단언한다.)
#    ⚠️ 시드를 바꾸는 것은 원래 금지에 가까운 변경이지만 **지금은 무해하다** —
#    §11 이 적은 대로 **본실행이 한 번도 돌지 않았고**(대조군 풀 구성 0건 · `run_selection.py`
#    미작성 · 결과변수 0건), 따라서 «다른 대조군이 나오는» 산출물이 존재하지 않는다.
#    🔴 **동결 커밋(0-g) 이후에는 이 변경이 금지된다.**
SEED_CB = 20260803             # C-B-raw          (주 판정, PREREG_v3.md §4.1)
SEED_CB_MATCHED_TV = 30260803  # C-B-matched-TV   (필수 동반 · 교정 arm, §4.1-g)
SEED_CA = 40260803             # C-A              (보조 · 팩터 귀속, §4.3)
SEED_SHUFFLE = 50260803        # 귀무 실측 짝 셔플 (§6-A)

# `(k, rep)` 스트림 분기 규칙. **기점은 위 명시 상수다.**
# 🔑 파생 「함수」를 여기 두지 않는다 — 이 파일은 순수 데이터 모듈이고, 함수를 두면
#    해시 대상 안에 실행 가능한 자유도가 생긴다. 규약은 **문자열**로 못 박고,
#    본실행 코드가 그 문자열대로 계산한다.
# 출처: (신규)
ARM_SEED_DERIVATION_RULE = (
    "arm_seed(arm, k, rep) = ARM_BASE_SEEDS[arm] "
    "+ ARM_SEED_K_MULTIPLIER * k "
    "+ rep"
    "  where k ∈ ARM_SEED_K_VALUES, "
    "rep ∈ [ARM_SEED_REP_BASE, ARM_SEED_MAX_REP]"
    "; k 항의 적용 범위 = ARM_SEED_K_SCOPE "
    "(대조군 추출은 k 무관 단일 추출 — 같은 (d, τ) 는 k=1/5/20 이 같은 대조군을 쓴다)"
)

# ==============================================================================
# 🔴 `(k, rep)` 의 **정의역** — 이것이 없어서 arm 시드 구간이 실제로 겹쳤다
# ==============================================================================
# **무엇이 틀렸나**: 위 파생 규칙은 `(k, rep)` 를 받는데, **`k` 와 `rep` 이 각각 어떤
# 값을 도는지가 이 파일 어디에도 없었다.** 정의역이 없으면 arm 의 «도달 시드 집합»을
# 계산할 수 없고, 계산할 수 없으면 **겹치는지도 확인할 수 없다.**
#
# 🔴 실제로 겹쳤다 (관리자 실측, 오프셋 간격이 1,000 · `K_MULTIPLIER` 가 31 이던 판본):
#       파생 폭 = ARM_SEED_K_MULTIPLIER(31) × max(k)(20) + max(rep) = 1,619
#       간격 1,000 < 1,619   ⇒  인접 arm 쌍마다 **589개 시드 충돌**
#         C-B-raw × C-B-matched-TV  ·  C-B-matched-TV × C-A  ·  C-A × gate_A_null_shuffle
#    ⚠️ 관리자 재현치는 **590** 이다 — `rep` 을 `[0, 1000]`(1,001개)로 잡았기 때문이며,
#       아래에서 `[0, 999]`(1,000개 = `GATE_A_SHUFFLE_REPS`)로 «선언» 하면 589 가 된다.
#       🔑 ***정의역이 선언돼 있지 않으면 결함의 크기조차 사람마다 다르게 나온다*** —
#          그 1건 차이가 정의역 미선언이 근본원인이라는 것의 작은 증거다.
#
# 🔴 **가장 나쁜 쌍은 `C-A × gate_A_null_shuffle` 이었다** — 귀무 실측 팔이 추정 팔과
#    **같은 난수열**을 쓸 수 있고, 위 :136-137 이 스스로 적어 둔 대로 **그 사실은
#    산출물에 흔적을 남기지 않는다.**
#
# 🔑 ***근본원인은 간격이 좁았던 것이 아니라 「폭을 계산할 수 없었던 것」이다.***
#    주석은 *"간격은 파생 폭보다 크게 잡았다"* 고 적었지만 **파생 폭이 상수로 없었으므로
#    그 주장은 검사할 수 없는 문장**이었고, 테스트는 「간격 ≥ 1000」이라는 **주석이 주장한
#    불변식이 아닌 것**을 단언해 이것을 통과시켰다.
#    ⇒ 교정은 **정의역을 선언**하고, 테스트를 **「arm 쌍 도달집합 교집합 = 0」**으로 바꾸는 것이다.
#
# 출처: (신규) — 2026-08-04 독립 재검증 🔴-B
ARM_SEED_K_VALUES = (1, 5, 20)

# 🔴 **`rep` 의 기점은 0-base다.** `rep ∈ [ARM_SEED_REP_BASE, ARM_SEED_MAX_REP]` 이며
#    **양 끝을 포함**한다 ⇒ 스트림 수 = 999 − 0 + 1 = **1,000 = `GATE_A_SHUFFLE_REPS`**.
#    ⚠️ 1-base 로 읽으면 도달집합이 한 칸 밀리고, 그 어긋남은 산출물에 안 보인다.
#       테스트가 `MAX_REP − REP_BASE + 1 == GATE_A_SHUFFLE_REPS` 를 단언한다.
# 출처: (신규) — 2026-08-04 독립 재검증 🔴-B
ARM_SEED_REP_BASE = 0
ARM_SEED_MAX_REP = 999

# 🔴 **비셔플 arm 도 `rep` 을 쓰는 것으로 «선언»한다.**
#    현 설계에서 `rep` 을 실제로 도는 것은 셔플 arm 하나뿐이다. 그럼에도 도달집합은
#    **전 arm 이 `rep` 전 구간을 쓰는 것으로 계산**한다.
# 🔑 이유: 재검증자가 *"해석 A(셔플만 rep 사용)/B(전 arm 사용) 중 B 를 고르면 충돌이
#    실제로 발생하고 흔적이 안 남는다"* 고 지적한 그 자리다. 「안 쓴다」로 선언하면
#    나중에 부트스트랩·재추출이 붙는 순간 도달집합이 **조용히** 넓어진다.
#    ⇒ **넓은 쪽으로만 실패**하도록(= 보호 과잉) `all_arms` 로 못 박는다.
# 출처: (신규) — 2026-08-04 독립 재검증 🔴-B
ARM_SEED_REP_SCOPE = "all_arms"

# ==============================================================================
# 🔴 `k` 의 **적용 범위** — `rep` 에는 답했는데 `k` 에는 답하지 않았던 자리
# ==============================================================================
# **무엇이 틀렸나**: 위 `ARM_SEED_DERIVATION_RULE` 은 `k` 를 시드에 넣는다. 그런데 문서
# `PREREG_v3.md` §4.1 절차 4 와 §4.3 은 *"남은 풀에서 m 개 무작위 추출 (시드 = SEED_CB)"*
# 라고만 적고 **`k` 를 언급하지 않는다.** ⇒ **두 해석이 다 성립하고 결과가 갈린다:**
#
#   (A) `(d, τ)` 당 **한 번** 추출 — k=1/5/20 이 **같은 대조군**을 공유한다
#   (B) `k` 마다 **재추출** — k 마다 다른 대조군. Δ 차이에 **재추출 잡음**이 섞인다
#
# 🔑 ***이 파일은 `rep` 에 대해서는 이 질문을 명시적으로 닫았다***(`ARM_SEED_REP_SCOPE`,
#    바로 위 절). **`k` 에는 같은 처방이 없었다** — 같은 클래스의 미선언이 하나 더
#    남아 있었던 것이다.
#
# ✅ **(A) 를 택한다 — `k` 무관 단일 추출.** 사유 둘:
#   1. **§8 이 max-t 를 쓴다.** 보조 가족 `k ∈ {1,5,20}` 을 max-t 로 묶으려면 세 통계량이
#      **같은 대조군 위에서** 계산된 **짝 자료**여야 상관을 제대로 반영한다. (B) 로 가면
#      k 사이 차이에 재추출 잡음이 섞여 max-t 분포가 **보수적이지도 정확하지도 않게** 된다.
#   2. **문서 서술이 이미 (A) 쪽이다.** §4.1-4·§4.3 은 추출을 `(d, τ)`·`(d, i)` 단위로
#      적고 `k` 를 조건에 넣지 않는다. (B) 로 가려면 문서를 고쳐야 하고, 그 편이 더 큰 변경이다.
#
# ⇒ **`k` 항은 «셔플·부트스트랩 전용»이다.** 대조군 추출 스트림은 `k` 를 쓰지 않는다.
#
# ⚠️ **그럼에도 도달집합은 `k` 전 구간으로 계산한다** — `ARM_SEED_REP_SCOPE` 와 **같은
#    처방**이다. 좁게 계산하면 나중에 k 의존 스트림(예: 창별 부트스트랩)이 붙는 순간
#    도달집합이 **조용히** 넓어지고 그 사실은 산출물에 흔적을 안 남긴다.
#    ⇒ **넓은 쪽으로만 실패**하도록(= 보호 과잉) 겹침 검사는 `k` 전 구간에서 돈다.
# 출처: (신규) — 2026-08-04 최종 델타 리뷰 🔴-2 (§4.1-4 · §4.3 서술과의 모순 해소)
ARM_SEED_K_SCOPE = "shuffle_and_bootstrap_only"

# arm 이름 → 기저 시드. 🔴 **본실행이 실제로 읽는 표는 이것이다.**
# 출처: (신규)
ARM_BASE_SEEDS = {
    "C-B-raw": SEED_CB,
    # 🔧 3판 개명: `C-B-matched` → `C-B-matched-TV` (매칭 축이 거래대금 단독이 됐다, §4).
    #    **시드 값은 일부러 유지한다** — 이름은 설계 기술이고 시드는 난수 좌표다.
    "C-B-matched-TV": SEED_CB_MATCHED_TV,
    "C-A": SEED_CA,
    "gate_A_null_shuffle": SEED_SHUFFLE,
}

# 🔧 **오프셋 표는 「기저 시드가 어떻게 배치됐는가」의 기록으로만 남긴다.**
#
# 🔴 **간격은 10,000,000 이다 (3판의 1,000 에서 교정).**
#    근거 = **파생 폭의 최소 100배 여유**:
#        파생 폭 = ARM_SEED_K_MULTIPLIER(1,000) × max(k)(20) + ARM_SEED_MAX_REP(999)
#                = 20,999
#        요건    = 100 × 20,999 = 2,099,900  ≤  10,000,000  (실여유 **476배**)
#
# ⚠️ **왜 「폭보다 크게」가 아니라 「100배」인가.** 3판의 간격 1,000 은 *"폭보다 크게
#    잡았다"* 고 «주장» 했으나 실제 폭은 1,619 였다 — 주장과 값이 갈라졌는데
#    **폭이 상수로 없어서 아무도 대조할 수 없었다.** 여유를 크게 잡으면 정의역이
#    조금 늘어도(`k` 추가 · `rep` 증가) 다시 겹치지 않는다.
#    ⇒ `test_arm_seed_streams_do_not_overlap` 이 **도달집합 교집합 = ∅** 과
#      **간격 ≥ 100 × 폭** 을 둘 다 단언한다.
#
# 🔑 **간격을 벌려도 arm 의 «슬롯 순서»는 그대로다** (raw 0 · matched-TV 1 · C-A 2 ·
#    shuffle 3). 개명 규칙이 지키려던 것은 슬롯이며, 슬롯이 유지되면 「어느 arm 이
#    어느 자리인가」는 흔들리지 않는다.
#
# ⚠️ **본실행은 이 표가 아니라 `ARM_BASE_SEEDS` 를 읽는다.** 둘의 정합은 테스트가 단언한다.
# 출처: (신규) — 간격은 2026-08-04 독립 재검증 🔴-B 교정
ARM_SEED_OFFSETS = {
    "C-B-raw": 0,                       # 주 판정            (PREREG.md §4.1-g)
    "C-B-matched-TV": 10_000_000,       # 필수 동반 · 교정 arm (PREREG.md §4.1-g + 3판 C1 교정)
    "C-A": 20_000_000,                  # 보조 · 팩터 귀속     (PREREG.md §4.3)
    "gate_A_null_shuffle": 30_000_000,  # 귀무 실측 셔플       (PREREG.md §6-A)
}

# 🔴 **`k` 블록의 보폭. `rep` 의 폭보다 커야 한다** — 아니면 «같은 arm 안에서» 시드가 겹친다.
#
# ⚠️ **이 값은 3판에서 31 이었고, 그것은 틀린 값이었다.** `rep` 이 0~999 를 도는데
#    보폭이 31 이면 `(k=1, rep=186)` 과 `(k=5, rep=31)` 이 **같은 시드**를 받는다.
#    실측: arm 하나의 도달집합이 3,000 이 아니라 **1,589** 였다 —
#    ***청산 창이 다른 두 검정이 같은 난수열을 돌고 있었다.***
# 🔑 이것은 arm «사이» 충돌(🔴-B)과 **같은 근본원인의 다른 얼굴**이다:
#    `(k, rep)` 의 정의역이 상수로 없어서 **보폭이 폭보다 큰지 아무도 계산할 수 없었다.**
#    ⇒ 정의역을 선언하자마자 새 테스트의 «비-공허» 단언이 이것을 먼저 잡았다.
#
# 값의 근거: `rep` 스트림 수와 **정확히 같게** 잡아 `(k, rep) ↦ K_MULT·k + rep` 이
#    **단사(injective)** 가 되고 k 블록이 빈틈없이 타일링된다.
#    ⇒ `ARM_SEED_K_MULTIPLIER == ARM_SEED_MAX_REP − ARM_SEED_REP_BASE + 1` 을 테스트가 단언한다.
# 출처: (신규) — 2026-08-04 독립 재검증 🔴-B (파생)
ARM_SEED_K_MULTIPLIER = 1000

# ⚠️ `power/step2_power.py:13` 의 `RNG_MASTER = 20260802` 와
#    `power/step3_mde.py:82` 의 `RNG0 = 20260802` 는 **검정력(MDE) 계산 전용**이며
#    본실행 시드 체계가 아니다. 그 스크립트들은 이미 실행이 끝났고 산출물
#    (`power/mde_final.csv` 등)이 고정돼 있으므로 여기로 옮기지 않는다.
#    두 체계를 섞지 말 것.
# 출처: power/step2_power.py:13 · power/step3_mde.py:82 (참조 전용)
POWER_TRACK_LEGACY_SEED = 20260802


# ==============================================================================
# 2. 정규식 — 🔴 전부 원본에서 **복사·붙여넣기**. 손으로 옮겨 적지 않았다.
# ==============================================================================
# §13.2-1 실측: 관리자가 실행한 판본(`\s*` 포함)은 **72글**, 지시문에 리터럴 공백으로
# 전사한 판본은 **67글**. **5글 차이가 아무 경고 없이 났다.**
#
# 🔑 그래서 이 절의 값은 사람이 대조하지 않는다 —
#    `tests/test_frozen_constants.py::test_type_regex_matches_upstream_原本` 이
#    `power/step1_freeze_and_data.py` 와 `PREREG.md` §2.3-(c) 코드블록에서
#    **기계적으로 재추출해 바이트 대조**한다. 손댔으면 그 테스트가 FAIL 한다.

# --- 2.1 유형 판정 정규식 T1~T5 --------------------------------------------
# 출처: PREREG.md:313-321 (§2.3-(c)) == power/step1_freeze_and_data.py:39-46
#       (두 블록은 바이트 동일함을 테스트가 단언한다)
MA_TOK_PATTERN = r"(?:\d+\s*일\s*(?:이동평균선|이동평균|이평선|이평|선)|이동평균선|이평선|이평|장기\s*이평)"

T3_MA_BOUNCE_PATTERN = MA_TOK_PATTERN + r"[^,/]{0,6}?(?:반등|지지|눌림|터치)|눌림목"
T4_MA_BREAK_PATTERN = MA_TOK_PATTERN + r"[^,/]{0,6}?돌파"
T2_PREV_HIGH_PATTERN = r"전일\s*고"
T1_PRIOR_HIGH_PATTERN = r"직전\s*고|전고\b|전고점|전고\s*돌파|신고가"
T5_CONSEC_UP_PATTERN = r"연속\s*상승|연속\s*양봉"

# 🔑 **삽입 순서가 곧 우선순위**(`PRI = list(RX)`)다. 주 판정은 교집합 배정이라
#    이 순서를 쓰지 않지만, 사전 선언된 민감도(우선순위 단일배정)가 쓴다.
#    순서를 바꾸면 그 민감도의 결과가 조용히 바뀐다.
# 출처: PREREG.md:314-321 (§2.3-(c) `RX` OrderedDict 의 삽입 순서)
TYPE_PATTERNS_IN_PRIORITY_ORDER = (
    ("T3_ma_bounce", T3_MA_BOUNCE_PATTERN),
    ("T4_ma_break", T4_MA_BREAK_PATTERN),
    ("T2_prev_high", T2_PREV_HIGH_PATTERN),
    ("T1_prior_high", T1_PRIOR_HIGH_PATTERN),
    ("T5_consec_up", T5_CONSEC_UP_PATTERN),
)

# --- 2.2 `N`(이평선 기간) 룩백 파서 — **C6 누락 ②. 여기서 처음 동결된다** ----
# 🔴 이 정규식은 2판 어디에도 실리지 않았다 — §2.3-(f)·§4.1-(d) 는 파싱 **결과**
#    (276 성공 / 52 실패)만 싣고 파서 자체를 안 실었다. `CLOSING.md:432` 가 그 결함이다.
#    T3/T4 풀 정의 **전체**가 이 파서에 달려 있는데 해시 대상 밖이었다.
#
# 출처: 세션 스크래치패드 `cb_coverage2.py:92` (= §2.3-(f) 표를 실제로 만든 코드).
#   원문:  ma["N"] = ma["method"].str.extract(r"(\d+)\s*일\s*(?:이동평균선|이동평균|이평선|이평|선)")[0]
#   전체 경로(휘발성):
#     %LOCALAPPDATA%\Temp\claude\D--GIT-kis-trading-template\
#       1dd9ae10-8d1b-4c7e-84f2-4db43d13ad16\scratchpad\cb_coverage2.py
#   ⚠️ OS 임시 트리라 언제든 삭제될 수 있다. **이 파일이 이제 그 유일한 영속 사본이다.**
#
# 🔑 **재현으로 확인했다** (추측이 아니다). `labels_v4.csv` 에 이 파서를 다시 돌려
#    `328행 / 276 성공 / 52 실패 / 분포 5:47·10:78·20:104·60:15·120:32 / 「장기」 49`
#    를 **전부 정확히** 재현했다 — PREREG.md:387-388 과 일치.
N_LOOKBACK_PARSER_PATTERN = r"(\d+)\s*일\s*(?:이동평균선|이동평균|이평선|이평|선)"

# ✅ 동결 차단 플래그 ② — 원본 발견 + 수치 재현 확인으로 해소.
N_LOOKBACK_PARSER_RESOLVED = True

# 🔴 파서의 **적용 규약**. 정규식만 옮기면 조용히 다른 것을 센다.
#   - 적용 함수: pandas `Series.str.extract(pattern)[0]`
#     = **첫 캡처군만**, `re.search` 의미(앵커 없음·첫 매치), 미매치는 NaN
#   - 적용 대상 행: `cb_type ∈ {T3_ma_bounce, T4_ma_break}`
#
# ⚠️ **여기에 비대칭이 하나 있다 — 사전에 적어 둔다.**
#    276/52 를 만든 `cb_type` 은 `PRI` **우선순위 단일배정**으로 정해졌다
#    (`cb_coverage2.py:34` 의 `PRI` 순서). 그런데 주 판정의 배정 규약은
#    `CB_TYPE_ASSIGNMENT = "intersection"` (교집합)이다.
#    ⇒ **교집합으로 가면 T3/T4 대상 행 집합이 328 과 달라지고, 따라서 276/52 도
#      그대로 성립하지 않는다.** 본실행에서 이 수치를 「이미 검증된 값」으로
#      재사용하지 말고 **배정 규약대로 다시 셀 것.**
N_LOOKBACK_PARSER_APPLY = "pandas.Series.str.extract(pattern)[0]"
N_LOOKBACK_PARSER_SCOPE = ("T3_ma_bounce", "T4_ma_break")
N_LOOKBACK_PARSER_SCOPE_ASSIGNMENT = "priority_single"

# 파서 재현 검증용 기대값. 🔴 위 경고대로 **우선순위 단일배정 기준**이다.
# 출처: PREREG.md:387-388 (§2.3-(f)) · cb_coverage2.py 재실행으로 확인
N_LOOKBACK_PARSE_EXPECTED = {
    "assignment": "priority_single",
    "rows_t3_t4": 328,
    "success": 276,
    "failure": 52,
    "value_counts": {5: 47, 10: 78, 20: 104, 60: 15, 120: 32},
    "failure_with_장기": 49,
}


# ==============================================================================
# 3. C-B 풀 정의 (주 판정)
# ==============================================================================
# T1 룩백. 🔴 **저자 근거 없음** — T1 644행 중 643행이 룩백 길이를 안 적었다.
#    `L=20` 은 **우리의 가정**이며 「그가 20일 신고가를 봤다」가 아니다 (§2.3-f · §9).
# 출처: PREREG.md:563 (§4.1-(a) 표)
CB_T1_LOOKBACK_L = 20

# 풀에서 대체 없이 뽑는 대조 종목 수. D-2 확정.
# 출처: PREREG.md:590 (§4.1-(c)) · PREREG.md:1213 (D-2)
CB_SAMPLE_M = 10

# T5 「연속 상승」 = close(d) > close(d−1) > close(d−2) ⇒ 연속 **2일** 확정.
# 출처: PREREG.md:567 (§4.1-(a) 표 "(2일 연속 확정)")
CB_T5_CONSECUTIVE_DAYS = 2

# 복수 유형 행(157행 2유형 · 6행 3유형)의 배정 규약.
# 🔑 **교집합이 기본안**이다 — 대조군의 존재 이유가 「같은 셋업」이므로 그가 두 조건을
#    적었으면 대조군도 두 조건을 만족해야 대칭이다. 우선순위 단일배정은 **사전 선언된
#    민감도로만** 돌리고 판정에 쓰지 않는다.
# ⚠️ D-9 는 CLOSING.md §6 기준 **열린 채로 종결**됐다. 이 값은 2판의 기본안이다.
# 출처: PREREG.md:572-581 (§4.1-(b)) · 기본안 지위는 PREREG.md:1227 (D-9)
CB_TYPE_ASSIGNMENT = "intersection"
CB_TYPE_ASSIGNMENT_SENSITIVITY = "priority_single"

# 🔴 「장기」(49행) 및 무표기(3행) = 52행은 **C-B 표본 밖**으로 사전 선언.
#    2판 초판의 「없으면 20」은 폐기됐다 — 저자가 명시한 「장기」(그의 표기 체계에서
#    60·120)에 단기 이평선을 대입하게 되고, 그 사실은 산출물에 흔적을 안 남긴다.
#    `N=120` 대입은 **사전 선언된 민감도로만** 돌린다.
# 출처: PREREG.md:589 (§4.1-(c) 표) · PREREG.md:601-607 (§4.1-(d)) · PREREG.md:1216 (D-5)
#       ⚠️ 초안은 `:591` 이었다 — 591 은 「신고가 해석」 행이고 「`N` 미파싱」 행은 589 다.
CB_N_UNPARSED_POLICY = "exclude_from_cb_sample"
CB_N_UNPARSED_SENSITIVITY_VALUE = 120

# 「신고가」 408행의 룩백 해석. 기본안 = `직전고` 와 같은 풀(L=20). 52주는 민감도.
# ⚠️ D-10 은 **열린 채로 종결**됐다 (CLOSING.md §6).
# 출처: PREREG.md:609-617 (§4.1-(e)) · PREREG.md:1228 (D-10)
CB_NEW_HIGH_LOOKBACK_L = 20
CB_NEW_HIGH_LOOKBACK_SENSITIVITY_L = 250

# T1 룩백 민감도 (사전 선언).
# 출처: PREREG.md:587 (§4.1-(c) 표)
#       ⚠️ 초안은 `:588` 이었다 — 588 은 「`N` (T3/T4)」 행이고 「`L` (T1 룩백)」 행은 587 다.
CB_T1_LOOKBACK_SENSITIVITY_L = (10, 60)


# ==============================================================================
# 4. 매칭 (C-A · C-B-matched-TV) — 🔴 3판 C1 교정으로 **축이 뒤집힌 절**
# ==============================================================================
# ⚠️ **2판(§4.3·§4.1-g)의 「(log 시총, log 거래대금) 5×5 = 25셀」은 폐기됐다.**
#    2판을 인용해 이 절을 되돌리지 말 것. 사유는 아래 커버율 곡선이다.
#
# ------------------------------------------------------------------------------
# 실측 — `market_cap > 0` 커버율 곡선 (kis_template.daily_prices, 관리자 실측 재현)
# ------------------------------------------------------------------------------
#     2021        0.00%          2024-01~02   0.35%
#     2022        0.00%          2024-03      67.71%   ← 백필 진행 중
#     2023        0.24%          2024-04~     99.53%
#
#   일별로 보면 경계가 **하루 만에 선다**: 03-12 **1.41%** → 03-13 **99.44%**.
#   ⇒ **실질 가용 시작 = 2024-03-13** (백필 경계이지 시장 사실이 아니다).
#
# 🔴 **`min(date) WHERE market_cap > 0` 으로 가용 시작일을 판정하는 것을 금지한다.**
#    그 쿼리는 **2023-04-25** 를 준다. 하지만 2023-04~2024-02 의 양수 행은 월 152~176행
#    ≈ **8종목**뿐이고, 그것이 만든 `min()` **아티팩트**다. `CLOSING.md` §5-C1 이 인용한
#    「양수 최초일 2023-04-25」가 바로 이 함정에 빠진 값이다.
#    ⇒ 가용 시작은 **커버율 곡선**으로 판정하라. 존재하는 최초 행이 아니라 «쓸 수 있게
#      되는 시점»이 필요한 것이다. (§8-8 ***NULL 을 셌다고 0 을 센 것이 아니다*** 의 동형)
#
# 🔴 **노출 규모**: 처치군(`labels_v5`, N_T=1,361) 중 `post_date < 2024-03-13` 이
#    **1,123행 = 82.51%**. ⚠️ `CLOSING.md` 의 **56.3%** 는 **과소**였다(구판·v4 기준).
#
# ⇒ **시총 축으로는 처치군의 5/6 에서 셀을 만들 수 없다.** 반면 `trading_value` 는
#    전 구간 가용하다(아래 `MATCH_VAR_AVAILABILITY_MIN` 주석의 실측).
#    거래대금이 「거래량이 잘못 라벨된 컬럼」이 아님도 확인됐다 — `close × volume` 과
#    대조해 **중앙값 비가 4개 연도 전부 정확히 1.0000**(5~95%분위 ±0.5%).

# 🔴 **주 매칭 축 = `log 거래대금` 단독.** 2축이 아니다.
# 출처: (3판 C1 교정) — 2판 PREREG.md:717·:673 을 대체한다
MATCH_AXES = ("log_trading_value",)

# R4(외부 시총 시계열 조달) 충족 시에만 도는 **조건부 민감도** arm 의 축.
# 🔴 `MATCH_R4_SATISFIED = False` 인 동안 이 2축 arm 은 **돌지 않는다.**
#    「데이터가 없는데 일단 돌려서 되는 구간만 본다」는 곧 2024 이후만 보는 것이고,
#    그 하위표본은 생존편향이 가장 약한 연도라 **결과가 계통적으로 낙관**된다.
MATCH_AXES_R4_SENSITIVITY = ("log_market_cap", "log_trading_value")
MATCH_R4_SATISFIED = False

# 🔧 값은 5 그대로지만 **의미가 바뀌었다**: 「각 축 5분위 → 25셀」이 아니라
#    **「거래대금 단독 5분위 → 5셀」**. 격자 경계는 **그날 유니버스 기준**이고
#    C-A 와 C-B-matched-TV 가 **동일 격자**를 쓴다.
# 출처: PREREG.md:1213 (D-2, 분위 수 5) · 축 구성은 3판 C1 교정
MATCH_QUANTILE_BINS = 5

# ==============================================================================
# 🔴 매칭 «방식» — D-12 미결. 동결 차단 조건 ③
# ==============================================================================
# C7 이 드러낸 것: **해상도와 풀 크기가 정면충돌한다.**
#   · 5분위 격자로 매칭해도 양팔 거래대금 중앙값 비가 **9.14배**로 남는다
#     (처치군 **99.7%(1,356/1,360)** 가 최상위 분위 하나에 몰려 있다).
#   · 원척도 비를 문턱 안에 넣으려면 **100분위**가 필요한데, C-B 유형 풀
#     `P(d, τ)`(≥30종목)에서는 셀당 **0.3~3종목** 이라 `m=10` 을 못 뽑는다.
#
# ⇒ **방식 자체가 관리자 판정 사항이다** (PREREG_v3.md §12.4 **D-12**).
#    기본안은 **캘리퍼**(`|log tv_ctrl − log tv_treat| ≤ ε`) — 풀 크기에 무관하게
#    해상도를 내지만 매칭 실패 행이 늘고 그 건수가 보고 항목이 된다.
#
# 🔴 **`None` 은 「아직 못 정했다」이지 「기본값으로 돌아라」가 아니다.**
#    §13.2-6: *기본값 대입은 「채워졌다」가 아니라 「틀린 것으로 채워졌다」일 수 있다.*
#    `D12_RESOLVED = False` 인 동안 본실행은 **시작할 수 없다**(아래 동결 차단 조건 ③).
# 출처: (신규) — PREREG_v3.md §4.1-(g-2) · §6-B · §12.4 D-12
MATCH_METHOD = None

# 캘리퍼 폭 `ε`(로그차 단위). 🔴 **실측 전 확정 금지** — §4.1-(g-2) 의 (1)~(4) 절차로
#    C-B 유형 풀에서 스윕해 정한 뒤 여기 박고 동결한다(그 뒤 변경 금지).
# ⚠️ `ε ≤ GATE_B_LOG_DIFF_MAX` 로 잡으면 게이트 B ⑥ 은 **구성상 만족**되지만,
#    그 대가가 **매칭 실패 행수 = 커버율 하락**이므로 「구성상 만족」을 공짜로 읽지 말 것.
# 출처: (신규) — PREREG_v3.md §12.4 D-12-③
MATCH_CALIPER_EPS = None

# 🔴 동결 차단 플래그 ③ — D-12 가 닫히기 전에는 §12.1 PASS 조건 ③ 의
#    **충족 가능성 자체가 미확정**이다 (PREREG_v3.md §12.1 C7 재검토표).
D12_RESOLVED = False

# 셀에 후보가 `m` 미만이면 결정론적으로 인접 셀까지 확장한다.
# 🔧 **확장 축이 뒤집혔다.** 2판은 *"시총 축으로만 확장, 거래대금 축은 확장하지 않는다"*
#    였다. 이제 축이 거래대금 하나뿐이므로 **확장도 그 축에서 일어난다.**
# 🔑 순서가 결정론성의 전부다 — 바꾸면 같은 시드로도 다른 대조군이 나온다.
# 출처: 순서는 PREREG.md:675-676 (§4.1-(g)) · 축은 3판 C1 교정
#       ⚠️ 초안은 `:676-677` 이었다 — 확장 «순서»(`−1 → +1 → −2 → +2`)는 675 에 있다.
MATCH_CELL_EXPANSION_ORDER = (-1, +1, -2, +2)
MATCH_CELL_EXPANSION_AXIS = "log_trading_value"

# 매칭 변수 가용률 문턱 — 축의 `> 0` 가용률이 이 값 미만이면 그 축을 쓰는 arm 을
# **차단**한다. 「기록하고 진행」 금지 (4차 교훈).
#
# 🔑 **이 값은 임의로 정했지만, 결정은 이 값에 둔감하다.** 처치군 1,361행 실측 가용률:
#
#       market_cap     238 / 1,361 = **0.1749**      ⇒ 차단
#       trading_value  1,360 / 1,361 = **0.9993**    ⇒ 통과
#
#   두 축의 판정이 갈리는 문턱 구간을 풀면 **둔감 구간 = (0.1749, 0.9993]** 이다
#   (τ 가 이 안이면 시총 차단·거래대금 통과가 항상 성립). **0.95 는 그 구간 안의
#   한 점**이고, 구간 폭이 **0.82** 라 현 데이터에서는 문턱을 어디에 두든 **결정이
#   같다** ⇒ 임의성이 판정에 닿지 않는다.
#
# ⚠️ **「둔감하다」를 「아무 값이나 된다」로 읽지 말 것.** 위 구간은 **현 데이터의 성질**
#    이며, 새 데이터(R4 로 시총 시계열을 조달하거나 기간을 늘리면)에서는 두 가용률이
#    움직여 구간이 좁아지거나 **0.95 를 포함하지 않게 될 수 있다.**
#    ⇒ 데이터가 바뀌면 **둔감 구간을 다시 계산하고**, 0.95 가 여전히 그 안인지 확인할 것.
#
# ⚠️ 위 두 가용률은 **처치군 행 기준**이다. 유니버스 전체 기준은 다르다
#    (`trading_value` 연도별 **97.83 / 98.17 / 98.79 / 97.82%**). 문턱이 적용되는
#    모집단은 **매칭이 필요한 행 = 처치군**이므로 처치군 기준으로 잰다. 두 수를 섞지 말 것.
#
# 출처: (신규 · 관리자 확정 대기) — CLOSING.md §5-C1 이 요구한 항목.
#       가용률·둔감 구간은 이번 작업에서 `labels_v5.csv` × `kis_template` 직접 실측.
MATCH_VAR_AVAILABILITY_MIN = 0.95


# ==============================================================================
# 5. 풀 크기 · 커버율 문턱
# ==============================================================================
# `(날짜 d, 유형조합 τ)` 풀이 결함 필터 후 이 값 미만이면 그 `(d, τ)` 는 C-B 표본 밖.
# 근거: `m=10` 을 대체 없이 뽑으려면 풀이 최소 3배는 돼야 추출 분산이 폭주하지 않는다.
# ⚠️ C-A 의 300 과 **다른 값이다.** 같은 숫자를 쓰면 C-B 가 통째로 죽는다.
# 출처: PREREG.md:686 (§4.1-(h)) · PREREG.md:991 (§6-F 절차 ②)
CB_POOL_MIN_STOCKS = 30

# C-A 풀(그날 유니버스 전체, 평균 1,931종목) 하한.
# 출처: PREREG.md:991 (§6-F 절차 ②)
CA_POOL_MIN_STOCKS = 300

# 게이트 G — 미달 시 **주 판정 p 계산 자체를 skip**. D-6 확정.
# 출처: PREREG.md:1011 (§6-G) · PREREG.md:765 (§4.5) · PREREG.md:1217 (D-6)
CB_COVERAGE_MIN = 0.50


# ==============================================================================
# 6. 게이트 통과 기준 (숫자)
# ==============================================================================
# 게이트 A — 짝 셔플 반복 수.
# 출처: PREREG.md:924 (§6-A)
GATE_A_SHUFFLE_REPS = 1000

# 게이트 A — 셔플 Δ 분포의 평균 |Δ_null| 상한. 0.05%p = 0.0005 (수익률 단위).
#
# ✅ **3판 확정: 게이트 A 의 통과 기준은 이 조건 «단독»이다.**
#    2판 §6-A 의 두 번째 조건(*"셔플 분포의 95 분위 < 관측 Δ"*)은 **게이트에서 삭제**되어
#    **주 판정의 보고 항목으로 이동**했다. 사유는 CLOSING.md §5-C2 —
#    그 조건에는 **주 판정 자체가 들어 있어** ① 귀무 세계에서 반드시 실패하므로
#    **깨끗한 FAIL 을 보고할 수 없고**, ② 게이트가 앞순위라 **실행자가 답을 먼저 본 뒤**
#    나머지 게이트를 돌리게 되어 코드 순서 강제의 목적이 통째로 무효화된다.
#    (§8-9: ***게이트의 통과 기준에 주 판정을 넣지 말 것***)
# 출처: PREREG.md:925 (§6-A) 첫 번째 조건 + 3판 C2 교정
GATE_A_NULL_DELTA_MAX = 0.0005

# 게이트 B — ①②③④ 각각 양팔 비율 차 상한. 1.0%p = 0.010.
# 출처: PREREG.md:937 (§6-B)
GATE_B_RATE_DIFF_MAX = 0.010

# 게이트 B — ⑥ 양팔 거래대금 중앙값의 **로그차** 상한. (D-19 확정)
#
# ============================================================================
# 🔴 **C7 교정 — 「비 ∈ [0.90, 1.10]」을 여기 두면 안 된다**
# ============================================================================
# **이 자리에 무엇이 있었나**: `GATE_B_MEDIAN_RATIO_RANGE = (0.90, 1.10)`.
# **왜 틀렸나 — 셋이 동시에 틀렸다.**
#   1. **척도가 안 적혀 있다.** 「비」가 원척도 비인지 `med(log tv)` 의 비인지 정해져
#      있지 않다. 이 파일을 import 한 실행자가 `med(log tv)` 비에 적용하면
#      **D-19 가 명시적으로 «금지»한 해석**을 실행하게 된다
#      (`med(log tv_C) ≈ 23.43` 이라 비 1.10 은 거래대금 **10.41배**를 허용 = 판별력 0).
#   2. **수치도 달랐다.** D-19 의 원척도 하한은 `1/1.10 = 0.90909…` 인데 저장값은 `0.90`.
#      ⇒ 비 **0.905** 인 설계가 이 상수로는 통과하고 D-19 로그차로는 차단된다.
#   3. **문서에만 도달한 교정이었다.** `PREREG_v3.md` §12.3 D-19 는 로그차로 재선언했는데
#      **해시 대상인 이 파일에는 C7 흔적이 0건**이었고, 그 상태로 71 passed 가 나왔다.
#      ⇒ 그래서 `tests/test_frozen_constants.py` 검사 8(문서↔상수 정합)이 신설됐다.
#
# **무엇으로 바꿨나**: **척도가 이름에 드러나는 로그차 상한 하나**로 교체한다.
#
#     통과 조건:  | log med(tv_T) − log med(tv_C) |  ≤  GATE_B_LOG_DIFF_MAX
#     값의 유도:  ln(1.10) = 0.09531017980432493  (= repr(math.log(1.10)), 배정밀도 정확값)
#     🔑 **허용 배수 병기(D-19 규약)**: 원척도로 **0.909091 ~ 1.100000 배**
#        (= exp(−GATE_B_LOG_DIFF_MAX) ~ exp(+GATE_B_LOG_DIFF_MAX))
#
# 🔴 **금지**: `med(log tv_T) / med(log tv_C) ∈ [0.90, 1.10]` — 2판의 문자 그대로의
#    해석. 실측상 6개 격자 전부 통과(유동성 **9.14배**도 통과) ⇒ **판별력 0**이라
#    선택지가 아니다 (PREREG_v3.md §6-B 스윕 · §12.3 D-19).
#
# ⚠️ **리터럴로 적는다.** 이 파일은 순수 데이터 모듈이라 `math.log(1.10)` 을 쓸 수 없다
#    (함수 호출 금지 — 해시 대상 안에 실행 자유도를 두지 않는다). 위 유도식이 출처이며,
#    테스트가 `math.log(1.10)` 과 바이트 수준으로 대조한다.
#
# 출처: PREREG_v3.md §6-B (C7 교정 코드블록) · §12.3 D-19
GATE_B_LOG_DIFF_MAX = 0.09531017980432493

# 게이트 B — ⑤⑥ 의 «사유 분리» (C4).
#
# 🔴 **3판 C4 교정: ⑤ 와 ⑥ 은 사유가 다르다. 묶어서 적지 말 것.**
#    2판은 둘을 같은 근거(「처치 정의의 누출」)로 묶어 면제했다. 실제로는 —
#
#   **⑥ log 거래대금 — 「누출 우려로 안 하는 것」**
#     · `C-B-matched-TV` 에서 **차단 기준으로 복원**된다. C1 교정으로 이 arm 이
#       **전 구간에서 돌게 됐기** 때문이다(2판에서는 82.5% 가 계산 불가였다).
#     · `C-B-raw` 에서는 **여전히 보고 항목**이다 — 누출 우려(§4.1-g: 그의 후보 선정에
#       거래대금이 실제로 들어 있다)는 그대로다.
#     · `C-A` 에서도 차단 기준.
#
#   **⑤ log 시가총액 — 「데이터가 없어서 못 하는 것」**
#     · 면제가 아니라 **측정 불가**다. §4 의 커버율 곡선 참조(처치군 82.51% 가 결측).
#     · **R4(외부 시총 시계열 조달) 조건부.** `MATCH_R4_SATISFIED` 가 True 가 되기
#       전까지 ⑤ 는 어느 arm 에서도 차단 기준이 될 수 없다.
#     · ⚠️ 「측정 불가」를 「대칭이다」로 읽지 말 것 — **모르는 것이지 같은 것이 아니다.**
#
# 🔑 **면제·미측정된 비대칭의 크기를 먼저 적는다** (4차 규칙: *면제한 비대칭의 크기를
#    먼저 재라 — 면제 논리가 옳아도 면제된 것이 이 설계에서 가장 큰 비대칭이면 그건
#    면제가 아니라 구멍이다*). 처치군의 그날 유니버스 내 **거래대금 백분위**:
#
#       중앙 **0.9896** · 평균 **0.9813** · 하위 5%분위 **0.9394**
#       그날 유니버스 중앙값의 **172.4배** · **98.8%** 가 상위 10% 안
#       (날짜당 유니버스 중앙 **2,156종목 — 모집단 = 처치 661일**.
#        ⚠️ §10 의 모집단은 거래일 **983일**이고 그 중앙은 **2,161종목**이다.
#           두 수는 다른 모집단의 값이므로 섞어 쓰지 말 것)
#
#    ⇒ 처치군은 사실상 **거래대금 최상위 집단**이다. ⑥ 을 C-B-raw 에서 면제한다는 것은
#      **이 설계에서 가장 큰 비대칭을 면제**한다는 뜻이며, 그래서 `C-B-matched-TV` 가
#      선택이 아니라 **필수 동반 교정 arm** 이다.
#
# ⚠️ **인용 시 복구 전/후를 병기할 것.** 위 값은 **2026-08-03 `daily_prices` 복구 «후»**
#    재측정치다. `CLOSING.md` §3.1 의 **0.9883 / 146.7배 / 98.5%** 는 **복구 «전»** 계산이라
#    구판이다. 두 판을 섞어 인용하면 같은 지표가 두 값을 갖게 된다.
#
# 🔴 **⑤⑥ 의 문턱 상수는 위 `GATE_B_LOG_DIFF_MAX` 하나뿐이다.**
#    2판의 `GATE_B_MEDIAN_RATIO_RANGE = (0.90, 1.10)` 은 **삭제됐다**(C7).
#    되살리지 말 것 — 척도가 안 적혀 있고, 하한도 `1/1.10 = 0.90909` 가 아니라 `0.90` 이었다.
#
# 출처: ⑤⑥ 분리는 3판 C4 교정 · 문턱은 PREREG_v3.md §12.3 D-19 ·
#       비대칭 실측은 관리자 복구 후 재측정(이번 작업에서 독립 재현: 중앙 0.9891 ·
#       평균 0.9809 · 5%분위 0.9387 · 172.4배 · 98.68%. 차이 ≤0.0007 은 백분위
#       동순위 처리 방식 차이이며 판정에 영향 없다)

# 게이트 D — 종목 블록 SE / 날짜 블록 SE 비의 상한.
# 출처: PREREG.md:963 (§6-D)
GATE_D_SE_RATIO_MAX = 3.0


# ==============================================================================
# 7. 판정
# ==============================================================================
# 주 판정 청산 창. 사전에 하나로 못 박았으므로 max-t 를 걸지 않는다.
# 출처: PREREG.md:1084 (§8) · PREREG.md:1219 (D-7 아래 "주 판정 `k`")
#       ⚠️ 초안은 `:1082` 였다 — 1082 는 «표 헤더»(`| 검정 가족 | 구성 | 보정 |`)다.
PRIMARY_K = 5

# 보조 가족 — max-t 보정 대상 (주 판정 k=5 를 포함해 3개 셀).
# 출처: PREREG.md:1087 (§8)
#       ⚠️ 초안은 `:1086` 이었다 — 1086 은 「필수 동반 ②」 행이고 「청산 창 k ∈ {1,5,20}」 은 1087 이다.
SECONDARY_K_FAMILY = (1, 20)

# 양측 유의수준. 주 판정·필수 동반 판정 전부 보정 없음.
# 출처: PREREG.md:1084 (§8) · PREREG.md:1032 (§7.1 4단계)
#       ⚠️ 초안은 `:1082`·`:1039` 였다 — 각각 표 헤더와 §7.1 «본문 주석» 이다.
ALPHA = 0.05

# MDE 계산용 검정력.
# 출처: PREREG.md:1032 (§7.1 4단계)
#       ⚠️ 초안은 `:1039` 였다 — 1039 는 §7.1 «본문 주석» 이고 4단계 식은 1032 다.
POWER = 0.80


# ==============================================================================
# 8. 비용 — 🔴 원출처 미확인 (동결 차단 조건 0-d)
# ==============================================================================
# 연도별 **왕복** 비용 (수수료 양방향 + 거래세 매도 편도).
# 출처: power/step3_mde.py:42 (`COST = {2021: 0.0026, ...}`)
#
# 🔴 **이 값들은 확인되지 않았다.** PREREG.md §3.3 은 거래세율 표를 `⟨____⟩%` 로
#    비워 두고 *"이 표를 기억으로 채우지 말 것 — 초판 값들은 **작성자의 기억이고
#    원출처로 확인되지 않았다**"* 라고 명시한다. CLOSING.md §6 은 `step3_mde.py` 가
#    바로 그 미확인 값을 **가정값으로** 썼음을 기록한다.
#
# 🔴 사소한 상수가 아니다 — **절대 판정(§4.2)의 통과선이 이 값 위에 서 있고**,
#    처치군 행의 **55.3%(753/1,361)가 2021~2022** 에 몰려 있다(**v5 기준**).
#    연도별 처치군(v5): 2021 **369** · 2022 **384** · 2023 **361** · 2024 **247**.
#    ⚠️ **판 기준을 반드시 밝힐 것** — PREREG.md §3.3 의 *"56.3%(716/1,271)"* 는
#      **v4 기준**이다(산문 편입 전 N_T=1,271). v5 는 N_T=**1,361** 이라 분모가 다르다.
#      두 판의 비율이 비슷해 보여 섞어 써도 티가 안 나는 것이 오히려 위험하다.
#    (상대 판정 Δ 에는 양팔이 같은 비용을 내므로 영향 없다.)
#
# 확정 절차 (§3.3): **기획재정부 / 한국거래소 고시**를 원출처로 확인하고,
#    **코스피·코스닥 구분**과 **적용 시점(연중 개정 여부)** 까지 확인한다.
ROUNDTRIP_COST_BY_YEAR = {2021: 0.0026, 2022: 0.0026, 2023: 0.0023, 2024: 0.0021}

# 🔴 동결 차단 플래그 ① — `PREREG.md` §0-d. 위 표의 원출처가 확인되기 전까지 False.
COST_SOURCE_VERIFIED = False

# 초판 고정값. **민감도로만** 병기한다 (판정 승격 금지).
# 출처: PREREG.md:466 (§3.3 표 "보조")
#       ⚠️ 초안은 `:464` 였다 — 464 는 「거래세」 행이고 「보조」 행은 466 이다.
ROUNDTRIP_COST_FIXED_SENSITIVITY = 0.0021

# 편도 수수료 0.015%.
# 출처: PREREG.md:463 (§3.3 표)
COMMISSION_RATE_ONE_WAY = 0.00015

# 슬리피지 0 가정. 🟡 양팔 대칭이 아니다 — 처치군은 돌파 직후 고변동 종목이라
# 스프레드가 넓다 ⇒ 방향은 **처치군 불리**. 교정하지 않는다(일봉에 호가가 없다).
# 출처: PREREG.md:467 (§3.3 표 「슬리피지」)
#       ⚠️ 초안은 `:466` 이었다 — 466 은 「보조」 행이고 「슬리피지」 행은 467 이다.
SLIPPAGE = 0.0


# ==============================================================================
# 9. 층 (§5.2) — 사전 선언, 사후 변경 금지
# ==============================================================================
# **층은 아래 4개가 전부이며 실행 중 추가 불가.** 사후 이상치 제거 금지 (§6-C).
#
# 🔴 `src` 층은 주 판정 안에서 **구조적으로 결손**이다 (실측):
#      `both`+`body` 1,160행 · `both+prose` **2행** · `title_only` **0행** · `prose` **0행**
#    ⇒ 빈칸을 「차이 없음」으로 읽으면 안 된다 — **표본이 없는 것과 효과가 없는 것은 다르다.**
#
# 🔧 **`both+prose` 를 열거에 넣었다 (3판 교정).** 그 전 열거는 세 칸뿐이라
#    **결손표가 스스로 세는 2행을 분류할 곳이 없었다.** §6-C 가 실행 중 층 추가를
#    금지하므로, 열거에 없는 칸의 행은 **조용히 어디로도 안 간다.**
#    (`labels_v5.csv` 실측: `src="both+prose"` **8행** — `usable` **2행** / 불가 6행.
#     `harvest/prose_incorporation.csv` 의 `중복(src갱신)` 8건이 그것이다.)
#
# 🔴 **`src` 는 「필수 부수 판정」이지만 세 칸 중 둘이 주 판정 표본 0행이다.**
#    ⇒ 「필수」는 **보고 의무의 등급**이지 「그 층에서 판정이 나온다」가 아니다.
#      `title_only`·`prose` 의 표기 편향 점검은 **절대 판정에서만** 달성된다(§5.2·§9-4·§9-5).
#      빈칸에 대해 「차이 없음」·「대칭이다」를 쓰지 말 것.
#
# 🔧 **`year` 는 「필수 부수 판정」 → 「부수」로 강등됐다** (§5.1).
#    2판의 강등 전 지위를 인용해 되돌리지 말 것 — 근거였던 연도별 「손실률」이
#    상폐율이 아니었음이 `CLOSING.md` §4 로 확정됐다.
# 출처: PREREG_v3.md §5.2 (층 목록) · 지위 강등은 PREREG_v3.md §5.1
#       (2판 원표는 PREREG.md:839-862 — 초안 `:839-863` 의 863 은 «빈 줄»이다)
STRATA = {
    "year": {
        "values": (2021, 2022, 2023, 2024),
        "status": "부수",
    },
    "src": {
        "values": ("both+body", "both+prose", "title_only", "prose"),
        "status": "필수 부수 판정",
    },
    "trigger_type": {
        "values": ("T1", "T2", "T3", "T4", "T5"),
        "status": "부수",
    },
    "date_density": {
        "values": ("1건", "2건 이상"),
        "status": "부수",
    },
}

# 주 판정은 **전체(2021~2024 · `src` 전체) 단일 값**이다. 위 층은 전부 보고 의무가 있고
# 판정에 쓰지 않는다 (계산했는데 안 실으면 6차의 `s-vs-C2` 누락과 같은 사고가 난다).
# 출처: PREREG.md:848-849 (§5.2)
#       ⚠️ 초안은 `:849-850` 이었다 — 850 은 «빈 줄»이다.
PRIMARY_STRATUM = "all"
