# 라벨 동결 — 8번째 글 (2026-09-18) · 🟢 **검수 3패스 반영 · 🔒 사장님 결정 반영(2026-09-18 21:xx KST)** · 미커밋(커밋은 관리자)

준거 `PREREG_EXIT_V2.md` §2 판별표(`:41-46`) + `PREREG_POST6.md` §1-1(`EXIT-X4` `MIX` 불확장 승계) + 🆕 **`PREREG_POST8.md` §2 `P8-형변량`**(`:195-210` · 첫 구속) + [`PREDECISION_2026-09-18_post8.md`](PREDECISION_2026-09-18_post8.md) PD-2·PD-5·PD-7·PD-10·**PD-17**(원장 플래그 인코딩)·PD-18·PD-20.
라벨은 **저자 서술로만** 정한다(`PREREG_EXIT_V2.md:38-39` *「수익률 값을 보고 정하지 않는다(그러면 순환이다)」*). 우선순위(4·5·6·7번째 글 전례 · `LABELS_2026-09-15_post7.md:4`): `MANUAL` > `MIX`/`SL` > `TP`. 「한 항목 = 한 라벨」.
판별표 동결 문언(`PREREG_EXIT_V2.md:43-46` 축자 · 표 4행): **`TP`** = *「프리셋이 **`표준형/HDR*`** 이고 손절 서술이 없다」*(🔴 **조건이 둘이다**) · **`SL`** = *「저자가 「분할손절」·「손실률」 기전을 명시」* · **`MIX`** = *「한 건 안에 본전 청산과 분할손절이 **둘 다** 서술됨」* · **`MANUAL`** = *「「수동」 개입 서술」*.
🆕 **`P8-형변량`**(`PREREG_POST8.md:197-205`): 프리셋을 **`(형, HDR수치)`** 로 정규화 · 형 화이트리스트 = **`{표준형}`** · 형 = 「표준형」 ∧ 손절 서술 없음 ⇒ `TP` · 형 문자열 없이 `HDR<n>` 만 ⇒ `TP` · **다른 형 이름 ⇒ `unknown`** + `TP` 갈래 의무 민감도 + 「미등록 형: `<이름>`」 신고. 🔴 **정규화는 «인테이크의 사람 판단»이고 기계 검사가 아니다**(`:209-210`) — 이 문서가 그 판단의 동결 자리다.

> 🔴 **라이브 채택 아님** — `PREREG.md:15`(「§0 2번」) *「🔴 **라이브 채택 금지.** 저자가 *"사람이 할 일은 종목 고르는 것까지"* 라고 적었다. 후보 선정이 재량이면 규칙을 복원해도 자동화 대상이 아니다. 이 검정의 산출물은 **기록**이지 전략 후보가 아니다.」* · `PREREG_POST8.md:26`. 라벨은 **「이 문장을 어느 규칙으로 잴 것인가」**만 정한다.

| # | 종목 | 저자 서술(종료 · 축자 · txt 줄) | `(형, HDR)` | **라벨** | 근거 | 완결 | `breakeven_note` | 「손실률」 레그 | 구분 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 빛과전자 🔁후속 | 「이번주 9월 14일 목표 매도가 2차, 3차 분할매도 완료」(:7) | `(표준형, 60)` | `TP` | 손절·본전·수동 서술 없음 ∧ 형 = 표준형 | 완결 | 0 | 0 | 후속(post7 #11) — **EXIT 주 분모 밖**(「신규 건에만」 `PREREG_EXIT_V2.md:55`) |
| 2 | 로보티즈 🔁후속 | 「이번주 9월 14일, 2차 목표 매도가에서 매도 후, 3차 매도는 직전고점 부근에서 **직접**매도 처리」(:12) | `(표준형, 60)` | **`MANUAL`** | 「직접」= 수동 개입(post5 한켐·SK아이이테크놀로지 전례 문언) · 우선순위상 `MANUAL` | 완결 | 0 | 0 | 후속(post7 #9) |
| 3 | 우리로 🔀 | 「9월 11일 자동매매 등록 후 (HDR 60%, 표준형), 1차 매수 후 → 1분할 매도, **본전 위협에 전량매도** / **한번 더** 자동매매 등록(기존 셋팅동일), 1차 매수 후 → 1,2,3차 차례로 분할매도 **완료**」(:17-18) | `(표준형, 60)` × 2 | `TP` | 두 사이클 모두 손절·수동 서술 없음 ∧ 형 = 표준형(사이클 2 「기존 셋팅동일」) · 본전 청산만 · 「손실률」 없음 ⇒ `MIX` 아님 | 완결(서술 · `~` 불일치) | **1** | 0 | 신규 · 🔀 **항목 내 2 사이클**(post5 한켐 A-9 이후 두 번째) · 「항목 내 2 사이클」 표시(`WRC-R5` `구조차단`) · `EXIT-` = **포함 + 「우리로 제외」 갈래**((i) 준용 · 🔒 #1-b · 🔒 2026-09-18 사장님 채택 (가)) |
| 4 | 범한퓨얼셀 🔁후속 | 「이번주, 9월 14일 일부 매도 후, **본전가 위협에 따라 본전매도**」(:23) | `(표준형, 60)` | `TP` | 본전 청산만 · 「손실률」 없음 ⇒ `MIX` 아님(post7 해치텍 · 코데즈·레메디·아이티센 전례) | 완결 | **1** | 0 | 후속(post7 #13) — EXIT 주 분모 밖 · **E2 누적 포함** |
| 5 | 원익 🔂재진입 | 「9월 14일, 1차 매수 → 1차,2차 매도 후 나머지 물량은 **직접** 매도처리」(:28) | `(표준형, 60)` | **`MANUAL`** | 「직접」 = 수동 개입 · 우선순위상 `MANUAL` | 완결 | 0 | 0 | 신규(`none` · 🔒 2026-09-18 사장님 채택 · 🔒 #2) |
| 6 | JW신약 | 「9월 1일 자동매매 등록(HDR 60%, 표준형) 1차 매수 → 1차,2차 매도 후 3차는 **직접** 매도」(:33) | `(표준형, 60)` | **`MANUAL`** | 동일 | 완결 | 0 | 0 | 신규(`exact`) |
| 7 | 헥토파이낸셜 🔂재진입 | 「… 1차 매수 후 → 1차 매도 후, **본전 위협에 전량매도**」(:38) + 「**손실률** -2.28%」(:34) | `(표준형, 60)` | **`MIX`** | 「손실률」(= `SL` 판별 근거의 낱말) **∧** 본전 서술 ⇒ post7 한라캐스트 · post6 광전자/삼양 · post4 PS일렉과 **동형** | 완결 | **1** | **1** | 신규(`approx`) |
| 8 | 액스비스 | 「9월 11일, 자동매매 등록(HDR 60%, 표준형) 1차 매수 후 → 1차 매도 후, **본전 위협에 전량매도**」(:43) | `(표준형, 60)` | `TP` | 본전 청산만 · 「손실률」 없음 ⇒ `MIX` 아님 | 완결 | **1** | 0 | 신규(`exact`) |
| 9 | 코데즈컴바인 🔂재진입(3번째) | 「8월말 자동매매 추가 등록 후(**사분위수, 표준형**), 2차 매수 후 → 1~4차 분할매도, 나머지 물량 보유 중」(:47) | 🔴 **`(표준형, 없음)`** | `TP` | 손절·본전·수동 없음 **∧ 형 = 표준형** ⇒ `P8-형변량` 표 1행 · post5 혜인 *「프리셋 Q1~Q3 도 `표준형`」*·post7 서산 전례와 같은 검사 | **미완결** | 0 | 0 | 신규(`approx`) |
| 10 | 우리기술 | 「9월 9일, 자동매매 등록(HDR 60%, 표준형) 1차 매수 후 → 1~4차 분할매도, 나머지 물량은 보유 중」(:53) | `(표준형, 60)` | `TP` | 손절·본전·수동 없음 | **미완결** | 0 | 0 | 신규(`exact`) |

**집계: `TP` 6 · `MANUAL` 3 · `MIX` 1 · `SL` 0 · `unknown` 0.**
신규 7건 = `TP` 4(우리로 · 액스비스 · 코데즈 · 우리기술 — 완결 2 · 미완결 2) + `MANUAL` 2(원익 · JW신약) + `MIX` 1(헥토) · 후속 3건 = `TP` 2(빛과전자 · 범한퓨얼셀) + `MANUAL` 1(로보티즈).
🟢 **`P8-형변량` 신고 줄 = 「미등록 형: 없음」** — 10/10 형 = 「표준형」. `(표준형, 60)` 9 · `(표준형, 없음)` 1.
⇒ `TP` 신규 **4 ≥ 3** ⇒ `EXIT-E1`/`X5`·`E4`·`X6` **판정 가능**(`PREREG_EXIT_V2.md:55`) · `EXIT-X2` 는 **완결** `TP` 신규 **2 < 3** ⇒ ⛔ 보류(`PREREG_POST6.md:807`) · **`SL` 0** ⇒ `EXIT-E3`/`X7` ⛔(누적 1 < 3 · `:810`) · `EXIT-X9` **0건 · 누적 0**.

## 우리로 — 한 항목 · 두 사이클의 `EXIT-` 처리 · 기본값 (가) = 포함 + 「우리로 제외」 갈래(REC/WRC 동결 문형의 «준용») · 🔒 #1-b (🔴 검수 B-1·B-3 · 2차 R-2·Y-c 정정판) · 🔒 2026-09-18 사장님 채택: (가)

- **라벨은 `TP` 로 확정**(판별표·`P8-형변량` 문언을 두 사이클 모두 충족 · 「한 항목 = 한 라벨」). 🔒 확인 대상은 **라벨이 아니라 분모 처리**다.
- 🔑 **이 모양을 직접 다루는 동결 문언은 둘이다 — `REC-`(A-9)와 `WRC-`(`WRC-R5`)**(초판이 놓쳤다 · 초판이 이 모양을 계열의 첫 사례로 적은 것은 **틀렸다**): `RESULTS_RECONSTRUCT_POST5.md:53` **A-9**(*「한켐(post5)은 한 항목 안에 **두 사이클**(본전매도 → 재진입). flag」* · `PREREG_POST6.md:596` 이 A-1~A-11 전부 승계) · `PREREG_WEIGHTED_RECON.md:432-435` **`WRC-R5`**(= `WRC-D6` · 사장님 승인 `:170` — *「**재진입(같은 항목 안 2사이클)** … **포함하되 `구조차단` 플래그를 달고 «제외 민감도»를 의무 인쇄**」*) · `PREREG_POST6.md:282-284` §1-5(*「분모에 넣는다 … 재진입 건을 뺀 값을 항상 나란히 인쇄」*). 라벨 선례: post5 한켐(`MANUAL`)은 항목을 지우지 않고 **「첫 2레그만 `TP`」 민감도를 병기**했다(`LABELS_2026-08-29_post5.md:21`).
- **(가) 기본값 = 포함 + 「항목 내 2 사이클」 표시 + 「우리로 제외」 갈래 의무 인쇄** — 🔴 **`EXIT-` 에 대해선 «동결 문형»이 아니라 (i)(`REC-`·`WRC-`)의 «준용»**이다: 레그 축이라 **두 사다리가 한 시퀀스에 섞이는** 같은 모양의 문제를 안는다(`PREREG_WEIGHTED_RECON.md:435`). 「우리로 제외」를 `P8-갈래계수` 갈래로 **세는** 것(판정 효과 있음)은 (i) 준용의 **귀결** — `WRC-R5` 는 제외 갈래에 판정 효과를 준다(*「이 건이 판정을 가르면 ⇒ ⛔ `WRC-V1`」* `PREREG_WEIGHTED_RECON.md:435`) · §1-5 도 같다(*「두 값이 판정을 가르면 ⇒ 「재진입 의존」」* `PREREG_POST6.md:283-284`). post5 한켐의 *「「첫 2레그만 `TP`」 도 병기 인쇄하되 판정엔 안 씀」*(`LABELS_2026-08-29_post5.md:21`)은 **`MANUAL` 항목의 라벨 독법 민감도**이고 **§1-5 이전**(그 줄 = 커밋 `938687e` 2026-08-29 12:33 · §1-5 = `53ca394` 같은 날 21:31 · git 실측)이라 **이 자리에 해당하지 않는다**(3차 검수 정정). 보강(단위): `PREREG_EXIT_V2.md` 의 「건」은 **원장 행(매매건)** 이다(`:76` 「현재 33건」 = `ledger_trades.csv` 행 수 · `PREREG.md:3`). `:27`(「비증가는 한 사다리 안에서만 뜻이 있다」)은 §1 **「진단 · 검정 아님」**(`:11`) 절 문장이라 정의 절(§2·§3)을 넘지 않는다(`PREREG_POST8.md:679-680` 논거 3 과 같은 구조).
- **(나) 대안 = 주 분모 «밖»** — ⓐ post6 PD-2 4 문형(`PREDECISION_2026-09-04_post6.md:67-68` · 단 **글을 넘는 후속 시퀀스**의 결정이라 모양이 다르다) 또는 ⓑ `PREREG.md:50` 「한 번 더 ⇒ 별개 사이클」 · 레그 배분 불가 ⇒ 「그 건의 시퀀스」 미정의 ⇒ `unknown` 처리(`PREREG_EXIT_V2.md:81-83`) **준용**.
- 🔴🔴 **E4 는 선택에 따라 «개폐가 갈린다»**(🔴 초판의 «주 갈래 선택과 무관하게 E4 는 (가)의 답 · 갈리는 자리는 E1 하나» 서술은 **철회** — 여는 쪽 독법을 사실처럼 적은 재량이었다 · 검수 B-3):
  - **(가) 주**: E4 주 갈래 n = **3** · 제외 갈래 n = 2 < 3 ⇒ `P8-갈래계수`(`PREREG_POST8.md:340` *「최소 n 을 채운 갈래가 1개뿐이면 그 갈래의 답을 그대로 쓴다」*) ⇒ **주 갈래의 답**.
  - **(나) 주 — 근거가 ⓐ 든 ⓑ 든 E4 = `GT-D`**(2차 Y-c): ⓐ(post6 PD-2 4 문형)이면 우리로는 정의상 밖이라 포함 갈래가 **아예 없다**(`PREDECISION_2026-09-04_post6.md:67-68`) ⇒ E4 = n **2 < 3** ⇒ **`GT-D`** · ⓑ(`unknown` 준용)이면 `PREREG_POST8.md:207`(*「`unknown` 은 판정 분모에 «들어가지 않는다» — … 주 분모는 `unknown` 제외, `TP` 포함 갈래는 **민감도 열로만** 인쇄한다」*) 구조로 포함 갈래는 **답을 댈 수 없다** ⇒ E4 = n **2 < 3** ⇒ **`GT-D`**.
  - **(가′) 포함 + 「우리로 제외」는 인쇄만(한켐식)**: 지지에 더 유리 · **근거 약함**(위와 같이 한켐 선례는 이 자리에 해당하지 않는다) — 대안으로만 적는다(🔒 #1-b).
  - **E1**: (가) n 4 · 제외 n 3 — 둘 다 ≥ 3 ⇒ 「우리로 제외」를 갈래로 세면(초안 · (i) 준용의 귀결) 답이 갈릴 때 `GT-C [갈래 의존]`(선언 억제).
- 🔴🔴 **방향 자기신고 — (가) 는 `EXIT-` 에서 «유리한» 쪽이다**: 저자 목록(24.20 → 19.94 → 17 → 16.26 → 13.32 → 12.44 → 0.54)이 **항목 내부 비증가 · 레그 7** 이라 E1·E4 에 유리 · 마지막 레그 **0.54**(`|ret| < 1%`)라 E2 에 유리 · **E4 를 연다**(2 → 3). 🔴 ***유리함은 근거가 아니다*** — 근거는 (i) 동결 문형의 **준용**(두 사다리 = 같은 모양)과 「건 = 원장 행」 단위뿐이다.
- **다른 축은 축별로 다르다**(🔴 2차 초안의 「다른 축도 같은 한 가지 처리」는 과대 적용 · 2차 R-2 · PD-3 ③): **(i) `WRC-`** = 동결 문형(`WRC-R5` · 포함 + `구조차단` + 「우리로 제외」 민감도) · **`REC-`** = A-9 플래그 + `WRC-R5` 유추(두 평단 구조 동일)로 같은 처리 · `WRC-` 는 **`fill_n = 1`** 로 판정 분모 밖(「두 평단」이 사유가 아니다) · **(i′) `ANC-N4` ②** = 해석 여지(초안 살아 있음) · **(ii) 등록일 축(`SEL-`·`REG-`·`Q1-`·`P6-M1′`·`RNK-`·`SEC-`·`S5`) + `LAD-`** = 포함 · 「우리로 제외」는 **인쇄만 · 판정 효과 없음**(재량 · 측정 등록 = 첫 사이클이라 §1-5 재진입 논거가 서지 않는다) · `LAD-` N = 1. 🔒 #1.
- 🔴 사장님이 (나)를 고르면 이 표의 「구분」 칸 · 아래 분모 수 · `INTAKE` §2-13·§5 를 **계산 «전»** 고친다(그때 E4 = `GT-D`). — 🔒 2026-09-18 사장님 채택: **(가)** ⇒ 이 분기는 해당 없음(수 변화 0).

## 약한 결정 (미리 적는다)

- 🔴 **`MANUAL` 3건**(로보티즈 · 원익 · JW신약): 판별표 낱말은 「**수동**」이고 본문 「수동」은 **0**. 「직접」을 「수동 개입」으로 읽는 것은 **post5 가 이미 한 해석**이다(*「한켐 … 「직접」= 수동 개입」* · SK아이이테크놀로지 *「곧바로 직접 매도」* · `LABELS_2026-09-15_post7.md:33`). 그 해석을 **넓히지 않았다** — 본문 「직접」 **7곳** 중 **종목 서술 3곳만** 셌고 산문·홍보 4곳(*「제작자인 제가 직접 써보니」*:73 · *「직접 수집한 통계 데이터」*:75 · *「HTS에서 직접 스윙매매할 때」*:78 · *「직접 확인하고 싶으신 분」*:118)은 제외했다(post6 PD-6 · post7 PD-7 과 같은 잣대).
  🔴 **이번 세 건의 공통 모양**: 「직접」이 **마지막 분할(3차 · 나머지 물량)에만** 걸린다 — 앞 분할은 자동 목표가 매도다. post5 한켐·SK 와 같은 「부분 수동」 계열이며 **라벨 규칙은 부분/전체를 가르지 않는다**(판별표 문언 「「수동」 개입 서술」) ⇒ `MANUAL`.
  🔴 **방향 자기신고**: 신규 `MANUAL` 2건(원익 19.49·17.61·15.54·13.14 · JW신약 16.28·12.68·11.05·10.78)은 **항목 내부 비증가 ∧ 레그 4** 다 ⇒ `TP` 로 읽었다면 E1·E4 에 **유리**했을 건을 빼는 쪽 = **불리한 방향**. 유리한 쪽을 고르지 않았다.
  민감도(판정엔 안 씀): 「`TP` 로 읽을 때」 E1 분모 +2(원익 · JW신약) · E4 +2 병기.
  `LAD-` 축: `RESULTS_LADDER_TRANCHE.md:38` **B-2** carve-out(*「매도 쪽만 수동이면 포함」*) — 세 건 모두 **매수 쪽 수동 서술 없음** ⇒ **LAD 분모 포함**(단 원익은 `none` · 로보티즈는 후속이라 등록일 축 밖 ⇒ 실제 LAD 신규 행은 **JW신약** 1건).
- 🔴 **헥토파이낸셜 `MIX`**: 「손실률」 낱말(:34)과 본전 청산 서술(「본전 위협에 전량매도」 :38)이 **한 항목 안에 둘 다** 있다 ⇒ 판별표 `MIX` 문언의 두 성분(본전 청산 ∧ 「손실률」=분할손절 기전 낱말) 충족 — **post7 한라캐스트와 같은 종류의 약함**(「분할손절」이라고 쓰지는 않았다 · post6 초판의 「분할손절 명시가 정의」 재정의는 verifier 가 철회시켰다 `LABELS_2026-09-15_post7.md:37`).
  `EXIT-E3` 는 `SL` 전용이라 대상 아님 · −2.28 은 관측 인쇄 · `EXIT-X2`·`X8` 은 동결 적용 대상(`TP` 완결) 밖 ⇒ 확장 민감도에서만 「손실률 제외 후 마지막 = 1.39 ≠ 마지막 −2.28」(구분 가능 1건)로 인쇄.
- 🔴 **우리로 완결 판정**: 값 줄에 `0.54% ~` 가 있는데 서술은 「전량매도」·「분할매도 **완료**」다 — **`~` 와 서술 불일치 2번째 사례**(post7 한전기술이 첫). PD-5 대로 **서술 기준(완결)** 을 주로 두고 `~` 기준을 민감도로 병기(`~` 기준 = 미완결 ⇒ X6 로 0.54 제외 ⇒ 6레그 · X2 완결 분모에서 빠짐). 🔴 우리로는 `TP` 라 이 결정이 **E1·X2 분모 모양**에 닿는다 — 영향: X2 완결 분모 (가) 2 ↔ `~` 기준 1(둘 다 < 3 ⇒ ⛔ 불변) · 생략 편향 2/4 ↔ 3/4 · E4 분모 수는 같음(6레그도 ≥ 3).
- 🔴 **범한퓨얼셀 `TP`(후속)**: 「본전가 위협에 따라 본전매도」 = 본전 청산 서술 · 「손실률」 없음 ⇒ `MIX` 아님 · `TP`. 후속이라 **E1/E4/X2 주 분모 밖**(`PREREG_EXIT_V2.md:55` 「신규 건에만」) — 🔴 **계열 첫 사례인 `TP` 후속**이라 라벨이 아니라 이 문언으로 빠진다(PD-2 3).
- **코데즈컴바인 `TP`**: 프리셋에 HDR 숫자가 **없다**(「사분위수, 표준형」) — `P8-형변량` 표 **1행**(형 = 표준형 ∧ 손절 서술 없음)에 걸린다(2행 「형 없이 `HDR<n>` 만」이 아니다). post5 혜인(`Q1~Q3/표준형`)·post7 서산(`Q2~MAX/표준형`)과 **같은 검사 결과**. 🔴 사분위수 관측 n=3 을 `HDR-D2`/`BUY-L4` 변량으로 **승격하지 않는다**(PD-9).
- **`EXIT-X9` 0건 · 두 갈래**: 동결 트리거 = 한켐형 *「**자동으로 본전매도** → **이후** 직접 매도」*(`PREDECISION_2026-09-15_post7.md:196`). 이번 `MANUAL` 3건은 전부 **「자동 «익절» 매도 → 이후 직접 매도」**이고 자동 «본전»매도가 없다 ⇒ **0**. `PREREG_POST6.md:811` 의 가설 이름 「`MANUAL` 안 자동 사이클」로 넓게 읽으면 **3** — 두 갈래를 **나란히 인쇄**하고 판정엔 **0** · 3 을 쓰면 「3건 쌓이면 사전등록」에 **바로 닿는다** ⇒ 0 선택은 **게이트를 여는 쪽이 아니다**.

## `EXIT-X2` 본전매도 레그 식별 (동결 승계 · `PREREG_POST6.md` §1-8 = `EXIT-X6` 미적용 · 저자 «원» 시퀀스의 마지막)

규칙: 「손실률」 레그 제외 → 남은 레그 중 **마지막**. **주 분모 = 신규 완결 `TP`**(`PREREG_POST6.md:807` · §7-C 4):
**(가)** 우리로(**0.54**) · 액스비스(**0.31**) = **2 < 3 ⇒ ⛔ 보류** · 「우리로 제외」 액스비스 1 ⇒ ⛔. 🔴 두 갈래 모두 최소 n 미달이라 `P8-갈래계수` 상 **`GT-D` 예고**(답 없음).
X2 민감도(의무 인쇄): (a) 좁은 분모 = 완결 `TP` ∧ note = **위 2건과 같은 집합**(이번엔 넓은 분모 = 좁은 분모 ⇒ 방향 차이 구성상 0) (b) 후속·`MIX`·`MANUAL` 포함 확장 갈래 = 🔴 **동결 적용 대상(「`TP` 완결 건」) 밖**임을 표기.
**`EXIT-X8`**(X2 가 고른 레그 ≠ 마지막 레그 건 수): 주 분모 안 **0**(「손실률」 레그 없음 · 구성상) ⇒ 「구분 불가」(**4글 연속**) · 확장 갈래(헥토 `MIX`) 포함 시 **1** — 민감도에서만 「구분 가능」.

`EXIT-E2` 원규칙(마지막 레그 · 분모 = `breakeven_note` 건 · **라벨·등록 사건 무관** · post5 한켐 `MANUAL` 전례) — **누적 계열 = post7 누적 14 + 이번 4 = 18**((가) · 「우리로 제외」 17): 우리로(0.54) · 범한퓨얼셀(0.43 · 후속) · 헥토파이낸셜(−2.28 · `MIX`) · 액스비스(0.31). **병기 의무**(`E2`↔`X2` 분모 충돌 승계). **신규만 갈래**(완결 `TP` ∧ note) = 우리로 · 액스비스 **2 < 3** ⇒ 신규 단독 ⛔.
🔴 **우리로의 E2 레그**: 「본전 위협에 전량매도」는 **사이클 1** 의 서술인데 원규칙은 **목록의 마지막 레그(0.54)**를 고른다 — 두 사이클이라 「마지막 = 본전매도 레그」 전제가 **확인되지 않는다**. (가)는 원규칙 축자(0.54) · 「우리로 제외」 민감도에서는 빠진다 — 🔒 #1 과 같은 확인에 묶는다.

## 미완결 처리 (`EXIT-X6` · E1·E4 한정 · PD-5)

서술 기준 미완결 `TP` **2건**(코데즈컴바인 5레그 → 4 · 우리기술 4레그 → 3)의 **마지막 레그 하나 제외** · 제외 후 레그 0 이 되는 건 **0**.
⇒ **E1 분모 = 4**((가) 기본값 · 우리로 7 · 액스비스 2 · 코데즈 4 · 우리기술 3) · 「우리로 제외」 3.
`EXIT-E4`(`TP` ∧ 레그 ≥3 · X6 적용 후) = 우리로 7 · 코데즈 4 · 우리기술 3 = **3건**((가)) · 「우리로 제외」 2 — 🔴 **(가) 주면 `PREREG_POST8.md:340` 으로 주 갈래의 답 · (나) 주면 `:207` 준용으로 `GT-D`**(🔒 #1-b · 초판의 「(가) 답만 셈」은 한쪽 독법이었다).
`~` 문자 기준 갈래(우리로 1건 · 0.54 제외)는 **민감도**(표기 의존).
**생략 편향 비율**(의무 인쇄): 계열 = post4 **2/4** → post5 **4/6** → post6 **3/9** → post7 **3/7** → **post8 2/4**((가) · `TP` 신규 분모) · 「우리로 제외」 **2/3** · 신규 7 기준 **2/7** 병기.

## 범위 서술 (PD-18)

`RANGE_ONLY` **0건**. 우리로의 *「0.54% ~」* 는 **끝값이 없어** 「A% ~ B%」가 아니다 ⇒ **미완결 표지**(post5 한국화장품제조 「11.26% ~」 · post7 한전기술 「-1.70% ~」 형).
🔴 코데즈·우리기술의 *「1~4차」* 의 `~` 는 **차수 표기**라 범위 서술도 미완결 표지도 아니다 — 기계 판별 시 함정(PD-5 2 · post7 서산 `Q2~MAX` 전례의 확장).

## 🔁 후속 3건의 라벨 효력 (PD-2 3번)

`EXIT-E1`·`E4`·`X2`·`X6` 는 **정의상 대상 아님** — 🔴 이번엔 라벨(`TP` 2 · `MANUAL` 1)로 설명되지 않으므로 **`PREREG_EXIT_V2.md:55` 「다음 글의 신규 건에만 적용한다」** 가 근거다(계열 첫 사례인 `TP` 후속) · `EXIT-E2` 누적 분모 = **범한퓨얼셀 포함**(note 1) · 빛과전자·로보티즈는 note 0.
🔴🔴 **연결 시퀀스는 비증가와 양립하지 않는다 — 3/3**: 빛과전자 12.17 → **23.47** · 로보티즈 9.13 → **17.95** · 범한퓨얼셀 7.94 → **20.99**. 세 건 모두 **1차 매수만**(평단 불변 서술)이다 — 라벨과 무관한 구조 사실이며 `INTAKE` §2-6 에 인쇄한다(기록만 · 원인 해석 안 함 · 판정에 안 씀). 🔴 방향: 연결을 안 넣는 것 = E1 **유리** · post8 단독 레그(항목 내 비증가)를 안 넣는 것 = E1 **불리** — «혼합».

## 원장 플래그 (PD-17 단일 축)

`stoploss_plan` = **0**(10행 전부 · 「분할손절」 서술 0) · `manual_exit` = 로보티즈 · 원익 · JW신약 **1** · `breakeven_exit` = 우리로 · 범한퓨얼셀 · 헥토파이낸셜 · 액스비스 **1** · 라벨 SSOT = `narrative` 첫 토큰 「라벨 X」 · `is_loss` = 헥토 −2.28 **1** · `leg_open_ended` = 우리로 0.54 · 코데즈 마지막(6.59) · 우리기술 마지막(4.75) **1**.
🔴 **`stoploss_plan` 으로 라벨 건수를 세지 말 것** — 라벨 집계 SSOT 는 이 문서다.

[[PREDECISION_2026-09-18_post8]] · [[INTAKE_2026-09-18_post8]] · [[LABELS_2026-09-15_post7]] · [[LABELS_2026-09-04_post6]] · [[LABELS_2026-08-29_post5]] · [[LABELS_2026-08-22_post4]] · [[PREREG_POST8]] · [[PREREG_POST6]] · [[PREREG_EXIT_V2]]
