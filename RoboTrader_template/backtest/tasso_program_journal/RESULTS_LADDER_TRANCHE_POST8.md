# 결과 — 사다리 정렬 축 **8번째 글 누적 재계산** (`LAD-T1`·`T2`·`T3` · `LAD-P1`~`P3`)

**실행 2026-09-24 00:51 KST** · 사전등록 `PREREG_LADDER_TRANCHE.md`(**2026-08-22 동결**)
승계 `PREREG_POST6.md` §1-4·§1-5·§1-6·§2-4 · §4 표 **#21~#24** · 🆕 **`PREREG_POST8.md`(동결 `04cd785` · 첫 구속 회차)** `D-1`·`D-3`·`D-5`·`D-7`·`D-9`
해석 결정 `RESULTS_LADDER_TRANCHE.md` §1 **B-1~B-8** · 수집 후·계산 전 결정 `PREDECISION_2026-09-18_post8.md`
**PD-1**(창) · **PD-2**(후속 제외) · **PD-3**(재진입 · 우리로 🔒 #1-(ii)) · **PD-4**(`exact` 분모 · 창 규약) · **PD-12**(post7 절단 값 대체) ·
**PD-13**(`first_only`) · **PD-19**(D-1) · **PD-21**(D-3) · **PD-23**(D-5) · **PD-25**(D-7) · **PD-27**(D-9 착수 조건 · 혼합 빈티지) ·
항목표 `INTAKE_2026-09-18_post8.md` §1·§2-10·§2-11·§5 · 라벨 `LABELS_2026-09-18_post8.md`(B-2 carve-out)

표본 = **누적 32건**(post4 6 + post5 6 + post6 10 + post7 6 + **post8 주 표본 4**) ·
숫자 원본 = [`RESULTS_LADDER_TRANCHE_POST8_NUMBERS.md`](RESULTS_LADDER_TRANCHE_POST8_NUMBERS.md)
(`run_ladder_tranche_post8.py` 생성 · 통계 핵·표 포맷은 `run_ladder_tranche.py`, 가드·판정 함수는 `run_ladder_tranche_post6.py`,
누적 목록 28건·건별 원표 함수 `build_rows` 는 `run_ladder_tranche_post7.py` 에서 **import** — **세 파일 다 수정하지 않았다**)
🟢 **세 번 실행 byte 동일**: 🆕 정정 1차 재생성(A-B2 · 02:36:03 · 02:36:34 · 02:37:05 KST)의 `_NUMBERS.md` 작업트리(CRLF) md5 = `5aa92f6d7d62c7b78c187b205ad13c33` · 블롭(LF) md5 = `4d23254e65e221774ee9ecefa63c7602` × 3 (`README.md:95` — 무엇의 md5 인가를 적는다 · `core.autocrlf=input` 이라 블롭 = LF) · ① 칸은 지문 동일이라 최초 실행 00:51:38 을 잇는다 · 초판(00:51:38 · 00:51:48 · 00:51:59) 작업트리 md5 `cc6ebcd5…` 는 정정 전 값(§7-3 `approx` 행 + (나)4 줄만 다르다).
`δ` = **1.0%p** · `NULL_SEED` = **20260815** · 순열 표본 **200,000** · 판정 문턱 **누적 비교가능 쌍 40** — 🔴 **넷 다 동결 그대로 손대지 않았다.**

🔴 **창 종료 2026-09-18 = 발행 당일(금 · 거래일) 봉 «포함» · B-1 · ANC §2-1 `END` · 전 축(`WRC-` 포함) · PD-1.**
🔴 **실행 시 `max(date)` = 2026-09-23 · 그 날짜 행수 2,764 — 기록만(창 아님).** 창 종료 2026-09-18 행수 **2,764**.
🔴 **라이브 채택 대상이 아니다**(`PREREG.md` §0 2번 · `PREREG_POST8.md` §0-1) — 이 산출물은 **기록**이지 전략 후보가 아니다.
🔵 접두 규약: `T1`·`T2`·`T3`·`P1`~`P3` 은 전부 **`LAD-`** 축 · 플래그·가드는 **`P6-`** · 이번 회차 규약은 **`P8-`**.
🔴 **등급 이름은 한 개도 적지 않는다** — 등급은 `INTAKE_2026-09-18_post8.md` §6 단계에서 매긴다(PD-16·PD-28).
🔴 **숫자는 전부 `_NUMBERS.md` 에서 옮겨 적었다 — 이 문서에서 새로 계산한 값은 0 이다.**

---

## §0. 판정 표 — 예측 ID · 예측 문언 · 관측값 · 판정 (`PREREG_POST6.md` §4 #21~#24)

| 예측 ID | 예측 문언(동결) | 관측값 | 판정 |
|---|---|---|---|
| (게이트) | *「누적 비교가능 쌍이 `40` 이상이 될 때까지 판정하지 않는다」*(`PREREG_LADDER_TRANCHE.md:115`) | 창5 누적 **327쌍** ≥ 40 | **열림** — 판정한다 |
| **`LAD-T1`** | 차수 ↔ 낙폭(창5) 정렬 — *「누적 표본에서 `p < 0.05`」*면 지지(§5-2) | `V` = **161/327** · 귀무 평균 164.81 · **`p` = 0.4567** | **불성립** — `p` ≥ 0.05. 차수는 낙폭에 대해 무작위 배정과 구별되지 않는다(게이트가 열린 뒤 4회차 연속 · post5~post8) |
| **`LAD-T2`** | `DD/σ₂₀` 정규화축 `p` 가 원축보다 «낮으면» 가설 B 지지(§5-2) | 정규화 **0.7689** ≥ 원축 **0.4338**(같은 30건 · 같은 쌍 289) | ⛔ **불성립**(가설 B 지지 없음) — 부호만 본다 · post6·post7 과 같은 부호(3글 연속) |
| **`LAD-T3`** | 경과 거래일 축 `p < 0.05` 면 **T1 영구 취소**(§5-2) | `p` = **0.2372**(143/329) | **미발동** — 취소 사유 없음. 부호 감시: T3(0.2372) < 창5(0.4567) — **4글 연속 유지** |
| **`LAD-P1`** | 신규 건에서 저자가 체결 차수를 명시(§5-3 · 기록) | **7/7**(분모 = DB 있는 신규 건 7 · 항목 10 중 명시 10) | **성립** — 5글 연속(6/6 → 6/6 → 10/10 → 10/10 → 7/7) · 기록이지 기각 사유 아님 |
| **`LAD-P2`** | 신규 건 `DD`(창5) 중앙값 15%~35%(§5-3 · 기록) | 주 분모 `exact` 4 = 23.18 · 21.40 · 24.19 · 17.87 ⇒ **중앙값 22.29%** | **성립**(구간 안 · 하한까지 +7.29%p) · 기록 |
| **`LAD-P3`** | `first_only` 건 ≥ 1(§5-3 · 기록) | 신규 **6건**(그중 `exact` 4) | **성립** — 0 → 1 → 3 → 6 → **6** · 기록 |
| `P6-창5절단가드-A` | 절단 건 / **이번 글 신규 건** ≥ 1/3 ⇒ 판정 불가(PD-11 3번 · `PREREG_POST8.md` §7) | 주 **0/7 = 0.0%** · `exact` **0/4 = 0.0%** | **미발동** · 분모 갈래: **갈리지 않음** · 🆕 2회 연속 계수 **1회차(미도달)** |
| `P6-창5절단가드-B` | 절단 제외 민감도가 `LAD-T1` 판정을 뒤집으면 판정 불가 | 누적 풀 절단 **0** ⇒ 제외본 = 주 표본 **항등** · 대체 전/후도 **161/327 · 0.4567** 로 같다 | **미발동**(구성상 · 「창5 절단 의존」 없음) |
| 재진입(§1-5 · B-7) | 재진입 제외 값이 판정을 가르면 「재진입 의존」 | 제외본 27건 창5 `V` **111/227** · `p` **0.4410** | 판정이 **갈리지 않는다**(불성립 = 불성립) · post8 몫은 **항등**(exact 안 재진입 0) |

🔑 ***게이트가 열린 네 번째 회차에서도 「차수는 낙폭을 따라가지 않는다」가 나왔다*** — 표본 28 → 32건, 쌍 266 → 327 로 늘었는데
`p` 는 0.4064 → **0.4567** 로 무작위 영역 한가운데다(`V_obs` 161 ↔ 귀무 평균 164.81).
🔑 ***이번 글이 누적에 더한 쌍 61 은 전부 «옛 건과의 쌍»이다*** — post8 주 표본 4건이 **전부 N = 1** 이라 그 글 «단독» 쌍은 **0** 이다(§3).
⚠️ **`p` 의 오르내림(0.5160 → 0.4914 → 0.4064 → 0.4567)을 추세로 읽지 말 것** — 전부 0.4~0.6 이고 표본이 매회 통째로 커진다.

---

## §1. 계산 «전»에 고정된 해석 결정 — **전부 승계 · 신설 0**

| # | 이번 회차에서 실제로 걸린 자리 |
|---|---|
| **B-1** | post8 발행 **09-18(금) = 거래일** ⇒ 창A 끝 = **2026-09-18(발행 당일 포함)**. post6(09-04 금)과 **같은 갈래**이고 post4·post5·post7(토 발행)과 **다른 갈래**다. 옛 글 행의 창A 는 **그 글의 발행일**에서 끝난다(원표 「창A 끝」 칸이 토요일 날짜로 찍히는 것은 post7 판 표기 그대로 · 데이터는 직전 거래일까지) |
| **B-2** | 🔴 **`MANUAL` 3건은 라벨 때문에 빠지지 «않는다»** — JW신약은 매수 쪽 수동 서술이 없어 **포함**, 원익은 **`none`** 이라, 로보티즈는 **후속**이라 빠진다(`LABELS_2026-09-18_post8.md`: *「실제 LAD 신규 행은 **JW신약** 1건」*) |
| **B-3** | T3 는 `δ_E` = 0(정확히 같은 값만 제외) — 그대로 |
| **B-4** | σ₂₀ 결측 **2건**(post5 삼양바이오팜 · post7 해치텍) ⇒ T2 표본 **30건**. post8 4건은 전부 σ₂₀ 있음(액스비스 DB 첫 봉 04-13 이라도 21종가는 찬다) |
| **B-5** | 서로 다른 배정 **291,117,562,633,497,600** > 200,000 ⇒ 시드 고정 200,000 표본 |
| **B-6** | `E` = `[D, 발행일]` 거래일 수 — post8 주 표본 = 우리로 6 · JW신약 14 · 액스비스 6 · 우리기술 8 |
| **B-7** | 2번째 사이클 = 한켐·코데즈컴바인(post5) + 지투파워·현대약품(post6) + 빛과전자(post7) = **5건** · 🔴 **post8 은 `exact` 안 0**(원익 `none` · 헥토·코데즈 `approx`) ⇒ 제외 민감도의 post8 몫은 **항등** |
| **B-8** | 창3·창5 의 「D+k거래일」은 그 종목의 봉으로 — post8 신규 4건 **전부 창5 5봉 완전** |
| **PD-2** | 후속 **3건**(빛과전자·로보티즈·범한퓨얼셀)은 **신규 분모 밖**(이중계상 금지 · 그 등록 사건은 post7 행이 들고 있다) |
| **PD-3 · 🔒 #1-(ii)** | **우리로 = 항목 내 2 사이클**(post5 한켐 A-9 이후 두 번째) — **포함 · 「우리로 제외」 값은 «인쇄만»**. 측정 등록(09-11)이 사이클 1 이라 §1-5 재진입 논거가 서지 않는다(`P6-PRIOR_CYCLE_IN_WINDOW` = 0) |
| **PD-4** | 🔴 **주 표본 = 신규 ∧ `exact` 4건 — 전부 N = 1** · `approx` 2건은 **갈래별 민감도**(§6) · `none` 1건은 **축 밖** |
| **PD-12** | post7 절단 2건(빛과전자·범한퓨얼셀) + 지투파워 `approx` 3갈래의 창5 가 완전해져 **값 대체**(§5-4) |
| **PD-13** | `REC-Z4` 의 「즉시 `L` 판정」 귀결은 🔒 결정 ④로 **소멸** ⇒ `first_only` **건 수만** 기록(§7) |
| **PD-27** | 착수 조건 = 09-21 sweep 뒤 · 행수 3회 안정 — 관리자 실측(2026-09-24 00:07:44 KST) + 이 레인이 ②③④ 를 **직접 다시 읽었다**(§2) |

🔴 **어느 것도 값을 본 뒤에 고치지 않았다.** 🔴 **새 예측을 만들지 않았다.**
🔴 **재량 1건**(§2 ① 읽은 시각의 byte 재현 처리) — §8 1번에 신고한다.

---

## §2. 🆕 `D-9` — 읽은 시각 · 빈티지 · 혼합 빈티지 신고 (`PREREG_POST8.md` §9 · PD-27)

| 의무 | 값(`_NUMBERS.md` §0) |
|---|---|
| ① 쿼리 실행 시각(KST) | **2026-09-24 00:51:38**(첫 실행 · 재실행 00:51:48·00:51:59 는 ② 지문이 같아 이 값을 이었다) |
| ② 창 구간 `max(updated_at)` | 공통 창 `[07-24, 09-18]`(전 종목 109,179행) `max` **2026-09-23 15:46:25.855518** · `min` **2026-09-23 15:45:10.623955** · 이 축이 읽은 봉(`[07-06, 09-18]`)도 같은 두 값 |
| ③ 09-18 봉 | **「09-18 봉은 D+1(09-21) sweep 이후 읽음」** — 09-18 행 `min(updated_at)` 2026-09-23 15:45:10 ≥ 09-21 15:35: **예** |
| ④ 창 구간 `min(updated_at)` | **「창 구간 `min(updated_at)` = 2026-09-23 15:45:10 ≥ 09-21 15:35: 예」** — 🔴 기록이다(통과 조건 아님 · `updated_at` 은 일괄 갱신값이라 빈티지 «증거»가 아니다 · PD-27 (라)) |
| `H`·`L`·`L₅` 정의 | `daily_prices.high/low` 의 **D+1 안정 빈티지**(시간외분이 들어간 채로 · `P8-빈티지`) · 「정규장만」 갈래 **열지 않음** · `adj_factor` 산술 **0** |

**⑤ `P8-혼합빈티지신고` — `LAD-` 창5 걸침 8줄**(실측 재계수 = PD-27 (바) 표와 **일치**):

- 우리로(post8): 창 `[2026-09-11, 2026-09-17]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 1 봉 / 후 4 봉 · 혼합 빈티지
- 액스비스(post8): 창 `[2026-09-11, 2026-09-17]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 1 봉 / 후 4 봉 · 혼합 빈티지
- 우리기술(post8): 창 `[2026-09-09, 2026-09-15]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 3 봉 / 후 2 봉 · 혼합 빈티지
- 빛과전자(post7 대체): 창 `[2026-09-08, 2026-09-14]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 4 봉 / 후 1 봉 · 혼합 빈티지
- 범한퓨얼셀(post7 대체): 창 `[2026-09-09, 2026-09-15]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 3 봉 / 후 2 봉 · 혼합 빈티지
- 지투파워(post7 `approx` D=09-08 대체): 창 `[2026-09-08, 2026-09-14]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 4 봉 / 후 1 봉 · 혼합 빈티지
- 지투파워(D=09-09): 창 `[2026-09-09, 2026-09-15]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 3 봉 / 후 2 봉 · 혼합 빈티지
- 지투파워(D=09-10): 창 `[2026-09-10, 2026-09-16]` 은 제도 경계 2026-09-14 를 걸친다 — 경계 전 2 봉 / 후 3 봉 · 혼합 빈티지

- JW신약 창5 `[09-01, 09-07]` 은 **안 걸친다**. 참고 병기(의무 목록 밖): 민감도 창 창A 는 post8 4건 전부, 창3 은 우리로·액스비스가 걸친다(`_NUMBERS` §0-2).
- 🔴 **방향 추론(「`H` 를 높이고 `L` 을 낮춘다」)은 실측이 아니다** — 인용하지 않는다(`PREREG_POST8.md` §9 (마)).
- 🔴 **누적 풀이 이제 «두 제도»를 섞는다** — 분모를 쪼개지 않았다(쪼개려면 새 사전등록 · `PREREG_POST8.md` §14).

---

## §3. 🆕 `D-1` — 비교가능 쌍 «세 수» (`ANC-P3` 게이트 분모 · PD-19)

| 수 | 값 |
|---|---|
| ① 그 글 «단독»(post8 주 표본 4건끼리) | **0** — 차수 [1, 1, 1, 1] |
| ② 누적(창5 · 동률 대역 제외 후 · 32건) | **327** |
| ③ 게이트 판정에 쓴 수(= ②) | **327** ≥ 40 ⇒ 열림 |

**누적 비교가능 쌍 = 327** · **그 글 열 비교가능 쌍 0(구성 · exact 4건 전부 N=1)**.

- 🔑 ***①이 0 인 것은 값을 보고 닫은 것이 아니라 저자의 체결 차수 서술이 «구성»으로 닫은 것이다*** — `approx`(코데즈 N=2)를 넣어 그 글 열을 채우는 경로는 쓰지 않는다(PD-21).
- 🔴 이 세 수는 `ANC-` 레인이 `ANC-P3` 게이트에 쓰는 수다. `ANC-P3` 판정은 그 레인이 낸다.
- post8 행이 낀 쌍(= 누적 327 − post7 풀 266) = **61** — ①과 **다른 수**다(①은 post8 끼리, 이 61 은 post8 × 옛 건).

---

## §4. 주 판정 `LAD-T1` — 창5 · 순열 귀무 (§4-5 의무 1~3)

| 축 | `V_obs` | 비교가능 쌍 | `δ` 로 버린 쌍 | 귀무 평균 `V` | **`p = P(V ≤ V_obs)`** | `P(V=0)` |
|---|---|---|---|---|---|---|
| 창3 `[D,D+2]` | 171 | 315 | 74 | 159.92 | **0.6557** | 0.00000 |
| **창5 `[D,D+4]` (주)** | **161** | **327** | 61 | **164.81** | **0.4567** | 0.00000 |
| 창A `[D,발행일]` | 149 | 324 | 64 | 163.65 | **0.3164** | 0.00000 |

### 4-1. 계열 — 🆕 post4~post7 풀을 **같은 스냅샷에서 다시 읽었다**(옮겨 적기 아님)

| 회차 | 표본 · 창 종료 | 이 실행의 재계산 | 그 회차 인쇄값 | 재현 |
|---|---|---|---|---|
| post4(1회차 · δ 현행 규칙) | 6건 · 08-28 | 4/7 · 0.6664 | 4/7 · 0.6664 | ✅ |
| post5 | 12건 · 08-28 | 21/41 · 0.5160 | 21/41 · 0.5160 | ✅ |
| post6 | 22건 · 09-04 | 80/161 · 0.4914 | 80/161 · 0.4914 | ✅ |
| post7 | 28건 · 09-11 | 127/266 · 0.4064 | 127/266 · 0.4064 | ✅ |
| **post8(이번)** | **32건 · 09-18** | **161/327 · 0.4567** | — | — |

- 🟢 ***네 회차의 인쇄값이 09-24 스냅샷에서 한 자리도 안 틀리고 재현됐다*** — 그리고 원표의 옛 28행을 post7 `_NUMBERS` 원표와 칸별로 대조하면 **26행은 모든 칸(`H`·min_low·DD3/DD5/DD_A·`E`·σ₂₀)이 같고**, 대체 2건(빛과전자·범한퓨얼셀)은 **창5 봉수만 4·3 → 5 로 늘고 값은 같다**(이 레인의 칸별 대조 · 산문 확인 — `_NUMBERS` 에는 없는 대조다). ⇒ 09-23 sweep 의 전 행 `updated_at` 일괄 갱신은 **이 축이 읽는 옛 값을 움직이지 않았다**. 🔴 이 재계산은 **탐색·재현 확인**이다(소급 = 탐색 · 옛 판정을 바꾸지 않는다).
- `P(V=0)` = **0.00000**(세 창) ⇒ 「공통 사다리 성립/기각」은 판별력이 없는 참이다(5회 연속).
- §1 의 부호 반전(창3 나쁨 / 창5·창A 나음) 기계 판정은 이번 **「재현」**이다(창3 0.6557 > 0.5 > 창5 0.4567). 🔴 **0.5 문턱의 knife-edge 조건**(post6 §9 1번 신고 · post7 은 「재현되지 않음」)이라 증거로 읽지 않는다.

---

## §5. 반증축 · 민감도

### 5-1. `LAD-T2`(σ₂₀ 정규화) — 표본 30건 · 같은 쌍 289

| 축 | `V_obs` | 비교가능 쌍 | **`p`** |
|---|---|---|---|
| 원축 `DD` | 141 | 289 | **0.4338** |
| **정규화축 `DD/σ₂₀`** | 164 | 289 | **0.7689** |
| 정규화축(부수: `δ_norm` = 0.1478 직접 적용) | 180 | 313 | **0.8120** |

- 정규화 `p` ≥ 원축 `p` ⇒ **가설 B 지지 없음** — post6(0.7626 ≥ 0.5185)·post7(0.6361 ≥ 0.3947)과 **같은 부호 · 3글 연속**. 🔴 차이 자체는 검정된 적이 없다 — 부호만 본다.

### 5-2. `LAD-T3`(경과 거래일) — **미발동** · 부호 감시

| 회차 | T3 `p` | 창5 원축 `p` | 부호 |
|---|---|---|---|
| post5 | 0.2008 | 0.5160 | T3 가 낮다 |
| post6 | 0.1909 | 0.4914 | T3 가 낮다 |
| post7 | 0.1182 | 0.4064 | T3 가 낮다 |
| **post8** | **0.2372** | **0.4567** | **T3 가 낮다 — 4글 연속** |

- `p` = 0.2372 ≥ 0.05 ⇒ **`LAD-T1` 영구 취소 조항 미발동**(취소 축이라 ✅ 를 쓰지 않는다). ⚠️ post7 의 0.1182 에서 **올라갔다** — 0.05 쪽으로 «다가가는 중»이라는 읽기는 이번 값으로도 뒷받침되지 않는다.

### 5-3. 🆕 `D-7` · `P6-창5절단가드-A` — 두 산술 (`PREREG_POST8.md` §7 · PD-25)

| 분모 갈래 | 산술 | 판정 |
|---|---|---|
| **주 — 이번 글 신규 건 7** | **0/7 = 0.0%** vs 33.3% | **미발동** |
| `exact` 분모 갈래 4(의무 민감도) | **0/4 = 0.0%** vs 33.3% | **미도달** |

- **분모 갈래 신고**: *「주 분모 0/7 · `exact` 분모 0/4 · 문턱 1/3」* — **분모 갈래: 갈리지 않음.**
- 🆕 **2회 연속 계수 1회차(미도달)** — post8 이 1회차다. 🔴 **post7 의 2/6 = 33.3% 도달은 「참고 전건」**이고 계수에 세지 않는다(소급 금지 · `PREREG_POST8.md` §7 (나) 4).
- 대칭 단언: 이 조항은 가드를 «켜는» 분모 전환을 닫았다 — 그 대신 `exact` 갈래가 2회 연속 문턱에 닿으면 재료로 올라간다. 이번 회차는 **어느 분모로도 0** 이라 그 경로는 시험되지 않았다.

### 5-4. post7 절단 값 «대체» — 대체 전/후 `V`·`p` (B-5 3번 의무)

| 종목 | post7 「절단 시점 값」(인용) | 09-11 로 다시 읽음 | post8(대체) | 차이 |
|---|---|---|---|---|
| 빛과전자 09-08 | 4봉 / **20.33%** | 4봉 / 20.33% ✅ | 5봉 / **20.33%** | ±0.00%p |
| 범한퓨얼셀 09-09 | 3봉 / **6.42%** | 3봉 / 6.42% ✅ | 5봉 / **6.42%** | ±0.00%p |

| 창5 누적 | `V` / 쌍 | `p` | 판정 |
|---|---|---|---|
| 대체 «전»(절단 시점 값) | 161 / 327 | 0.4567 | 불성립 |
| 대체 «후»(= 주 판정) | 161 / 327 | 0.4567 | 불성립 |

- ⇒ **판정이 갈리지 않는다 — 「창5 절단 의존」 없음.** 🟢 두 건 다 5번째 봉까지 저가가 앞 봉의 최저를 못 깼다(post7 의 post6 대체 전례와 같다). 🔴 ***절단 시점 값이 «언제나» 맞는다는 근거가 아니다*** — 아래 지투파워 갈래가 반례다.
- 지투파워(post7 `approx` 갈래 · 판정 밖 민감도): D=09-08 16.39 → **16.39** · D=09-09 10.96 → **15.40**(+4.44%p) · D=09-10 9.98 → **14.55**(+4.57%p). 🔴 **절단 시점 값이 크게 움직인 자리가 실재한다**(그래서 대체 규약이 있다). 인쇄만 한다.
- 🔴 `PREREG_LADDER_TRANCHE.md` §4-2 B-5 2번(*「옛 값은 산출물에 남기지 않는다」*)과 3번(대체 전/후 `V`·`p` 의무 인쇄)·5번(「절단 시점 값」 표기 유지)은 문언상 긴장이 있다 — **옛 값은 «인용·표기»로만** 남기고 누적 계산에는 대체값만 넣었다(post7 판 선례 · 모호 · §8).

### 5-5. 재진입(B-7) 제외 — 27건

| 축 | `V_obs` | 비교가능 쌍 | **`p`** |
|---|---|---|---|
| 창3 | 119 | 223 | 0.6203 |
| **창5(주)** | **111** | **227** | **0.4410** |
| 창A | 103 | 228 | 0.3073 |
| T3 | 106 | 227 | 0.4082 |

- 제외본 게이트 227 ≥ 40 · 판정 **불성립** = 주 판정 ⇒ **「재진입 의존」 아님.** 🔴 post8 은 제외 대상에 **한 건도 더하지 않았다**(exact 안 §1-5 재진입 0) — 이 민감도의 post8 몫은 항등이다.
- `P6-PRIOR_CYCLE_IN_WINDOW`(이번 글): 우리로 **0** · 원익 계산 안 함(`none`) · 헥토 **2/7 갈래 1** · 코데즈 **7/7 갈래 1** — 플래그 1 은 **`approx` 갈래에만** 있다.

### 5-6. 창5 절단 제외 — **항등**

누적 풀 32건 중 창5 절단 **0** ⇒ 제외본 = 주 표본. **구분 불가(항등)임을 명시**하고 다른 정의로 대체하지 않는다.

### 5-7. 「우리로 제외」 — **인쇄만 · 판정 효과 없음**(🔒 #1-(ii))

31건 창5 `V` = 151/314 · `p` = **0.4189**(참고: 불성립) · `LAD-P2` 3건 중앙값 **21.40%**(구간 안). 🔴 **어느 판정·계수에도 쓰지 않는다.**

---

## §6. `approx` 2건 — **갈래별 값** (PD-4 2번 · 판정 언어 금지)

| 건 | N | 갈래 | DD5 범위(갈래 7개) | 창5 절단 갈래 | 비고 |
|---|---|---|---|---|---|
| 헥토파이낸셜 | 1 | 「8월말」 08-21~08-31 | **6.68% ~ 17.29%** | 0/7 | 🔂 재진입(직전 08-28) · 🔴 **모순 갈래 5/7**(08-21~08-27 은 「한번 더」와 논리 모순 — 좁히지 않고 표시만) |
| 코데즈컴바인 | 2 | 〃 | **5.47% ~ 20.54%** | 0/7 | 🔂 재진입 3번째 사이클(직전 08-21 · 그 갈래는 같은 날) |

- 🔑 ***날을 고르면 이 축의 주 변수가 갈래에 따라 크게 흔들린다***(헥토 6.68~17.29 · 코데즈 5.47~20.54) — 그래서 고르지 않고 전 갈래를 인쇄했다(값은 `_NUMBERS` §7-1).
- 🔴 **이 표는 판정이 아니다**(`RNK-D5` · `PREREG_S5_FUND_NEWS_OOS.md` §1-1 · `PREREG_ANCHOR_REDESIGN.md` §2-3). §5-5 재진입 민감도와 겹치지만 **합치지 않는다**.
- 🆕 **`D-3` 신고 줄**: *「`approx` 포함 시 최소 n 이 차는 축: 없음 · `exact` 분모 4 / `approx` 포함 분모 6」* — `LAD-` 의 최소 n(누적 쌍 40)은 `exact` 만으로 이미 327 이다. (참고 · `ANC-P3` 그 글 열: `exact` 0쌍 / `approx` 포함 ≤ 5쌍 — 판정에 쓰지 않는다)

---

## §7. 🆕 `D-5` · `P8-갈래계수` — 갈래마다 `(갈래 이름, n, 답)` (PD-23 `LAD-` 행)

최소 n = **누적 비교가능 쌍 40**(동결값 그대로).

| 갈래 | n | 답(`LAD-T1` · 창5) | 계수 |
|---|---|---|---|
| **주**(누적 · 대체 후) | 32건 · 327쌍 | **불성립**(`p` 0.4567) | 계수 |
| 창5 절단 제외 | 32건 · 327쌍 | **불성립** | **항등**(절단 0) |
| 재진입 제외(B-7) | 27건 · 227쌍 | **불성립**(`p` 0.4410) | 계수 |
| post7 대체 «전» | 32건 · 327쌍 | **불성립**(`p` 0.4567) | 계수(B-5 3번) |
| 「우리로 제외」 | 31건 · 314쌍 | 불성립(`p` 0.4189) | 🔴 **인쇄만 — 세지 않는다**(🔒 #1-(ii)) |
| `approx` 포함(주 32건 + 헥토·코데즈 갈래 1개씩 · 7×7 = 49 조합) | 34건 · 364~374쌍 | `p` 0.3235~0.4027 · 주 갈래와 같은 쪽 49/49(판정 언어 없음) | 🔴 `D-3` (나)4 검사용 — 두 갈래 모두 최소 n(40) 충족 · **갈리지 않음** |

⇒ **최소 n 을 채운 계수 갈래 3개가 전부 「불성립」 — 갈리지 않는다 ⇒ 주 갈래의 답을 그대로 쓴다.**
🔴 **`D-3` (나)4 검사**(`PREREG_POST8.md:250`): `approx` 포함 49조합 · 쌍 364~374(전 조합 ≥ 40) · `p` 0.3235~0.4027 ⇒ 주 갈래와 같은 쪽 **49/49** ⇒ **갈리지 않는다 — 판정 불변**(NUMBERS §7-3). 🔴 **정정 1차(verifier A B2)** — 초판은 이 행의 n·답을 「갈래별 건 값 · — · `D-3` 우선」으로 비워 `PREREG_POST8.md:342`(세 쪽 중 하나라도 빠지면 무효)를 어겼다. 「`D-3` 우선」은 `approx` 로만 최소 n 이 차는 경우(`:253` · PD-23)의 규칙이라 두 갈래가 모두 최소 n 을 채운 이 경우엔 해당하지 않는다 ⇒ 스크립트가 풀 검정(동결 `pairset`·`statV`·`permute_null` · 같은 시드)을 인쇄하게 고쳐 **재생성**했다(가분성 `:94-96` = 인쇄 보충 · 판정·문턱·분모 불변).
🔑 대칭 단언: 「우리로 제외」를 계수했더라도 답은 같았다(불성립) — 🔒 #1-(ii) 가 이번 회차의 답을 움직이지 않았다는 사실을 적어 둔다(근거가 아니라 기록).

---

## §8. 모호 지점 · 재량 · 한계

- 🔴 **`--rerun` 한계 — 다음 sweep 뒤**(🆕 정정 1차 · verifier B 공통) — 다음 D+1 sweep(예정 **2026-09-28 15:35**)이 창 구간 `updated_at` 을 일괄 갱신하면 D-9 ②(`max(updated_at)`)·DB 지문이 **반드시** 바뀐다 ⇒ 그 뒤 `regen_gate.py --rerun` 은 이 산출물에서 byte 불일치를 낸다 — 값이 틀렸다는 뜻이 아니라 동결 규칙(D-9 ② 인쇄 의무)의 귀결이다(byte 재현은 «같은 스냅샷» 안에서만 성립).
1. 🔴 **재량 — ① 쿼리 실행 시각과 byte 재현의 충돌**: `PREREG_POST8.md` §9 (나) 2 는 ① 실행 시각(KST)을 **산출물에** 박으라 하고(없으면 무효), 계열 규약은 **재실행 byte 동일**(`regen_gate.py --rerun`)을 요구한다. 벽시계는 재실행마다 달라 둘을 동시에 만족할 수 없다. ⇒ **① = 이 파일을 처음 만든 실행의 시각**을 박고, 재실행은 **② 지문 줄(창 구간 `max/min(updated_at)`·행수·`max(date)`)이 byte 같을 때만** 그 값을 잇는다 — 지문이 움직이면 새 시각을 박는다. 각 실행의 실제 시각은 stdout `[run]` 줄에 찍힌다(00:51:38 · 00:51:48 · 00:51:59). 🔴 **대안**: ① 을 산출물 밖(stdout·별도 파일)에만 두면 byte 재현은 되지만 §9 (나) 2 의 「산출물 인쇄」를 어긴다. 관리자 판단 사안이다.
2. **B-5 2번 ↔ 3·5번 문언 긴장**(§5-4) — 옛 값은 인용·표기로만 남겼다(post7 선례).
3. **우리로(항목 내 2 사이클)** — 이 축은 레그를 쓰지 않고 등록 사건의 N·창5 만 잰다 ⇒ 「두 평단」 문제가 이 축에는 서지 않는다. 「우리로 제외」는 인쇄만.
4. **헥토 모순 갈래** — 창 규약을 좁히지 않았다(`PREREG_POST6.md` §1-4 「자유도가 가장 작은 규약」).
5. 🔴 **제도 경계 이후 봉을 읽는 창이 처음 누적에 들어왔다**(§2 ⑤ 8줄) — `overtime_daily` 는 09-14 `ovtm_vol>0` **0** · 09-15 이후 행이 없어 시간외분을 `H`·`L` 에서 **뺄 수 없고**, 15:30 마감 분봉 소실로 정규장 상한을 **만들 수 없다**(관리자 실측 인용). ⇒ 「정규장만」 갈래를 열지 않았다.
6. `H` = 등록일 고가는 가정 — `ANC-` 레인 산출물과 함께 읽어야 한다.
7. `V` 는 개수이고 귀무에서 비교가능 쌍 수가 함께 흔들린다(327 ↔ 귀무 평균 329.76).
8. 글 간 풀링이 국면(5주 · 두 제도)을 섞는다 · 표본은 저자가 올리기로 고른 매매다.
9. `N` 은 저자 서술 의존 · 총 분할 수는 모른다.
10. `prog_ver`(1.0.42) 를 공변량으로 쓰지 않는다 ⇒ `D-8` 수준 목록 의무 해당 없음 · `n_up` sd 없음 ⇒ `D-6` 해당 없음.
11. 이 분석은 **라이브 채택 대상이 아니다**(`PREREG.md` §0 2번 · `PREREG_POST8.md` §0-1).

[[PREREG_LADDER_TRANCHE]] · [[PREREG_POST8]] · [[PREREG_POST6]] · [[PREDECISION_2026-09-18_post8]] · [[INTAKE_2026-09-18_post8]] ·
[[LABELS_2026-09-18_post8]] · [[RESULTS_LADDER_TRANCHE_POST7]] · [[RESULTS_LADDER_TRANCHE]]
