# 결과 — `WRC-` 가중 평단 복원 축 (8번째 글) · **post8 단독 0건 · 누적 분모 5(이 스냅샷 재계산) ⇒ `WRC-G1` 4/5 발동으로 축 전체 ⛔**

**실행 2026-09-24 스냅샷**(`daily_prices` `max(date)` = 2026-09-23 · 그 날 2,764행 · 창 구간 `max(updated_at)` = 2026-09-23 15:46:25.855518)
· 사전등록 **`PREREG_WEIGHTED_RECON.md`** §4·§4-6·§4-8·§6-1·§7-B #24(2026-09-02 동결 · `WRC-D1`~`D7` 사장님 확정)
+ 동결 **`FREEZE_WRC_2026-09-02.md`**(주 모델 `WRC-A1` · `X1` 3번 관대 읽기 · 탐색 창 종료 09-02)
+ 🆕 **`PREREG_POST8.md`**(동결 `04cd785` · 머지 `729f28b` · **첫 구속 회차** — `D-3`·`D-4`·`D-5`·`D-8`·`D-9`)
· 수집 «후» 결정 **`PREDECISION_2026-09-18_post8.md`**(PD-1·PD-3·PD-4·PD-11·PD-13·PD-21~23·PD-26·PD-27) → 인테이크 **`INTAKE_2026-09-18_post8.md`** §1·§2-19·§5 → 원장 `b302f7f` → 이 문서. **순서가 증거다.**
숫자 원본 = **[`RESULTS_WRC_POST8_NUMBERS.md`](RESULTS_WRC_POST8_NUMBERS.md)**(`run_wrc_post8.py` 생성 · `run_wrc_post7.py`·`run_wrc_post6.py` 승계 ·
재사용(=import) `run_wrc_explore.py` 의 `build_cases`·`read_ledger`·`make_ctx`·`CODEMAP`·문턱 상수 · `run_reconstruct_post5.feasible_exact`) · 건별 원자료 `wrc_post8/gate.json`.

> 🔴 **라이브 채택 대상이 아니다**(`PREREG.md` §0 2번 · `PREREG_POST8.md` §0-1) · 성과·엣지 추정 금지.
> 이 문서의 어떤 수도 매매 규칙·파라미터 변경의 근거가 아니다. 🟢 **이번 회차도 «차수별 매수 밴드 `bₖ`» 모양은 «하나도» 나오지 않았다.**
> 🟢 라이브 코드 0줄 · DB 는 **SELECT 만** · `adj_factor` 산술 0건 · 원장은 «읽기»만.
> 🔴 **이 문서는 새 예측·새 문턱을 만들지 않는다** — 문턱은 전부 동결본에서 **import** 했다.
> 🔴 **숫자는 «거의 전부» `_NUMBERS.md` 에서 옮겨 적었다.** ⚠️ **산문 산술 1건**: §7 의 「분모 13 이상」(= 분자 4 고정에서 `4/n < 1/3 ⇔ n ≥ 13`). 🔴 그 한 건 말고는 새로 계산한 값이 없다 — ***「하나도 없다」고 적으면 그 한 건이 숨는다.***
> 🔴 **등급 이름을 이 문서에 적지 않는다**(§6 단계 · PD-16·PD-28).

---

## §0. 한 줄 결론

🔴🔴 **`WRC-P1`·`N1`·`N2`·`V1` = ⛔ 「판정 불가(`WRC-G1` 발동)」 · `WRC-G1` = 발동(⛔ 조건 자체) · `WRC-O1` = 조건부 인쇄 불성립(판정 열이 ⛔ 라 「탐색 편향 의심」을 인쇄할 조건이 서지 않는다 · `PREREG_WEIGHTED_RECON.md:803` — O1 은 ⛔ 열이 없는 «조건부 인쇄» 항목).**

🆕 **`D-4`(`P8-WRC단독열`) 세 수 — 같은 행**(NUMBERS §3-1):

| 회차 | **단독**(post8) | **누적**(post6 이후) | **판정에 쓴 분모**(= 누적) | ⛔ 을 만든 것 |
|---|---|---|---|---|
| post8 | **0** | **5**(post6 5 + post7 0 + post8 0) | **5** | 🔴 **`WRC-G1` 발동**(4/5 = 80.0%) — **「최소 n 미달」이 «아니다»** |

(§3-1 에서 옮김)

- 🔴 **단독 0 은 「최소 n 미달」이 아니다** — 누적 5 ≥ 3 이라 게이트는 «열렸다»(`PREREG_POST8.md` §4 (나) 1). **닫은 것의 이름 = `WRC-G1` 발동.**
- 🔴 **두 사유는 다음 글에서 열리는 «조건»이 다르다** — 「최소 n 미달」은 건수로, 「`WRC-G1` 발동」은 **커버리지**로 열린다
  (`PREREG_WEIGHTED_RECON.md:777` *「한 글의 판정 분모에서 ① DB부재 + ④ 해 0개가 1/3 미만」*). ***건수만 늘려서는 이 축이 열리지 않는다.***
- 🟢 **post7 인테이크의 예고가 그대로 이어졌다** — post7 은 *「`WRC-G1` 누적 4/5 = 80.0%(커버리지)로 닫혔다」*(`INTAKE_2026-09-15_post7.md:308`),
  post8 인테이크는 *「단독 0 은 그 수를 바꾸지 않는다」*(`INTAKE_2026-09-18_post8.md` §2-19)고 적었다. **같은 사유가 이어졌다** — 실측이다(§2).

🔑 ***값을 보고 닫힌 것이 아니라 «구성»으로 닫혔다*** — post8 `exact` 4건(우리로·JW신약·액스비스·우리기술)이 **전부 1차 체결**(`fill_n = 1`)이다.
***저자의 체결 차수 서술이 닫은 것이고, 우리가 고른 것이 아니다***(INTAKE §2-10 · PD-13).

🔴 **표기 의무**: **창 종료 2026-09-18 = 발행 당일(금 · 거래일) 봉 «포함» · B-1 · 전 축(`WRC-` 포함) · PD-1** ·
**실행 시 `max(date)` = 2026-09-23 · 그 날짜 행수 2,764** — 🔴 **기록만(창 아님)**.

---

## §1. 판정표 — 예측 ID · 예측 문언 · 관측값 · 판정 (4열 · 등급 열 없음)

| 예측 ID | 예측 문언(동결 · `PREREG_WEIGHTED_RECON.md` §4) | 관측값(NUMBERS) | 판정 |
|---|---|---|---|
| `WRC-P1` | `WRC-A1` 이 차수별 밴드를 **정보 있는 폭**으로 좁힌다 — 건별 `bₖ` 폭 중앙 **< 3%p** 인 건이 **과반**(최소 n 3) | 판정 분모(누적) **5** · 밴드 계산 **0건**(§7-B #24 문언대로 `WRC-G1` 발동 뒤 «돌리지 않았다») | ⛔ **판정 불가** — 사유 `WRC-G1` 발동(4/5 = 80.0% ≥ 1/3) |
| `WRC-N1` | (대칭) 무작위 대조와 구분되는가 — `WRC-A4` feasible 성립률 **≥ 50% ⇒ 강등** | 대조군 **미실행**(같은 사유) | ⛔ **판정 불가** — 사유 `WRC-G1` 발동 |
| `WRC-N2` | (대칭) 좁은 밴드가 모델 때문인가 창 때문인가 — `A4` 폭 중앙 < 3%p 비율 **≥ 50% ⇒ 강등** | 대조군 **미실행** | ⛔ **판정 불가** — 사유 `WRC-G1` 발동 |
| `WRC-G1` | (커버리지) **(①DB부재 + ④해0개) / 판정 분모 ≥ 1/3 ⇒ ⛔** | 누적 **4/5 = 80.0%**(① 0 · ④ 4 = 한라캐스트·원익·우리기술투자·비에이치) — **이 스냅샷 재계산** · 옮겨 적은 post6 값 4/5 와 **일치** | 🔴 **발동** — 이 축의 ⛔ 조건 자체 |
| `WRC-O1` | (분리) 탐색(post4·5) ↔ 판정(post6~)을 **항상 나란히** · 괴리가 판정을 가르면 「탐색 편향 의심」 | 탐색 동결 6/10 = 60.0% · 탐색 재계산(같은 창 09-02) **6/10 = 60.0%** · 판정 누적 **4/5 = 80.0%** | **조건부 인쇄 불성립** — 판정 열이 `WRC-G1` 로 ⛔ 라 「괴리가 판정을 가른다」가 서지 않는다 ⇒ 「탐색 편향 의심」 인쇄 없음(병기만) · 🔴 정정 1차: 초판 「⛔ 판정 불가」는 post7 선례 표기(`RESULTS_WRC_POST7_NUMBERS.md:75`)를 따른 것 — 선언이 없다는 뜻은 같고 `PREREG_WEIGHTED_RECON.md:803`(O1 = 조건부 인쇄 · ⛔ 열 없음) 문언 쪽으로 옮겼다 |
| `WRC-V1` | (민감도) 4축(잔차·수수료·재진입/미완결·모델) 병기 · **갈리면 선언 금지** | 최소 n 을 채운 갈래 **전부 같은 답**(⛔ `WRC-G1` 발동) · 미완결 제외(n 2) 는 인쇄만 | ⛔ **판정 불가** — 사유 `WRC-G1` 발동 · 🔴 **이 회차가 더한 갈림은 없다** |
| `WRC-P2` | 그 성립이 글을 넘어 반복된다 — **기록만**(검정 통계량 아님) | 부호 누계: post6 ⛔ · post7 ⛔ · post8 ⛔ | 기록(누계 인용 금지 조항 유지) |

- 🔴 **6항목 전부 «지지»도 «불성립»도 아니다.** 🔑 ***「못 좁힌다」도 「잴 건이 없었다」도 아니라 「못 쟀다」다***(`PREREG_WEIGHTED_RECON.md` §7-C 4번).
- 🔴 **문턱 `1/3` 을 올려 여는 것은 금지**(§4-6). `3.0%p` · `50%` · `레그 ≥ 3` · 잔차 `THR (0.02, 0.022)` 도 전부 import 한 그대로다.
- `WRC-B1` 은 이번 회차 **계산 대상이 없다**(`A1` 을 돌리지 않았다) · 🔴 **`WRC-X1`(§4-7) 은 적용과 충족을 나눠 적는다**(정정 1차): 1~3번(`A1`·`A4` 결과 위에 서는 조건)은 **적용 대상 없음** · 4번(`tick`·격자·해찾기 재작성 흔적)·5번(`adj_factor` 산술)은 **적용 · 위반 0**(NUMBERS §3-3) — 둘 다 «지지»를 만드는 항목이 아니다(§4-4·§4-7).

---

## §2. 🔴 이번 회차의 «다른» 자리 — **누적을 옮겨 적지 않고 «다시 쟀다»**

post7 은 누적 분모를 **옮겨 적은 상수**로 썼다(`run_wrc_post7.py` `P6_WRC` · *「옮겨 적은 상수이고 재계산이 아니다」*).
이 회차는 ① `WRC-R11` 의 *「이후 글에서는 **누적 전체로 다시 계산**한다(같은 검정의 갱신)」*(`PREREG_WEIGHTED_RECON.md:750`)와
② 공통 의무 *「post4~7 표본을 같은 09-24 스냅샷에서 재계산해 나란히 · 직전 문서 숫자 옮겨 적기 금지」*를 따라
**`WRC-G1` 을 글별 «동결 창 종료» 그대로 다시 쟀다**(NUMBERS §3-2):

| 글 | 창 종료(출처) | 판정 분모 | ① DB부재 | ④ 해 0개 | **`WRC-G1`(재계산)** | 옮겨 적은 동결값(대조) | 일치 |
|---|---|---|---|---|---|---|---|
| 🔬 탐색 post4+post5 | 2026-09-02(`FREEZE_WRC_2026-09-02.md` §2) | 10 | 1(레메디) | 5(금호건설·혜인·한국화장품제조·한켐·광전자) | **6/10 = 60.0%** 🔴 발동 | 6/10 | 🟢 일치 |
| post6 | 2026-09-04(`RESULTS_WRC_POST6_NUMBERS.md` §0) | 5 | 0 | 4(한라캐스트·원익·우리기술투자·비에이치) | **4/5 = 80.0%** 🔴 발동 | 4/5 | 🟢 일치 |
| post7 | 2026-09-11(PD-1 post7) | 0 | 0 | 0 | —(분모 0) | 0/0 | 🟢 일치 |
| post8 | 2026-09-18(PD-1) | 0 | 0 | 0 | —(분모 0) | —(신규) | — |
| 🔒 **누적 = 판정 분모** | 글별 | **5** | 0 | 4 | **4/5 = 80.0%** 🔴 발동 | 4/5 | 🟢 일치 |

(§3-2 에서 옮김)

- 🟢 **재계산이 동결값을 재현했다** — 탐색 10건도 post6 5건도 **같은 부류**(① / ④ / 해 있음)로 떨어졌다. ⇒ 2026-09-02~09-04 이후
  `daily_prices` 이력 변동이 **이 축의 `A0` 판정을 한 건도 움직이지 않았다**(판정값 이동 0).
  🔴 **「byte 가 같다」가 아니다** — 이 축의 재계산은 `A0` «부류»만 대조했다(`A1` 밴드·대조군은 이번에 안 돌렸다).
- 🔴 **탐색 행은 판정 분모에 «넣지 않는다»**(`WRC-O1` · §4-5 · 🔬 탐색 = 판정 분모 밖). 같은 스냅샷에서 다시 잰 **대조 기록**이다.
- 🔴 **소급 = 탐색** — 위 표의 탐색·post6·post7 행은 같은 스냅샷에서 **소급 재계산**한 값이다. **채택 근거로 쓰지 않는다.** post6·post7 행은
  `WRC-R11` 누적 분모를 «다시 잰» 것이지 그 회차의 판정을 다시 매긴 것이 아니다 — post6·post7 판정 라벨은 그대로 둔다(`PREREG_POST8.md` (라) post7 소급 금지).
- 🔴 **이 축의 판정 분모 재계산 창(탐색 `~09-02` · post6 `~09-04`)은 제도 경계 2026-09-14 «전»**이다 ⇒ 누적 분모 안의 혼합 빈티지 **0**(NUMBERS §7-1).

---

## §3. 표본 — 원장 재측정 ↔ 인테이크 «계산 전» 구성

원장 `post_log_no = 224416253270` 행 **10건** 재측정 · 🟢 **인테이크 §1 표와 전건 일치**(이름·정밀도·`fill_n`·서로 다른 값 레그 수 · NUMBERS §1).

| # | 종목 | 코드 | 정밀도 | `fill_n` | 서로 다른 값 레그 | 게이트 | 탈락 사유(구성) |
|---|---|---|---|---|---|---|---|
| 1 | 빛과전자 | `069540` | none | 1 | 2 | 탈락 | 후속(PD-2) |
| 2 | 로보티즈 | `108490` | none | 1 | 2 | 탈락 | 후속(PD-2) |
| 3 | 우리로 | `046970` | **exact** | **1** | 7 | 탈락 | 🔀 항목 내 2 사이클 · **`구조차단`**(`WRC-R5`) — 🔴 **사유는 `fill_n = 1`** |
| 4 | 범한퓨얼셀 | `382900` | none | 1 | 2 | 탈락 | 후속(PD-2) |
| 5 | 원익 | `032940` | none | 1 | 4 | 탈락 | 신규 `none`(🔒 #2) |
| 6 | JW신약 | `067290` | **exact** | **1** | 4 | 탈락 | 1차 체결 |
| 7 | 헥토파이낸셜 | `234340` | approx | 1 | 4 | 탈락 | 정밀도 · 1차 체결 |
| 8 | 액스비스 | `0011A0` | **exact** | **1** | 2 | 탈락 | 1차 체결 |
| 9 | 코데즈컴바인 | `047770` | approx | **2** | 5 | 탈락 | 🔴 정밀도만 — 나머지 두 조건은 통과(민감도 갈래 · §4) |
| 10 | 우리기술 | `032820` | **exact** | **1** | 3 | 탈락 | 1차 체결 |

(§1 에서 옮김 · 탈락 사유 열은 §2 표를 행별로 옮긴 것)

🔑 **post7 과 같은 모양이 한 번 더 나왔다** — post7 은 *「등록일이 적힌 건은 1차 체결이었고, 다차수 체결 건은 등록일이 안 적혀 있었다」*였다.
**post8 도 그렇다**: `exact` 4건은 전부 1차 체결이고, 다차수(`fill_n = 2`)인 유일한 건(코데즈컴바인)은 등록일이 「8월말」(`approx`)이다.
⇒ ***`WRC-` 가 이번에 못 잰 이유도 「데이터가 없어서」가 아니라 「정밀도와 체결 차수가 서로 «반대편» 건에 붙어서」다.***

🔴 **우리로 — `구조차단`(항목 내 2 사이클 · 1)과 §1-5 `P6-PRIOR_CYCLE_IN_WINDOW`(0)는 다른 플래그다**(PD-3). 두 값을 한 칸에 섞지 않는다.

---

## §4. 🆕 `D-5` 갈래표 — `(갈래 이름, n, 답)` (NUMBERS §5 · 최소 n = 판정 분모 3)

| 갈래 | n | 최소 n | 답 |
|---|---|---|---|
| 주 — 누적 · `exact` · `gross` · 재진입·미완결 포함(판정) | **5** | 충족 | **⛔ `WRC-G1` 발동**(4/5) |
| `WRC-V1` 축③ 재진입 제외(post6 지투파워) | **4** | 충족 | **⛔ `WRC-G1` 발동**(4/4) |
| `WRC-V1` 축③ 미완결(`open_ended = 1`) 제외 | **2** | 🔴 미달 | 인쇄만(2/2) — 「갈렸다」의 근거로 쓰지 않는다 |
| 「우리로 제외」(`WRC-R5` · 🔒 #1-(i)) | **5** | 충족 | **⛔**(4/5) — **항등**(우리로는 애초에 분모 밖) |
| `WRC-V1` 축① 잔차 0.022 ↔ 0.020 | **5** | 충족 | **항등** — `WRC-G1` 은 잔차 문턱을 입력으로 쓰지 않는다 |
| `WRC-V1` 축② 수수료 `gross` ↔ `net` | **5** | 충족 | **⛔ `WRC-G1` 발동**(4/5 · `net` 재측정) |
| `WRC-V1` 축④ 모델 `A1` ↔ `A3` | **5** | 충족 | **항등** — `WRC-G1` 은 «현행 `A0`» 해 0개로 정의된다 |
| `approx` 포함(코데즈컴바인 · 「8월 말」 7갈래 D=08-21…08-31) | **6** | (`approx` 로 채운 n) | 7갈래 **전부** 5/6 = 83.3%(`A0` 해 0개 · 봉수 15~21 = 등록일 포함 · `PREREG_WEIGHTED_RECON.md:681`) — 🔴 **비율만 · 판정 언어 붙이지 않음**(`D-3` (나)2) · `D-3` (나)4 검사 = 주 갈래와 «다른 쪽» 0/7 ⇒ **갈리지 않음** |
| post8 «단독»(갈래 아님 · 참고) | **0** | — | — |

(§5 에서 옮김)

- ⇒ **최소 n 을 채운 갈래의 답이 전부 같다**(⛔ `WRC-G1` 발동) ⇒ `P8-갈래계수` 상 **「갈렸다」 없음** · `WRC-V1` 은 **이 회차가 더한 갈림이 없다.**
- 🔑 **`WRC-V1` 의 ⛔ 는 post6·post7·post8 에서 «같은 기호, 다른 사유»다** — post6 은 *「4축 중 3축이 갈린다」*(갈려서 선언 금지) ·
  post7 은 *「이 글이 갈래를 하나도 더하지 못했다」* · **post8 은 「갈래는 더했으나(approx 7갈래 · 재진입·미완결·수수료 재측정) 전부 같은 답이고, 그 답이 `WRC-G1` ⛔ 다」**.
- 🔴 **「우리로 제외」 항등** — `WRC-R5` 의 «제외 민감도 의무»는 이행했다(행이 있다). 우리로가 `fill_n = 1` 이라 **이 축에서는 판정 효과가 원리적으로 0** 이다(PD-3 ④).

---

## §5. 🆕 `D-3` · `D-8` · 커버리지

- 🆕 **`P8-approx의존신고`**: *「`approx` 포함 시 최소 n 이 차는 축: **없음** · `exact` 분모 **5**(누적 · 단독 0) / `approx` 포함 분모 **6**」*
  (NUMBERS §6 · 구성 예고 PD-21 = 「없음」과 **같다** — `n_exact` 이 이미 최소 n 3 을 채운다).
- 🆕 **`P8-결측수준`**(행·글 두 단위 · 🔒 #3 = 부호 갈림 판정은 다음 사전등록까지 **보류**): `1.0.30` 23/1 · `1.0.32` 3/1 · `1.0.33` 7/1 · `1.0.36` 8/1 ·
  `1.0.39` 9/1 · `1.0.40` 12/1 · **`1.0.42` 10/1** · `missing` 13/1(행/글 · NUMBERS §6). 🔴 이 축은 버전을 **기록만** 한다 — 판정에 쓰지 않는다.
- 🔴 **커버리지는 축별로 «다른 수»다**(PD-11): `daily_prices` 계열 4/4 · `SEC-` 3/4 · `S5` 3/4 · **`WRC-` 0/4(post8 단독)** —
  ***`SEC-`·`S5` 의 「25.0%」는 「DB 에 없다」(액스비스)이고, `WRC-` 의 「0/4」는 「저자가 1차 체결로 매매했다」다.*** 한 칸에 적지 않는다.

---

## §6. 🆕 `D-9` 빈티지 — 읽은 시각 · 혼합 빈티지 (NUMBERS §0·§7)

| 의무 | 값 |
|---|---|
| ① 쿼리 실행 시각(KST) | **2026-09-24 00:47:45** — 🔴 이 DB 스냅샷 지문을 스크립트가 «처음» 읽은 실행의 시각(`wrc_post8/read_stamp.json`) · 같은 지문 재실행은 이 값을 다시 쓴다(아래 §8 재량) |
| ② 창 구간 `max(daily_prices.updated_at)` | 스크립트가 읽는 창 전 구간 `[2026-08-05, 2026-09-18]` = **2026-09-23 15:46:25.855518** · 관리자 프로브 창 `[07-24, 09-18]` 같은 값 |
| ③ | **「2026-09-18 봉은 D+1(2026-09-21) sweep 이후 읽음」** — 예 |
| ④ (기록 · 통과 조건 아님) | **「창 구간 `min(updated_at)` = 2026-09-23 15:45:10.623955 ≥ 09-21 15:35: 예」** — 🔴 `updated_at` 은 일괄 갱신 값이라 빈티지 «증거»가 아니다(PD-27 (라)) |
| ⑤ 혼합 빈티지 | 이 축이 읽는 걸침 창 = **코데즈컴바인 `approx` 갈래 7개뿐**(경계 전 16~10봉 / 후 5봉) · 판정 분모 재계산 창은 전부 경계 «전» · PD-27 (바) 표의 걸침 칸 30줄도 공통 의무로 실측 인쇄(NUMBERS §7-2 · 인테이크 표와 전/후 봉수 전건 같다) |

- 🔴 **「정규장만」 갈래는 열지 않는다** — `ovtm_vol` 채널 소멸(09-15 이후 행 없음) · 15:30 마감 분봉 09-14 이후 사실상 0(관리자 착수 증거 `p8_probes_postfetch_0924.txt`) ⇒ 뺄 수단이 없다(`PREREG_POST8.md` §9 (나) 4).
- 🔴 **이번 회차 이 축의 판정은 빈티지와 무관하다** — 판정을 닫은 `WRC-G1` 의 분모 5건이 전부 경계 «전» 창이다. 걸침은 `approx` 민감도(판정 분모 밖)에서만 생긴다.

---

## §7. 대칭 단언 — 무엇을 열고 무엇을 닫았나

| 방향 | 이번 회차 |
|---|---|
| **열린 것** | 누적 게이트(5 ≥ 3) — 🔴 그래서 ⛔ 의 이름이 「최소 n 미달」이 아니라 **`WRC-G1`** 이다 |
| **닫은 것** | `WRC-G1` 4/5 = 80.0% ≥ 1/3 ⇒ `WRC-P1`·`N1`·`N2`·`V1`(⛔) · `WRC-O1`(인쇄 조건 불성립) |
| **이 판정이 틀렸다면** | 재계산이 동결값과 **어긋났을** 것이다(§2) — 🟢 **어긋나지 않았다**(탐색 6/10 · post6 4/5 재현). 🔴 단 `A0` «부류»만 대조했으므로 `A1` 밴드·대조군의 재현은 **이 회차가 보이지 않았다** |
| **다음 글에서 열리는 조건** | ***커버리지***: 그 글의 판정 분모(= `exact` ∧ `fill_n ≥ 2` ∧ 다른 값 레그 ≥ 3)에서 **「① + ④」 < 1/3**. 🔴 누적이면 post6 의 ④ 4건이 계속 분자에 남는다 — 누적 비율이 1/3 아래로 가려면 **해 있는 다차수 `exact` 건이 누적으로 충분히 쌓여야** 한다(산술: 분자 4 고정이면 분모 13 이상) |

🔴 **`PREREG_POST8.md` §4 (다) 「이 조항이 틀렸다면」 행 신고** — *「「단독」 열이 여러 회차 연속 0 인데 누적 판정이 계속 갱신된다 ⇒ `WRC-P1` 이 옛 표본만으로 굴러간다는 뜻이고,
그 사실 자체를 신고해야 할 자리」* — **단독 열이 post7·post8 두 회차 연속 0 이다. 여기 신고한다.** 🔴 **문턱은 바꾸지 않는다.**

---

## §8. 모호 지점 · 재량 (판정 아님)

| # | 항목 | 처리 |
|---|---|---|
| 1 | 🔴 **`WRC-G1` 발동 시 「나머지 계산을 «돌리지 않는다»」(§4-6·§7-B #24) ↔ post6 전례 「계산하되 판정 표기 ⛔」(관리자 지시)** | 이 회차는 **문언 쪽**을 택했다(`A1`·`A3`·대조군 미실행). 두 읽기 모두 판정 표기는 ⛔ 로 **같다** — 판정 이동 0. post6·post7 §6 4번 미해소를 **그대로 이월**한다 |
| 2 | 🔴 **누적 분모 «재계산» vs 「과거 산출물을 다시 재지 않는다」**(`PREREG_POST6.md` §5-1-5) | 동결 산출물(`RESULTS_WRC_EXPLORE.md`·`RESULTS_WRC_POST6/7_NUMBERS.md`)은 **한 바이트도 안 건드렸다**(시험 `test_post7_and_earlier_untouched_vs_head`). 재계산은 **이 산출물 안의 대조 열**로만 두었다 — `WRC-R11` 「누적 전체로 다시 계산」 + 공통 의무. 옮겨 적은 값과 **일치**했으므로 판정 이동 0 |
| 3 | 🟡 **D-9 ① 「쿼리 실행 시각」 ↔ 바이트 결정론**(`FREEZE_WRC_2026-09-02.md` §2 「벽시계는 본문에 적지 않는다」) | **재량**: 본문 ① = **같은 DB 스냅샷 지문을 «처음» 읽은 실행의 KST 시각**(`wrc_post8/read_stamp.json` 에 지문과 함께 보관) · 재실행 벽시계는 stdout. 지문(②·④·`max(date)`·행수)이 바뀌면 새 시각이 박힌다. 대안 = ① 을 매 실행 시각으로 적고 바이트 결정론을 포기(→ `regen_gate.py --rerun` 이 이 줄 때문에 항상 FAIL) |
| 4 | 🟡 `WRC-O1` 의 「탐색값을 어느 창·어느 스냅샷에서 인용할 것인가」 문언 부재(post6 §8 2번 · post7 §6 3번 승계) | **양쪽 인쇄**(동결 인용 + 같은 창 재계산) · 판정 열이 ⛔ 라 비교 자체가 성립하지 않는다 ⇒ 미해소 그대로 이월 |
| 5 | 🔴 `approx` 갈래(코데즈 · `fill_n = 2`)를 「게이트 통과」로 적지 않는 것 | NUMBERS §2 — ***「나머지 두 조건 통과」와 「게이트 통과」는 다른 말***이다 |

---

## §9. 한계 (§9 승계 · 값과 무관하게 미리 적어 둔 것)

- 🔴 **`--rerun` 한계 — 다음 sweep 뒤**(🆕 정정 1차 · verifier B 공통) — 다음 D+1 sweep(예정 **2026-09-28 15:35**)이 창 구간 `updated_at` 을 일괄 갱신하면 D-9 ②(`max(updated_at)`)·DB 지문이 **반드시** 바뀐다 ⇒ 그 뒤 `regen_gate.py --rerun` 은 이 산출물에서 byte 불일치를 낸다 — 값이 틀렸다는 뜻이 아니라 동결 규칙(D-9 ② 인쇄 의무)의 귀결이다(byte 재현은 «같은 스냅샷» 안에서만 성립).
- 🔴 **단독 분모 0 은 우리 가설의 증거가 아니다** — 저자가 이번 주 `exact` 건을 1차 체결로 끝낸 이유를 모른다(장세 / 프리셋 / 우연).
- 🔴 **`WRC-` 는 post6 한 회차의 표본으로만 굴러간다**(§7) — ***「살아 있다」와 「검증됐다」는 여전히 다르다.***
- 🔴 이 축은 **「해 0개」를 원리적으로 풀지 못한다**(§2-4 산술 정리) — 🔑 **이번에도 그 정리에 닿기 전에 게이트에서 멈췄다.**
- 🔴 **`WRC-A1`(균등 `1/N`)이 참이라는 근거가 없다** — 저자는 차수별 비중을 적지 않는다.
- 🔴 판정 대상이 «해가 있는» 건뿐이라 **선택편향**이며, 그 배제는 무작위가 아니다(§5-3 2번 두 문장 묶음).
- 🔴 **누적 분모가 «두 제도»를 섞을 수 있다**(`PREREG_POST8.md` §14) — 이번 누적 5건은 전부 경계 «전» 창이라 **이번엔 섞이지 않았다.**
- 🔴 **프로그램 `1.0.42`(업데이트 중)** · 저자가 **추석 전 통계 데이터 갱신**을 예고했다(INTAKE §4) — post9 부터 규칙이 바뀌었을 수 있다(기록만).
- 🔴 **`adj_factor` 를 곱하지도 나누지도 않는다** · DB 는 SELECT 만 · 원장은 읽기만.
- 이 분석은 **라이브 채택 대상이 아니다**(`PREREG.md` §0 2번 · `PREREG_POST8.md` §0-1).

---

## §10. 재현

```bash
cd RoboTrader_template/backtest/tasso_program_journal
python run_wrc_post8.py            # → RESULTS_WRC_POST8_NUMBERS.md · wrc_post8/gate.json · wrc_post8/read_stamp.json
python -m pytest test_post8_wrc.py -q -p no:cacheprovider
```

- 🟢 **세 번 실행 byte 동일** — 🆕 정정 1차 재생성(2026-09-24 02:51:07 · 02:51:10 · 02:51:13 KST 실행분) `RESULTS_WRC_POST8_NUMBERS.md` 작업트리(CRLF) md5 **`eab7a1a47f0d6e22a3f69297de085842`** · 블롭(LF) md5 **`3a6a5c375c435d556a86570532dc93c4`** ×3 (`README.md:95` — 무엇의 md5 인가를 적는다 · `core.autocrlf=input` 이라 블롭 = LF) ·
  `wrc_post8/gate.json` `5efe831749a45274157a885710f4af7d` ×3 · `wrc_post8/read_stamp.json` `54f53be57635ccf3322836c5ca7a3274` ×3(둘 다 정정 전과 같다 · 작업트리 md5) · 초판(00:48) NUMBERS 작업트리 md5 `a5917497…` 는 정정 전 값.
- 🔴 **HEAD 해시를 본문에 적지 않는다**(`FREEZE_WRC_2026-09-02.md` §2) · 벽시계는 stdout.
- 🔴 **창 종료 `2026-09-18`**(B-1 · PD-1)에 종속된다. 실행 시 `max(date)` 2026-09-23 은 **기록**일 뿐 창이 아니다.

[[RESULTS_WRC_POST8_NUMBERS]] · [[PREREG_WEIGHTED_RECON]] · [[FREEZE_WRC_2026-09-02]] · [[PREREG_POST8]] ·
[[RESULTS_WRC_POST7]] · [[RESULTS_WRC_POST6]] · [[RESULTS_WRC_EXPLORE]] · [[INTAKE_2026-09-18_post8]] ·
[[PREDECISION_2026-09-18_post8]] · [[LABELS_2026-09-18_post8]]
