# 결과 — 사다리 정렬 축 **7번째 글 누적 재계산** (`LAD-T1`·`T2`·`T3` · `LAD-P1`~`P3`)

**실행 2026-09-15** · 사전등록 `PREREG_LADDER_TRANCHE.md`(**2026-08-22 동결**)
승계 `PREREG_POST6.md` §1-4·§1-6·§2-4 · §4 표 **#21~#24** · 해석 결정 `RESULTS_LADDER_TRANCHE.md` §1 **B-1~B-8**
수집 후·계산 전 결정 `PREDECISION_2026-09-15_post7.md` **PD-1**(창) · **PD-2**(후속 제외) · **PD-3**(재진입) ·
**PD-4**(`exact` 분모 · 창 규약) · **PD-7**(`MANUAL` carve-out) · **PD-12**(창5 절단) · **PD-13**(`REC-Z4` 귀결 소멸) ·
항목표 `INTAKE_2026-09-15_post7.md` §1·§2-12·§5

표본 = **누적 28건**(post4 6 + post5 6 + post6 10 + **post7 주 표본 6**) ·
숫자 원본 = [`RESULTS_LADDER_TRANCHE_POST7_NUMBERS.md`](RESULTS_LADDER_TRANCHE_POST7_NUMBERS.md)
(`run_ladder_tranche_post7.py` 생성 · 통계 핵·표 포맷은 `run_ladder_tranche.py`, 누적 목록·가드·판정 함수는
`run_ladder_tranche_post6.py` 에서 **import** — **두 파일 다 수정하지 않았다** · 🟢 **두 번 실행 byte 동일** 확인)
`δ` = **1.0%p** · `NULL_SEED` = **20260815** · 순열 표본 **200,000** · 판정 문턱 **누적 비교가능 쌍 40**
— 🔴 **넷 다 동결 그대로 손대지 않았다.**

🔴 **창 종료 2026-09-11 = 발행일(2026-09-12 토) 휴장 ⇒ 마지막 거래일 · B-1**(`WRC-` 포함 전 축) —
***고른 값이 아니라 동결 문언이 지시한 한 값이다***(PD-1 3번).
🔴 **실행 시 `max(date)` = 2026-09-15 · 그 날짜 행수 2,765**(창으로 쓰지 «않는다» · PD-1 2번) ·
창 종료 **2026-09-11 행수 2,767**. 스크립트 `END` = 2026-09-11 이고 **DB 최신 봉과 불일치**한다(DB 가 전진했고 창은 고정).
`adj_factor` 산술 0건 · 라이브 트리 import 0건 · DB 는 SELECT 만.

🔴 **라이브 채택 대상이 아니다**(`PREREG.md` §0-2) — 이 산출물은 **기록**이지 전략 후보가 아니다.
🔵 **접두 규약**(§0-3): 여기서 `T1`·`T2`·`T3`·`P1`~`P3` 은 전부 **`LAD-`** 축이다.
플래그·가드는 **`P6-`** 접두를 그대로 단다(`P6-WIN5_TRUNC` · `P6-창5절단가드-A/B` · `P6-PRIOR_CYCLE_IN_WINDOW`).
🔴 **숫자는 전부 `_NUMBERS.md` 에서 옮겨 적었다** — 산문에서 새로 계산한 값은 **없다**.

---

## §0. 판정 요약 — 한 줄씩 (`PREREG_POST6.md` §4 #21~#24)

| # | 항목 | 결과 | 출처 절 |
|---|---|---|---|
| — | **판정 게이트**(§5-1 누적 비교가능 쌍 ≥ 40) | ✅ **열려 있다 — 창5 비교가능 266쌍**. 13 → 41 → 161 → **266** | §3 |
| **#21** | **`LAD-T1`**(주 · 창5) 「차수는 낙폭을 따라간다」 | ⛔ **불성립** — `V` = **127 / 266** · 귀무 평균 **133.42** · **`p` = 0.4064**(문턱 0.05) | §2·§3 |
| | 창3 민감도 | `V` = 123/249 · **`p` = 0.4565** | §2 |
| | 창A 민감도 | `V` = 114/264 · **`p` = 0.2297** — 세 창 중 가장 낮다(post6 0.2832) · **문턱과는 멀다** | §2 |
| | `P(V=0)`(§4-5 의무 2) | 세 창 모두 **0.00000**(200,000 표본에서 0회) ⇒ 「공통 사다리 기각」은 **판별력 ≈ 0** | §2 |
| **#22** | **`LAD-T2`**(반증축 · σ₂₀ 정규화) | ⛔ **가설 B 지지 없음** — 정규화축 `p` **0.6361** ≥ 원축 `p` **0.3947**(같은 26건·같은 쌍 집합). 🔑 **post6 과 같은 부호 — 2글 연속** | §4 |
| **#23** | **`LAD-T3`**(반증축 · 경과 거래일) | **미발동** — `p` = **0.1182** ≥ 0.05 ⇒ T1 취소 사유 없음. 🔴🔴 **부호 감시: T3(0.1182) < 창5 원축(0.4064) — 3글 연속** | §5 |
| **#24** | **`LAD-P1`** 차수 명시 | ✅ **10/10** — 분모 = 「DB 있는 신규 건」 10건(항목 13 중 차수 명시 13 · 후속 3건은 PD-2 로 분모 밖) · **4글 연속** | §8 |
| **#24** | **`LAD-P2`** 신규 `DD`(창5) 중앙 15~35% | ✅ **20.11%** — 구간 «안» · **하한까지 +5.11%p**. 계열 23.53 → 20.34 → 15.62 → **20.11** 로 3회 연속 하락이 **끊겼다** | §8 |
| **#24** | **`LAD-P3`** `first_only` ≥ 1건 | ✅ **6건**(신규) — 0 → 1 → 3 → **6 = post6 의 두 배** | §8 |
| — | **`P6-창5절단가드-A`** | **미발동** — **2/10 = 20.0%** < 1/3. 🔴🔴 **그러나 `exact` 분모 갈래는 2/6 = 33.3% ⇒ 발동 조건에 «닿는다»** | §6-1 |
| — | **`P6-창5절단가드-B`** | **미발동** — 절단 2건 제외본도 `p` = 0.5817 ⇒ **판정(불성립)이 뒤집히지 않는다** | §6-2 |
| — | 재진입 제외 민감도(23건) | 창5 `V` = 83/177 · `p` = **0.3721** ⇒ 판정 **안 갈린다**(「재진입 의존」 아님) | §6-3 |
| — | `P6-PRIOR_CYCLE_IN_WINDOW` | 🔴 **주 표본 안 = 0**(빛과전자 · **구성상 0**) · 글 전체 **2**(지투파워·한국화장품제조 — 둘 다 `approx`) | §6-3 |
| — | 🆕 **post6 절단 2건 값 «대체»** | 현대약품 24.00% → **24.00%**(완전) · 비에이치 14.00% → **14.00%**(완전) ⇒ **±0.00%p** | §6-4 |

🔑🔑 ***게이트가 열린 세 번째 회차에서도 「차수는 낙폭을 따라가지 않는다」가 나왔다*** —
표본을 22 → 28건, 비교가능 쌍을 161 → 266 으로 늘렸는데 `p` 는 0.4914 → **0.4064** 로 여전히 무작위 영역이다
(`V_obs` 127 ↔ 귀무 평균 133.42).
🔑🔑 ***그리고 `LAD-T3` 의 부호가 «세 글 연속» 유지됐다*** — T3 `p`(0.1182)가 창5 원축 `p`(0.4064)보다 낮고,
**세 회차 중 이번이 가장 낮다**(0.2008 → 0.1909 → **0.1182**). ⚠️ 여전히 0.05 위다.

---

## §1. 계산 «전»에 고정된 해석 결정 — **전부 승계 · 신설 0**

`RESULTS_LADDER_TRANCHE.md` §1 의 **B-1 ~ B-8** 을 그대로 쓰고, 이번 글 고유의 상황은
**`PREDECISION_2026-09-15_post7.md`(계산 전 커밋)**가 이미 못 박은 것만 적용했다.

| # | 이번 회차에서 실제로 걸린 자리 |
|---|---|
| **B-1** | post7 발행 **09-12(토) = 휴장** ⇒ 창A 끝 = **2026-09-11**(직전 거래일). post6(09-04 금 = 발행 당일 포함)과 **다른 갈래**이고, post4·post5(토 발행)와 **같은 갈래**다 |
| **B-2** | 🔴 **`MANUAL` 2건(아난티·우리기술투자)은 라벨 때문에 빠지지 «않는다»** — B-2 는 *「매도 쪽만 수동이면 포함」*이고 PD-7 이 *「두 건 다 매수 쪽 수동 서술이 없다 ⇒ **LAD 분모 포함**」*이라 적었다. ***빠지는 사유는 「후속」이다***(§1-2) |
| **B-3** | T3 는 `δ_E` = 0(정확히 같은 값만 제외) — 그대로 |
| **B-4** | σ₂₀ 결측 **2건**(post5 삼양바이오팜 + 🆕 **post7 해치텍**) ⇒ T2 표본 **26건**. 해치텍은 2026-08-25 첫 봉이라 **등록일 직전 21종가를 못 채운다** — ***값을 보고 뺀 것이 아니라 «구성»이다*** |
| **B-5** | 서로 다른 배정 **5,788,349,757,590,400** > 200,000 ⇒ 시드 고정 200,000 표본 |
| **B-6** | `E` = `[D, 발행일]` 거래일 수(양끝 포함) — post7 주 표본 6건 = **3~7** |
| **B-7** | 2번째 사이클 = 한켐·코데즈컴바인(post5) + 지투파워·현대약품(post6) **+ 🆕 빛과전자(post7)** ⇒ 제외 민감도 §6-3 |
| **B-8** | 창3·창5 의 「D+k거래일」은 그 종목의 봉으로 — **절단 2건이 여기서 드러났다**(§6-1) |
| **PD-2** | 「기존 건 후속」 **3건**(한라캐스트·아난티·우리기술투자)은 **신규 분모 밖**(이중계상 금지) |
| **PD-4** | 🔴🔴 **주 표본 = 신규 ∧ `exact` 6건** · `approx` 2건은 **갈래별 민감도**(§7) · `none` 2건은 **축 밖** |
| **PD-12** | 창5 절단 2건은 **분모에 넣고** 플래그·건수·봉수·제외 민감도·가드 A/B 를 전부 인쇄(§6) · post6 절단 2건은 **값 대체**(§6-4) |
| **PD-13** | `REC-Z4` 의 「즉시 `L` 판정」 귀결이 🔒 결정 ④로 **소멸** ⇒ 이 축은 `first_only` **건 수만** 기록(§8) |

🔴 **어느 것도 값을 본 뒤에 고치지 않았다.** 특히 `δ`·창5·문턱 40·시드·순열 표본 수는 **손대지 않았다.**
🔴 **새 예측을 만들지 않았다.** 이 문서에 처음 등장하는 숫자는 없다(가드 문턱 1/3 도 §1-6 차용분 그대로).

---

## §2. 🔴🔴 표본 구성 — 「이번 글 신규 10」과 「주 표본 6」이 **이 계열 최초로 갈린다**

| 구분 | 건 | 이 축에서의 지위 |
|---|---|---|
| 신규 ∧ `exact` | **6** | 🟢 **주 표본** — 창 `[D, 2026-09-11]` 를 세울 `D` 가 있다 |
| 신규 ∧ `approx` | **2** | ⚪ **갈래별 민감도**(§7) — `PREREG_POST6.md` §1-4 창 규약 **첫 발동** |
| 신규 ∧ `none` | **2** | 🔴 등록일 축 밖 — `D` 가 없다 |
| 기존 건 후속 | **3** | 🔴 등록일 축 밖 — PD-2 이중계상 금지 |
| **이번 글 신규 계** | **10** | 🔴 **가드-A 의 «분모»는 이 수**(post6 PD-11 3번 문언 그대로) |
| 항목 계 | 13 | `INTAKE` §1 |

(§1-0 에서 옮김)

post6 까지는 신규 건이 **전부 `exact`** 였다 ⇒ 두 수가 같아서 구분할 필요가 없었다. **post7 에서 처음 갈린다.**
🔴 ***두 수를 한 수로 합치지 않는다*** — 표본 수(6)와 가드 분모(10)가 **다른 수**가 되었고, **둘 다 인쇄**했다.
🔑 ***어느 한쪽으로 통일하면 그것이 새 자유도다***(§9 4번).

### 2-1. 등록일 축 «밖» 5건 — **사유를 라벨과 분리해 적는다**

| 종목 | N | 라벨 | 밖인 사유 |
|---|---|---|---|
| 한전기술 | 4 | `SL` | `none` — 등록일 «문장»이 없다(PD-4). 「8월 26일」은 **급등 사유일**이고 등록 문장과 **다른 줄**이라 승격하지 않는다 ⇒ 창 `[D, END]` 를 세울 `D` 가 없다 |
| 한전산업 | 5 | `SL` | `none` — 〃 |
| 한라캐스트 | 5 | `MIX` | **후속**(post6 #3) — PD-2 이중계상 금지. 🔴 연결 시퀀스가 비증가와 «양립하지 않는다»(post6 −2.89 → post7 11.22)지만 **이 축은 낙폭만 잰다** |
| 아난티 | 1 | `MANUAL` | **후속**(post6 #5) — PD-2 |
| 우리기술투자 | 4 | `MANUAL` | **후속**(post6 #11) — PD-2 |

🔴🔴 **B-2 carve-out 재확인**: `MANUAL` 2건은 이 축에서 **라벨을 이유로 빠지지 않는다**.
***빠지는 사유는 「후속」이고, 그 둘을 섞어 적으면 다음 회차에 「`MANUAL` 은 LAD 에서 뺀다」로 읽힌다***(§1-0).
🔑 **`EXIT-` 축에서는 같은 두 건이 «라벨 때문에» 분모 밖**이다(`MANUAL` ⇒ `EXIT-E1` 밖).
***같은 두 건이 두 축에서 서로 다른 사유로 빠진다*** — 사유를 적어두지 않으면 구분할 수 없다.

### 2-2. post7 주 표본 6건 — 차수가 **1차에 몰려 있다**

| 종목 | N | 등록일 | `H` | **DD3** | **DD5(주)** | **DD_A** | `E` | σ₂₀ | `P6-WIN5_TRUNC` |
|---|---|---|---|---|---|---|---|---|---|
| 서산 | **1** | 2026-09-03 | 8,600 | 24.19% | **27.91%** | 27.91% | 7 | 0.1126 | 0 |
| 강동씨엔앨 | **1** | 2026-09-03 | 2,310 | 17.97% | **21.17%** | 21.17% | 7 | 0.0856 | 0 |
| 로보티즈 | **1** | 2026-09-04 | 315,000 | 15.87% | **15.87%** | 15.87% | 6 | 0.0386 | 0 |
| 해치텍 | **4** | 2026-09-07 | 16,090 | 19.89% | **19.89%** | 19.89% | 5 | 🔴 **—** | 0 |
| 빛과전자 | **1** | 2026-09-08 | 3,640 | 20.33% | **20.33%**(4봉) | 20.33% | 4 | 0.0453 | **1** |
| 범한퓨얼셀 | **1** | 2026-09-09 | 23,350 | 6.42% | **6.42%**(3봉) | 6.42% | 3 | 0.0480 | **1** |

(§1 에서 옮김)

- 주 표본의 차수 다중집합 = **[1, 1, 1, 1, 1, 4]** — 🔴🔴 **post6(1:3·2:1·3:3·4:2·5:1)보다 «1차에 몰려 있다»**
  ⇒ ***비교가능 쌍 수가 «구성으로» 줄어든다***(§1). 실제로 N 동률로 **원리적으로 비교 불가한 쌍이 78/378** 이다(§1-3).
  🔑 ***즉 이번 글이 누적에 더한 105쌍은 「10건이 왔으면 나왔을 쌍」보다 적다.***
- 🔴 **강동씨엔앨은 DB명이 「강동씨앤엘」**이다(PD-10 4번 · 이름 한 글자 차) — 인테이크 §1 표의 코드를 유일 출처로 썼다.
- 🔴 **해치텍 σ₂₀ = 결측** — 2026-08-25 첫 봉이라 21종가를 못 채운다(§4 B-4).
- **기존 22건의 값은 절단 2건 대체분(§6-4)을 빼면 한 자리도 안 바뀌었다**(§1 원표 대조).

### 2-3. 창5 `DD` 퍼짐 (계열 승계 · 「잡음 띠」 점검)

범위 **6.42~27.91** · 폭 **21.48%p** · `Q1` **16.72** · `Q2` **20.23** · `Q3` **23.50** ⇒ **IQR 6.79%p**
🔑 **산술 출처**(§1-4 에서 옮김): 폭 = `max − min` 을 **전정밀도**로 빼고 반올림한 값
(`27.906976744186053 − 6.423982869379019 = 21.4830` → **21.48%p**).
⚠️ **인쇄된 반올림값끼리 빼면 `27.91 − 6.42` = 21.49%p** 로 다르다 — ***두 수는 「틀린 값」이 아니라 「다른 잣대」다.***
이 문서는 **전정밀도 쪽(21.48)** 하나만 쓴다(계열 전체 동일).
(post5 누적 12건: 범위 9.79%p · IQR 3.25%p · post6 누적 22건: 범위 14.45%p · IQR 6.49%p
 — 출처 **`RESULTS_LADDER_TRANCHE_POST6_NUMBERS.md` §1-4**. 🔴 초판은 절 번호만 적어 **어느 파일의 §1-4 인지** 알 수 없었다)

⇒ ***「잡음 띠」는 세 회차 연속 넓어졌다.*** 그럼에도 `p` 는 0.5160 → 0.4914 → 0.4064 로
**문턱 근처에도 못 갔다** — **T1 불성립을 「띠 탓」으로 돌릴 수 없다**(post5 §2 결론 3회 연속 유지).

---

## §3. 주 판정 `LAD-T1` — 창5 · 순열 귀무 (§4-5 의무 1~3)

| 축 | `V_obs` | 비교가능 쌍 | `δ` 로 버린 쌍 | 귀무 평균 `V` | **`p = P(V ≤ V_obs)`** | `P(V=0)` | 귀무 평균 비교가능 쌍 |
|---|---|---|---|---|---|---|---|
| 창3 `[D,D+2]` | 123 | 249 | 60 | 126.30 | **0.4565** | 0.00000 | 252.37 |
| **창5 `[D,D+4]` (주)** | **127** | **266** | 42 | **133.42** | **0.4064** | 0.00000 | 266.66 |
| 창A `[D,발행일]` | 114 | 264 | 44 | 132.63 | **0.2297** | 0.00000 | 265.08 |

### 누적 계열 (「41쌍 → 재계산」 · `PREREG_POST6.md` §2-4)

| 회차 | 표본 | 비교가능 쌍(창5) | 신규 쌍 | `V` | **`p`** | 판정 |
|---|---|---|---|---|---|---|
| 1회차(post4) | 6건 | 13 | — | 7\* | 0.643\* | 보류(게이트 미달) |
| post5 | 12건 | 41 | +28 | 21 | **0.5160** | ⛔ 불성립 |
| post6 | 22건 | 161 | +120 | 80 | **0.4914** | ⛔ 불성립 |
| **post7(이번)** | **28건** | **266** | **+105** | **127** | **0.4064** | ⛔ **불성립** |

\* 1회차 표의 「위반 4 · p 0.228」은 **자료 절단의 산물**이었다(`RESULTS_LADDER_TRANCHE.md` §7) — 여기엔 **재계산값**을 적는다.

- 🟢 **게이트**: 창5 비교가능 **266 ≥ 40** ⇒ 판정한다.
- ⛔ **`LAD-T1` 불성립**: `p` = **0.4064 ≥ 0.05**. `V_obs` **127** 이 귀무 평균 **133.42** 와 사실상 같다
  ⇒ ***차수는 낙폭에 대해 무작위 배정과 구별되지 않는다.***
- 🔑 **세 회차 동안 표본이 12 → 28건(2.33배), 쌍이 41 → 266(6.49배)으로 늘었는데 `p` 는 0.5160 → 0.4064 로
  «문턱 근처로도 가지 않았다».** 검정력이 없어서 못 잡은 것이 아니라 **잴 것이 없다**는 쪽의 증거다.
  🔴 다만 이 문장은 **부재의 증거**로만 쓰고, 「효과 크기가 0 이다」로 확대하지 않는다.
- ⚠️ **`p` 가 세 회차 연속 «조금씩» 내려간 것을 추세로 읽지 말 것**(0.5160 → 0.4914 → 0.4064).
  전부 0.2~0.6 무작위 영역 안이고, 표본이 매회 통째로 커진다.
- 🔑 `P(V=0)` = **0.00000**(세 창 모두 · 200,000 표본에서 0회). post4 0.0056 → post5 0.00002 → post6 0 → post7 0 ⇒
  ***「공통 사다리 성립/기각」은 이제 완전히 판별력이 없는 참이다***(4회 연속 강화).
- **§1 의 부호 반전(1회차: 창3 나쁨 / 창5·창A 나음)은 재현되지 않았다**(§2) —
  🔑 **post6 은 문자 판정상 「재현」이 찍혔던 자리**인데(post6 §9 1번: *「0.5 문턱 위의 knife-edge」*),
  이번엔 창3 0.4565 도 0.5 아래라 그 조건이 **깨졌다**. ⇒ ***post6 의 「재현」이 knife-edge 였다는 자기신고가 옳았다.***

---

## §4. `LAD-T2`(반증축 · 필수) — `DD/σ₂₀` 정규화

**표본 26건**(제외 2건 = post5 삼양바이오팜 + 🆕 **post7 해치텍**).
🔴 **해치텍은 2026-08-25 첫 봉**이라 등록일(09-07) 직전 21종가를 못 채운다 —
***제외 목록에 그 이름이 있으면 그것이 사유다(값을 보고 뺀 것이 아니라 «구성»이다)***(§4).
`median(σ₂₀)` = **0.0677** ⇒ `δ_norm` = 1.0%p / (100·σ) = **0.1478**.

| 축 | `V_obs` | 비교가능 쌍 | 버린 쌍 | 귀무 평균 `V` | **`p`** | `P(V=0)` |
|---|---|---|---|---|---|---|
| 원축 `DD`(같은 26건) | 110 | 232 | 35 | 116.45 | **0.3947** | 0.00000 |
| **정규화축 `DD/σ₂₀`**(같은 쌍 집합) | 123 | 232 | 35 | 116.38 | **0.6361** | 0.00000 |
| 정규화축(부수: `δ_norm` 직접 적용) | 132 | 245 | 19 | 122.81 | **0.6703** | 0.00000 |

- 사전등록 문언: *「정규화 축의 `p` 가 원축보다 «낮으면» 가설 B(밴드가 종목별 스케일 비례) 지지」*
  ⇒ **0.6361 ≥ 0.3947** 이므로 ⛔ **가설 B 는 지지 없음.**
- 🔑 **post6 과 같은 부호 — 2글 연속**(post6: 0.7626 ≥ 0.5185). 🔴 **post5 는 반대 부호**였다(0.5562 < 0.6767).
  ⇒ ***post5 §4 가 미리 단 단서 — 「이 비교가 주는 것은 부호뿐이고 「B 가 지지된다」를 증거로 인용하면 안 된다」 — 가
  두 회차째 옳았다.*** 세 회차 모두 두 `p` 가 0.4~0.8 안에 있고, **차이 자체는 검정된 적이 없다.**
- 부수 민감도(`δ_norm` 직접 적용)도 **0.6703** 으로 같은 자리 ⇒ **δ 처리 방식에는 민감하지 않다**(post5·post6 과 같은 성질).
- ⚠️ **post5 값은 표본 11건짜리**였다(원축 0.6767 · 정규화 0.5562). ***세 회차의 표본이 11 → 21 → 26 건으로 서로 다르다***
  — 회차 간 `p` 를 직접 비교하지 않는다. **부호만 본다.**

---

## §5. `LAD-T3`(반증축 · 필수) — 축을 「등록일 → 발행일 경과 거래일」로

| 축 | `V_obs` | 비교가능 쌍 | 동률로 버린 쌍 | 귀무 평균 `V` | **`p`** |
|---|---|---|---|---|---|
| T3 `경과 거래일 E` | 103 | 262 | 45 | 132.06 | **0.1182** |

- `p` = **0.1182 ≥ 0.05** ⇒ **미발동 — `LAD-T1` 취소 사유 없음.**
  🔵 **표기 규약**(§8-11): T3 는 *「`p` < 0.05 면 T1 취소」* 라는 **취소 축**이므로 「통과」에 **✅ 를 쓰지 않는다**.
  `TV-N1`·`TV-N2` 와 같이 **「미발동」**으로 적는다. (T1 이 이미 불성립이라 이번에도 실효는 없다.)
- 🔑🔑🔑 **부호 감시(승계 · `RESULTS_LADDER_TRANCHE.md` §10)**:

| 회차 | T3 `p` | 창5 원축 `p` | 부호 |
|---|---|---|---|
| post5 | 0.2008 | 0.5160 | T3 가 낮다 |
| post6 | 0.1909 | 0.4914 | T3 가 낮다 |
| **post7** | **0.1182** | **0.4064** | **T3 가 낮다 — 3글 연속** |

⇒ ***정렬 신호가 조금이라도 있는 쪽은 낙폭이 아니라 「오래 들고 있었나」다.***
사전등록이 *「이번 표본에서 이미 위험하다」*고 적어둔 그 방향이 **표본을 2.33배 늘려도 유지됐고, 이번이 가장 낮다.**
🔴 **다음 회차에 `p` 가 0.05 밑으로 내려가면 `LAD-T1` 은 영구 취소**된다. 지금은 **취소되지 않았다** — 값만 기록한다.
⚠️ 🔴 **0.2008 → 0.1909 → 0.1182 을 「접근 중」으로 읽지 말 것** — 표본이 매회 바뀌고, 세 값 전부 0.05 의 2배 이상이다.

- 경과일 분포(post7 주 표본): 범한퓨얼셀 3 · 빛과전자 4 · 해치텍 5 · 로보티즈 6 · 서산 7 · 강동씨엔앨 7(§5).

---

## §6. 민감도 — 의무 인쇄분

### 6-1. 창5 절단 2건 · `P6-창5절단가드-A` — 🔴🔴 **`exact` 갈래가 문턱에 «닿는다»**

**절단 = `P6-WIN5_TRUNC` 1** — 빛과전자(09-08 등록 · 창5 **4봉**/5 · DD5 **20.33%**) ·
범한퓨얼셀(09-09 등록 · 창5 **3봉**/5 · DD5 **6.42%**).
두 값은 **「절단 시점 값」**이며 다음 글에서 창5 가 완전해지면 **대체**한다(PD-12 1번).

| 분모 갈래 | 산술 | 판정 |
|---|---|---|
| **주 판정 — 「이번 글 신규 건」 10** | **2/10 = 20.0%** vs 33.3% | 🟢 **미발동** |
| 🔴🔴 **`exact` 분모 갈래 6** | **2/6 = 33.3%** vs 33.3% | 🔴 **발동 조건에 «닿는다»** |
| `approx` 갈래 포함(§7) | 3/10 = 30.0% | 🟢 미발동 불변 |
| 참고 병기(누적 28건) | 2/28 = 7.1% | 참고 — 판정 분모가 아니다 |

(§1-1 에서 옮김)

- 🔴🔴 **post6 PD-11 3번이 분모를 «이번 글 신규 건»으로 못박아 두었으므로 주 판정은 신규 10 을 쓴다** —
  ***그러나 문턱을 넘는 갈래가 있다는 사실을 숨기지 않는다***(PD-12 3번).
  🔑 ***이것이 §2 의 「신규 10 ≠ 주 표본 6」 분기가 «판정에 닿은» 첫 자리다.***
- 🟢 **해치텍 창5 는 5봉 «완전»**이라 분자에 안 들어간다(`[D−19,D]` 가 10/20 인 것과 **다른 창**이다 · §6-5).
- 🔴 **방향 자기신고**(PD-1 4번 · 선택이 아니다): ***창이 09-15 였다면 이번 절단은 0건이고 `exact` 갈래 가드도 33.3% → 0% 로 꺼졌다.***
  동결 문언이 지시한 **09-11 은 가드를 «켜는 쪽 = 우리에게 불리한 쪽»**이지만 **고른 것이 아니라 정해져 있던 값**이다.
- ⚠️ 문턱 **1/3** 은 `REC-Y3` 에서 **차용**한 값이다(§1-6 6번 고지 승계) — **절단 비율에 대해 검증된 값이 아니다.**
- 🔑 전례: `RESULTS_LADDER_TRANCHE.md` §7 — 1회차의 창5 는 «잘려» 있었고 그 「위반 4」는 **실재하지 않는 값**이었다.
  **그래서 이번에도 절단을 플래그로 드러내고 제외 민감도를 함께 낸다.**

### 6-2. 절단 2건 제외 (PD-12 2번) · `P6-창5절단가드-B` — 표본 26건

| 축 | `V_obs` | 비교가능 쌍 | 버린 쌍 | 귀무 평균 `V` | **`p`** |
|---|---|---|---|---|---|
| 창3 | 116 | 219 | 51 | 110.45 | **0.6117** |
| **창5(주)** | **120** | **231** | 37 | 116.09 | **0.5817** |
| 창A | 108 | 230 | 39 | 115.29 | **0.3809** |
| T2 원축 `DD`(σ₂₀ 있는 24건) | 103 | 198 | 32 | 99.46 | **0.5852** |
| T2 정규화축 `DD/σ₂₀`(같은 쌍) | 110 | 198 | 32 | 99.48 | **0.7278** |
| T3 `경과 거래일 E` | 102 | 227 | 42 | 114.05 | **0.3001** |

- **`P6-창5절단가드-B` 미발동**: 주 판정 **불성립**(p 0.4064) ↔ 제외본 **불성립**(p 0.5817 · 게이트 231 ≥ 40)
  ⇒ **판정이 뒤집히지 않는다.**
- **T2 부호**: 제외본도 정규화(0.7278) ≥ 원축(0.5852) ⇒ 주 판정과 **같은 방향** ⇒ 갈리지 않는다.
- **T3**: 제외본 0.3001 vs 주 0.1182 — 둘 다 ≥ 0.05 ⇒ 0.05 문턱 기준 **갈리지 않는다**.
  ⚠️ 🔴 **다만 두 값의 «거리»는 크다**(0.1182 ↔ 0.3001) — 절단 2건이 T3 의 `p` 를 **절반 아래로 끌어내린다**.
  두 건 다 `E` 가 **3·4 로 가장 짧은** 건이라 경과일 축에 큰 영향을 준다. **판정은 안 갈리지만 이 사실을 적어 둔다.**
- **`LAD-P2`**: 절단 2건 제외 4건 중앙값 **20.53%** ⇒ 여전히 구간 안 ⇒ **갈리지 않는다**(§8).
- 🔑 이 표는 **`ANC-P3`(앵커 재설계)가 쓰는 창5 `L₅` 와 «같은 창·같은 제외 규칙»**이다 —
  그 레인은 `build_rows()` 를 import 해 쓴다(**새 코드 0줄** · §6).
- ⇒ **이 회차의 어떤 결론도 「창5 절단 의존」이 아니다.**

### 6-3. 재진입(2번째 사이클) 제외 (B-7 + PD-3) — 23건

제외: 코데즈컴바인(post5) · 한켐(post5) · 현대약품(post6) · 지투파워(post6) · **🆕 빛과전자(post7)**.

| 축 | `V_obs` | 비교가능 쌍 | 버린 쌍 | 귀무 평균 `V` | **`p`** |
|---|---|---|---|---|---|
| 창3 | 79 | 169 | 33 | 85.63 | **0.3680** |
| **창5(주)** | **83** | **177** | 23 | 89.54 | **0.3721** |
| 창A | 74 | 179 | 22 | 89.89 | **0.1973** |
| T3 `경과 거래일 E` | 75 | 173 | 31 | 86.33 | **0.2738** |

- 게이트 **177 ≥ 40** ⇒ 이 민감도 자체로도 판정 가능하고, 결과는 **불성립**으로 주 판정과 **같다**
  ⇒ 🟢 **「재진입 의존」 아님**(PD-3 2번의 ⛔ 조건 미발동 · post6 에 이어 2회 연속).
- **`P6-PRIOR_CYCLE_IN_WINDOW`** — 이번 글 재진입 **3건**: 지투파워 **1** · 한국화장품제조 **1** · 빛과전자 **0**(합 **2**).
  🔴🔴 **주 표본(`exact` 6) 안의 재진입은 빛과전자 1건뿐이고 그 건의 플래그가 «0»** 이다
  ⇒ **판정 분모 안 플래그 = 0**(직전 등록일 08-05 < 창 시작 08-11 ⇒ **구성상 0** — ***관측이 0 인 것과 다르다***).
  글 전체 플래그 2건은 **둘 다 `approx` 갈래**다.
- 🔴 **지투파워는 이 계열 최초의 «3번째 사이클»**이다(post4 08-13 → post6 08-26 → post7 「9월 초」).
  §1-5 문언은 *「2번째 사이클」*이라 쓰여 있으나 플래그 정의가 *「자기 **직전** 사이클의 등록일」*이라
  **3번째에도 그대로 적용된다**(***문언 확장이 아니다*** · PD-3).
- 🔴 **두 민감도를 합치지 않는다**: 재진입 3건 중 **2건이 `approx`** 라 §6-3 의 제외와 §7 의 `approx` 갈래가 **겹친다**.
  ***겹친다고 한 표로 합치면 어느 축이 판정을 움직였는지 구분할 수 없게 된다***(PD-3 마지막 줄).

### 6-4. 🆕 post6 절단 2건의 값 **«대체»** (PD-12 4번 · 누적 쌍 재계산 규약)

주 창이 2026-09-11 로 전진해 post6 의 절단 2건은 창5 가 **완전**해졌다 ⇒ 이번 회차에 값을 대체한다.

| 종목 | 등록일 | post6 절단 시점 창5 봉수 / **DD5** | post7 창5 봉수 / **DD5(대체)** | 차이 |
|---|---|---|---|---|
| 현대약품 | 2026-09-01 | 4봉 / **24.00%** | 5봉 / **24.00%** ✅ 완전 | **+0.00%p** |
| 비에이치 | 2026-09-01 | 4봉 / **14.00%** | 5봉 / **14.00%** ✅ 완전 | **−0.00%p** |

(§1-1b 에서 옮김)

- 🟢 ***두 건 다 값이 «움직이지 않았다»*** — post6 의 절단 시점 값이 **결과적으로 정확했다**.
  🔴 그러나 이것은 **우연**이다: 5번째 봉의 저가가 앞 4봉의 최저를 못 깼다는 뜻일 뿐이고,
  ***절단 시점 값이 언제나 맞는다는 근거가 아니다***(post6 이 「대체한다」고 적어둔 규약은 그대로 유효하다).
- 🔴 **왼쪽 열은 인용이다 — 재계산하지 않았다.** 출처 = `RESULTS_LADDER_TRANCHE_POST6_NUMBERS.md` §1-1·§8.
  ***그 파일은 한 byte 도 고치지 않았다*** — 대체값은 **이 산출물 안에서만** 산다(md5 불변 의무).
- 이 대체는 **§3 이후의 모든 누적 표(`LAD-T1`·`T2`·`T3`)에 이미 반영**돼 있다.

### 6-5. PD-12 재확인(참고) — 주 표본 6건의 `[D−19, D]` 창 봉수

20봉 미만 = **1건**(해치텍 **10**/20) ⇒ PD-12 의 「분자 1」과 **일치 ✅**.
`P6-절단가드-A`(**선정·등록일 축**) 산술 = **1/6 = 16.7%** vs 1/3 ⇒ **미발동**.

🔴 **이 축의 판정에는 쓰지 않는다** — ***이름만 같고 대상이 다르다***(PD-11 3번 · 이름 분리).
🔴 **방향**(§1-6 2번 승계): *「창이 짧으면 「창 최고」가 되기 쉽다 ⇒ `M1` 관측에 유리」* —
해치텍의 짧은 창은 `Q1-R2`·`P6-M1′` 에 **유리한** 방향이고, 그 축의 절단 제외 민감도는 `run_regday_post7.py` 레인이 낸다.

---

## §7. 민감도 — `approx` 2건의 **갈래별** 값 (PD-4 2번 · §1-4 창 규약 «첫 발동»)

저자가 「9월 초」·「8월 말」이라 적은 두 건은 **등록일이 한 값이 아니다** ⇒ 창 규약대로
**그 구간의 «모든 거래일»을 갈래로 만들어** 값을 전부 인쇄한다. **주 판정에는 넣지 않는다.**

### 7-1. 지투파워(`388050`) · N = 3 · 「9월 초」 = `[2026-09-01, 2026-09-10]` · 갈래 **8개**

🟢 PD-4 2번 실측 **8개**와 **일치**. DD5 갈래 범위 = **4.54% ~ 16.39%**(§7-1 표).
🔴 **창5 절단 갈래 = 3/8**(D = 09-08 4봉 · 09-09 3봉 · 09-10 2봉) — PD-12 5번 실측과 **일치 ✅**.

### 7-2. 한국화장품제조(`003350`) · N = 1 · 「8월 말」 = `[2026-08-21, 2026-08-31]` · 갈래 **7개**

🟢 PD-4 2번 실측 **7개**와 **일치**. DD5 갈래 범위 = **7.60% ~ 23.69%**(§7-1 표).
🟢 **창5 절단 갈래 = 0/7**(7갈래 전부 완전) — PD-12 5번 실측과 **일치 ✅**.

- 🔑 **두 건의 DD5 가 갈래에 따라 «크게» 흔들린다** — 지투파워 4.54~16.39%p(폭 11.85%p) ·
  한국화장품제조 7.60~23.69%p(폭 16.09%p). ***날을 고르면 이 축의 주 변수를 3~4배 움직일 수 있다.***
  그래서 **고르지 않고 전 갈래를 인쇄**했다.
- 🔴 `approx` 갈래를 가드-A 분자에 넣어도 **3/10 = 30.0% < 1/3 ⇒ 미발동 불변**(§6-1).
- 🔴 **이 표는 판정이 아니다.** `RNK-D5`·`PREREG_S5_FUND_NEWS_OOS.md` §1-1·`PREREG_ANCHOR_REDESIGN.md` §2-3 세 동결본이
  *「판정 분모 = `exact` 만 · `approx` 는 의무 민감도」*를 지시한다 ⇒ **값을 인쇄하되 판정 언어를 쓰지 않는다**.
- 🔴 **§6-3 과 겹친다**: 두 건 다 재진입이다. 위에서 적은 대로 **합치지 않는다**.

---

## §8. 개별 예측 `LAD-P1`~`P3` (§5-3 · **값 기록 · 기각 사유 아님**)

- **`LAD-P1`** 「저자가 체결 차수를 명시」 — 7번째 글 **항목 13건 중 차수 명시 13건**.
  **분모 정의 = 「DB 있는 신규 건」 = 10건** ⇒ **10/10 = 100%**
  (코드 **13/13 DB 존재** — 해치텍 `0155E0` 포함 · PD-11) ⇒ ✅ **성립 · 4글 연속**(6/6 → 6/6 → 10/10 → 10/10).
  🔑 **분모가 「신규 10」이고 주 표본 6 이 아니다** — `LAD-P1` 은 창을 안 쓰므로 `none` 2건도 셀 수 있다.
  ***같은 문서 안에서 항목마다 분모가 다르다는 사실을 그대로 적는다.***
- **`LAD-P2`** 「신규 건 `DD`(창5) 중앙값 15~35%」 — 🔴 **주 분모 = 신규 ∧ `exact` 6건**
  (「신규 10」 갈래는 `none` 2 의 `D` 가 없어 ***계산 자체가 안 선다*** · `approx` 2 는 §7 갈래 표).
  값 = 27.91 · 21.17 · 15.87 · 19.89 · 20.33 · 6.42 ⇒ **중앙값 20.11%** ⇒ ✅ **구간 안**.
  - 절단 2건 제외(PD-12 2번): 4건 중앙값 **20.53%** ⇒ ✅ 구간 안 ⇒ **갈리지 않는다**.
  - 🆕 **하한 이탈 관찰**(보고서 §6 #7 · 이번 글 의무): post6 중앙값 **15.62%** 는 하한 15% 까지 **0.62%p** 였다
    ⇒ 이번 값 **20.11%** 는 하한까지 **+5.11%p**(상한까지 +14.89%p) ⇒ **하한 이탈 없음**.
    🔴 ***계열의 3회 연속 단조 하락(23.53 → 20.34 → 15.62)이 이번에 끊겼다.***
    🔑 ***`LAD-P2` 는 「구간 안이냐」만 묻는 예측이라, 하한에 붙어 있다는 사실은 «예측의 강도»가 아니라
    «다음 회차의 취약성»으로만 기록한다*** — 그 취약성이 이번엔 **해소된 쪽**으로 움직였다.
  - ⚠️ **분모가 6 이라 중앙값이 3·4번째 값의 평균**이다 — **범한퓨얼셀 6.42%**(절단 3봉)라는 극단값이 있는데도
    중앙값이 견뎠다. ***평균이었다면 18.60% 근처로 내려갔을 것이고, 동결 문언이 중앙값이라 그렇게 되지 않았다.***
- **`LAD-P3`** 「`first_only`(1차만 체결) 건 ≥ 1」 — 7번째 글 신규 **6건**(서산·강동씨엔앨·로보티즈·빛과전자·
  한국화장품제조·범한퓨얼셀 · 전부 N = 1 · 그중 `exact` **5건**) + 후속 아난티 1 ⇒ ✅ **성립**
  (post4 **0** → post5 **1** → post6 **3** → **post7 6 = post6 의 두 배**).
  🔴 `REC-Z4`(「`first_only` ∧ 서로 다른 값 레그 3개」) 충족 건의 **귀결(즉시 `L` 판정)은 🔒 결정 ④로 소멸**했다(PD-13 1번)
  ⇒ 이 축은 **「건 수」만 기록**한다.
  🔑🔑 ***`LAD-P3` 의 6건이 §2-2 의 「1차 몰림」과 같은 사실이다*** — 같은 관측을 두 항목이 서로 다른 각도로 적는다.
  **두 항목을 독립 증거로 세지 않는다.**

---

## §9. 🟡 모호 지점 — 양쪽 인쇄 (값 보고 규칙을 바꾸지 않았다)

1. 🔴🔴 **`P6-창5절단가드-A` 가 분모 갈래에 따라 갈린다** — 신규 10 분모 **2/10 = 20.0%**(미발동) ↔
   `exact` 6 분모 **2/6 = 33.3%**(문턱에 **닿는다**). **post6 PD-11 3번이 「이번 글 신규 건」으로 못박아 주 판정은 신규 10** 이고,
   ***문턱을 넘는 갈래가 있다는 사실을 숨기지 않았다***(§6-1).
2. 🔴🔴 **「신규 10」과 「주 표본 6」이 갈린 첫 회차**다(§2). 표본 수와 가드 분모가 다른 수가 되었고, **둘 다 인쇄**했다.
   ***어느 한쪽으로 통일하면 그것이 새 자유도다.***
3. **창5 절단 2건**(빛과전자 4봉 · 범한퓨얼셀 3봉) — 「절단 시점 값」이다. 분모 포함(주)/제외(민감도) **양쪽 인쇄**(§6-1·§6-2).
4. **post6 절단 2건의 값 대체**(§6-4) — 「절단 시점 값 → 대체 값」을 둘 다 적었다.
   🔴 **옛 산출물은 한 byte 도 고치지 않았다**(md5 불변 의무).
5. **재진입 3건** — 분모 포함(주)/제외(민감도) 양쪽 인쇄(§6-3). 🔴 주 표본 안 플래그는 **0**이고, 플래그 1 인 두 건은
   **`approx` 갈래**다(§7). **두 민감도를 합치지 않았다.**
6. 🔴 **`MANUAL` 2건은 라벨 때문에 빠진 것이 아니다**(B-2 carve-out) — **후속**이라 빠졌다(§2-1).
   규칙을 넓히지도 좁히지도 않았다. ⚠️ **같은 두 건이 `EXIT-` 축에서는 «라벨 때문에» 빠진다.**
7. 🟡 **창A `p` = 0.2297 이 세 창 중 가장 낮고**, 재진입 제외본에서는 **0.1973** 까지 내려간다.
   창A 는 **민감도**이고 주 판정은 창5 로 동결돼 있다. 어느 창도 0.05 근처가 아니므로 **결론은 바뀌지 않는다.**
   🔴 그러나 창A 는 **매도 완료 후 저가를 섞는 창**이고 `E`(보유 기간)와 구조적으로 얽혀 있다 —
   **T3 의 부호 감시와 같은 방향의 관측**이라 기록해 둔다. **창을 바꾸지 않는다.**
8. 🟡 **`LAD-T2` 부호가 post5 ↔ post6·post7 사이에서 뒤집혔다.** 세 회차 다 0.4~0.8 안이다
   ⇒ **「부호뿐」이라는 단서가 유지된다.** 어느 회차의 부호도 증거로 인용하지 않는다.
9. 🟡 **post6 §9 1번의 「부호 반전 재현」 knife-edge 가 이번에 «깨졌다»**(§3) — post6 은 기계 판정상 「재현」이 찍혔는데
   이번엔 창3 `p` 0.4565 < 0.5 라 조건이 안 선다. ***그 knife-edge 자기신고가 옳았다는 실증***이다.
10. 🟡 **T3 의 `p` 가 절단 2건 제외로 0.1182 → 0.3001 로 크게 움직인다**(§6-2) — 판정은 안 갈리지만
    ***T3 의 값이 「가장 짧은 `E` 두 건」에 크게 의존***한다는 사실을 적어 둔다.

---

## §10. 한계 (승계 + 이번 고유)

- 🔴 **`V` 는 개수이고 귀무에서 비교가능 쌍 수가 함께 흔들린다**(관측 266 ↔ 귀무 평균 266.66).
  사전등록이 정규화하지 않기로 동결했으므로 그대로 뒀다. `p` = 0.4064 는 문턱에서 멀어 이 흔들림으로 결론이 바뀌지 않는다.
- 🔴🔴 **post7 주 표본이 1차에 몰려 있어**(차수 [1,1,1,1,1,4]) **N 동률로 원리적 비교 불가 쌍이 78/378** 이다(§1-3).
  ⇒ ***이번 글이 더한 105쌍은 「10건이 왔으면 나왔을 쌍」보다 적다.*** 표본 수와 정보량이 비례하지 않는다.
- 🔴 **`N` 이 저자 서술 의존**이다 — *「N차 매수된 상태」*를 「N차까지 체결」로 읽었고 **총 분할 수는 모른다**.
- 🔴 **앵커 `H` = 등록일 고가는 가정**이고 사전등록은 앵커 민감도를 넣지 않았다.
  🔴 그 앵커가 post6 에서 **무너졌고**(`REC-Z3` · 1/5 = 20.0% 였지만 민감도에서 뒤집혔다),
  이번 회차에 `PREREG_ANCHOR_REDESIGN.md` 의 `ANC-P1`~`P3` 이 **처음 열린다**
  ⇒ ***`run_anchor_redesign.py --mode post7` 산출물과 «함께» 읽어야 한다***(§4 #44).
- 🔴 **매도 완료일을 모른다.** 창3/창5/창A 전부 인쇄했고 셋 다 `p` ≥ 0.22 다.
- 🔴 **글 간 풀링이 국면을 섞는다** — post4~post7 은 **4주 연속**이고 그 사이 KOSPI 급락일이 있었다.
- 🔴 **표본이 저자가 «올리기로 고른» 매매다**(`PREREG_SELECTION.md` §0 결과 조건화 위협).
- 🔴 **해치텍은 2026-08-25 첫 봉**(신규 상장)이라 `[D−19,D]` 가 **10/20** 이고 04-01~08-04 구간은 **0봉**이다.
  이 축은 창5·창A 만 쓰므로 막히지 않지만, **σ₂₀ 의 21종가 창을 못 채워 T2 표본에서 빠진다**(§4).
- 🔴 **우리기술투자는 `daily_prices` 04-01~08-04 봉 36/85**(191종목 구멍 계열) —
  σ₂₀ 이 「구멍을 건너뛴 21봉」일 수 있다. **값은 그대로 쓰고 사실만 적는다.**
- 🔴 **라벨 `SL` 2건(한전기술·한전산업)은 `none` 이라 축 밖**이고, `MIX`/`MANUAL` 3건은 **후속이라 축 밖**이다 —
  §4-1 이 제외하는 것은 **`MANUAL` 뿐**인데 **이번 글에서 그 규칙이 발동할 자리가 없었다**(§2-1).
  규칙을 넓히지도 좁히지도 않았다.
- 🔴 **레메디(post5 · 차수 5)는 DB 에 없어 여전히 빠져 있다** — 누적은 28건이 아니라 29건이 될 수 있었다.
- 🔴 **프로그램 버전이 이 계열 최초로 «없다»**(PD-8) ⇒ 버전 공변량을 쓰는 분석에서 post7 행은 **분모 밖**.
- 이 분석은 **라이브 채택 대상이 아니다**(`PREREG.md` §0-2).

---

## §11. 🟢 다음 회차에 그대로 이어붙일 것 (동결 유지)

- **`δ`·창5·게이트 40·시드·순열 표본 수를 손대지 않는다.** 창5 로 계속 누적해 다시 `p` 를 낸다.
- **`LAD-T3` 를 매회 함께 낸다.** 부호(T3 `p` < 창5 `p`)가 **3글 연속** 유지됐고 이번이 가장 낮다(0.1182) —
  ***「차수는 낙폭이 아니라 보유 기간을 따라간다」*** 쪽이 여전히 유일하게 살아 있는 설명이다.
  `p` < 0.05 로 내려가면 **T1 영구 취소**. ⚠️ **0.2008 → 0.1909 → 0.1182 을 추세로 읽지 않는다.**
- **실행 시점의 DB 최신 봉과 «창 종료»를 둘 다 산출물에 박는다** — 이번엔 **불일치**했다
  (DB 2026-09-15 ↔ 창 2026-09-11). 🔑 ***그 불일치 자체가 절단 2건을 만들었다***(§6-1 방향 자기신고).
- **창5 절단 2건(빛과전자·범한퓨얼셀)의 DD5 를 다음 글에서 «완전한 창»의 값으로 대체**하고,
  그때 `P6-WIN5_TRUNC` 를 0 으로 내린다(PD-12 4번). 🔑 **이번 회차가 post6 분에 대해 그 규약을 이미 실행했다**(§6-4).
- **`LAD-P2` 의 중앙값 계열(23.53 → 20.34 → 15.62 → 20.11)을 계속 적는다** — 구간을 벗어나면 그 사실만 적고 문턱은 안 옮긴다.
- **`P6-창5절단가드-A` 의 분모를 「신규 건」으로 유지한다** — 🔴 `exact` 갈래가 이번에 문턱에 닿았으므로
  ***다음 회차엔 갈래가 발동할 수 있다.*** **분모 정의를 값 보고 바꾸지 않는다** — 바꾸려면 사전등록에서.
- **`LAD-T2` 부호는 회차 간에 뒤집힌다는 것이 실증됐다** — 다음 회차에 어느 부호가 나오든 **증거로 인용하지 않는다.**

---

### 재현 방법

```
cd RoboTrader_template/backtest/tasso_program_journal
python run_ladder_tranche_post7.py     # → RESULTS_LADDER_TRANCHE_POST7_NUMBERS.md (약 10초)
```

- 🟢 **두 번 실행해 byte 동일**을 확인했다(2026-09-15 20:13 실행분 ↔ 21:13 재실행분 · 시드 고정이라 결정적).
- `run_ladder_tranche.py`·`run_ladder_tranche_post6.py`(동결 산출물 생성기)는 **수정하지 않았다** —
  통계 핵·표 포맷·누적 목록·가드·판정 함수를 **import 해 재사용**했다.

[[RESULTS_LADDER_TRANCHE_POST7_NUMBERS]] · [[RESULTS_LADDER_TRANCHE_POST6]] · [[RESULTS_LADDER_TRANCHE]] ·
[[PREREG_LADDER_TRANCHE]] · [[PREREG_POST6]] · [[PREDECISION_2026-09-15_post7]] · [[INTAKE_2026-09-15_post7]] ·
[[LABELS_2026-09-15_post7]] · [[PREREG_ANCHOR_REDESIGN]] · [[RESULTS_ANCHOR_POST7]] · [[RESULTS_RECONSTRUCT_POST7_NUMBERS]]
