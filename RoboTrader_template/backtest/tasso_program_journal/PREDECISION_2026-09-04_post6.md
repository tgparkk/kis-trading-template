# 6번째 글 — 수집 «후» · 계산 «전» 결정 (2026-09-04) — verifier 4패스 APPROVE 판

**대상** `logNo=224401108114` · 「9월 1주차 자동매매 프로그램을 이용한 매매내역」 · **발행 2026-09-04(금)** · 프로그램 **1.0.40**
**수집** `python fetch_post.py 224401108114` — 2026-09-04 **18:50:04 KST**(html mtime) · 워크트리 `D:/tmp/kis-wt-tasso-p6s5`(브랜치 `fix/tasso-post6-s5-fixes` · HEAD `2a17e81`)
**보관** `D:\archive\tasso-program-journal-20260904\` — html 218,400B · txt 8,197B · images.json (README 규약대로 원문은 커밋하지 않는다)

| 파일 | md5 |
|---|---|
| `post_224401108114.html` | `191f4dbecd6075d0eb533050698740c8` |
| `post_224401108114.txt` | `896abe5dd5f0bba8228dc472f446c2fb` |
| `post_224401108114_images.json` | `e06ffd146387dd883d6537d8ca27f423` |

> 🔴 **이 문서의 지위** = `PREREG_POST6.md` §0-2 3번 — *「§1 에 없는 결정이 수집 후에 «새로» 필요해지면 그때 별도 파일로 적고 계산 «전» 커밋한다」*.
> 아래 결정은 전부 **저자 문장·원장 구조·DB «존재» 사실**만 보고 정했다. **판정 수치는 하나도 보지 않았다**(§0-2 「4번과 6번 사이에 어떤 수치도 보지 않는다」).
> 🔴 **예상(09-05 토)보다 하루 이른 금요일 발행**이다. 동결 4커밋은 전부 fetch «전»이다(§PD-0).
> ✅ verifier(opus · 읽기 전용 레인) **4패스 APPROVE**: 1패스 블로커 7(B1~B7 · 라벨 판별표·X2 분모 «문언 복귀» 5 + 신설 결정 2)·개선 7 → 2패스 블로커 2(N1 `SL` 최소 n 출처 없음 → X7 대로 n=1 · N2 MANUAL 근거 문구)·개선 10 → 3패스 블로커 1(BL-1 PD-14 원장 두 축 혼합 → 단일 축 복귀) → 4패스 블로커 0. 원문 대조·DB 봉수·달력·git 순서·누출 감사는 매 패스 전건 통과.

---

## PD-0. 순서 증거 — 동결 커밋이 fetch 보다 앞선다 (그리고 «증거 기제»를 정정한다)

| 동결 | 커밋 | 시각(KST) | origin 푸시 | fetch(09-04 18:50:04) 보다 |
|---|---|---|---|---|
| `PREREG_POST6.md` §5 측정 장치 6건(C-17~C-22) | `7712e03` | 2026-08-30 17:55:33 | ✅ `origin/fix/tasso-post6-s5-fixes` | **앞** ✅ |
| RNK 훈련·선택 동결 `FREEZE_RANKING_2026-08-31.md` | `8d28e14` | 2026-08-31 22:24:29 | ✅ | **앞** ✅ |
| WRC 탐색·동결 `FREEZE_WRC_2026-09-02.md` | `88afe62` | 2026-09-02 22:54:39 | ✅ | **앞** ✅ |
| SEC 배선·동결 `FREEZE_SECTOR_2026-09-03.md` | `2a17e81` | 2026-09-03 00:19:11 | ✅ | **앞** ✅ |

(`git branch -r --contains` 4/4 — 로컬 mtime 외에 **제3자 타임스탬프(origin)**가 붙어 있다.)

🔴 **증거 기제 충돌 신고**: `FREEZE_RANKING` §7-B **#18** · `FREEZE_WRC` §7-B **#22** · `FREEZE_SECTOR` §7-B **#23** 은 *「`post_<logNo>.html` 을 «추가한» 커밋」*을
순서 증거로 지정했다. 그런데 **`README.md` 「🔴 원문은 저장소에 없다」 규약**(타인 저작물 — 커밋 금지)과 충돌하고,
실측 **post1~5 의 html 은 어느 브랜치에도 커밋된 적이 없다**(`git ls-files | grep post_` = 0 · `git log --all --diff-filter=A -- '*post_2*'` = 0건).
⇒ **그 증거는 처음부터 발생할 수 없는 증거였다**(죽은 가드 · 세 동결본 공통). **대체 기제(동결)**:

- 순서 증거 = **이 문서(`PREDECISION_2026-09-04_post6.md`)를 «추가한» 커밋** · 이 문서가 원문 3파일의 **md5** 를 박는다(위 표).
- #18·#22·#23 판정 = `git log --diff-filter=A -- PREDECISION_2026-09-04_post6.md` 의 커밋이 `8d28e14`·`88afe62`·`2a17e81` 의 **후손**인가.
- 🔑 계열 규칙 후보: ***증거 파일이 «규약상 커밋 금지»면 그 증거는 영원히 안 생긴다 — 증거 기제를 동결할 때 그 파일이 커밋 가능한지 먼저 본다.***

## PD-1. 발행일·창 종료일 — **2026-09-04(금) · 거래일**

- `RESULTS_LADDER_TRANCHE.md` §1 **B-1**(창A 의 「글 발행일」 규약) · `PREREG_WEIGHTED_RECON.md` **`WRC-D7`**(*「실행 시 DB 스냅샷 최대일」*) — 둘 다 **2026-09-04** 를 준다(발행일 = 거래일 = DB 최대일).
  ⇒ **창A = `[D, 2026-09-04]`** · **DB 최신 봉 = `2026-09-04`**(`daily_prices` 09-04 2,766행 · 09-03 2,766 · 09-02 2,767).
- 산출물 표기(§7-B #13 형식): **「창 종료 2026-09-04 = 발행 당일 봉 «포함»」**을 매 산출물에 박는다.
- ⚠️ 한계(기록): 저자가 09-04 **장중**에 썼을 가능성을 본문으로 가릴 수 없다. 규약대로 09-04 를 포함하며 **새 민감도를 만들지 않는다**(자유도 추가 금지).
- 🟢 **재현성 창**: (E′) 첫 발효 = 09-07(월) 07:40 W1. 그 전까지 `daily_prices` 쓰기 0 ⇒ **주말 계산은 같은 스냅샷 위에서 돈다.**
  `regen_gate.py --update` 가 지문 이동(08-28 → 09-04)을 잡아야 한다(C-19 수정 후 첫 실전).

## PD-2. «기존 건 후속» 2건 — 광전자(post5 #6) · 삼양바이오팜(post5 #5)

저자가 **같은 등록일·같은 사이클**에 대해 **post5 에 없던 레그**를 적었다(post5 시퀀스 `15.81, 9.21, 9.21, 6.99, 0.93 ~` / `11.50, 11.50 ~` 는
post6 에 **다시 나오지 않는다**). 저자의 시점 표기는 둘 다 **「지난주」**다(광전자 *「지난주 나머지 물량은 본전매도 처리」* · 삼양 *「지난주 추가매수 중」*) —
그 시점이 어느 주인지는 본문으로 확정할 수 없으므로 **「이번 주」라고 쓰지 않는다**. 사전등록에 이 형태의 규정이 없다(§1-5 는 «다른 등록일» 재진입만).

**결정(동결)**:

1. **원장** — post6 행으로 **신규 등재**(원장 단위 = 글 · 전례: post4 솔트룩스·빛과전자 「기존 건 종료」 · post5 한성기업 「7월31일 글 기존 건」).
   **`reg_date` 비움 · `reg_date_precision = none`** — 한성기업 전례 그대로. 저자 재명시 등록일(08-05 · 08-21 오후)과
   `CONTINUATION post5#6` / `CONTINUATION post5#5` 는 **`narrative` 에** 적는다.
   🔴 **post5 원장 행(open_ended=1)은 손대지 않는다** — `RESULTS_*_POST5_NUMBERS.md` md5 불변 의무(`PREREG_POST6.md` §5-5 6번).
2. **등록일 축 전부**(`SEL-`·`P6-S1h`·`Q1-`·`P6-M`·`REG-`·`LAD-`·`REC-`·`WRC-`·`RNK-`·`SEC-`·`TV-`) — **신규 분모에서 «제외»**.
   근거 = **이중계상 금지**: 이 등록 사건은 post5 신규 분모에서 이미 계상됐다. 값을 보고 뺀 것이 아니라 **정의(등록일 미명시 행과 같은 처리)로** 빠진다.
3. **EXIT 축** — 라벨은 부여한다(`LABELS` 대상 · 한성기업 전례). 두 건 다 **`MIX`**(PD-6) ⇒ `EXIT-E1`·`EXIT-E4` 는 **정의상 대상 아님**(`PREREG_EXIT_V2.md` §3 *「`MIX`·`SL` 건에는 적용하지 않는다 … 값만 기록」*).
   - **`EXIT-E2`(병기 · 누적 계열)**: 분모 = *「저자가 **본전 매도**라고 적은 레그」*(§1-1 `breakeven_note`) — **라벨·등록 사건과 무관**하다(post5 전례: E2 를 깬 한켐은 `MANUAL` 인데 E2 분모 2/3 에 있었다 — `PREREG_POST6.md` §7-C 2).
     후속 2건의 본전매도 레그는 post5 에 없던 **새 관측**이라 이중계상이 아니다 ⇒ **누적 E2 계열은 후속 «포함»(신규 완결 note 2 + 후속 2 = 4)으로 잇는다.** 신규만(2)은 민감도.
   - **`EXIT-X2`·`EXIT-X8`**(동결 적용 대상 = *「`TP` 완결 건」* · post5 X2 절): 후속 `MIX` 는 **동결 적용 대상 밖** ⇒ **확장 민감도**로만 인쇄(판정이 갈리면 🔴 「후속 의존」 — 지지 선언 금지).
4. **연결 시퀀스**(post5 + post6: 광전자 10레그 · 삼양 7레그)는 **«기록만»**. 삼양은 **2차 매수 «후» 레그**라 「하나의 평단」 전제가 깨진다
   ⇒ `REC-` 축(복원)에 넣지 않는다. 광전자도 post5 4차 매수 상태의 후속이라 평단 불변 보장이 없다 — 같은 처리.
5. 🔴 **방향 자기신고(E2)**: 후속 2건의 E2 레그는 저자 시퀀스상 **−7.51 · −1.73**(`LABELS` X2 절)이라 둘 다 `|ret| < 1%` 를 못 넘는다 ⇒ **후속 «제외»는 E2 에 «유리한» 방향**이다. 그래서 3번대로 누적 계열은 **포함(4)**으로 잇고, 제외(2)는 민감도로만 인쇄한다. E2 는 병기 항목이라 판정을 만들지 않지만 두 분모를 같은 표에 둔다.

## PD-3. 재진입(2번째 사이클) 2건 — `PREREG_POST6.md` §1-5 그대로

| 종목 | 직전 사이클 | 이번 등록일 | `[D−19, D]` 창(거래일 달력) | **`P6-PRIOR_CYCLE_IN_WINDOW`** |
|---|---|---|---|---|
| 지투파워 | post4 #4 · **08-13** | 08-26 | `[07-29, 08-26]` | **1** (08-13 ∈ 창) |
| 현대약품 | 07-31 글 #12 · **등록일 미명시**(`none`) | 09-01 | `[08-04, 09-01]` | **0** — 직전 글 발행 07-31 < 창 시작 08-04 ⇒ 구성상 0 · 산출물에 「직전 등록일 미상」 병기 |

- 분모 **포함** + 재진입 제외 **민감도 의무 인쇄** + 플래그 건수 인쇄(§1-5 1·2·3 그대로). `WRC-D6` 동일.

## PD-4. 미완결 판정 — **이번 글에는 `~` 가 없다** (문언 부류 · 신설)

post4·post5 의 미완결 표지 `~` 가 **0건**. 대신 서술이 가른다:

| 서술 | 판정 | 건 |
|---|---|---|
| 「나머지 물량은 보유 중」 | **미완결** | 한라캐스트 · 아난티 · 지투파워 · 우리기술투자 (**4**) |
| 「전량매도」「전량 매도처리」「분할매도 완료」 | **완결** | 헥토파이낸셜 · 아이티센글로벌 · 현대약품 · 원익 · 쿠콘 · 비에이치 (**6**) + 후속 광전자 · 삼양 |

**결정(동결)**: **주 판정 = 서술 기준**(`PREREG_EXIT_V2.md` §2 「미완결 레그는 제외한다」의 실질). `EXIT-X6` 동결 문언은 *「`~` 건의 마지막 레그 하나만 빼고」*라
**글자 그대로는 이번 글 0건에 적용**된다 — 그래서 **`~` 문자 기준(X6 적용 0건)을 민감도로 병기**하고, 두 기준이 판정을 가르면 🔴 **「표기 의존」**으로 적는다.
`EXIT-X6` 은 **`EXIT-E1`·`EXIT-E4` 한정**(§1-8). 서술 기준 미완결 `TP` 3건(아난티·지투파워·우리기술투자)에 적용 · 아난티(1레그)는 적용 시 레그 0 ⇒ `EXIT-E1` 분모에서 빠진다(인쇄).
🔴 **방향 자기신고**: `~` 0건 → 서술 기준으로 `EXIT-X6` 을 «확대» 적용하는 것은 **마지막(대개 가장 낮은) 레그를 버리는 쪽**이라 `EXIT-E1`/`EXIT-X5`(비증가 ≥ 90%)에 **유리한 방향**이다
(생략 편향 §2-3 과 같은 방향). 아난티 분모 제외는 반대로 분모를 줄이는 쪽이다. 둘 다 인쇄한다.
**생략 편향 비율**(의무 인쇄): 계열 연속값 = **신규 `TP` 9건 기준 3/9**(§2-3 계열 *「post4 2/4 → post5 4/6」* 은 `TP` 분모) · 신규 10건 기준 4/10 은 병기.

## PD-5. `TV`(거래대금/시총) 자가보고 — **0건** ⇒ 축 전체 «미룸»

본문에 「배」·「거래대금/시총」·절대 거래대금 진술이 **종목 단위로 0건**(*「시장의 거래대금은 말라붙고」*는 시장 서술 · 종목 아님 — grep 확인).
⇒ `PREREG_POST6.md` §1-3 부류 A~F 해당 0 · `TV-W1`~`W9`·`P6-W10`·`TV-N1`~`N3` 전부 ⛔ **「미룬다」**(§6 그대로 · 규칙을 넓혀 열지 않는다).
§1-4 창 규약(「N월 초」)도 발동 0. `SEL-S2`(우리가 재는 백분위)는 **저자 자가보고와 무관**하므로 그대로 잰다.

## PD-6. 라벨 문언 부류 (동결 판별표 그대로) · `EXIT-E2`/`X2` 분모

### 라벨 — `PREREG_EXIT_V2.md` §2 판별표 문언대로

| 판별 근거(동결 문언) | 이번 글 해당 |
|---|---|
| `SL` = *「저자가 「분할손절」·「손실률」 기전을 명시」* | 「손실률」 낱말 있는 건 = 광전자 · 삼양 · **한라캐스트**(3) · 「분할손절」 0 |
| `MIX` = *「본전 청산과 분할손절이 **둘 다** 서술」* | 「손실률」 ∧ 본전 서술 = **광전자 · 삼양**(2) — **post4 PS일렉과 «동형»**(`LABELS_2026-08-22_post4.md` §3-(3): *「「손실률」 한 낱말이 판별 근거에 직접 해당 … 본전 청산 서술도 함께 있으므로 `MIX`」*) |
| `MANUAL` = 「수동」 개입 서술 | **항목(종목) 단위 0건** — 「수동」 0 · 말미 산문의 「손매매」 1(*「확실히 손매매보다는, 통계에 근거한 …」*)·홍보 문구의 「직접」 1(*「… 직접 확인하고 싶으신 분」*)은 종목 서술 아님(grep 확인) |
| `TP` = 손절 서술 없음 | 나머지 **9**(신규) |

⇒ **`TP` 9(신규) · `SL` 1(한라캐스트 · 신규) · `MIX` 2(광전자·삼양 · 후속) · `MANUAL` 0.** 상세·약한 결정은 `LABELS_2026-09-04_post6.md`.
🔴 **초판 자기정정**: 초판은 셋을 `TP` 로 두고 「`SL` 은 「분할손절」 명시가 정의」라 적었다 — 그것은 동결 판별표의 **두 낱말 중 하나를 지운 재정의**였고,
「PS일렉 전례와 다른 점 = 분할손절 서술 없음」은 **사실이 아니었다**(PS일렉도 분할손절 서술이 없었다). verifier B1. 정정 방향 = **동결 문언 복귀**.
귀결: `EXIT-X7` 동결 문언(*「`SL` 건이 **시퀀스로**(값 2개 이상, 범위 아님) 적히면 E3(손절 레그 비증가, ε 동일)를 **판정한다**」*)의 조건이 **한라캐스트(−2.87, −2.89)로 처음 충족**된다
⇒ **문언대로 `EXIT-E3` 를 판정한다(n=1).** 🔴 **`SL` 에는 어느 동결본에도 「3건 미만 보류」 게이트가 없다**(계열의 3건 게이트는 전부 `TP`·`BUY`·`TV` 분모에 걸려 있다 — verifier 2패스 grep) ⇒
초판의 「최소 n 3 미달 ⇒ 미룸」은 **출처 없는 새 문턱**이었으므로 철회한다. n=1 은 검정력이 없으므로 **결과가 어느 쪽이든 「지지/불성립」을 선언하지 않고 값과 n 을 인쇄**한다(`PREREG_EXIT_V2.md` §3 E3 문언 그대로 · 누적은 다음 글부터).
방향 자기신고: 두 손절 레그의 순서는 저자 문장에 그대로 보인다 — 미루는 선택은 그 관측을 숨기는 쪽이었다.

### `EXIT-X2` 주 분모 = **신규 완결 `TP`** (동결 문언 그대로 · 6건)

- 동결 문언 3곳: `PREREG_POST6.md` §2-3 표 *「완결 `TP` 3건 미만이면 보류」* · §7-C 4 *「최소 n = 완결 `TP` 3건」* · post5 `LABELS_2026-08-29_post5.md` X2 절 *「적용 대상 `TP` 완결 건: 코데즈 · 레메디」*.
  ⇒ 주 분모 = 헥토파이낸셜 · 아이티센글로벌 · 현대약품 · 원익 · 쿠콘 · 비에이치 = **6 ≥ 3 ⇒ 판정한다**(보류 아님).
- 🔴 **초판 자기정정**: 초판은 분모를 「완결 `TP` ∧ `breakeven_note`」(2건)로 좁혀 「보류」를 만들었다. 근거로 든 §1-1 은 **`EXIT-E2`** 의 분모 문장이고,
  post5 전례(코데즈·레메디 둘 다 note 1)는 **우연의 일치**였다(그 글의 완결 `TP` 가 마침 둘뿐). verifier B2. **동결 문언으로 복귀**한다.
- 🔴 **충돌 신고(§1-8 형식)**: `EXIT-E2` 분모 = `breakeven_note` 건(§1-1 · 동결) ↔ `EXIT-X2` 분모 = 완결 `TP`(§2-3·§7-C · 동결). **두 동결 문언이 서로 다른 분모를 지시한다.**
  둘 다 그대로 인쇄한다(같은 표 · 병기 의무). 정의를 어느 쪽으로도 «고치지 않는다».
- 🔴 **방향 자기신고(§7-C 2번 형식)**: 좁은 분모(note 건)는 **전량 익절 건**(마지막 레그가 본전에서 먼 건)을 분모에서 빼므로 **`X2` 에 유리한 방향**이다.
  동결(넓은) 분모를 주 판정으로 쓰는 이 결정은 **`X2` 에 «불리한» 방향**이다. 좁은 분모(신규 완결 note 건 2 · 후속 포함 4)는 **민감도로 의무 인쇄**.
- `EXIT-X8`(X2 가 고른 레그 ≠ 마지막 레그 건 수)은 주 분모 6 안에서 센다. 주 분모 6건엔 「손실률」 레그가 없어 **구성상 0** ⇒ 「구분 불가」로 적힐 예정.
  후속 `MIX` 2건(광전자·삼양)에서는 규칙이 다른 레그를 고른다 — **민감도에서만 「구분 가능」**이며 그 사실을 그대로 인쇄한다.

## PD-7. 「손실률」 레그 인라인 — 3건 · 라벨 귀결

저자가 한 항목 안에 *「수익률 a, b, c , 손실률 −x, −y」* 형태로 적었다: 광전자(−7.22·−7.51) · 삼양(−1.73) · 한라캐스트(−2.87·−2.89).
⇒ `is_loss = 1` 레그로 등재 · **그 낱말이 `SL` 판별 근거**(PD-6)라 세 건은 `TP` 가 아니다. `TP` 신규 9건에는 「손실률」 레그가 **0** ⇒ `EXIT-E1` 시퀀스에 손실 레그가 섞이는 건 이번 글엔 없다.
`EXIT-E3`(`SL` 손절 레그 비증가)의 대상 = 한라캐스트 2레그(값 2개 순서대로 = `EXIT-X7` 조건). `EXIT-X6` 은 `EXIT-E1`·`EXIT-E4` 한정이므로 `EXIT-E3` 엔 적용하지 않는다(미완결 사실만 병기).

## PD-8. 그 밖의 기록 (판정 아님)

- 저자 자가보고 **「이번주는 원금 대비 8.3%」** — 계좌 단위 정량 진술 2번째(post5 「8월 27% · 7월 20%」). **기록만**(`PREREG.md` §0-1 · 재현·인용 금지).
- 광전자 *「현금+신용 합산으로 약손실 금액으로 매도」* — **신용 사용** 첫 명시. 비중 미공개 계열. 기록만.
- 프로그램 **1.0.40**(1.0.39 →) — 공변량 기록(`PREREG.md` §2).
- 프리셋 **12/12 HDR60/표준형** ⇒ `HDR-D2`·`BUY-L4` 변량 **0건**(누적 n=1 유지 · 판정 없음).
- 등록일 「오후」 표기 2건(삼양·한라캐스트 08-21 오후) = **`exact`**(post5 삼양 전례).
- 급등일 ≠ 등록일: 한라캐스트(급등 08-14 · 등록 08-21) · 지투파워(08-25 · 08-26) · 아이티센(08-20~24 · 08-20) · 광전자(08-07 · 08-05 후속) — 등록일 축은 **등록일** 그대로 잰다(값 보고 안 바꾼다). 「급등일 등록」 기전의 반례 후보로 기록.

## PD-9. 절단·결손·스냅샷 (DB «존재» 실측 · 판정값 아님)

| 종목 | `[D−19, D]` 봉수(등록일 포함) | 비고 |
|---|---|---|
| 신규 10건 전부 | **20/20** | `P6-절단가드-A` 분자 **0**(계산 단계에서 재확인) |
| 삼양바이오팜(후속) | 12(첫 봉 08-05) | 분모 밖(PD-2) |
| 우리기술투자 | 20/20 | 🔴 **04-01~08-04 봉 36/85** — 191종목 구멍 계열 후보. 창 20봉은 완전하나 **60봉 특징(`f9_newhigh`)은 C-17 NaN 규약** 적용 대상(`run_selection.py:80` `.where(prev_max.notna())` 반영 확인) |
| 현대약품 · 비에이치(09-01 등록) | 20/20 | `[D, 09-04]` = **4봉** ⇒ 창5 `[D, D+4]` **절단** — 처리는 **PD-11** |

- `corp_events` 2026-01-01 이후 12종목 **0건** · `stock_info.market_cap` 12/12 존재.
- **`stock_industry` 스냅샷**(`PREREG_SECTOR_COMOVE.md` #22 「이 표는 자란다」): 행 **2,556** · `max(updated_at)` **2026-08-07 09:38:39+09** · `max(collected_at)` 2026-08-07 09:37:23+09 · 12종목 중 **11 존재(삼양바이오팜 `0120G0` 없음 — 후속이라 `SEC-G1` 분모 밖)**.
- 저자 항목번호 **1..12 결번 0** · 번호 없는 산문 항목 **0** · 이미지 43.

## PD-10. 「범위 서술」 분모 규약 — `PREREG_POST6.md` §8-2 가 «이 인테이크에» 지시한 것

> *「범위 서술을 「건」으로 셀 때의 규약이 없다 … 다음 인테이크에서 못박을 것.」*

**결정(동결)**: **`RANGE_ONLY` 건(저자가 「A% ~ B%」 범위만 적은 건)은 어떤 «비증가 「건」 분모»에도 계상하지 않는다** — `EXIT-E1`/`E3`/`E4`/`X5`/`X7` 의 분모·분자 모두 밖 · 값(범위 양끝)만 원장에 기록.
근거: `EXIT-X7` *「범위 서술은 시퀀스로 «승격하지 않는다»」*의 귀결 — 시퀀스가 아닌 것은 「비증가」의 정의역 밖이다. post5 INTAKE §2-8 의 **7/7**(한성기업 제외)이 이 규약과 같은 셈법이다.
🟢 이번 글 해당 **0건** — 판정에 영향 없는 시점에 못박는다(사후적합 위험 0). 한라캐스트(−2.87, −2.89)는 «범위»가 아니라 «값 2개»라 이 규약의 대상이 아니다.

## PD-11. 창5 `[D, D+4]` 절단 — 09-01 등록 2건 (현대약품 · 비에이치) · 이 계열 최초

거래일 달력상 `[09-01, 09-04]` = **4봉**(원익 08-31 등록은 5봉 = 완전). 창5 절단 처리 규정은 어느 동결본에도 없다(B-1 은 창A 규약).
`RESULTS_LADDER_TRANCHE.md` §7 이 **절단 창5 가 가짜 숫자를 만든 전례**(*「3봉밖에 없었고 … 「위반 4」가 실재하지 않는 값」*)를 이미 신고했다.

**결정(동결) — `PREREG_POST6.md` §1-6 절단 문형을 «승계»한다(새 숫자 없음)**:

1. **분모에 넣는다**(값을 보고 빼지 않는다) — 가용 봉으로 `DD5` 를 계산하되 **절단 플래그 `P6-WIN5_TRUNC = 1` · 건수(2) · 봉수(4) 를 의무 인쇄**.
2. **의무 민감도**: 절단 2건 **제외** 값을 `LAD-T1`·`LAD-T2`·`LAD-T3`·`LAD-P2` 전부에 나란히 인쇄.
3. **가드 — 이름을 §1-6 의 `P6-절단가드-A/B`(`[D−19, D]` 창 봉수)와 «분리»한다(§0-3 2번)**:
   - **`P6-창5절단가드-A`**: 절단 건 / **이번 글 신규 건**(§1-6 과 같은 분모 = 신규 건) ≥ 1/3 ⇒ ⛔ 판정 불가. 이번 = **2/10 = 20%** ⇒ 미발동(산술 인쇄). 누적 쌍 기준(2/22 계열)은 «참고» 병기.
   - **`P6-창5절단가드-B`**: 제외 민감도가 `LAD-T1` 판정을 **뒤집으면 ⇒ ⛔ 「판정 불가(창5 절단 의존)」**.
   - 문턱 1/3 은 §1-6 6번의 **차용 고지**를 그대로 승계한다(검증된 값 아님).
4. 다음 글에서 두 건의 창5 가 완전해지면 **그때 값으로 «대체»**한다(누적 41쌍 재계산 규약 그대로) — 이번 값은 산출물에 「절단 시점 값」이라 표기.

## PD-12. 삼양바이오팜의 `first_only` «누적» 지위 — post6 가 2차 매수를 적었다

`PREREG_POST6.md` §1-2 동결: *「5번째 글 삼양바이오팜(1차)은 **이미 누적 분모 1건으로 들어가 있다**(중점 `b₁` = 19.99% 도 함께 보존)」*.
이번 원문: *「지난주 추가매수 중 → 2차 매수 후 …」* ⇒ 그 사이클은 이제 `first_only` 가 아니다.

**결정(동결)**: **(가) as-of-post5 유지** — post5 의 레그(11.50·11.50)는 **1차만 체결된 상태에서의 매도**였으므로 그 복원값은 여전히 「first_only 상태의 측정」이다. 동결 문언대로 누적 분모에 둔다.
**(나) 제외**는 **의무 민감도**로 나란히 인쇄(`P6-L1′`·`P6-L2′`·`P6-L-대칭` 전부). 두 갈래가 판정을 가르면 🔴 「누적 정의 의존」 ⇒ 지지 선언 금지.
🔴 **방향 자기신고**: 보존된 `b₁` 19.99% 는 **5% 문턱 «위»의 관측 1건**이다 — (가)는 그 건을 분모에 «유지»하는 선택이다. 다른 세 건의 `b₁` 을 아직 모르므로 「중앙값을 올린다」고 단정하지 않는다(값 하나를 더할 때 중앙값은 내려갈 수도 있다). (가)는 동결 문언 그대로다.
게이트 개방 자체는 `REC-Z4`(헥토파이낸셜)와 신규 3건으로 어느 갈래에서도 성립(누적 4 또는 3 ≥ 3).

## PD-13. 적용할 예측 목록 (전부 동결본 그대로)

- `PREREG_POST6.md` §4 **54항목** · `PREREG_RANKING.md` §4(`RNK-`) · `PREREG_WEIGHTED_RECON.md` §4(`WRC-`) · `PREREG_SECTOR_COMOVE.md` §4(`SEC-`).
- 🔴 **이번 글에서 «처음» 열리는 것**(문언 구조로 확정 · 값 아님): `REC-Z4`(헥토파이낸셜 = `first_only` 3레그 서로 다른 값 ⇒ **`P6-L1′`·`P6-L2′`·`BUY-L5` 즉시 판정**) · `P6-L-누적`(누적 4 ≥ 3 ⇒ 판정 의무 · **`P6-L-대칭`** 살아 있음) · `REC-Y1`(레그 ≥4 신규 5건) · **`EXIT-X2`**(완결 `TP` 6 ≥ 3) · **`EXIT-E3`/`EXIT-X7`**(한라캐스트 `SL` 2값 — 문언대로 판정 · n=1 이라 지지/불성립 선언 없음 — post5 `HDR-D2`/`BUY-L4` 「관측 시작 n=1」과 같은 처리).
- 🔴 **이번 글에서 «안» 열리는 것**: `TV-` 전부(PD-5) · `EXIT-X9`(`MANUAL` 0) · `HDR-D2`/`BUY-L4`(변량 0) · `P6-R1′`(`full` 신규 0 ⇒ 관측만) · 승/패 대조(`all_loss = 1` 신규 0 — 3회 연속 미실시).

## PD-14. 원장 인코딩 — `ledger_trades.csv` 에 라벨 컬럼이 없다

실측(50행 전수 · verifier 3패스): `stoploss_plan = 1` 인 행은 **전부 「분할손절」 서술이 있는 건**(모나미 ×2 · 지엔씨에너지 · 한성기업 ×2)이고, post4 PS일렉(`MIX`)은 분할손절 서술이 없어 **0** 이다 ⇒ 이 컬럼은 «라벨» 축이 아니라 **「분할손절」 서술 명시 여부라는 단일 축**이며 예외 0건. 라벨은 `narrative`(PS일렉 「라벨 MIX」)에만 있다.
**결정(단일 축 «유지» · 옛 행 불변 — post4·post5 산출물 md5 불변)**: `manual_exit = 1 ⟺ MANUAL` · `breakeven_exit = 1 ⟺ breakeven_note` · **`stoploss_plan = 1 ⟺ 저자가 「분할손절」을 명시**(50행과 정합 · 라벨과 무관) · **라벨의 SSOT = `narrative` 첫 토큰 「라벨 X」**(PS일렉 전례 그대로).
⇒ 한라캐스트 `0·0·0` 「라벨 SL」 · 광전자·삼양 `0·1·0` 「라벨 MIX · CONTINUATION post5#N」 · 나머지 9건 `0·note·0` 「라벨 TP」. 🔴 초판 PD-14(「`stoploss_plan=1 ⟺ SL/MIX`」)는 **한 컬럼에 두 축을 섞는 것**(`P6-G-E` 가 `fill_level` 에서 금지한 모양 · §5-5-3 「post6 행부터 예외」 거절)이라 철회.
`verify_ledger` G-A~G-D·`P6-G-E` 는 이 컬럼을 읽지 않으므로 게이트 결과와 무관. 🔴 **`stoploss_plan` 으로 라벨 건수를 세지 말 것** — 라벨 집계는 `LABELS_*.md` 가 SSOT.

---

[[PREREG_POST6]] · [[INTAKE_2026-09-04_post6]] · [[LABELS_2026-09-04_post6]] · [[FREEZE_RANKING_2026-08-31]] · [[FREEZE_WRC_2026-09-02]] · [[FREEZE_SECTOR_2026-09-03]] · [[INTAKE_2026-08-29_post5]] · [[PREDECISION_2026-08-29_post5]] · [[LABELS_2026-08-22_post4]]
