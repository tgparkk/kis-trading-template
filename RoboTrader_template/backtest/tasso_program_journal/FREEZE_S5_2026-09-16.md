# 동결 — `S5` 재무·뉴스 OOS 예측 한 줄 (`PREREG_S5_FUND_NEWS_OOS.md` · 🔒 사장님 결정 ⑥)

**동결 2026-09-16 KST** · 워크트리 `D:/tmp/kis-wt-tasso-p8prep` · 브랜치 `docs/tasso-post8-prereg`
· 사전등록 `PREREG_S5_FUND_NEWS_OOS.md`(작성 2026-09-14 밤 · v0.3 · 개정 2026-09-15)
· 절차 근거 `PREREG_POST8.md` **§12**(= §13-1 **6단계**) · 발동 근거 🔒 **사장님 결정 ⑥**(2026-09-16 07:0x ·
`memory/decision-2026-09-16-tasso-post7-seven-decisions-and-merge.md`): *「지금 안 함 · **post8 «전» 별도 동결 선언**(이번 값과 무관) · Holm 가족 +0」*

> 🔴 **라이브 채택 금지 · 성과·엣지 추정 금지** — `PREREG.md` 「§0 2번」 동결분이 계열 전체를 구속한다.
> `PREREG_S5_FUND_NEWS_OOS.md:40` 이 그것을 승계해 적었다: *「**라이브 채택 아님** … 🔴 *등급이 「성립」이어도 라이브 채택 금지는 그대로다.*」*
> 🟢 **라이브 코드 0줄** — 이 동결이 만지거나 만든 파일은 전부 `backtest/tasso_program_journal/` 안이다.
> 🔴🔴 **이 문서에는 post7 의 «관측값»이 하나도 없다.** 결정 ⑥ 이 *「이번 값과 무관하게」*라고 못박았으므로
> `RESULTS_S5_POST7.md` 의 어떤 수도 인용하지 않는다(`PREREG_POST8.md` §12 (나) 말미).
> 🔴🔴 **이 동결은 `PREREG_S5_FUND_NEWS_OOS.md` 본문을 «0바이트» 바꾼다.** 동결은 «선언»이고, 선언은 이 파일이 한다.

---

## 1. 동결 대상 — 한 줄

🔒 **`S5`** : *post7 이후 새 글의 «신규» 건의 **영업적자 비율**은, 그날 「거래대금/시총 상위 1%」 대조군의 적자 비율과 **±10%p 안에서 같다**.*
(`PREREG_S5_FUND_NEWS_OOS.md:12` 원문 그대로 · **한 글자도 바꾸지 않았다**)

부속 동결분도 그대로다 — **PIT** `dart_financials_asfiled`(`status='000'` · `rcept_dt ≤ 글 게시일`)에서 가장 최근 사업연도 **하나**만(`:13`) ·
**대조군** 같은 날 유니버스 `거래대금/시총` 백분위 상위 1%(`:14`) · **최소 n** 신규 «판정 가능» 건 **3건 미만이면 미룬다**(`:15` · `PREREG_SELECTION.md` §7 선례) ·
**등록일 정밀도** `exact` **28 만 주** · `approx` **2 민감도 인쇄만** · `after` **1 제외**(§1-1 `:17-26`).

⇒ 🔴 **이 동결은 「선언을 붙였다」이지 「가설이 성립했다」가 아니다.** `S5` 판정은 **post8 부터**다.

---

## 2. 동결 지문 — 🔴 **아래 표는 「작업트리 md5」다**

🔴🔴 **표 안에 잣대를 적는다** — *「🔴🔴 **그래서 모든 동결 표는 「무엇의 md5 인가」를 «표 안에» 적는다** — 표기 없는 md5 표는 무효다.」*(`README.md:95`).
**작업트리 md5** = 디스크에 있는 «그 바이트»를 `md5sum <파일>` 로 잰 값 · **블롭 md5** = `git cat-file blob <rev>:<경로> | md5sum`.

- 🟢 **`core.autocrlf` 실측 = `input`** (`git -C D:/tmp/kis-wt-tasso-p8prep config core.autocrlf` · 2026-09-16)
  ⇒ 이 트리에서는 **작업트리 md5 = 블롭 md5**(`README.md:90`). `true` 였다면 갈렸을 것이다(`README.md:91`).

### 2-1. 동결 대상 — 3칸 전수 (`PREREG_POST8.md` §12 (다) 3)

| 파일 | **작업트리 md5** | **블롭 md5**(`HEAD` = `04cd785`) | 같나 |
|---|---|---|---|
| **`PREREG_S5_FUND_NEWS_OOS.md`** | `cb9485d37f6e7ff3d6e09dd7449a9f4e` | `cb9485d37f6e7ff3d6e09dd7449a9f4e` | ✅ |

🟢 **교차 확인** — 이 값은 `PREREG_POST8.md` §13-2 의 동결 표(같은 브랜치 `04cd785`)에 적힌 값과 **한 글자도 다르지 않다.**
🔴 **이 커밋이 그 파일을 0바이트 바꾼다는 것의 기계 증거**가 바로 이 일치다(§4).

### 2-2. 그 문서가 «읽어서 옮긴 입력»의 md5 (`PREREG_POST8.md` §12 (다) 5)

| 파일 | **작업트리 md5** | 어디가 읽었나 |
|---|---|---|
| `ledger_trades.csv` | `726018feca4a50dc9530130d51758368` | §1-1 `:19` — *「실측 `ledger_trades.csv` **62행**의 `reg_date_precision` = **exact 28 · approx 2 · after 1 · none 31**」* |
| `PREREG_SELECTION.md` | `fcd8a91373a9cdded97a8cc02efad4d3` | §1 `:15` — 최소 n «선례»(*「3건 미만이면 판정을 미루고 다음 글까지 모은다」* §7) |
| `PREREG.md` | `9a84e856b259ef0c48164570a9795232` | §3 `:40` — 「§0 2번」 라이브 채택 금지 승계 |
| `regen_gate.py` (**이 커밋의 등재를 반영한 값**) | `2c4a4af3cdbc5cefd5ca688da002de1c` | §5 — `MANUAL_DOCS` 등재 |

🟢 `PREREG.md` 의 값은 `PREREG_GRADE_TIERS.md` §4-2(`:215`)·`PREREG_POST8.md` §13-2 의 동결값과 **일치**한다(교차 확인).

🔴 **`regen_gate.py` 행 주의** — 이 문서를 쓰는 시점의 `HEAD`(`04cd785`)에는 **§13-1 4단계 값 `9b2c46af1e49850df01ba459ad8f3d89`** 가 들어 있다.
위에 적은 `2c4a4af3…` 는 **이 커밋이 `FREEZE_S5_2026-09-16.md` 를 `MANUAL_DOCS` 에 넣은 «뒤»의 값**이며,
`PREREG_POST8.md` §12 (다) 4 가 *「`FREEZE_S5_*.md` 자신의 등재는 **동결 커밋과 같은 커밋**이므로 `regen_gate.py` md5 는 **그 커밋 «후»의 값**을 적는다」*고 지시한 그대로다
(`PREREG_GRADE_TIERS.md:216`·`:218-219` 관용).

### 2-3. 🔑 이 파일 자신의 md5 는 여기 적을 수 없다

**자기참조**다 — **커밋 해시가 그 역할을 한다**(`PREREG_GRADE_TIERS.md:222-223` · `FREEZE_RANKING_2026-08-31.md` §2 · `FREEZE_SECTOR_2026-09-03.md` §2-4 관용 승계).
🔴 **HEAD 해시를 본문에 적지 않는 관용도 같이 따른다** — *「🔴 **HEAD 해시는 산출물 본문에 «적지 않는다»** — 이 동결 커밋이 HEAD 를 바꾸므로, 해시를 본문에 적으면
`regen_gate.py --rerun` 의 byte 비교가 **구조적으로** 깨진다」*(`FREEZE_WRC_2026-09-02.md` §2).
🔑 ***커밋마다 바뀌는 값을 산출물에 적으면 그 산출물은 자기 자신을 재현할 수 없게 된다.***

⚠️ **커밋 직전에 `md5sum` 을 다시 돌려 위 표와 대조할 것**(`FREEZE_RANKING_2026-08-31.md` §2 관용 승계).

---

## 3. 🔴 순서 증거 — **네 줄** (`PREREG_POST8.md` §12 (다) 7)

🔴 **동결본 `PREREG_S5_FUND_NEWS_OOS.md:45` 가 적은 「post7 `fetch_post.py` 수집 시각」을 post8 로 «바꿔 적지 않는다»** — 그건 동결문의 낱말을 고치는 일이다.
네 줄을 **있는 그대로** 적는다.

| # | 줄 | 값 | 뜻 |
|---|---|---|---|
| ① | **`regen_gate.py` `MANUAL_DOCS` 등재 커밋 SHA** | **`31933c4`** (2026-09-14) | 🟢 이 동결보다 **«앞»**이다 — `PREREG_S5_FUND_NEWS_OOS.md` 는 `regen_gate.py:399` 에 **이미** 등재돼 있었다(실측) |
| ② | **post7 `fetch_post.py` 수집 시각** | 🔴 **이미 «지났다»** (post7 = `224409404744` · 2026-09-12 발행) | 🔴🔴 ***이 줄은 순서 증거가 아니라 「순서를 못 지켰다」의 기록이다.*** 이 동결은 post7 **«뒤»**이며, **그래서 post7 회차의 `S5` 판정이 0회였다**(PD-15 · `INTAKE_2026-09-15_post7.md:276` — *「사전등록 커밋 `f3ff8bc` 가 fetch «앞»이지만 메시지가 「초안 v0.3 … **미동결**」이라 판정 라벨을 0개 선언했다」*) |
| ③ | **이 동결 커밋 SHA** | 🔴 **본문에 적지 않는다** — 이 문서를 **추가한 커밋**이 곧 그 SHA다(§2-3 자기참조 · `FREEZE_WRC_2026-09-02.md` §2 관용). 확인: `git log --oneline --diff-filter=A -- FREEZE_S5_2026-09-16.md` | 🟢 이 동결의 시점 |
| ④ | **post8 `fetch_post.py` 수집 시각** | ⬜ **미정** — post8 은 아직 발행되지 않았다(예상 2026-09-19 금 ~ 09-20 토 · 발행일은 저자 재량) | 🟢 **③ < ④ 여야 하고, 이것이 이 동결이 «실제로» 만드는 순서 증거다** |

### 🔴 ③ < ④ 는 «나중에» 확인한다

`fetch_post.py` 는 로그를 남기지 않으므로 git 사실이 유일한 증거다(`FREEZE_WRC_2026-09-02.md` §7 관용):

```bash
git log --oneline --diff-filter=A -- post_<logNo>.html        # post8 원문을 추가한 커밋
git log --oneline --diff-filter=A -- FREEZE_S5_2026-09-16.md  # 이 문서를 추가한 커밋
```

⬜ **지금은 ✅ 로 적지 않는다** — 아직 일어나지 않은 일을 통과로 적으면 그게 죽은 가드다.
🔑 ***자기보고("먼저 썼다")는 순서의 증거가 아니다***(`PREREG_GRADE_TIERS.md:41-42`).

---

## 4. 🔴🔴 본문 «0바이트» 변경 — 이 동결의 정의

`PREREG_POST8.md` §12 (나) 1번이 이 동결의 **유일한 합격 검사**를 적었다:

> *「🔴🔴 `git diff <직전>..<동결 커밋> -- RoboTrader_template/backtest/tasso_program_journal/PREREG_S5_FUND_NEWS_OOS.md` 가 **비어 있어야** 한다.
> 비어 있지 않으면 **동결이 아니라 개정**이고 그 커밋을 되돌린다.」*

🟢 **실측 — 이 커밋이 `PREREG_S5_FUND_NEWS_OOS.md` 를 건드리지 않았다.**
`git diff --stat` 에 그 파일이 **나타나지 않는다**(변경 파일은 `FREEZE_S5_2026-09-16.md` 신규 + `regen_gate.py` **+4줄**(주석 3 + 등재 1)뿐).
§2-1 의 md5 가 `PREREG_POST8.md` §13-2(`04cd785`)의 값과 같다는 것이 **같은 사실의 기계 증거**다.

🔴 **그래서 「동결」은 이 파일이 한다.** v0.4 도 개정도 아니다 — 원문은 v0.3 그대로이고, 이 문서가 **선언을 붙일 뿐**이다.
🔴 **`git checkout -- <파일>` 을 쓰지 않았다** — 줄끝 변환이 끼면 지문이 깨진다(`README.md:96-98`).

---

## 5. 등재 (`regen_gate.py` `MANUAL_DOCS`)

- 🔒 **`FREEZE_S5_2026-09-16.md`(이 문서)를 «같은 커밋»에서 등재했다** — *「`FREEZE_*.md` 는 **만드는 «순간» 등재**한다」*
  (`regen_gate.py:375-378` · 전례 `:369` `FREEZE_RANKING_2026-08-31.md` · `:372` `FREEZE_WRC_2026-09-02.md` · `:380` `FREEZE_SECTOR_2026-09-03.md`).
  🔑 ***등재와 파일 생성이 같은 커밋이라 G-1 이 한 번도 「없는 파일」을 보지 않는다*** — 이름만 먼저 박으면 G-1 이 상시 FAIL 해서 진짜 가드가 죽는다(`regen_gate.py:377-378`).
- ⚠️ **`PREREG_S5_FUND_NEWS_OOS.md` 는 다시 넣지 않았다** — `regen_gate.py:399` 에 **이미 있다**(등재 커밋 `31933c4` · 2026-09-14).
- 🔴 **`--update` 도 `--rerun` 도 돌리지 않았다**(`PREREG_GRADE_TIERS.md:197-198`). 등재 «후»에는 **G-1 줄만** 본다.

**검사**(비변경 기본 모드 `python regen_gate.py`): 🟢 **`✅ MANUAL_DOCS 73건 전부 존재`**
(71 → 72 → **73** · **72** = §13-1 4단계 `PREREG_POST8.md`(`1b7cb28`) · **73** = 이 문서).
🔴 전체 종료코드는 1 이지만 **FAIL 2건은 둘 다 DB 지문**이다(`regen_gate.py:592`·`:594` — `daily_prices[>=2026-04-01]` 스냅샷이 `2026-09-15` → `2026-09-16` 로 전진).
⇒ *「DB 지문은 스냅샷 전진으로 **FAIL 이 정상**」*(`PREREG_ANCHOR_REDESIGN.md` §9-3). **`MANUAL_DOCS` 실패 0건.**

---

## 6. 🔒 판정 라벨 선언 (`PREREG_POST8.md` §12 (다) 8)

🔒 **`S5` 의 판정 라벨 3개를 «이 시점에» 선언한다** — `PREREG_S5_FUND_NEWS_OOS.md:34-36` 원문:

| 라벨 | 조건 |
|---|---|
| **(S5-성립)** | `abs(선정 적자율 − 대조군 적자율) ≤ 10%p` |
| **(S5-이탈)** | `> 10%p` — 「저자가 재무를 본다」의 **관측**이지 검정이 아니다 |
| **(S5-보류)** | 신규 판정 가능 건 **< 3** |

- 🔴 **Holm 가족 +0 유지** — *「값만 기록 · 핵심 주장 아님 · Holm 가족(f1~f9)에 넣지 않는다 ⇒ **등록부 주 검정 +0**」*(`:37`). **이 동결이 그것을 바꾸지 않는다.**
- 🔴 **검정력이 없다** — *「…***애초에 기각도 지지도 못 한다*** — 그래서 「한 줄」이다」*(`:38`). ⇒ 「(S5-성립)」이 나와도 **「재무를 안 본다」의 증거가 아니다.**
- 🟢 **이 선언이 푸는 것** — post7 §6 표에서 `S5` 가 ⓗ(*「동결 「선언」 부재 ⇒ 판정 0회」*)로 `—`(등급 없음)였다(`INTAKE_2026-09-15_post7.md:276`).
  **post8 부터 `S5` 는 `GT-` 등급이 붙는 자리가 된다.** 🔴 **post7 을 소급 재판정하지 않는다** — post7 의 `S5` 행은 `—` 그대로 둔다(`PREREG_POST8.md` §12 (라)).

---

## 7. 동결 시점의 판정 불가·미해소 항목

| 항목 | 상태 |
|---|---|
| `S5` 판정 | ⛔ **판정 전** — **post8 부터**. 신규 «판정 가능» 건 < 3 이면 **(S5-보류)**(`:15`·`:36`) |
| 🔴 **오염 고지** | `PREREG_S5_FUND_NEWS_OOS.md` §0(`:7-9`)이 *「현재 62건에 대한 어떤 재무·뉴스 사전등록도 이제 사후적합이다」*라고 **스스로** 적었다 ⇒ 이 축은 **post7 이후 «새» 건에만** 예측을 건다. **그 제약은 이 동결로 풀리지 않는다** |
| 이름→코드 매칭 | 🔴 **39/47 = 83.0% 가 «상한»** · 미매칭 8종목은 「모른다」(`:10`). ***이름 매핑은 「비슷한 이름」에서 조용히 틀린다*** — 재무를 붙이면 그 오류가 플래그 «값»으로 전파된다 |
| 희소성 가드 | ⚠️ **자동 발동하지 않는다** — *「가드가 «자동으로» 발동한다고 쓰지 않는다 — **새 축에 같은 잣대를 다시 등록해야** 발동한다」*(`:42` · CRITIC §2-3) |
| `f10`(재무·뉴스 축) | 🔴 **만들지 않는다**(결정 C-8 · `:41`). 태쏘 고도화는 `f1~f9` 로 간다 |
| 뉴스 τ일 라벨 | ⬜ **`NW2` 곡선이 나온 «뒤»로 줄 세운다**(`:43` · `docs/prereg_2026-09-14_news_event_curve.md`) |
| §3 ③ < ④ 순서 확인 | ⬜ **post8 수집 «후»에** — 지금 ✅ 로 적지 않는다 |
| `regen_gate --rerun` | ⬜ **관리자** — 이 동결에서는 돌리지 않았다(§5) |

---

## 8. 다음 단계 (`PREREG_POST8.md` §13-1)

| # | 단계 | 상태 |
|---|---|---|
| 4 | 등재 커밋 `PREREG_POST8.md` (`1b7cb28`) | ✅ 완료 |
| 5 | `PREREG_POST8.md` 동결 (`04cd785`) | ✅ 완료 |
| **6** | **이 문서 커밋** (`S5` 동결 + 같은 커밋 등재) | ✅ **이 커밋** |
| 7 | `main` 머지 (`--no-ff`) | ⬜ **관리자** |
| 8 | 🔴 **그 «뒤»에야** `python fetch_post.py <logNo>` | ⬜ **7 «후»에만** |

🔴🔴 **7번 이전에 fetch 하면 `PREREG_POST8.md` 전체가 무효다**(§13-1 8단계).
🔑 **이 문서의 커밋 해시가 「post8 을 보기 전에 선언했다」의 증거다.**

---

[[PREREG_S5_FUND_NEWS_OOS]] · [[PREREG_POST8]] · [[PREREG_SELECTION]] · [[PREREG]] ·
[[INTAKE_2026-09-15_post7]] · [[RESULTS_S5_POST7]] ·
[[FREEZE_RANKING_2026-08-31]] · [[FREEZE_WRC_2026-09-02]] · [[FREEZE_SECTOR_2026-09-03]] · [[README]]
