# 8번째 글 검증 기록 — `RNK-` 후보 랭킹 축 (`PREREG_RANKING.md` §0-3 **6단계**)

**실행 2026-09-24 스냅샷**(`daily_prices` `max(date)` = 2026-09-23 · 그 날 2,764행 · 창 구간 `max(updated_at)` = 2026-09-23 15:46:25.855518)
· 대상 = **post8 `224416253270`(발행 2026-09-18 금 · 거래일)** · 브랜치 `intake/tasso-post8`
· 사전등록 `PREREG_RANKING.md`(818줄 · **동결 `8e97577`**) · 동결 `FREEZE_RANKING_2026-08-31.md`(선택 규칙 `RNK-A1` · 계수 없음)
+ 🆕 **`PREREG_POST8.md`**(동결 `729f28b` · **첫 구속 회차** — `D-3`·`D-5`·`D-8`·`D-9`)
· 수집 «후» 결정 `PREDECISION_2026-09-18_post8.md` → 인테이크 `INTAKE_2026-09-18_post8.md` → 원장 `b302f7f` → 이 문서. **순서가 증거다.**
· 숫자 원본 = [`RESULTS_RANKING_POST8_NUMBERS.md`](RESULTS_RANKING_POST8_NUMBERS.md)(`run_ranking.py --stage post8` 생성 · 수정 금지) · 읽은 시각 도장 `ranking_post8/read_stamp.json`

> 🔴 **라이브 채택 대상이 아니다**(`PREREG.md` §0 2번 · `PREREG_POST8.md` §0-1) · 성과·엣지 추정 금지.
> 이 문서는 **기록**이지 전략 후보가 아니다. 라이브 전략·파라미터 변경의 근거가 아니다.
> 🔴 **「+15%」 문턱이 이 축 «안»에 남아 있다** — `f6_spikes60` = 「60일 내 일간 +15% 이상 봉 수」(§0-2 ②).
> 🔴🔴 **이 축은 «닫혀 있다»** — 동결 선택이 `RNK-A1`(`f1` 단독)이므로 §4-3 **(다)**에 의해
> **「새 정보 없음 = `REG-M4` 재진술」**이다. ***`RNK-P1` 이 문턱을 넘어도 «지지»로 선언하지 않는다.***
> 🔴 **승/패 대조 5회 연속 미실시** ⇒ 이 축의 최대치는 **「기술」**이지 「선정 규칙」이 아니다(§0-2 ③).
> 🔴 **창 종료 2026-09-18 = 발행 당일(금 · 거래일) 봉 «포함» · B-1 · 전 축(`WRC-` 포함) · PD-1** ·
> **실행 시 `max(date)` = 2026-09-23 · 그 날짜 행수 2,764**(기록만 한다 · **창으로 쓰지 않는다**).
> 🔴 **이 문서는 새 예측·새 문턱을 만들지 않는다** — §3 표의 항목만 판정한다.
> 🔴 **숫자는 «거의 전부» `_NUMBERS.md` 에서 옮겨 적었다.** ⚠️ **산문 산술 2건**: ① §4 의 중앙값 풀이 `(2 + 7) / 2 = 4.5`(= NUMBERS §3 `RNK-A1` `m_rank` 네 값 `[0, 2, 7, 22]`(정렬)의 2·3번째 값 평균) ② §4 의 「액스비스 = 1위」(`m_rank` 0 + 1 · 순위 = `m_rank + 1` · §2-4). 🔴 **그 두 건 말고는 새로 계산한 값이 없다** — ***「하나도 없다」고 적으면 그 두 건이 숨는다.***
> 🔴 **등급 이름을 이 문서에 적지 않는다**(§6 단계 · PD-16·PD-28).

---

## §0. 한 줄 결론

⛔ **판정 = 「새 정보 없음 — `REG-M4` 재진술」.** `PREREG_RANKING.md` §4-3 (다) 동결 문언 그대로다:

> *「**선택된 규칙이 `RNK-A1` 이면 이 축은 ⛔ 「새 정보 없음 — `REG-M4` 재진술」로 닫고,
> `RNK-P1` 을 «지지»로 선언하지 않는다.**」*

값은 **두 문턱을 다 넘었다**(귀무 `p` = **0.00000** < 5% ∧ `m_rank` 중앙 **4.5** < 30 · NUMBERS §4).
🔴 **그래도 그건 「지지」가 아니다.** 인쇄되는 내용은 ***`REG-M4` 가 이미 발표한 것(「`f1` 안 순위」)의 재진술***이다.

🔴🔴 **이번 회차의 «다른» 자리 — `RNK-V1` 이 post7 에 처음 갈렸고, 이번엔 갈리지 않았다**(§5).
🔑 ***그러나 그것이 축을 열지 않는다*** — post7 의 선언 차단 사유는 둘(§4-3 (다) 축 닫힘 + `RNK-V1` 발동)이었고 **서로 독립**이었다.
이번에 빠진 것은 `RNK-V1` 하나뿐이고 **(다)는 그대로 남는다.** ⇒ 판정 문장은 post6·post7 과 **같다**.

---

## §1. 판정표 — 예측 ID · 예측 문언 · 관측값 · 판정 (4열 · 등급 열 없음)

| 예측 ID | 예측 문언(동결 · `PREREG_RANKING.md` §3) | 관측값(NUMBERS) | 판정 |
|---|---|---|---|
| `RNK-P1` | 동결 규칙이 저자 종목을 유니버스 상위에 놓는다 — 글 단위 `pctl` 중앙의 **귀무 백분위 < 5%** ∧ **`m_rank` 중앙 < 30**(최소 n `exact` 3) | `exact` **4** · 귀무 `p` **0.00000** · `m_rank` 중앙 **4.5** · `RNK-G1` 0/4 · `RNK-V1` 미발동(§4·§7) | ⛔ **판정 불가** — 사유: **선택 규칙 = `RNK-A1`** ⇒ 「새 정보 없음 = `REG-M4` 재진술」(§4-3 (다)). 두 문턱 AND 는 «값»으로 섰다(기록 · 지지 선언 금지) |
| `RNK-N1` | (대칭) 무작위 종목과 구분되는가 — 재추출 귀무 백분위 **≥ 5% ⇒ 불성립** | 재추출 **0.00000** · 해석적(MC) 0.00000 | **성립**(무작위와 구분됨) — 🔴 **«증거 아님»**: `f1` 을 쓰는 규칙은 그 높이를 물려받는다 · ***「바닥을 통과했다」이지 「가설이 맞았다」가 아니다***(§4-1) |
| `RNK-N2` | (판별력) `m_rank` 중앙 **≥ 30 ⇒ 「판별력 없음」 강등** | **4.5**(문턱까지 25.5) | **성립**(강등 안 됨) — 기록 · ⛔ 경로 없음(`prev_close` 를 뺐다 · §2-1 M-4) |
| `RNK-B1` | (대칭) `f1` 단독보다 «유의하게» 낫다 — 비영 짝 단측 부호검정 `p < 0.05`(최소 비영 짝 5) | 비영 짝 **0**(전 짝 4 · 동률 4) | ⛔ **판정 불가** — 영구 미개시(`Δ ≡ 0` · 선택 규칙 = `RNK-A1`) |
| `RNK-B2` | (대칭) 동전던지기와 구분되는가 — `f1` 을 이긴 건 / 전 짝 **≤ 50% ⇒ 구분 불가** | **0.0%**(0/4) | ⛔ **판정 불가** — 축 폐쇄(§4-3 (다)) |
| `RNK-G1` | (커버리지) 규칙별 측정 불가 / `exact` **≥ 1/3 ⇒ ⛔** | 전 규칙 **0/4 = 0.0%**(`RNK-A3` 은 계수 미동결 ⇒ 측정 대상 아님) · `n_up` 갈래도 전 규칙 0/4 | **미발동** — 게이트 열림(⛔ 경로: 측정 불가 **2건**이면 50.0% 로 발동) |
| `RNK-O1` | (과적합) 훈련 LOO ↔ 검증이 문턱(30)의 다른 쪽이면 「과적합 의심」 인쇄 | 훈련 LOO **18.5** ↔ post8 **4.5**(둘 다 < 30) | **인쇄 조건 미충족** — 🔴 단 `RNK-A1` 은 자유모수 0 이라 애초에 적합할 것이 없었다(방어선이 아니다) |
| `RNK-X1` | (절차) `RNK-A5` 가 LOO 1위 ⇒ 절차 무효 | post8 엔 **선택 절차 없음**(08-31 동결) · 대조군 `p` **0.90665** | **재판정 대상 아님** — 대조군 값만 의무 인쇄 |
| `RNK-V1` | (민감도) 4축 병기 · **하나라도 판정을 가르면 선언 금지** | 전 갈래 같은 답 · `n_up` 갈래 `p` **0.00080**(post7 **0.07870**) | **미발동** — 🔴 post7 발동 뒤 이번 회차 재판정 = 갈리지 않는다(§5 · post7 판정은 소급 수정하지 않는다) |
| `RNK-P2` | 그 성립이 글을 넘어 반복된다 — **기록만**(검정 통계량 아님) | 두 문턱 AND «값»: post6 충족 · post7 충족 · post8 충족 | **기록** — 🔴 **«지지» 부호가 아니다**(세 글 모두 축 판정 ⛔ · post7 은 `RNK-V1` 겹침) · 누계 인용 금지 |

- 🔴 **`RNK-P1` 은 두 문턱의 «AND»다** — 이번엔 둘 다 넘었고, **그런데도** (다)가 지지를 막는다. ⇒ ***「문턱을 넘었다」와 「지지한다」는 다른 문장이다.***
- 🔴 **`RNK-N1`·`N2` 의 «성립»은 `RNK-P1` 을 열지 않는다** — 둘 다 `RNK-P1` 의 반증축이 «울리지 않았다»는 기록이고, `RNK-P1` 을 닫은 것은 반증축이 아니라 **(다)** 다.

---

## §2. 표본 — 판정 분모와 그 «밖»

post8 원장 **10행** · `exact` **4** · `approx` **2** · `after` **0** · `none` **4**(NUMBERS §1)

| # | 종목 | 코드 | 등록일 | 창 `[2026-04-01, D]` 봉수(유니버스 술어 통과) | `daily_prices` 원행 · 시총 결손 행 | 비고 |
|---|---|---|---|---|---|---|
| 3 | 우리로 | `046970` | 2026-09-11 | **63** | 112 · 49 | 🔀 항목 내 2 사이클(`구조차단`) · `P6-PRIOR_CYCLE_IN_WINDOW` 0 |
| 6 | JW신약 | `067290` | 2026-09-01 | **104** | 104 · 0 | — |
| 8 | 액스비스 | `0011A0` | 2026-09-11 | **27** | 104 · 77 | `stock_info` 미수록(낡은 명부) · `daily_prices` 로 측정 가능 |
| 10 | 우리기술 | `032820` | 2026-09-09 | **68** | 110 · 42 | 🔴 ≠ 우리기술투자 `041190` |

(§1 에서 옮김)

- 🔴 **`none` 4건은 두 부류다**(NUMBERS §1): **신규이나 등록일 미기재** 원익(🔒 #2 — 날짜가 어느 사건에 붙는지 원문이 정하지 않는다) · **기존 건 후속** 빛과전자·로보티즈·범한퓨얼셀(그 등록 사건은 post7 분모에서 이미 계상 · 이중계상 금지).
  ***둘 다 「값이 나빠서」 빠진 것이 아니다*** — `reg_date` 가 비어 **어느 날의 유니버스인지 정할 수 없다**. 두 결정 다 **값을 보기 «전»에** 동결됐다.
- 🔂 **§1-5 재진입 3건**(원익 · 헥토파이낸셜 · 코데즈컴바인 **3번째 사이클**)은 **셋 다 판정 분모 «밖»**이다(원익 `none` · 헥토·코데즈 `approx`) ⇒ **재진입 제외 민감도는 «항등»**(§5-2 · 판별력 0 · 명시 인쇄).
- 🔴 **「봉수」 열은 유니버스 술어(`market_cap>0 ∧ close>0`)를 통과한 행이다** — 시총 결손 행이 우리로 49/112 · 액스비스 77/104 · 우리기술 42/110 이다.
  🔑 선택 규칙 `RNK-A1`(`f1` = 등록일 당일 거래대금/시총)은 **등록일 한 봉**만 읽으므로 **이 결손이 판정 값을 움직이지 않는다** — 60봉 특징을 쓰는 `RNK-A2`·`A4` 의 입력 이력만 짧아진다(동결 술어의 귀결 · 기록만).
- 유니버스 폭 **2,765 ~ 2,765**(차 0 · 3 등록일) · 시총 결손 0 · `drop_rate` 전부 0.00% ⇒ 표기 가드 미발동(NUMBERS §1).

---

## §3. 🔴 `RNK-G1` 커버리지 — 판정보다 «먼저» 본다

| 규칙 | 측정 가능 | 측정 불가 / 4 | 비율 | 게이트(1/3) |
|---|---|---|---|---|
| 🔒 `RNK-A1`(판정) | **4**/4 | **0** | **0.0%** | 🟢 미발동 |
| `RNK-A2` | 4/4 | 0 | 0.0% | 미발동 |
| `RNK-A3` | — | — | — | ⛔ **동결 계수 없음 ⇒ 측정 대상 아님**(지금 적합하면 post8 을 «보고» 만든 규칙이다) |
| `RNK-A4` | 4/4 | 0 | 0.0% | 미발동 |
| `RNK-A5` | 4/4 | 0 | 0.0% | 미발동 |

(§2 에서 옮김)

- **사유 ①(DB 종목코드 부재) = 0** — `exact` 4건 전부 코드 확정. 액스비스(`0011A0`)는 이름 4표 0건이고 `dart_corp_code.corp_name` 이 **3,990행 전부 공백**이라 이름으로는 안 잡힌다 — 코드는 `news.related_stocks`·`news_stock` 경유로 확정됐다(PD-11).
- 🔴 **「손실 0」을 「편향 없음」으로 읽지 않는다**(§4-4 6번) — 측정 불가는 신규 상장주 쪽으로 치우친다. 이번 손실이 0 인 것은 **이번 4건이 60봉 요구에 걸리지 않았다**는 뜻이다.
- ⚠️ **커버리지는 축별로 «다른 수»다**(PD-11) — 액스비스가 `stock_industry` 에 **없는** 것은 `SEC-G1` 의 분자이지 `RNK-G1` 의 분자가 아니다(이 축은 `daily_prices` 만 읽는다).
- 🔴 **`P6-절단가드-A`**(창 `[D−19, D]` < 20봉) 분자 **0/4** ⇒ 미발동(PD-12 예고 0/4 와 같다).
- ⚠️ **⛔ 경로는 여전히 가깝다** — 분모 **4** 에서 측정 불가 **2건**이면 50.0% 로 발동한다(NUMBERS §2 · post7 분모 6 도 2건 · post6 분모 10 은 4건). ***문턱(1/3)은 그대로 두고 그 사실만 적는다.***

---

## §4. `RNK-P1`·`N1`·`N2` — 값과 규칙별 대조

### 4-1. 건별 `m_rank`(선택 규칙 `RNK-A1`)

| 종목 | 등록일 | `N` | **`RNK-A1` `m_rank`** | `RNK-A2` | `RNK-A4` | `RNK-A5`(🔬 대조군) | `RNK-A1` `pctl` |
|---|---|---|---|---|---|---|---|
| 우리로 | 2026-09-11 | 2765 | **7** | 55 | 42 | 1619 | 99.75 |
| JW신약 | 2026-09-01 | 2765 | **2** | 25 | 8 | 2689 | 99.93 |
| 액스비스 | 2026-09-11 | 2765 | **0** | 12 | 22 | 1568 | 100.00 |
| 우리기술 | 2026-09-09 | 2765 | **22** | 22 | 20 | 2520 | 99.20 |

(전부 NUMBERS §3 에서 옮김)

- 🔑 **`m_rank` 는 「엄격히 좋은 종목 수 + 동률 수(자기 제외)」** — 동률을 전부 위로 = 보수적(§2-4). 순위 = `m_rank + 1` 이다.
- 🔴 **액스비스 `m_rank` 0 = 그날 `f1` 1위**(산문 산술 ② · 머리 참조). post7 해치텍(`m_rank` **1** · `RESULTS_RANKING_POST7.md` §4-1 인용)에 이어 **두 글 연속 «우리 명부(`stock_info`)가 모르는 종목»이 그 글 `exact` 중 `m_rank` 최솟값**이다 — ***명부 미수록 종목이 판정 갈래에선 맨 위로 들어온다.***
- 🔴 **중앙값 4.5 는 네 값의 2·3번째 값(2·7)의 평균**이다(산문 산술 ① · `(2 + 7) / 2`). 분모가 4 라 ***한 건의 순위가 바뀌면 중앙값이 크게 움직인다***.
- 🔴 **`N` 이 규칙마다 다르다**(NUMBERS §3 의무 인쇄): `RNK-A1`·`A5` 는 2,765, `RNK-A2`·`A4` 는 **2,485 ~ 2,677**. ***규칙 간 `m_rank` 를 직접 비교하지 않는다.***

### 4-2. 규칙별 값 — 의무 인쇄 (판정은 `RNK-A1` 하나로만 한다)

| 규칙 | `m_rank` 중앙 | `pctl` 중앙 | 재추출 귀무 `p` | 해석적(MC) `p` | `< 5%` |
|---|---|---|---|---|---|
| **`RNK-A1` 🔒** | **4.5** | **99.84** | **0.00000** | 0.00000 | 🟢 |
| `RNK-A2` | 23.5 | 99.09 | 0.00000 | 0.00000 | 🟢 |
| `RNK-A4` | 21.0 | 99.21 | 0.00000 | 0.00000 | 🟢 |
| `RNK-A5` 🔬 | **2069.5** | **25.13** | **0.90665** | 0.90505 | ⛔ |

(NUMBERS §4 에서 옮김)

- ⚠️ 귀무 = 각 건 등록일에 «같은 `U_A(D)`»에서 무작위 1종목 · **20,000회 · 시드 `20260815`** · 크기 정합. 해석적 갈래는 건수가 짝수라 **몬테카를로로 대체**했다(§2-4 명문).
- 🔴 **이 표의 🟢 는 «귀무를 넘었다»는 문턱 표시다** — `RNK-N1` 과 같은 이유로 ***증거가 아니다.***

---

## §5. `RNK-V1` — 🔴 **post7 에 처음 갈렸던 축의 «이번 회차 재판정»**

**4축을 «항상» 나란히 인쇄한다. 하나라도 판정을 가르면 ⇒ ⛔ 어느 쪽도 선언하지 않는다.**

| 축 | 갈래 | 판정 갈래인가 | `m_rank` 중앙 | 귀무 `p` | 두 문턱 AND |
|---|---|---|---|---|---|
| **유니버스** | 전체(`market_cap>0 ∧ close>0`) | ✅ **판정**(`RNK-D1`) | **4.5** | **0.00000** | 충족 |
| **유니버스** | `n_up`(+`prev_close`) | 민감도 | **3.5** | **0.00080** | 충족 |
| 집계 | 글 단위 중앙 | ✅ 판정 | 4.5 | 0.00000 | 충족 |
| 집계 | 건 단위 pooled 중앙 | 민감도 | 4.5 | 0.00000 | 충족 |
| 귀무 | 재추출 20,000 | ✅ 판정 | — | 0.00000 | 충족 |
| 귀무 | 해석적(MC 대체) | 민감도 | — | 0.00000 | 충족 |
| 등록일 정밀도 | `exact` 만 | ✅ 판정 | 4.5 | 0.00000 | 충족 |
| 등록일 정밀도 | `approx` 포함(**0건 추가**) | 민감도 | — | — | — |
| 창 절단 | 포함 | ✅ 판정 | 4.5 | 0.00000 | 충족 |
| 창 절단 | 제외(`[D−19, D]` < 20봉 · **0건**) | 민감도 | 4.5 | 0.00000 | 충족 |

(NUMBERS §7 에서 옮김 · 🔴 NUMBERS 의 🟢 를 이 표에서는 «충족»으로 옮겼다 — 판정 언어가 아니라 문턱 표시다 · `approx` 포함 행은 NUMBERS 도 «— (갈래 못 만듦 · 값 없음 · 갈림 계수 밖)»이다)

**갈림 판정** = **갈리지 않는다** ⇒ `RNK-V1` **미발동**

- 🔴 **post7 은 `n_up` 갈래 `p` = 0.07870 ≥ 5% 로 처음 갈렸다**(`RESULTS_RANKING_POST7_NUMBERS.md` §7 인용). 이번엔 같은 갈래 `p` = **0.00080** 이다
  ⇒ ***같은 갈래가 한 글 만에 문턱의 이쪽으로 돌아왔다.*** 🔴 **post7 의 발동을 소급 수정하지 않는다**(`PREREG_POST8.md` (라)).
- 🔑 **대칭 단언 — 「안 갈렸다」가 정보인 축은 «하나»뿐이다.**

| 축 | 이번 회차의 지위 | 왜 |
|---|---|---|
| 유니버스(전체 ↔ `n_up`) | 🟢 **살아 있다 — 그리고 안 갈렸다** | `m_rank` 4.5 ↔ 3.5 · `p` 0.00000 ↔ 0.00080 · post7 에 실제로 갈렸던 축이다 ⇒ ***갈릴 수 있었는데 안 갈렸다*** |
| 집계(글 단위 ↔ 건 pooled) | 🔴 **구성상 항등 = 판별력 0** | post8 은 **글이 하나**다 |
| 등록일 정밀도(`exact` ↔ `approx` 포함) | 🔴 **갈래를 못 만들었다 = 판별력 0** | `approx` 2건(헥토파이낸셜 · 코데즈컴바인)이 있으나 원장 `reg_date` 가 비어 «날짜»를 정할 수 없다(post7 과 같은 모양) |
| 창 절단(포함 ↔ 제외) | 🔴 **항등(절단 0) = 판별력 0** | `P6-절단가드-A` 분자 0 |
| 귀무(재추출 ↔ 해석적) | ⚠️ **표시상 구분 안 됨** | 둘 다 0.00000 · 🟢 갈릴 수 있음은 대조군이 보인다(`RNK-A5` 0.90665 ↔ 0.90505) |

⇒ ***「`RNK-V1` 미발동」은 유니버스 축 «하나»가 안 갈렸다는 뜻이다*** — 나머지 세 축은 이 글에서 **갈릴 수 없었다.** 값과 같은 무게로 적는다.
🔴 **`approx` 2건은 갈래에 넣지 않았다** — ***규칙을 넓혀 날짜를 «만들지» 않는다.*** `PREREG_POST6.md` §1-4 창 규약(「8월 말」 = 08-21~08-31 거래일 7일)의 갈래별 값은 등록일 측정 레인(`run_regday_post8.py`)이 인쇄한다(post7 문형 그대로).

### 5-1. 🆕 `D-5` · `P8-갈래계수` — `(갈래 이름, n, 답)` (NUMBERS §7-3)

최소 n = `exact` 3 · 「답」 = 그 갈래에서 `RNK-P1` 두 문턱 AND 가 서는가 · 🔴 **축의 «판정»은 어느 갈래에서도 (다) 「새 정보 없음」이다** — 이 표는 갈림을 세는 표다.

| 갈래 | **n** | 최소 n(3) | **답**(두 문턱 AND) | 「갈렸다」 계수에 |
|---|---|---|---|---|
| 유니버스 전체(판정) | 4 | 충족 | 충족 | 센다 |
| 유니버스 `n_up` | 4 | 충족 | 충족 | 센다 |
| 집계 글 단위(판정) | 4 | 충족 | 충족 | 센다 |
| 집계 건 pooled | 4 | 충족 | 충족 | 센다(항등 · 글 하나) |
| 귀무 재추출(판정) | 4 | 충족 | 충족 | 센다 |
| 귀무 해석적(MC) | 4 | 충족 | 충족 | 센다 |
| 등록일 정밀도 `exact` 만(판정) | 4 | 충족 | 충족 | 센다 |
| 등록일 정밀도 `approx` 포함 | 4 | 충족 | **— (측정 불가 · 갈래 못 만듦)** | 🔴 **세지 않는다**(답 없음) |
| 창 절단 제외 | 4 | 충족 | 충족 | 센다(항등 · 절단 0) |
| §1-5 재진입 제외 | 4 | 충족 | 충족 | 센다(항등 · 재진입 0) |
| 「우리로 제외」 | 3 | 충족 | 충족 | 🔴 **인쇄만 — 갈래로 세지 않는다**(🔒 #1-(ii)) |

(NUMBERS §7-3 에서 옮김)

⇒ **센 갈래 9개의 답이 전부 같다 ⇒ 갈리지 않는다.** 🔴 `approx` 포함 갈래는 **값이 없어서** 세지 않았다(n 은 `exact` 4건 그대로 · 포함 분모 6 은 «구성»일 뿐).

### 5-2. 재진입 제외 · 「우리로 제외」 · 🔴 `RNK-N2` 감시

| 갈래 | 건수 | `m_rank` 중앙 | 귀무 `p` | 문턱 30 까지 | 판정 효과 |
|---|---|---|---|---|---|
| **포함**(판정) | 4 | **4.5** | **0.00000** | **25.5** | ✅ 판정 |
| §1-5 재진입 제외(🔴 **제외 대상 0건 ⇒ 항등**) | 4 | 4.5 | 0.00000 | 25.5 | — |
| 「우리로 제외」(🔒 #1-(ii)) | 3 | 2.0 | 0.00000 | 28.0 | 🔴 **없음 — 인쇄만** |

(NUMBERS §7-1·§7-2 에서 옮김)

- 🔴 **`RNK-N2` 감시 — post7 은 재진입 1건 제외만으로 문턱까지 거리가 1.0 이었다**(`RESULTS_RANKING_POST7.md` §5-2 인용 · 29.0). 이번 판정 갈래 거리 **25.5** · 「우리로 제외」 **28.0**.
  ⚠️ ***「이번엔 여유가 있다」고 쓰지 않는다*** — 분모 4 에서 중앙값은 **두 값의 평균**이라, ***한 건이 20위권 밖으로 밀리면 거리는 곧바로 줄어든다***(post7 의 163 한 건이 그 모양이었다).
- 🔑 **「우리로 제외」가 판정을 흔들 수 있었나** — 흔들지 않는다(두 문턱 AND 둘 다 충족). 🔒 그리고 **흔들었어도 판정 효과가 없는 갈래다**(🔒 #1-(ii) · 측정 등록 = 사이클 1 · `P6-PRIOR_CYCLE_IN_WINDOW` 0 · ***없는 충돌에 민감도를 붙이면 그것이 신설 규칙이다***).

---

## §6. `RNK-X1` 대조군 · `RNK-N1` 보정 검사

| | 🔒 `RNK-A1`(판정) | 🔬 `RNK-A5`(대조군) |
|---|---|---|
| `m_rank` 중앙 | **4.5** | **2069.5** |
| `pctl` 중앙 | **99.84** | **25.13** |
| 재추출 귀무 `p` | **0.00000** | **0.90665** |
| 해석적(MC) `p` | 0.00000 | 0.90505 |

**같은 실행의 보정 검사**(`RNK-A5` 독립 실현 **200개** · post8 표본 4건):

| 항목 | 기대(귀무 하) | **실측(post8)** | 참고 — 훈련 표본 18건 |
|---|---|---|---|
| `p` 평균 | 0.500 | **0.455** | 0.548 |
| `p` 중앙 | 0.500 | **0.404** | 0.558 |
| **`p < 0.05` 비율(1종오류율)** | **5.0%** | **5.5%**(11/200) | 6.0%(12/200) |
| `p < 0.20` 비율 | 20.0% | **24.0%** | 18.0% |

(NUMBERS §6 에서 옮김)

⇒ **1종오류율이 명목 5%와 어긋나지 않는다 = 구현이 «보정돼 있다».**

- 🔴 **대조군이 이번엔 통과하지 않았다** — post6·post7 은 **2글 연속 통과**했었다(`RESULTS_RANKING_POST7.md` §6 인용 · post6 재추출 0.01105 · post7 0.02175).
  🔴 ***그러나 그것이 「가드가 이제 정상이다」가 아니다*** — `RNK-A5` 는 **정의상 약 5% 확률로 통과한다**(§4-1 M-3). 한 번의 실현으로는 구현에 대해 «말할 수 없고», 구현에 대한 진술은 **보정 검사 쪽**이다.
- ⚠️ **분모가 4건이라 `m_rank` 이산성이 훈련(18건)보다 굵게 들어온다** — `p < 0.20` 비율 **24.0%** 가 기대(20%)보다 높은 쪽이다(post7 26.5% 와 같은 방향 · 훈련 18.0% 와 반대). 1종오류율(5.5%)은 명목과 맞으므로 판정 문장은 바뀌지 않는다. **두 값을 같이 적는다.**
- `RNK-X1` 은 «선택 절차»에 걸린 가드다 — ***post8 에는 선택 절차가 없다*** ⇒ 재판정 대상이 아니다(값만 의무 인쇄).

---

## §7. `RNK-B1`·`B2` — ⛔ **영구 미개시**

| 항목 | 값 |
|---|---|
| 전 짝(동률 포함) | **4** |
| 동률(`Δ=0`) — B1 에서 버림 | **4** |
| **비영 짝** — B1 의 `n` | **0**(최소 5 🔴 미달) |
| `RNK-B2` 승률(동률은 「못 이긴 것」) | **0.0%** ≤ 50% |

(NUMBERS §5 에서 옮김)

⇒ 선택 규칙이 `RNK-A1` 이라 짝 차이가 **항등적으로 0** 이다. 🔴 **누적 짝 표본으로도 열리지 않는다** — ***「짝 표본을 모아 언젠가 연다」는 경로는 없다.*** 규칙 변경은 **새 사전등록으로만** 한다(§2-6).

---

## §8. `RNK-O1` — 훈련 ↔ 검증 · 🆕 같은 스냅샷 소급 재계산

### 8-1. 훈련(동결 상수) ↔ 검증(post8)

| 규칙 | 훈련 LOO `m_rank` 중앙(post1~5) | **검증(post8)** | 훈련 귀무 `p` | **검증 귀무 `p`** | 훈련 커버리지 손실 | **검증 손실** |
|---|---|---|---|---|---|---|
| 🔒 `RNK-A1` | 18.5 | **4.5** | 0.00000 | **0.00000** | 1/18 (5.6%) | **0/4 (0.0%)** |
| `RNK-A2` | 23.5 | **23.5** | 0.00000 | **0.00000** | 2/18 (11.1%) | **0/4 (0.0%)** |
| `RNK-A3` | 20.5 | ⛔ 계수 미동결 | 0.00000 | ⛔ | 2/18 | ⛔ |
| `RNK-A4` | 23.2 | **21.0** | 0.00000 | **0.00000** | 2/18 (11.1%) | **0/4 (0.0%)** |
| 🔬 `RNK-A5` | 1327.2 | **2069.5** | 0.51520 | **0.90665** | 1/18 (5.6%) | **0/4 (0.0%)** |

(NUMBERS §8 에서 옮김 · 훈련 열 = `RESULTS_RANKING_TRAIN_NUMBERS.md` 동결본에서 옮긴 상수 · 🔬 탐색 표기 · 판정 분모 밖)

- 훈련 **18.5**(🟢) ↔ 검증 **4.5**(🟢) ⇒ 문턱(30)의 **같은 쪽** ⇒ 「과적합 의심」 인쇄 조건 **미충족**.
- 🔴 **그래도 「과적합이 없다」가 아니다** — `RNK-A1` 은 자유모수 0 이라 애초에 적합할 것이 없었다. ***이 항목이 방어선이 되는 것은 `RNK-A3` 이 선택됐을 때뿐이다.***

### 8-2. 🆕 글별 `RNK-A1` — 같은 스냅샷(이 실행) 재계산 ↔ 발표값

🔴 **소급 = 탐색** — post2~7 행은 이 실행의 스냅샷으로 **소급 재계산**한 값이다. **채택 근거로 쓰지 않는다 · 판정은 post8 행으로만**. post2~5 는 🔬 훈련(탐색), post6·post7 은 그 회차의 판정을 **다시 매기지 않는다**(`PREREG_POST8.md` (라)).

| 글 | `exact` | 측정 | **`m_rank` 중앙(재계산)** | 재추출 귀무 `p` | 발표값 | 일치 |
|---|---|---|---|---|---|---|
| post2 🔬 | 2 | 2 | **27.0** | 0.00020 | 27.0(훈련 `per_fold`) | 🟢 |
| post3 🔬 | 3 | 3 | **17.0** | 0.00015 | 17.0(훈련 `per_fold`) | 🟢 |
| post4 🔬 | 6 | 6 | **8.0** | 0.00000 | 8.0(훈련 `per_fold`) | 🟢 |
| post5 🔬 | 7 | 6 | **20.0** | 0.00000 | 20.0(훈련 `per_fold`) | 🟢 |
| post6 | 10 | 10 | **15.5** | 0.00000 | 15.5(`RESULTS_RANKING_POST6_NUMBERS.md` §4) | 🟢 |
| post7 | 6 | 6 | **24.5** | 0.00000 | 24.5(`RESULTS_RANKING_POST7_NUMBERS.md` §4) | 🟢 |
| **post8** | 4 | 4 | **4.5** | 0.00000 | —(이 회차) | — |

(NUMBERS §8-1 에서 옮김 · 🟢 = «발표값과 같다»는 대조 표시)

- 🟢 **여섯 글 전부 발표값을 재현했다** — 훈련 동결(08-31) 이후 `daily_prices` 이력 변동(`FROZEN_STALE` 신고 계열)이 ***`RNK-A1` 의 글 단위 값을 한 글도 움직이지 않았다.***
  🔴 **「byte 가 같다」가 아니다** — 대조한 것은 `RNK-A1` 의 글 단위 `m_rank` 중앙뿐이다. `A2`·`A4` 의 60봉 입력은 대조하지 않았다.
- 🔴 **`RNK-P2` 부호 누계**(기록만): 두 문턱 AND «값» post6 충족 · post7 충족 · post8 충족 — ***«지지» 부호가 아니다***(§1 표 마지막 행).

---

## §9. 🆕 `PREREG_POST8.md` 의무 줄 — `D-3` · `D-8` · `D-9`

### 9-1. `D-3` · `D-8`

- 🆕 **`P8-approx의존신고`**: *「`approx` 포함 시 최소 n 이 차는 축: **없음** · `exact` 분모 **4** / `approx` 포함 분모 **6**」*
  (NUMBERS §9 · 구성 예고 PD-21 = 「없음」과 같다 — `exact` 가 이미 최소 n 3 을 채운다 · `approx` 갈래는 이 축에서 «값»을 만들지 못한다).
- 🆕 **`P8-결측수준`**(행·글 두 단위 · 🔒 #3 = 부호 갈림 판정은 다음 사전등록까지 **보류**): `1.0.30` 23/1 · `1.0.32` 3/1 · `1.0.33` 7/1 · `1.0.36` 8/1 ·
  `1.0.39` 9/1 · `1.0.40` 12/1 · **`1.0.42` 10/1** · `missing` 13/1(행/글 · NUMBERS §9). 🔴 이 축은 버전을 **기록만** 한다(`PREREG_RANKING.md:805-806`).

### 9-2. `D-9` 빈티지

| 의무 | 값 |
|---|---|
| ① 쿼리 실행 시각(KST) | **2026-09-24 01:00:24** — 🔴 이 DB 스냅샷 지문을 스크립트가 «처음» 읽은 실행의 시각(`ranking_post8/read_stamp.json`) · 같은 지문 재실행은 이 값을 다시 쓴다(§10 재량 3번) |
| ② 창 구간 `max(daily_prices.updated_at)` | 적재 창 `[2026-04-01, 2026-09-18]` = **2026-09-23 15:46:25.855518** · 관리자 프로브 창 `[07-24, 09-18]` · 09-18 봉(2,764행) 같은 값 |
| ③ | **「2026-09-18 봉은 D+1(2026-09-21) sweep 이후 읽음」** — 예 |
| ④ (기록 · 통과 조건 아님) | **「창 구간 `min(updated_at)` = 2026-09-23 15:45:10.623955 ≥ 09-21 15:35: 예」** — 🔴 `updated_at` 은 일괄 갱신 값이라 빈티지 «증거»가 아니다(PD-27 (라)) |
| ⑤ 혼합 빈티지 | 적재 창 `[2026-04-01, 2026-09-18]` 은 경계 2026-09-14 를 걸친다(경계 전 112봉 / 후 5봉) · 🔴 **판정 창 `[2026-04-01, D]` 는 걸치지 않는다**(판정 분모 `D` 최대 09-11) · PD-27 (바) 표의 걸침 칸 30줄도 공통 의무로 인쇄(NUMBERS §10-2) |

(NUMBERS 머리 표 · §10 에서 옮김)

- 🔴 **이번 회차 이 축의 판정은 빈티지와 무관하다** — 통계량은 등록일 `D` «이후» 봉을 읽지 않는다(`f1` 당일 · `n_up` 전일 종가·당일 고가 · 60봉 특징은 `D` 이전).
- 🔴 **「정규장만」 갈래는 열지 않는다** — `ovtm_vol` 채널 소멸 · 15:30 마감 분봉 09-14 이후 사실상 0(관리자 착수 증거) ⇒ 뺄 수단이 없다(`PREREG_POST8.md` §9 (나) 4).

---

## §10. 모호 지점 · 재량 (판정 아님)

| # | 항목 | 처리 |
|---|---|---|
| 1 | 🔴 **`approx` 갈래 처리가 축마다 다르다** — 이 축은 원장 날짜가 없는 `approx` 로 **갈래를 못 만든다**(답 없음 · 세지 않음), `SEC-` 축은 같은 2건을 «측정 불가»로 `SEC-G1` 분모에 넣어 **「판정 불가」라는 답**을 낸다 | **둘 다 post7 처리 그대로**다(이 축 = `RESULTS_RANKING_POST7.md` §5 · `SEC-` = `v1_axis_verdicts`). `P8-갈래계수`(§5 (나) 2)는 «최소 n»만 말하고 «값 없는 갈래»를 정하지 않는다 ⇒ **양쪽 인쇄** · 이 축의 판정 이동 0(세든 안 세든 (다)가 닫는다) |
| 2 | 🔴 **post7 모호 1번**(「`n_up` 갈래는 판정에 인용하지 않는다」 ↔ 「하나라도 갈리면 ⛔」) | 이번엔 `n_up` 갈래가 **안 갈려** 두 규약의 충돌이 **드러나지 않았다** — ***충돌이 해소된 것이 아니다.*** 문언 정비는 다음 사전등록의 몫(이월) |
| 3 | 🟡 **D-9 ① 「쿼리 실행 시각」 ↔ 바이트 결정론**(`regen_gate.py --rerun` 전제 · 벽시계는 stdout 전용) | **재량**: 본문 ① = **같은 DB 스냅샷 지문을 «처음» 읽은 실행의 KST 시각**(`ranking_post8/read_stamp.json` 에 지문과 함께 보관) · 재실행 벽시계는 stdout. 지문(②·④·`max(date)`·행수)이 바뀌면 새 시각이 박힌다. 대안 = 매 실행 시각을 적고 바이트 결정론 포기 |
| 4 | 🟡 **`POST8_REENTRY` 의 뜻** — 지시문은 재진입 집합을 요구했으나, 우리로는 §1-5 재진입이 아니라 **항목 «안» 2 사이클**이다(PD-3 · 🔒 #1) | **재량**: §1-5 재진입 3건 = `POST8_REENTRY`(원익·헥토·코데즈) · 우리로 = **별도 집합 `POST8_REENTRY_ITEM`**. 둘을 한 집합에 넣으면 §1-5 제외 민감도가 우리로를 빼게 되어 🔒 #1-(ii)(「인쇄만」)와 충돌한다 |
| 5 | 🟡 **`RNK-P2` 「부호」의 뜻** | NUMBERS 는 두 문턱 AND «값»을 «문턱 충족/미달»로 적는다(🟢 로 적지 않는다) — 축 판정은 세 글 모두 ⛔ 이므로 ***🟢 로 적으면 «지지» 부호로 읽힌다*** |

---

## §11. 한계 (§9 승계 · 새 항목 0)

- 🔴 **승/패 대조 5회 연속 미실시** — post8 신규 `exact` 4건에 `all_loss = 1` 이 **0건**(post4 1회 · post5 2회 · post6 3회 · post7 4회 · **post8 5회**).
  ⇒ ***이 축의 최대치는 「기술」이고 「선정 규칙」이라 부를 수 없다***(§0-2 ③).
- 🔴 **랭킹은 «등록일 종가가 확정된 뒤»의 정보로 매긴다** — `f1` 은 장 마감 후에야 확정된다. ⇒ ***라이브에서 재현할 수 없는 순위다.***
- 🔴 **테마·재료가 특징에 없다** — 저자는 이번 글에도 재료를 적었다(5G·통신장비·양자암호 · 탈모 치료제 · 클래리티법 · 로봇 액추에이터 · 북한 모멘텀 · 美 원전·대미투자 · INTAKE §4).
  ⇒ 불성립해도 「저자에게 규칙이 없다」가 아니라 **「우리 9특징에 그 축이 없다」**일 수 있다(판정은 `SEC-` 축이 코드로 한다).
- 🔴 **분모가 4건이다** — `m_rank` 중앙이 두 값의 평균이고, `RNK-G1` ⛔ 경로가 **2건**이며, 보정 검사의 이산성이 굵다(§3·§4·§6).
- 🔴 **유니버스가 «우리 DB»이지 «저자가 본 후보군»이 아니다** — 그리고 명부(`stock_info` 02-10)가 낡아 **명부 미수록 종목이 두 글 연속 판정 분모 안에 있다**(해치텍 → 액스비스).
- 🔴 **유니버스 구멍(191종목 코호트)에 가드가 없다** — post8 등록일(09-01~09-11)은 구멍 창 밖이라 이번 표본엔 안 걸리지만 **글 간 `pctl` 비교에는 계속 붙인다**.
- 🔴 **`RNK-B1`·`B2` 는 누적으로도 열리지 않는다**(§7) · **`RNK-P2` 는 기록만** — 글별 판정을 더하거나 `p` 를 곱하지 않는다.
- 🔴 **`S5`(재무·뉴스)는 이 축의 판정에 쓰지 않는다** — 자기 레인(`run_s5_post8.py`)이 판정한다 · Holm 가족 +0.
- 🔴 **프로그램 `1.0.42`(업데이트 중)** · 저자가 추석 전 통계 데이터 갱신을 예고했다(INTAKE §4) — post9 부터 규칙이 바뀌었을 수 있다(기록만).
- 🔴 `adj_factor` 를 곱하지도 나누지도 않았다 · DB 는 SELECT 만 · 라이브 트리 import 0건.
- 이 분석은 **라이브 채택 대상이 아니다**(`PREREG.md` §0 2번 · `PREREG_POST8.md` §0-1).

---

## §12. 재현

```bash
cd RoboTrader_template/backtest/tasso_program_journal
python run_ranking.py --stage post8     # → RESULTS_RANKING_POST8_NUMBERS.md · ranking_post8/read_stamp.json
python -m pytest test_post8_ranking.py -q -p no:cacheprovider
```

- 🟢 **세 번 실행 byte 동일**(2026-09-24 01:36:13 · 01:36:24 · 01:36:36 KST 실행분) — `RESULTS_RANKING_POST8_NUMBERS.md` md5 **`71f72a3f9cee8733e4a6b14d2eb42623`** ×3 ·
  `ranking_post8/read_stamp.json` `8d27a9c5d36bfdc66774b2584d999674` ×3 · 시드 `20260815` 고정 · DB 는 SELECT 만.
- 🔴 **`run_ranking.py` 는 «순수 덧붙임»이다** — 기존 함수 중 바뀐 것은 `main` 하나(`choices` 에 `post8` 추가 + 분기 1개)이고 `post6_main`·`post7_main`·공유 헬퍼 본문 md5 는 `HEAD` 블롭과 같다(시험 `test_S1_pure_addition_function_bodies_unchanged`). `--stage both` 류의 묶음 모드에 post8 을 넣지 않았다.
- 🔴 **HEAD 해시를 본문에 적지 않는다** · 벽시계는 stdout.
- 🔴 **훈련 산출물 `RESULTS_RANKING_TRAIN_NUMBERS.md` 는 손대지 않았다**(시험 `test_S4_prior_outputs_untouched_vs_head`) — 이 문서의 훈련 열은 그 동결본에서 옮겨 적은 상수다.

[[RESULTS_RANKING_POST8_NUMBERS]] · [[RESULTS_RANKING_POST7]] · [[RESULTS_RANKING_POST6]] · [[PREREG_RANKING]] · [[FREEZE_RANKING_2026-08-31]] ·
[[RESULTS_RANKING_TRAIN_NUMBERS]] · [[PREREG_POST8]] · [[INTAKE_2026-09-18_post8]] · [[PREDECISION_2026-09-18_post8]] · [[LABELS_2026-09-18_post8]]
