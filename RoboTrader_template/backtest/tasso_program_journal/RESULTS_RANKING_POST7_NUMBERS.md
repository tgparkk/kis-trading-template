# `RNK-` 후보 랭킹 — **7번째 글 검증** 수치 원본 (post7 `224409404744`)

> 🔴 **라이브 채택 금지 · 성과·엣지 추정 금지** — 이 산출물은 **기록**이지 전략 후보가 아니다
>   (`PREREG_RANKING.md` §0-1 · `PREREG.md` §0). 라이브 전략·파라미터 변경의 근거가 아니다.
> 🔴 **「+15%」 문턱이 이 축 «안»에 남아 있다** — `f6_spikes60` = 「60일 내 일간 +15% 이상 봉 수」
>   (§0-2 ②). `SEL-S1` 부활이 아닌 이유 셋은 그 절에 있고, **그래도 매 산출물에 인쇄한다.**
> 🔴🔴 **이 축은 «닫혀 있다»** — 동결 선택이 `RNK-A1`(`f1` 단독)이므로 `PREREG_RANKING.md` §4-3 (다)에 의해 **「새 정보 없음 = `REG-M4` 재진술」**이다.
>   ***`RNK-P1` 이 문턱을 넘어도 «지지»로 선언하지 않는다*** (`FREEZE_RANKING_2026-08-31.md` §1·§5). 아래 값은 전부 **기록**이다.
> 🔴 **승/패 대조가 4회 연속 미실시다**(post4 1 · post5 2 · post6 3 · **post7 4**) ⇒ 이 축의 최대치는 「기술」이지 「선정 규칙」이 아니다(§0-2 ③).
> 🔑 **정보는 `RNK-B1`·`RNK-B2`(f1 대비)와 `RNK-N2`(m_rank)에 있다**(§4-1). `RNK-N1` 통과는 「바닥을 통과했다」이지 「가설이 맞았다」가 아니다.

| 항목 | 값 |
|---|---|
| 사전등록 | `PREREG_RANKING.md`(818줄 · 동결 `8e97577`) |
| 동결 | `FREEZE_RANKING_2026-08-31.md` — 🔒 선택 규칙 **`RNK-A1`** · 계수 없음 |
| 인테이크 | `INTAKE_2026-09-15_post7.md` · 결정 `PREDECISION_2026-09-15_post7.md` |
| 실행 브랜치 | `intake/tasso-post7` |
| 🔴 창 종료 | **`2026-09-11`** = 발행일(2026-09-12 토) **휴장** ⇒ 마지막 거래일 · **B-1**(전 축 · `WRC-` 포함 · PD-1) |
| 🔴 실행 시 `max(date)` | **`2026-09-15`** · 그 날짜 행수 **2,765** — **기록만 한다**(창으로 «쓰지» 않는다 · PD-1 5번 표기 의무) |
| 유니버스 창 | `2026-04-01` ~ `2026-09-11` (시작일 고정 · §5-3) |
| 유니버스 술어 | `market_cap>0 ∧ close>0` · 의사티커 제외 · `prev_close` **미사용**(판정 갈래) |
| 시드 · 반복 | `20260815` · **20000회** — ⚠️ `run_selection.NREP = 2000` 과 «다르다»(§8-4) |
| 특징 정의 | `run_selection.build_features` **import**(재구현 0줄 · C-17 f9 NaN 포함) |
| `adj_factor` | 🔴 **곱하지도 나누지도 않았다**(§5-5) |
| 판정 분모 | post7 신규 `exact` **6건** (≥ 3 ⇒ 게이트 열림 · §2-5) |

## 0. 실행 전 점검

### 의사티커 전수 재확인 (§7-B #15)

- 실측 = **`KOSDAQ`, `KOSPI`, `KQ11`, `KS11`** (4개) · `run_selection.py:23` `PSEUDO` = `KOSPI`, `KOSDAQ`, `KS11`, `KQ11` (4개)
- 차집합(실측 − `PSEUDO`) = **없음 ⇒ 🟢 4개로 «전수»다**

### 종목코드 — `INTAKE` 표를 «옮겨 적은 것»이 맞는지 대조

🔴 이 스크립트는 **새 매핑을 만들지 않는다**(§2-2 문형 승계). 아래는 `INTAKE_2026-09-15_post7.md` §1 표의 코드를 **DB `stock_info`(코드+이름 동시 일치)** 와, **post4·post5·post6 계열이 이미 쓰던 코드**와, 그리고 **`daily_prices` 봉 존재**와 대조한 것이다.

🔴🔴 **이 글에서 `stock_info` 동시일치는 «측정 가능성»의 잣대가 아니다**(PD-11 1번). 그 표는 **명부 스냅샷**이고 실측 `max(updated_at)` = **2026-02-10 10:33:05.710624**(행 **2,115**)에서 멈춰 있다 — 서산·해치텍이 «그 표에» 없는 것은 두 종목의 문제가 아니라 **스냅샷의 문제**다. 이 축이 실제로 읽는 `market_cap` 은 **`daily_prices`** 에서 오므로(`run_selection_post6.py:120-122` 문형) ***미일치를 「측정 불가」로 승격하지 않는다.***

| 종목 | `INTAKE` 코드 | `stock_info` 동시일치 | DB 이름 | 계열 기존 코드 | `daily_prices` 봉 | 판정 |
|---|---|---|---|---|---|---|
| 강동씨엔앨 | `198440` | 0 | 강동씨앤엘 | —(이 글이 처음) | 112 | 🟡 **이름 표기 차**(저자 「강동씨엔앨」 ↔ DB 「강동씨앤엘」 · PD-10 4번) · 코드 일치 ⇒ **측정 가능** |
| 로보티즈 | `108490` | 1 | 로보티즈 | —(이 글이 처음) | 112 | 🟢 DB 이름·코드 동시 일치 |
| 범한퓨얼셀 | `382900` | 1 | 범한퓨얼셀 | —(이 글이 처음) | 112 | 🟢 DB 이름·코드 동시 일치 |
| 빛과전자 | `069540` | 1 | 빛과전자 | `069540` | 112 | 🟢 DB 이름·코드 동시 일치 |
| 서산 | `079650` | 0 | — | —(이 글이 처음) | 112 | 🟡 `stock_info` **미수록**(낡은 명부) · `daily_prices` 로 대체 확인 ⇒ **측정 가능** |
| 지투파워 | `388050` | 1 | 지투파워 | `388050` | 112 | 🟢 DB 이름·코드 동시 일치 |
| 한국화장품제조 | `003350` | 1 | 한국화장품제조 | `003350` | 112 | 🟢 DB 이름·코드 동시 일치 |
| 한전기술 | `052690` | 1 | 한전기술 | —(이 글이 처음) | 112 | 🟢 DB 이름·코드 동시 일치 |
| 한전산업 | `130660` | 1 | 한전산업 | —(이 글이 처음) | 112 | 🟢 DB 이름·코드 동시 일치 |
| 해치텍 | `0155E0` | 0 | — | —(이 글이 처음) | 14 | 🟡 `stock_info` **미수록**(낡은 명부) · `daily_prices` 로 대체 확인 ⇒ **측정 가능** |

🔑 **계열 규칙 승계**(PD-11 2번): ***이름으로 못 찾았을 때 「없다」고 적기 «전»에, 그 표에 이름 «열»이 있는지와 그 열이 «비어 있지 않은지»를 먼저 본다.*** 해치텍(`0155E0`)은 `dart_corp_code` 에 **행이 있는데 `corp_name` 이 비어** 이름 검색에 안 잡혔던 종목이다 — 코드는 `news.related_stocks`·`news_stock` 경유로 확정됐다.

### 사유 ① 실측 재현 — DB 종목코드 부재

🟢 **해당 없음** — post7 `exact` 6건 전부 코드가 확정된다(`INTAKE_2026-09-15_post7.md` §1 표 · PD-11). ⇒ `RNK-G1` 의 사유 ① 분자 = **0**.

⚠️ **`stock_industry` 이름검색 0행이 「섹터 측정 가능」을 부정하지 않는다** — 그건 `SEC-` 축의 잣대이고 이 축(`RNK-`)은 `daily_prices` 만 읽는다. 해치텍이 `stock_industry` 에 **미수록**인 것은 `SEC-G1` 의 분자이지 `RNK-G1` 의 분자가 아니다(PD-11 3번 — ***커버리지는 축별로 «다른 수»다***).

## 1. 판정 표본 — 원장 실측 (§2-5)

post7 원장 **13행** · `exact` **6** · `approx` **2** · `after` **0** · `none` **5**

| 정밀도 | 건 | 이 축에서의 처리 | 출처 |
|---|---|---|---|
| `exact` | **6** | ✅ **판정 분모** | §2-5 |
| `approx` | 2 | 판정 분모 «밖» · 의무 민감도 | §2-5 |
| `after` | 0 | 제외 | §2-5 · `PREREG_SELECTION.md` §5 |
| `none` | **5** | 제외 — 🔴 **두 부류가 «섞여 있다»**(아래) | PD-2 · PD-4 3번 |

🔴🔴 **post6 과 «다른» 자리 — `none` 이 «한 부류»가 아니다**(PD-4 표). post6 의 `none` 2건은 전부 「기존 건 후속」이었는데, post7 의 `none` 은 **두 사유**로 갈린다:

| 부류 | 종목 | 왜 `none` 인가 | 출처 |
|---|---|---|---|
| **신규이나 등록일 문장 부재** | 한전기술 · 한전산업 | 🔴 등록일 문장이 **없다**. 「8월 26일」은 **급등 사유일**이고 등록 문장과 «다른 줄»이다 ⇒ 급등일을 등록일로 **승격하지 않는다** | PD-4 |
| **기존 건 후속** | 한라캐스트 · 아난티 · 우리기술투자 | 같은 등록일·같은 사이클의 «추가 레그». 그 등록 사건은 **post6 신규 분모에서 이미 계상됐다**(이중계상 금지) | PD-2 2번 |

🔴 **둘 다 「값이 나빠서」 빠진 것이 아니다** — `reg_date` 가 «비어 있어» 어느 날의 유니버스인지 정할 수 없다. 두 결정 다 값을 보기 «전»에 동결됐다.

| # | 종목 | 코드 | 등록일 | 창 `[2026-04-01, D]` 봉수 | 재진입 | `P6-PRIOR_CYCLE_IN_WINDOW` |
|---|---|---|---|---|---|---|
| 7 | 서산 | `079650` | 2026-09-03 | **106** | — | — |
| 8 | 강동씨엔앨 | `198440` | 2026-09-03 | **106** | — | — |
| 9 | 로보티즈 | `108490` | 2026-09-04 | **107** | — | — |
| 10 | 해치텍 | `0155E0` | 2026-09-07 | **10** | — | — |
| 11 | 빛과전자 | `069540` | 2026-09-08 | **109** | 🔂 **예** | 0 |
| 13 | 범한퓨얼셀 | `382900` | 2026-09-09 | **110** | — | — |

🔂 **글 전체 재진입 3건**(지투파워 **3번째 사이클** · 한국화장품제조 · 빛과전자 — PD-3) 중 **판정 분모(`exact`) 안에 있는 것은 1건**(빛과전자) — 나머지 2건은 `approx` 갈래다.
- **분모에 넣는다**(`PREREG_POST6.md` §1-5 1번) · **제외 민감도는 §7 에 의무 인쇄**.
- 🔴 **`P6-PRIOR_CYCLE_IN_WINDOW` 판정 분모 안 건수 = 0** — 빛과전자는 직전 «등록일» **08-05** 가 창 시작 **08-11** 보다 앞서 **«구성상» 0**이다(PD-3 정정 ②: post4 행은 등록 사건이 아니라 «종료 기록»이라 직전 등록일이 아니다). 🔴 글 전체 플래그 2건(지투파워 · 한국화장품제조)은 **둘 다 `approx` 갈래**이므로 ***판정 분모 안 플래그는 0 이다*** — 두 수를 한 수로 합치지 않는다.
- ⚠️ **재진입 3건 중 2건이 `approx`** 라 **재진입 제외 민감도와 `approx` 제외 민감도가 겹친다** — 두 민감도를 **«따로» 인쇄하고 합치지 않는다**(PD-3 말미).
- 🔴 **3번째 사이클은 이 계열 최초다**(지투파워). §1-5 는 *「2번째 사이클」*이라 적혀 있으나 플래그 정의가 *「자기 **직전** 사이클의 등록일」* 이라 **3번째에도 그대로 적용된다**(문언 확장 아님). 2 사이클 전(08-13)은 **기록만** 한다.

### 유니버스 구성 — 등록일별 (🔴 «상수가 아니다»)

| 등록일 | 그날 전 종목(의사티커 제외) | `close>0` | **`market_cap>0 ∧ close>0` = 유니버스** | 시총 결손 |
|---|---|---|---|---|
| 2026-09-03 | 2764 | 2764 | **2764** | 0 (0.0%) |
| 2026-09-04 | 2765 | 2765 | **2765** | 0 (0.0%) |
| 2026-09-07 | 2765 | 2765 | **2765** | 0 (0.0%) |
| 2026-09-08 | 2765 | 2765 | **2765** | 0 (0.0%) |
| 2026-09-09 | 2765 | 2765 | **2765** | 0 (0.0%) |

⇒ 유니버스 폭 **2,764 ~ 2,765**(차 1) — `pctl` 의 분모 `N−1` 이 건마다 다르다. ⚠️ 훈련 표본에서 신고한 **「7월 등록일이 191종목 작다」**(창 안 첫 봉 코호트 · 2026-08-05 에 메워진 `daily_prices` 구멍)는 **post7 등록일이 전부 09-01 이후**라 이 표본엔 걸리지 않는다. 🔑 그래도 **글 간 `pctl` 비교에는 그 사실을 계속 붙인다.**

### §5-2(C-18) 5열 — 유니버스 `prev_close` 결손 공개

🔴 **판정 갈래는 `prev_close` 를 쓰지 않는다**(§2-1 M-4) — 아래 5열은 **`n_up` 민감도 갈래 전용**이며 `drop_rate` 는 «표기 가드»이지 판정 게이트가 아니다(§6 말미).

| 등록일 | `universe_mcap` | `universe_test` | `dropped` | `drop_rate` | `prev_bar_date` | `n_up` |
|---|---|---|---|---|---|---|
| 2026-09-03 | 2764 | 2764 | 0 | **0.00%** | 2026-09-02 | **55** |
| 2026-09-04 | 2765 | 2764 | 1 | **0.04%** | 2026-09-03 | **58** |
| 2026-09-07 | 2765 | 2765 | 0 | **0.00%** | 2026-09-04 | **74** |
| 2026-09-08 | 2765 | 2765 | 0 | **0.00%** | 2026-09-07 | **60** |
| 2026-09-09 | 2765 | 2765 | 0 | **0.00%** | 2026-09-08 | **62** |

- `drop_rate ≥ 1%` 인 날 = **없음** ⇒ 🟢 표기 가드 미발동(그래도 판정 갈래는 애초에 이 값에 안 걸린다)
- **편향 방향**(§5-2 승계): 결손을 빼면 `n_up` 은 *「더 커질 뿐 작아지지 않는다」* ⇒ `n_up` 갈래의 **순위 진술은 과대**일 수 있다.

## 2. `RNK-G1` 커버리지 가드 (§4-4) — **판정보다 «먼저» 본다**

분모 = post7 신규 `exact` **6건** · 분자 = 사유 ①②③ 중 하나로 «측정 불가»인 건. **게이트 `≥ 1/3` ⇒ ⛔ 판정 불가**(`RESULTS_RECONSTRUCT_POST4.md` §6 Y3 «차용» — 🔴 **커버리지에 대해 검증된 적이 없는 문턱이다**).

| 규칙 | 측정 가능 | 측정 불가 | 비율 | 게이트(1/3) | 사유별 종목 |
|---|---|---|---|---|---|
| `RNK-A1` 🔒**(판정)** | **6**/6 | 0 | **0.0%** | 미발동 | **없음** |
| `RNK-A2` | **5**/6 | 1 | **16.7%** | 미발동 | 해치텍(③ 요구 특징 결측) |
| `RNK-A3` | — | — | — | — | ⛔ **동결 계수 없음**(미선택 ⇒ `RANKING_A3_COEFS.json` 미생성) ⇒ **측정 대상 아님**. 지금 적합하면 그건 post7 을 «보고» 만든 규칙이다 |
| `RNK-A4` | **5**/6 | 1 | **16.7%** | 미발동 | 해치텍(③ 요구 특징 결측) |
| `RNK-A5` | **6**/6 | 0 | **0.0%** | 미발동 | **없음** |

**판정에 쓰는 비율 = «선택된 규칙»의 비율** = **0.0%** ⇒ 🟢 미발동 — 게이트를 연다(⛔ 경로: 측정 불가가 2건이 되면 33.3% 로 발동)

🔴 **편향 방향 신고(§4-4 6번)** — 측정 불가는 **신규 상장주 쪽으로 치우치고** 저자는 신규주를 자주 고른다. ⇒ ***측정 불가가 무작위가 아니다.*** 이번 표본의 손실은 **0건** 다.

🔴🔴 **이 글에는 그 편향의 «실물»이 들어 있다 — 해치텍(`0155E0`)은 2026-08-25 첫 봉의 신규 상장주다**(PD-11). 등록일 창 `[D−19, D]` 는 **10/20 봉**으로 «절단»돼 있고, 60봉 특징용 `2026-04-01~08-04` 구간은 **0봉**(상장 전)이다.
- 🔴 `f9_newhigh` 는 **C-17 NaN 규약**으로 보존되므로(`run_selection.py` `.where(prev_max.notna())`) 그 특징을 쓰는 규칙에서 이 건이 **분모에서 빠질 수 있다** — 위 표의 사유별 열이 그 자리다.
- 🟢 **그러나 선택 규칙 `RNK-A1` 는 `f1_tv_mcap` 단독**이라 60봉 이력을 **쓰지 않는다** ⇒ ***판정 갈래에서는 해치텍이 빠지지 않는다.*** 「빠지는 규칙이 있다」와 「판정이 막힌다」는 다른 말이고, 그 차이를 값과 같은 무게로 적는다.
- 🔴 **`P6-절단가드-A`**(창 `[D−19, D]` 봉수 < 20) 분자 = **1**(해치텍) / 분모 `exact` **6** = **16.7%** < 1/3 ⇒ **미발동**(산술 인쇄). 🔴 **방향 자기신고**: 창이 짧으면 「창 최고」가 되기 쉬워 **관측에 유리한 방향**이다 ⇒ 절단 제외 민감도를 §7 에 나란히 인쇄한다.

### `n_up` 민감도 갈래의 커버리지 — 🔴 **여기서는 게이트가 열린다**

| 규칙 | 측정 불가 / 6 | 비율 | 게이트 | 사유별 종목 |
|---|---|---|---|---|
| `RNK-A1` | 1/6 | **16.7%** | 미발동 | 강동씨엔앨 |
| `RNK-A2` | 2/6 | **33.3%** | 🔴 **발동 ⇒ ⛔** | 강동씨엔앨, 해치텍 |
| `RNK-A4` | 2/6 | **33.3%** | 🔴 **발동 ⇒ ⛔** | 강동씨엔앨, 해치텍 |
| `RNK-A5` | 1/6 | **16.7%** | 미발동 | 강동씨엔앨 |

⇒ **`RNK-D1` 이 «전체 유니버스»로 확정된 근거가 이 글에서도 확인된다**(§2-1 1번). `n_up` 한정은 「고가 +15% 미만」 종목을 구조적으로 밖에 두므로 그 갈래는 **커버리지가 깎인 상태의 값**이다 — ⚠️ **판정에 인용하지 않는다.**

## 3. 건별 `m_rank` · `pctl` · `N` (규칙 × 건)

`m_rank` = 「엄격히 좋은 종목 수 + 동률 수(자기 제외)」 — **동률을 전부 위로** = 보수적(§2-4). `pctl = 100(N−1−m)/(N−1)` 은 **귀무 검정 전용**(§7-C 백분위 포화).

| # | 종목 | 등록일 | `N`(RNK-A1) | `RNK-A1` `m_rank` | `RNK-A2` `m_rank` | `RNK-A4` `m_rank` | `RNK-A5` `m_rank` | `RNK-A1` `pctl` |
|---|---|---|---|---|---|---|---|---|
| 7 | 서산 | 2026-09-03 | 2764 | **2** | **27** | **11** | **504** | 99.93 |
| 8 | 강동씨엔앨 | 2026-09-03 | 2764 | **29** | **50** | **20** | **1153** | 98.95 |
| 9 | 로보티즈 | 2026-09-04 | 2765 | **37** | **17** | **14** | **451** | 98.66 |
| 10 | 해치텍 | 2026-09-07 | 2765 | **1** | ⛔ | ⛔ | **458** | 99.96 |
| 11 | 빛과전자 | 2026-09-08 | 2765 | **20** | **11** | **4** | **483** | 99.28 |
| 13 | 범한퓨얼셀 | 2026-09-09 | 2765 | **163** | **112** | **89** | **1448** | 94.10 |

### 규칙별 `N = |U_A(D)|` (규칙마다 다르다 — §2-4 의무 인쇄)

| # | 종목 | `RNK-A1` | `RNK-A2` | `RNK-A4` | `RNK-A5` |
|---|---|---|---|---|---|
| 7 | 서산 | 2764 | 2673 | 2673 | 2764 |
| 8 | 강동씨엔앨 | 2764 | 2673 | 2673 | 2764 |
| 9 | 로보티즈 | 2765 | 2673 | 2673 | 2765 |
| 10 | 해치텍 | 2765 | 2673 | 2673 | 2765 |
| 11 | 빛과전자 | 2765 | 2673 | 2673 | 2765 |
| 13 | 범한퓨얼셀 | 2765 | 2673 | 2674 | 2765 |

## 4. `RNK-P1` · `RNK-N1` · `RNK-N2` — 판정 (§3 표 1·3·4행)

🔒 **동결 규칙 `RNK-A1`** = `f1_tv_mcap` 단독 내림차순 (벤치마크 · 자유모수 0)

| 항목 | 통계량 | 문턱 (출처) | **실측** | 문턱 충족 | ⛔ 판정 불가 경로 |
|---|---|---|---|---|---|
| **`RNK-P1`** | 글 단위 `pctl` 중앙 **∧** `m_rank` 중앙 | 귀무 백분위 **< 5%**(`PREREG_REGDAY_MEASURE.md` §4-1) **AND** `m_rank` 중앙 **< 30**(`PREREG_D1_OOS.md` §4 N2) | `p` = **0.00000** · `m_rank` 중앙 = **24.5** | 🟢 **둘 다 넘는다** | `RNK-G1`(미발동) · `RNK-V1`(§7) · `RNK-X1`(§6) · `exact` < 3(6건) · 🔴 **선택 규칙 = `RNK-A1`** |
| **`RNK-N1`** | 재추출 귀무 백분위 | **≥ 5% ⇒ 불성립** | **0.00000** | 🟢 성립(< 5%) | 해석/재추출 갈림 ⇒ `RNK-V1` |
| **`RNK-N2`** | `m_rank` 중앙 | **≥ 30 ⇒ 「판별력 없음」 강등** | **24.5** | 🟢 강등 안 됨(< 30) | 🟢 없다(`prev_close` 를 뺐다 · §2-1 M-4) |

🔴🔴 **그러나 «판정»은 이것이다 — ⛔ 「새 정보 없음 = `REG-M4` 재진술」.**

- `PREREG_RANKING.md` §4-3 **(다)** 동결 문언: *「**선택된 규칙이 `RNK-A1` 이면 이 축은 ⛔ 「새 정보 없음 — `REG-M4` 재진술」로 닫고, `RNK-P1` 을 «지지»로 선언하지 않는다.**」*
- ⇒ 위 표의 `RNK-P1` 열이 **🟢 여도 「지지」가 아니다.** 인쇄되는 내용은 ***`REG-M4` 가 이미 발표한 것(「`f1` 안 순위」)의 재진술***이다.
- 🔑 ***이 축이 존재하는 이유는 「`f1` 을 넘어서는가」이지 「`f1` 이 높은가」가 아니다.*** 후자는 `SEL-S2` 가 이미 답했고 `REG-M4` 가 이미 판별력을 부정했다.
- 🔴 **`RNK-N1` 통과는 «증거가 아니다»**(§4-1 자기신고) — `f1` 을 쓰는 규칙은 그 높이를 물려받으므로 균등 귀무를 거의 항상 넘는다. ***「바닥을 통과했다」이지 「가설이 맞았다」가 아니다.***

**규칙별 값 — 의무 인쇄**(판정은 `RNK-A1` 하나로만 한다)

| 규칙 | `m_rank` 중앙 | `pctl` 중앙 | 재추출 귀무 `p` | 해석적(MC) `p` | `< 5%` |
|---|---|---|---|---|---|
| `RNK-A1` 🔒 | **24.5** | **99.11** | **0.00000** | 0.00000 | 🟢 |
| `RNK-A2` | **27.0** | **98.99** | **0.00000** | 0.00005 | 🟢 |
| `RNK-A4` | **14.0** | **99.48** | **0.00000** | 0.00000 | 🟢 |
| `RNK-A5` | **493.5** | **82.14** | **0.02175** | 0.01985 | 🔴 **통과 — 그런데 «대조군»이다**(§6) |

⚠️ 귀무는 **각 건의 등록일에 «같은 `U_A(D)`»에서 무작위 종목 1개** · **20000회 · 시드 20260815** · 크기 정합(같은 날짜 집합·같은 건수 · §2-4). 해석적 갈래는 건수가 짝수라 닫힌형이 아니므로 **몬테카를로로 대체했다**(§2-4 명문).
🔑 규칙 간 귀무는 **같은 스트림 이름**을 다시 불러 «공통난수(CRN)»로 돌렸다 — 의도한 것이다(스트림 분리는 관측·귀무·순열 «사이»에만 건다).

## 5. `RNK-B1`·`RNK-B2` — ⛔ **영구 미개시**

선택 규칙이 `RNK-A1` 이므로 짝 차이가 **항등적으로 0**(`Δm_rank ≡ 0`)이다.

| 항목 | 값 | 근거 |
|---|---|---|
| 전 짝(동률 포함) | 6 | 측정 가능 `exact` 건 |
| 동률(`Δ=0`) — B1 에서 버림 | **6** | 자기 자신과의 짝이므로 전부 동률 |
| **비영 짝** — B1 의 `n` | **0** (최소 5 🔴 **미달**) | §4-3 (가) 산술 유도 |
| `RNK-B2` 승률(동률은 「못 이긴 것」) | **0.0%** ≤ 50% | §4-3 (다) |

⇒ `FREEZE_RANKING_2026-08-31.md` §5 그대로 **⛔ 영구 미개시**. 🔴 **누적 짝 표본으로도 열리지 않는다** — 규칙이 바뀌지 않는 한 `Δ` 는 계속 0이다. 규칙 변경은 **새 사전등록으로만** 한다(§2-6).

## 6. `RNK-X1` 대조군 — 가드가 «살아 있음»의 증명 (§4-6)

🔴 **`RNK-X1` 은 «선택 절차»에 걸린 가드다**(*「`RNK-A5` 가 LOO 에서 **1위**면 절차 무효」* + `FREEZE_RANKING_2026-08-31.md` §4 의 «우회 경로» 조항). ***post7 에는 선택 절차가 없다*** — 규칙은 08-31 에 동결됐다. ⇒ **재판정 대상이 아니다.** 그 대신 **대조군 값을 인쇄**해 귀무 구현이 살아 있음을 보인다(§4-1·§4-6 의무 인쇄).

| 항목 | `RNK-A1`(판정) | `RNK-A5`(🔬 대조군) |
|---|---|---|
| `m_rank` 중앙 | **24.5** | **493.5** |
| `pctl` 중앙 | **99.11** | **82.14** |
| 재추출 귀무 `p` | **0.00000** | **0.02175** |
| 해석적(MC) `p` | 0.00000 | 0.01985 |

🔴 **대조군도 통과했다 — 이 회차의 귀무 구현을 의심하라**(1종오류이거나 구현 결함이다 · §4-1 M-3). 같은 산출물의 다른 결론에 이 문장을 붙인다.

### `RNK-N1` 보정 검사 — 🔴 **한 번의 실현으로는 「가드가 살아 있다」를 «말할 수 없다»**

`RNK-A5` 는 귀무와 «같은» 분포이므로 그 `p` 는 귀무 하 `U(0,1)` 이어야 한다. ⇒ **독립 실현 200개**를 뽑아 `p` 의 분포를 «직접» 잰다(훈련 실행과 같은 장치).

| 항목 | 기대(귀무 하) | 실측(post7 표본 6건) | 참고 — 훈련 표본 18건 |
|---|---|---|---|
| 실현 수 | — | **200** | 200 |
| `p` 평균 | 0.500 | **0.471** | 0.548 |
| `p` 중앙 | 0.500 | **0.445** | 0.558 |
| `p < 0.05` 비율(= 1종오류율) | **5.0%** | **5.5%** (11/200) | 6.0% (12/200) |
| `p < 0.20` 비율 | 20.0% | **26.5%** | 18.0% |

**판정** = 🟢 **1종오류율이 명목 5%와 어긋나지 않는다 ⇒ 귀무 구현이 «보정돼 있다»**

🔴🔴 **두 문장이 «같은 실행»에서 나왔다 — 어느 쪽도 지우지 않는다.**

- 위 §6 머리: *「대조군도 통과했다 — 이 회차의 귀무 구현을 «의심하라»」*(§4-1 M-3 문언).
- 바로 이 보정 검사: *「1종오류율이 명목 5%와 어긋나지 않는다 ⇒ 구현이 «보정돼 있다»」* (독립 실현 200개 · 실측 5.5%).
- 🔑 ***두 진술은 «다른 것»에 대한 것이다*** — 보정 검사는 **구현**에 대한 진술이고, M-3 의 경고는 **이 회차의 한 실현**에 대한 진술이다. 보정이 정상이라는 것은 ***「그러니 이번 통과는 1종오류일 가능성이 높다」***까지만 말해 주며, **「그러니 경고를 지워도 된다」를 뜻하지 않는다.**
- 🔴 **그래서 M-3 문언대로 경고를 이 산출물의 다른 결론에도 붙인다.** ⚠️ 단 이 회차의 «판정»은 애초에 §4-3 (다)로 닫혀 있으므로, 이 경고가 뒤집을 «지지»가 없다 — 그 사실도 함께 적는다(경고를 축소하는 것이 아니라 **적용 대상이 비어 있다**는 뜻이다).

⚠️ **분모가 10건이라 `m_rank` 이산성이 더 굵게 들어온다** — 훈련(18건)보다 «덜 매끄러운» 분포다. 훈련에서 신고한 «`p` 평균이 0.5 보다 약간 높다」의 원인(등호 포함 + 이산 동률)은 여기서도 같은 방향(보수적)이다.

## 7. `RNK-V1` 민감도 (§4-7) — 판정이 잣대에 종속되나

**4축을 «항상» 나란히 인쇄한다. 하나라도 판정을 가르면 ⇒ ⛔ 어느 쪽도 선언하지 않는다.**

| 축 | 갈래 | 판정 갈래인가 | `m_rank` 중앙 | 귀무 `p` | 두 문턱 AND |
|---|---|---|---|---|---|
| 유니버스 | 전체(`market_cap>0 ∧ close>0`) | ✅ **판정**(`RNK-D1` 확정) | **24.5** | **0.00000** | 🟢 |
| 유니버스 | `n_up`(+`prev_close`) | 민감도 | 13.0 | 0.07870 | ⛔ |
| 집계 | 글 단위 중앙 | ✅ **판정** | **24.5** | **0.00000** | 🟢 |
| 집계 | 건 단위 pooled 중앙 | 민감도 | 24.5 | 0.00000 | 🟢 |
| 귀무 | 재추출 20000 | ✅ **판정** | — | **0.00000** | 🟢 |
| 귀무 | 해석적(MC 대체) | 민감도 | — | 0.00000 | 🟢 |
| 등록일 정밀도 | `exact` 만 | ✅ **판정** | **24.5** | **0.00000** | 🟢 |
| 등록일 정밀도 | `approx` 포함(**0건** 추가) | 민감도 | — | — | 🟢 |
| 창 절단 | 포함(판정) | ✅ **판정** | **24.5** | **0.00000** | 🟢 |
| 창 절단 | 제외(`[D−19, D]` < 20봉) | 민감도 | 29.0 | 0.00000 | 🟢 |

**갈림 판정** = 🔴 **갈린다** ⇒ ⛔ `RNK-V1` — 어느 쪽도 선언하지 않는다

🔴 **`approx` 2건은 갈래에 못 넣었다** — 원장 `reg_date` 가 비어 있어 어느 날의 유니버스인지 정할 수 없다(지투파워 · 한국화장품제조). ***규칙을 넓혀 날짜를 «만들지» 않는다*** — `PREREG_POST6.md` §1-4 창 규약(「9월 초」 = 09-01~09-10 거래일 8일 · 「8월 말」 = 08-21~08-31 거래일 7일)의 갈래별 값은 **`run_regday_post7.py` 레인**이 인쇄한다.

🔴 **절단 제외 민감도** — 제외 대상 = **해치텍**(창 `[D−19, D]` 봉수 해치텍=10). `P6-절단가드-A` = **1/6 = 16.7%** < 1/3 ⇒ **미발동**. 🔴 **방향 자기신고**: 창이 짧으면 「창 최고」가 되기 쉬워 **관측에 유리한 방향**이다 — 그래서 이 갈래를 «반드시» 나란히 인쇄한다(§1-6 2번). ⇒ 🟢 **판정을 가르지 않는다**

🔴🔴 **그러나 «4축이 같은 답을 줬다»를 «4축이 판별력을 가졌다»로 읽지 않는다 — 이번 회차에 «두 축»은 구성상 항등이다.**

| 축 | 이번 회차의 지위 | 왜 |
|---|---|---|
| 집계(글 단위 ↔ 건 pooled) | 🔴 **구성상 항등 = 판별력 0** | post7 은 **글이 하나**다. 「글 단위 값들의 중앙」의 인자가 하나뿐이라 건 pooled 중앙과 **같은 수**가 된다 |
| 등록일 정밀도(`exact` ↔ `approx` 포함) | 🔴 **갈래를 못 만들었다 = 판별력 0** | post7 `approx` = **2건**이지만 원장 `reg_date` 가 비어 «날짜»를 정할 수 없다 ⇒ 이 회차엔 잴 수 없다(규칙을 넓히지 않는다) |
| 유니버스(전체 ↔ `n_up`) | 🟢 살아 있다 | 두 갈래의 `m_rank` 가 실제로 다르다(24.5 ↔ 13.0) |
| 귀무(재추출 ↔ 해석적) | ⚠️ **이번 회차엔 표시상 구분 안 됨** | 두 값이 **표시 정밀도에서 같다**(둘 다 0.00000) ⇒ 「갈릴 수 있었는데 안 갈렸다」를 이 축의 값으로는 «못 보인다». 🟢 **갈릴 수 있음은 대조군이 보인다** — `RNK-A5` 는 재추출 0.02175 ↔ 해석적 0.01985 로 다르다 |

🔑 ***「민감도가 안 갈렸다」는 「민감도가 갈릴 수 있었는데 안 갈렸다」일 때만 정보다.*** 집계 축은 이 회차에도 **갈릴 수 «없었다»**(글이 하나) — 그 사실을 값과 같은 무게로 적는다. 🟢 **등록일 정밀도 축은 post6 의 예고대로 이 글에서 살아났다** — 「축을 빼지 않고 «갈릴 수 없었다»고 적어 둔 것」이 한 글 뒤에 판별력으로 돌아온 자리다. 🔴 **문턱을 바꾸지도, 축을 빼지도 않는다.**

### 재진입 제외 민감도 (PD-3 · `PREREG_POST6.md` §1-5 2번 — «의무»)

| 갈래 | 건수 | `m_rank` 중앙 | 귀무 `p` | 두 문턱 AND |
|---|---|---|---|---|
| **포함**(판정) | 6 | **24.5** | **0.00000** | 🟢 |
| 제외(빛과전자) | 5 | 29.0 | 0.00000 | 🟢 |

⇒ 🟢 **두 값이 판정을 가르지 않는다** — 「재진입 의존」이 아니다
⚠️ 🔴 **「안 갈렸다」를 「여유가 있다」로 쓰지 않는다** — 제외 시 `m_rank` 중앙이 **24.5 → 29.0** 로 움직였고, 강등 문턱 **30** 까지 남은 거리가 **5.5 → 1.0** 다. ***1건을 빼는 것만으로 이만큼 움직이는 통계량이다.***
⚠️ **`P6-PRIOR_CYCLE_IN_WINDOW` 는 「제외」가 아니다** — *「이 건은 규칙상 통과할 수 없다」는 사실을 판정과 같은 무게로 인쇄하는 장치」*다(§1-5 3번). **판정 분모 안 건수 = 0** (빛과전자는 직전 등록일 08-05 < 창 시작 08-11 ⇒ «구성상» 0) · 🔴 **글 전체 2건**(지투파워 · 한국화장품제조)은 **둘 다 `approx` 갈래**다 — 두 수를 합치지 않는다(PD-3).

## 8. `RNK-O1` 과적합 신고 (§4-5) — 훈련과 검증을 «항상» 나란히

🔴 **훈련값은 `RESULTS_RANKING_TRAIN_NUMBERS.md`(동결본)에서 «옮겨 적은 상수»다 — 재계산이 아니다.** 🔬 **탐색적 표기이며 판정 분모에 «넣지 않는다»**(§4-5 · `PREREG_POST6.md` §2-1 ③).

| 규칙 | 훈련 LOO `m_rank` 중앙(post1~5) | **검증 `m_rank` 중앙(post7)** | 훈련 귀무 `p` | **검증 귀무 `p`** | 훈련 커버리지 손실 | **검증 커버리지 손실** |
|---|---|---|---|---|---|---|
| `RNK-A1` 🔒 | 18.5 | **24.5** | 0.00000 | **0.00000** | 1/18 (5.6%) | **0/6 (0.0%)** |
| `RNK-A2` | 23.5 | **27.0** | 0.00000 | **0.00000** | 2/18 (11.1%) | **1/6 (16.7%)** |
| `RNK-A3` | 20.5 | ⛔ **계수 미동결 ⇒ 측정 안 함** | 0.00000 | ⛔ | 2/18 | ⛔ |
| `RNK-A4` | 23.2 | **14.0** | 0.00000 | **0.00000** | 2/18 (11.1%) | **1/6 (16.7%)** |
| `RNK-A5` | 1327.2 | **493.5** | 0.51520 | **0.02175** | 1/18 (5.6%) | **0/6 (0.0%)** |

**`RNK-N2`(30) 기준 괴리 점검 — 선택 규칙 `RNK-A1`**

| 갈래 | `m_rank` 중앙 | `< 30` |
|---|---|---|
| 훈련 LOO(글 단위 중앙의 중앙 · 4폴드 27.0 · 17.0 · 8.0 · 20.0) | **18.5** | 🟢 |
| 훈련 건 pooled 중앙(18건 중 17건 측정) | 17.0 | 🟢 |
| **검증 post7**(6건 중 6건 측정) | **24.5** | 🟢 |

🟢 **훈련과 검증이 문턱(30)의 «같은 쪽»에 있다** ⇒ 「과적합 의심」 인쇄 조건 미충족. ⚠️ 🔴 **그래도 「과적합이 없다」가 아니다** — 선택된 `RNK-A1` 은 **자유모수 0**이라 애초에 적합할 것이 없었다. ***이 항목이 진짜 방어선이 되는 것은 `RNK-A3` 이 선택됐을 때뿐이고, 그 일은 일어나지 않았다***(§4-5 말미).

⚠️ **두 값은 «다른 잣대»다** — 훈련 LOO 는 **4개 글의 글 단위 중앙의 중앙**이고 검증은 **한 글의 중앙**이다. 🔑 계열 규칙(*「잣대가 다른 열은 «방향 참고»로만」*)대로 **차이의 크기를 해석하지 않는다** — 문턱(30)의 «어느 쪽»인지만 본다.

## 9. 천장·미해소 (§0-2 ③ · §6)

| 항목 | 상태 |
|---|---|
| 승/패 대조(`PREREG_SELECTION.md` §4) | ⛔ **4회 연속 미실시** — post7 신규 `exact` 6건에 `all_loss = 1` 이 **0건**(post4 = 1회 · post5 = 2회 · post6 = 3회 · **post7 = 4회**) |
| ⇒ 그 귀결 | 🔴 ***이 축의 최대치는 「기술」이다 — 「선정 규칙」이라 부를 수 없다***(§0-2 ③ · `PREREG_SELECTION.md` §4 *「§3 결과를 「선정 규칙」으로 인용 금지」*) |
| `RNK-B1`·`B2` | ⛔ **영구 미개시**(§5) |
| `RNK-P1` | ⛔ **지지 선언 금지**(§4-3 (다)) — 값만 기록 |
| `RNK-P2`(글 넘어 반복) | **기록만** — 검정 통계량 아님(`RESULTS_D1_OOS_POST5.md` §8). 부호 누계: 훈련(🔬 탐색) 성립 · post7 문턱 충족 |
| `RNK-A3` | ⛔ 계수 미동결 ⇒ **이 글에서도 열리지 않는다** |
| 유니버스 구멍(191종목 코호트) | 🔴 **가드 없음** — post7 등록일(09-03~09-09)은 구멍 창 밖이라 이번 표본엔 안 걸리지만, **글 간 `pctl` 비교에는 계속 붙인다** |
| `S5`(재무·뉴스) | 🔴 **이 축의 판정에 쓰지 않는다** — `PREREG_S5_FUND_NEWS_OOS.md` 는 **동결 선언이 없어** 이번 회차엔 **값 인쇄만**이다(PD-15) · Holm 가족 +0 |
| `GT-` 등급 열 | 🔴 **이 산출물에는 붙이지 않는다** — 등급은 `MANUAL_DOCS` 산출물에만 달고 **`PAIRS` 산출물(이 파일)은 한 글자도 고치지 않는다**(PD-16 표기 규약) |

🔴 **랭킹은 «등록일 종가가 확정된 뒤»의 정보로 매긴다**(§9 한계) — `f1`(당일 거래대금/시총)은 **장 마감 후에야 확정**되는데 저자는 장중에 골랐을 수 있다. ⇒ ***이 축은 라이브에서 재현할 수 없는 순위다.***
🔴 **테마·재료가 특징에 없다**(§9 · `PREREG_SELECTION.md` §6) — 저자는 매 건 재료를 적었다(이번 글도 13/13 — 웨스팅하우스/대미투자 ×2 · 로봇부품 수주 · 원자력 · 남북경협 · 가상화폐 · 호남 반도체 클러스터 ×2 · 골드만삭스 목표가 · ChatGPT · 6G 광통신 국산화 · 화장품 호실적 · 조선기자재/연료전지). ⇒ 불성립해도 「저자에게 규칙이 없다」가 아니라 「우리 9특징에 그 축이 없다」일 수 있다. 🔑 저자 스스로도 *「요즘은 테마 순환매 장세라, 여러 섹터에서 대체로 강한 종목들 위주로 선정했고요」*라고 적었다 — **기록만 하고 가설의 증거로 쓰지 않는다**(판정은 `SEC-` 축이 코드로 한다).
🔴 **차용 문턱 셋**(**30** · **50%** · **1/3**)은 전부 «다른 것을 재던 값»이며 이 대상에 대해 검증된 적이 없다. 이 글에서 그중 하나가 판정을 갈랐다면 **「차용 문턱에 걸렸다」**고 적는다 — 이번 회차 해당 = **없음(30 미발동 · 1/3 미발동 · 50% 는 `RNK-A1` 자기짝이라 구성상 결정)**.

## 10. 재현 정보

| 파일 | md5 |
|---|---|
| `run_ranking.py` | `ba8fa2b6cbd33da35d631e05737387e3` |
| `run_selection.py` | `fa366edf5f8beca0b22560e810fc5f34` |
| `run_tests.py` | `a122eecd3f17e7a8ded9af48d2fd6dfe` |
| `run_regday_post5.py` | `135cf7f9aca1ddafcd2abf9f41de9daf` |
| `PREREG_RANKING.md` | `1d0501fb8f3e6c0836620274429b37f5` |
| `FREEZE_RANKING_2026-08-31.md` | `788d28ff928cbdbf742a1bf83deb373e` |
| `RESULTS_RANKING_TRAIN_NUMBERS.md` | `a310e4a3fcbcbed5774f340c06266573` |
| `ledger_trades.csv` **판독 필드 지문**(아래 설명) | `09a3b2f4c9286cdc53192ba615a77fd2` |
| `ledger_trades.csv` prefix 51줄(= 훈련 시점 행) | `5d603de7d0ac1ed853eaaf960c1f0883` |

🔴 **통째 md5 를 «일부러» 안 박은 파일이 셋 있다 — `ledger_trades.csv` · `INTAKE_2026-09-15_post7.md` · `PREDECISION_2026-09-15_post7.md`.**

**실측 근거**: 이 스크립트를 연달아 돌리는 «사이»에 세 파일의 md5 가 **움직였다** (공유 레인이 같은 시간대에 `narrative` 열과 인테이크 §6 「판정 현황」을 채우고 있다 — 그 문서 자신이 *「이 절만 갱신」* 이라 적은 대로다). ⇒ 통째 md5 를 박으면 ***남의 «판정과 무관한» 편집 때문에 내 산출물이 재현 불가가 된다.***

**그래서 «의존하는 것»만 고정한다** — 파일 전체가 아니라 이 축이 실제로 읽는 것:

| 파일 | 이 축이 «읽는» 것 | 고정 방법 |
|---|---|---|
| `ledger_trades.csv` | `post_log_no` · `post_date` · `item_no` · `stock_name` · `reg_date` · `reg_date_precision` · `all_loss` (**`narrative` 는 안 읽는다**) | 위 **판독 필드 지문** = 그 7열만 뽑아 정렬해 해시 · 그리고 **prefix 51줄 md5**(훈련 시점 행 불변) |
| `INTAKE_2026-09-15_post7.md` | §1 표의 **종목코드 10건** | 모듈 상수 `POST7_CODES` (표를 «옮겨 적은 것») · 🔴 **post7 판 축자 대조 테스트는 아직 없다**(post6 은 `test_post6_ranking.py` P4a 가 지켰다 — 그 자리가 이 회차엔 비어 있다) |
| `PREDECISION_2026-09-15_post7.md` | PD-1(창 종료 09-11) · PD-2(후속 3건 `none` 제외) · PD-3(재진입 3건·플래그) · PD-4(`exact` 6 / `approx` 2 / `none` 5) · PD-11(코드·커버리지) · PD-12(창 절단) | 모듈 상수 `DB_UPTO_POST7`·`POST7_REENTRY`·`POST7_PRIOR_CYCLE`·`POST7_NONE_NEW`·`POST7_FOLLOWUP` |

🔑 **계열 규칙 후보**: ***여러 레인이 같은 파일을 동시에 쓰는 동안에는 「파일 md5」가 재현성 장치가 아니라 «재현성 파괴 장치»다*** — 박아야 할 것은 **내가 읽는 필드의 지문**이다.

🔴 **md5 의 «잣대» 고지 — 줄바꿈 형태를 함께 박는다.** 위 값은 전부 **이 워크트리의 실제 바이트**에 대한 md5 다. 이 트리는 git 체크아웃 산물이고 `core.autocrlf=input` 이라 **추적 파일이 LF** 로 놓인다 — 실측 `run_ranking.py` = **LF**.

| 형태 | `run_ranking.py` md5 |
|---|---|
| **LF**(이 트리의 실제 바이트 · 위 표의 값) | `ba8fa2b6cbd33da35d631e05737387e3` |
| CRLF 환산(같은 내용) | `5f262b3742d974aa49bbaf640034a50f` |

⚠️ **그래서 `FREEZE_RANKING_2026-08-31.md` §2 의 `run_ranking.py` = `1a0d19d2dd5e822054359762bfcfc9ae` 와 위 값을 «직접 비교하면 어긋난다».** 그 값은 **동결 당시 트리에서 갓 쓰인 CRLF 형태**의 md5이고 위 값은 **LF 형태**다 — 🟢 ***내용 불일치가 아니라 잣대 차이다.*** (같은 표의 `run_selection.py`·`run_tests.py`·`run_regday_post5.py`·`PREREG_RANKING.md` 는 그때도 체크아웃 산물이라 **LF 형태**로 적혔고 오늘 값과 그대로 일치한다 ⇒ 한 표 안에 **두 잣대가 섞여 있었다.**)

🔑 **계열 규칙 후보**: ***md5 를 동결할 때는 「작업트리 md5」인지 「블롭 md5」인지를 함께 박는다 — Windows·`autocrlf` 아래에서 두 값은 «항상» 다르다.*** 형태를 안 적으면 나중에 「파일이 바뀌었다」와 「줄바꿈이 다르다」를 구분할 수 없다.

🔴 **원장은 훈련 실행 «뒤»에 두 번 자랐다** — post6 12행 · **post7 13행** 추가(51줄 → 63줄 → **76줄**). 🟢 **훈련 시점 행까지의 prefix(51줄) md5 는 그대로 `5d603de7d0ac1ed853eaaf960c1f0883`**(위 표 · `FREEZE_RANKING_2026-08-31.md` §2 선언값과 일치) — 즉 **기존 51줄은 byte 불변**이고 훈련 표본이 «고쳐진» 것이 아니다. ⇒ `--stage train` 은 `post_date <= 2026-08-31` 로 그 행들만 읽는다(위 상수) — ***post7 행은 훈련 분모에 «구조적으로» 못 들어간다.***

### 🔴 훈련 산출물의 «재현성» — 동결 시점 이후 `daily_prices` 이력이 «바뀌었다»

아래 둘은 **순수 DB 집계**다(원장·스크립트와 무관 · `RESULTS_RANKING_TRAIN_NUMBERS.md` §1 이 발표한 값과 «같은 SQL·같은 창»). 재현되지 않으면 그 동결 산출물은 **같은 스크립트로도 재생성되지 않는다.**

| 프로브 | 동결본(2026-08-31) 발표값 | **오늘 실측(DB `max(date)` = 2026-09-15)** | 일치 |
|---|---|---|---|
| `2026-07-28` 그날 전 종목 / `close>0` / 유니버스 | 2572 / 2572 / 2570 | **2579 / 2579 / 2570** | 🔴 **불일치** |
| `2026-08-05` 창 안 «첫 봉» 코호트 | 191 | **184** | 🔴 **불일치** |

🔴🔴 **⛔ `RESULTS_RANKING_TRAIN_NUMBERS.md` 는 «FROZEN_STALE» 이다** — 동결 이후 `daily_prices` 의 **과거 이력**이 바뀌었고(구멍 메움·복원 계열), 그래서 그 파일은 오늘 다시 돌려도 **byte 로 재현되지 않는다.**

- 🟢 **이 축의 «동결 선택»은 영향받지 않는다** — 선택은 `RNK-A1`(f1 단독)이고 `f1 = 거래대금/시총` 은 바뀐 이력(과거 종가·고가)에 걸리지 않는다. 그래도 그 사실은 **재실행으로 확인**해야지 논증으로 때우지 않는다.
- 🔴 **이 산출물(post7)은 그 «바뀐» 스냅샷 위에서 계산됐다** — 훈련값과 검증값은 ***서로 다른 DB 상태에서 나온 수*** 다. `RNK-O1` 병기는 그 사실을 안고 읽는다.
- ⇒ **처리는 최종 레인(`regen_gate.py`)에 넘긴다**: `PAIRS` 재현 실패 사유 = **「입력 DB 가 이동」**이지 「스크립트가 바뀜」이 아니다. **동결값을 고쳐 쓰지 않는다.**

**유니버스 SQL 원문**(§2-3 6번 동결 대상 · 상한만 실행 시점 스냅샷):

```sql
SELECT stock_code, date, high, low, close, trading_value, market_cap
FROM daily_prices WHERE date BETWEEN '2026-04-01' AND '2026-09-11'
AND market_cap IS NOT NULL AND market_cap > 0 AND close > 0
-- 그 뒤 파이썬에서 의사티커 ['KOSPI', 'KOSDAQ', 'KS11', 'KQ11'] 제외
```

🔑 `RESULTS_RANKING_POST7_NUMBERS.md` 자신의 md5 는 이 파일 «밖»에서 잰다(자기참조 불가) — `RESULTS_RANKING_POST7.md`(사람이 쓰는 산문) 말미에 있다.

