# 사전등록 — 조건부 선정(귀무를 후보군으로) + 매도 갭체결 가설

**작성 2026-08-15 (동결 · 실행 전 커밋)**
선행: `PREREG_MINUTE_FLOW.md`(`766e7b0` → 결과 `b1104b7`·`fa2b5d8`, **§A·§B 반증축 기각 · §C 대조축이 해석 뒤집음**)

> 🔴 **오염 고지.** 같은 표본을 이미 봤다. §1·§2 는 **탐색적**이다. 확정 검정은 §3 의 다음 글 예측.

---

## §0. 왜 다시 파는가 — 지금까지의 귀무가 질문을 비껴갔다

🔑🔑 ***지금까지 선정 축의 모든 검정에서 귀무가 「유니버스 무작위 종목」이었다.***
그러면 f1~f14 가 유의한 건 거의 당연하다 — **후보군에 들었다는 사실 자체가 이미 상위**이기 때문이다.
우리가 답하려던 질문은 그게 아니라 ***「같은 날 후보군 18종목 중 왜 이 1~2개인가」*** 였다.

⇒ **귀무를 「유니버스」에서 「같은 날 후보군」으로 바꾼다.** 데이터 추가 없이 지금 가능하다.

매도 축도 마찬가지다. §B 는 「순서」만 물었고 **「시각 분포」는 한 번도 안 물었다.**
S4 기술통계에서 **7건 중 5건의 첫 매도가 09:00~09:21**, 씨피시스템은 5레그가 **8분**,
빛과전자는 2레그가 **6분** 안에 들어갔다. 이건 다른 기전을 가리킨다.

## §1. 후보 선정 — 조건부 검정

**후보군 정의 (동결 · 이미 고정된 것만 재사용)**
- 5축 `f1_tv_mcap·f2_tv·f4_vol20·f6_spikes60·f8_ma20dev` 을 **공통 문턱 τ = 90** 으로 AND.
  (`filter_curve.py` 가 쓴 그 τ. **실측: 447 종목-일 · 고유 113종목 · 일별 중앙 18 · 재현 5/7**)
- **양성** = 등록일이 특정된 7건 중 그날 후보군에 든 건(실측 5건).
- **음성** = 같은 날 후보군의 나머지.

**특징 (동결)**
- 기존 `f1~f9` · 수급 `f10~f14`
- 🆕 **정규화 수급** `f10r·f11r·f12r·f14r` = 순매수 금액 / (거래대금/1e6)
  🔑 `fa2b5d8` 에서 개인·외국인이 **같이** 상위라 「방향이 아니라 규모」 혐의가 떴다.
  거래대금으로 나누면 규모가 빠지고 **방향만 남는다.** `f13_short` 는 이미 비율이라 그대로.
  🔴 **정규화 후에도 `f12r`(개인)이 `f10r`(외국인)과 같은 부호로 상위면, 그건 지표가 아직도
     방향을 못 재고 있다는 뜻이다** — 그때는 수급 축 전체를 접는다.

**통계량**: 각 특징에 대해 **같은 날 후보군 안에서의 백분위 순위**(0~100).

**결정규칙 (동결)**
- 귀무 = 같은 날 후보군에서 **무작위로 1건**을 뽑아 같은 통계량. **20,000회 · 시드 고정.**
- ✅ **선별과 연관**: 양성 5건의 **중앙 순위**가 귀무 백분위 **< 5%** 그리고 **≥ 70**
- 🔴 **다중검정**: 검정하는 특징 수(f1~f9 + f10~f14 + f10r~f14r = **18개**)에 **Holm 보정**.
  family 를 18 로 못박는다.
- 🔴 **양측**: `PREREG_MINUTE_FLOW.md` §C 에서 *「부호 안 건다」*고 적고 한쪽 꼬리만 봐서
  `f13_short` 의 **낮은 쪽 방향성을 못 쟀다**(사전등록 결함 4회째). ⇒ **이번엔 부호를 안 건
  특징(`f12·f12r·f13`)은 양측으로 판정**한다. 즉 중앙 순위가 ≥70 **또는 ≤30** 이면 후보.

⚠️ **n = 5.** 검정력이 극히 낮다. **「유의하지 않음」이 「축이 아님」이 아니다.**
   그리고 5건은 τ=90 이라는 **우리가 고른 문턱**에 의존한다 — τ를 바꾸면 표본이 바뀐다.
   ⇒ **τ = 90 하나로 고정**하고, 다른 τ 로 다시 재지 않는다(사후 완화 금지).

## §2. 매도 타이밍 — 갭 체결 가설

**기전 가설**: 저자는 **지정가 매도 사다리를 미리 걸어둔다.** 그러면 **갭 상승한 시가에
여러 차수가 한꺼번에 체결**된다. 이 가설은 세 가지를 동시에 예측한다 —
첫 매도가 **장 초반**, 매도가가 **시가 근처**, 레그들이 **짧은 시간 안에**.

**통계량 (자유도 0)**
- 배정은 §B 와 동일하되 **최소(가장 이른) 배정과 최대(가장 늦은) 배정을 «둘 다»** 낸다.
  🔑 그리디가 이른 쪽을 고르면 편향된다 — **대칭으로 재는 것이 반증축이다.**
- `open_pos` = `(S₁ − 그날 시가) / (그날 고가 − 그날 시가)` (0 = 시가, 1 = 고가)

**예측과 결정규칙 (동결)**
- **G1**: 첫 매도가 **09:00~09:30** 인 건 — **최소 배정에서 과반 «그리고» 최대 배정에서도 과반.**
  🔴 최대 배정에서 깨지면 **그리디 편향이 만든 것**이므로 G1 기각.
- **G2**: `open_pos < 0.3` 인 건이 **과반** (매도가가 시가 근처 = 갭 체결).
- **G3 (반증축 · 필수)**: 그날 호가격자에서 무작위 `S'` 를 뽑아 G2 와 같은 비율을 잰다.
  **20,000회 · 시드 고정.** 관측이 **귀무 백분위 5% 밖**이면 **G2 지지를 취소**한다.
- **G4 (기술)**: 전 레그 소요 시간을 **최소 배정 span · 최대 배정 span** 둘 다 기록.
  🔑 **최대 span 도 짧으면** 「한 번에 쏟아졌다」가 강해지고, 길면 **판별 불가**로 적는다.

## §3. 🟢 다음 글 예측 (진짜 검정 · 동결)

⚠️ 신규 건이 **3건 미만이면 판정을 미룬다.**
- **Q1**: 신규 건에서도 §1 에서 Holm 통과한 특징의 후보군 내 중앙 순위가 같은 방향.
  통과한 게 없으면 **Q1 은 적용하지 않는다**(사후에 만들지 않는다).
- **Q2**: 신규 건의 첫 매도가 09:00~09:30 (최소·최대 배정 둘 다 과반).
- **Q3**: 신규 건의 `open_pos` 중앙값 < 0.3.

## §5. 추가 등록 — 다음 글에 대한 예측 (2026-08-15, **§1·§2 결과를 본 뒤** 작성)

🔴 **이 절은 위 §1~§4 보다 나중에 쓰였다.** 현재 표본에 대한 주장이 아니라 **아직 없는 데이터**
(다음 「자동매매 매매일지」, 예상 2026-08-21)에 대한 예측이므로 사후적합이 불가능하다.
`PREREG_SELECTION.md` §7 과 같은 처리다.

두 가지가 **기각된 검정의 «반대 방향»에서** 눈에 띄었다. 지금 그걸 지지로 쓸 수는 없다 —
결정규칙이 한쪽만 보게 돼 있었거나, 애초에 반대 방향을 예측하지 않았기 때문이다.
그래서 **다음 글의 표적으로만** 등록한다.

- **R-A (공매도 비중이 «낮다»)**: §1 에서 `f13_short` 의 후보군 내 중앙 순위가 **20.0**
  (귀무 58.3 · raw p **0.0324** · Holm 후 0.5832 로 소멸)이었다.
  ⇒ 다음 글 신규 건의 `f13_short` **후보군 내 중앙 순위 < 40**.
  🟢 이 방향이 보인 것은 **양측 판정으로 고친 덕**이다(직전 사전등록 결함 4회째의 수정).
- **R-B (매도가가 시가에서 «멀다»)**: §2 G2 는 `open_pos < 0.3`(시가 근처)을 예측했는데
  **0/7** 이었고, 실제 관측은 **0.50~0.96**(중앙 0.64)로 전부 반대쪽이었다.
  귀무는 평균 **3.11/7** 이 시가 근처인데 관측이 0 이다.
  ⇒ 다음 글 신규 건의 `open_pos` **중앙값 > 0.5**.
  🔑 갭 체결이 아니라 **장중 고점 부근 도달 시 매도**를 시사한다.

⚠️ **둘 다 지금은 증거가 아니다.** R-A 는 Holm 을 통과하지 못했고, R-B 는 사전등록이
   예측하지 않은 방향이다. **다음 글에서 재현되면 그때 증거다.**

## §4. 미리 적어두는 한계

- 🔴 **n = 5**(선정) · **n = 7**(매도). 어떤 결과도 이 표본을 넘지 못한다.
- 🔴 후보군은 **우리가 만든 필터**의 산물이다. 저자의 후보군이 아니다.
  ⇒ §1 이 유의해도 *「저자가 후보군에서 이렇게 고른다」*가 아니라
    *「우리 필터가 만든 후보군 안에서 올라온 매매가 이런 위치였다」*이다.
- 🔴 `S₁` 이 구간이라 `open_pos` 도 구간이다. **feasible set 전체의 중앙값**으로 줄인다.
- 🔴 매도 배정은 여전히 **「가능하다」**이지 **「그랬다」**가 아니다. 최대·최소 대칭이 그 한계를 드러낸다.
- 이 분석은 **라이브 채택 대상이 아니다**.
