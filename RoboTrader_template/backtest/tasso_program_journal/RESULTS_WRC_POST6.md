# 결과 — `WRC-` 가중 평단 복원 축 (6번째 글) · **판정 회차**

**실행 2026-09-04 스냅샷** · 사전등록 **`PREREG_WEIGHTED_RECON.md`**(2026-09-02 동결 · `WRC-D1`~`D7` 사장님 확정)
+ 동결 **`FREEZE_WRC_2026-09-02.md`**(주 모델 `WRC-A1` · `X1` 3번 관대 읽기 · (나) 갈래 ⛔ 무정보)
· 수집 «후» 결정 **`PREDECISION_2026-09-04_post6.md`**(PD-1·PD-2·PD-3·PD-4·PD-12) → 인테이크 **`INTAKE_2026-09-04_post6.md`** §1·§5 → 이 문서. **순서가 증거다.**
숫자 원본 = **`RESULTS_WRC_POST6_NUMBERS.md`**(`run_wrc_post6.py` 생성) · 기계 산출물 = **`wrc_post6/`**(건별 JSON 5 · `bands.tsv` · `controls_summary.json` · `gate.json` · `universe_snapshot.json` · `explore_freeze_md5.json` · `explore_same_window.json`)

> 🔴 **라이브 채택 금지 · 성과·엣지 추정 금지**(`PREREG.md` §0 · `PREREG_WEIGHTED_RECON.md` §0-1). 이 문서의 어떤 «차수별 매수 밴드 `bₖ`»도 매매 규칙·파라미터 변경의 근거가 아니다.
> ⚠️ 이 축은 계열에서 **오독 위험이 랭킹 다음으로 크다** — 산출물이 *「1차 −x%, 2차 −y% 에 걸어라」*처럼 **바로 주문으로 옮길 수 있는 모양**이기 때문이다.
> 🟢 **라이브 코드 0줄** · DB 는 **SELECT 만** · `adj_factor` 산술 0건 · 원장은 «읽기»만.
> 🔴 **이 문서는 새 예측·새 문턱을 만들지 않는다** — 쓰는 문턱은 전부 동결본에서 **import** 했다.

---

## §0. 한 줄 결론

🔴🔴 **`WRC-P1` = ⛔ 「판정 불가」.** 커버리지 가드 **`WRC-G1` 이 4/5 = 80.0%** 로 발동했다(문턱 1/3).
***「우리가 가진 제약으로는 차수별 밴드를 못 좁힌다」가 아니라 「이 표본에서는 못 쟀다」다*** — `PREREG_WEIGHTED_RECON.md` §7-C 4번이 *「둘은 다른 문장이다」* 라고 미리 적어 둔 그 자리다.

| 항목 | 값 | 판정 |
|---|---|---|
| 판정 분모(§6-1 게이트) | **5건** — 한라캐스트·원익·지투파워·우리기술투자·비에이치 | 🟢 3건 이상 ⇒ 게이트 열림 |
| **`WRC-G1`** 커버리지 | **4/5 = 80.0%**(① DB부재 **0** + ④ 현행 해 0개 **4**) | 🔴 **발동**(≥ 1/3) ⇒ ⛔ |
| `A1` 로 밴드가 나온 건 | **1건**(지투파워) | — |
| **`WRC-P1`**(폭 중앙 < 3%p) | **0/1** · 과반 필요 1건 | ⛔ **판정 불가**(G1) |
| `WRC-N1` (가) | **1.0%** (1,003/100,000) | 🟢 미발동(< 50%) |
| `WRC-N2` (가) | **22.9%** (230/1,003) | 🟢 미발동(< 50%) |
| `WRC-N1`·`N2` (나) | ⛔ **무정보 — 돌리지 않았다** | 동결 §3 |
| `WRC-B1` 축소율 | 전건 **1.0000**(측도 잣대 항등) | 지지 아님(설계) |
| `REC-Z3` 앵커(`H` < 창최고) | **1/5 = 20.0%**(원익) | 🟢 미발동(< 1/2) — **단 민감도에서 뒤집힌다(§6)** |
| `WRC-X1` 위반 | **0건**(1·2·3·4·5 전부) | 🟢 산출물 «유효» |
| `WRC-V1` 민감도 | **4축 중 3축이 갈린다**(②·③·④) | ⛔ **어느 쪽도 선언 금지** |

🔴 **표기 의무(`PREREG_WEIGHTED_RECON.md` §7-B #13 · PD-1)**: **창 종료 `2026-09-04` = 발행 당일 봉 «포함»**이다(발행일 = 거래일 = DB 스냅샷 최대일 세 값이 같다 · 주말 규약 `RESULTS_LADDER_TRANCHE.md` §1 `B-1` **발동 안 함**). 창 = `[등록일, 2026-09-04]`.

🔴 **문턱을 올려 열지 않았다.** `1/3`·`3%p`·`50%`·`레그 ≥3` 은 전부 동결본에서 import 한 그대로다.

---

## §1. §0-1 — 탐색본 **byte 불변 증명** (이 회차가 «가장 먼저» 한 일)

**왜 필요한가**: 이 회차는 원장에 post6 **12행/47레그가 append 된 «뒤»**에 돈다. 탐색 산출물(`RESULTS_WRC_EXPLORE.md`·`wrc_explore/`)이 그 append 로 움직이면 `WRC-O1`(탐색/판정 분리)이 **사후적으로** 무너진다.

### 1-1. 🔴🔴 먼저 발견한 것 — **탐색 스크립트에는 post6 «필터»가 없었다**

| 확인 | 실측 |
|---|---|
| `run_wrc_explore.build_cases` 에 걸린 원장 행 | **50 → 62** (post6 12행) |
| `reg_date_precision = exact` | **18 → 28** |
| **판정 분모** | 🔴 **10 → 15** — post6 5건이 «탐색» 분모에 그대로 들어온다 |

**원인**: `POST_LABEL = {"2026-08-22": "post4", "2026-08-29": "post5"}` 는 날짜에 **이름을 붙이는 맵**일 뿐 **필터가 아니었다.** 탐색 회차에는 post6 이 존재하지 않아 **증상이 없었을 뿐**이다.
🔑 ***「그 날짜가 아직 없으니 안 걸린다」는 필터가 아니다 — 날짜는 오고, 그때 조용히 들어온다.***

**수정**(이 회차가 기존 파일에 넣은 **유일한** 변경 · `run_wrc_explore.py` `main()` 안 5줄 + 주석):

```python
EXPLORE_UPTO = "2026-08-29"          # = post5 발행일. 그 «뒤» 글은 판정 표본이라 여기 오지 않는다.
tr = [r for r in tr if r["post_date"] <= EXPLORE_UPTO]
```

(post1~3 은 남긴다 — §1-4 의 「원장 전 글 50행」이 그 셋을 포함한다.)

### 1-2. 필터를 넣고 재실행 → **원장 유래 값은 전부 동일**

| 값 | 동결본 | 필터 후 재실행 | 일치 |
|---|---|---|---|
| 원장 전 글 / `exact` / 빈칸 / `fill_n=1` / 레그<3 / **판정 분모** | 50 / 18 / 2 / 4 / 2 / **10** | 50 / 18 / 2 / 4 / 2 / **10** | ✅ |
| 판정 분모 10건의 종목·`N`·레그·`open_ended` | — | — | ✅ 전건 동일 |
| `prog_ver` 집합 | `{1.0.30, 1.0.32, 1.0.33, 1.0.36, 1.0.39}` | 같음 | ✅ |
| `WRC-G1` 합계 | 6/10 = 60.0% | 6/10 = 60.0% | ✅ |

⇒ 🟢 ***원장 append 는 (필터를 넣은 뒤) 탐색 산출물의 어떤 «원장 유래» 값도 바꾸지 않는다.***

### 1-3. 🔴 그래도 재실행본은 동결본과 **byte 동일하지 않다** — 차분 전수 규명

재실행 산출물 vs 동결본 = **28 헝크 · 116줄**. **전부 두 원인으로 설명된다**(제3의 원인 0건):

| # | 원인 | 무엇이 움직였나 |
|---|---|---|
| **A** | 🔴 **DB 스냅샷 전진 `2026-09-02` → `2026-09-04`**(창이 2거래일 길어짐) — `WRC-D7`(창 종료 = 실행 시 DB 최대일)의 **정의된 귀결** | §0 최신 봉·창 규약 · 창 봉수(14→16 등)·`G_win`·창 최저 저가 · 밴드 폭(이노테크 중앙 11.36→13.14%p) · 대조군 (가) 성립률(7.0→7.4%)·좁은밴드율(10.8→11.3%) · 유니버스 스냅샷 · §6 재측정 3열 · 봉수 표 |
| **B** | 🟡 **`git grep -l --untracked WRC-` 의 파일 «집합»이 커졌다** — §7 #16 이 ✅ → 🔴 | 새로 잡히는 3건 = `FREEZE_WRC_2026-09-02.md` · `INTAKE_2026-09-04_post6.md` · `PREDECISION_2026-09-04_post6.md`. **셋 다 이 축의 «정상» 후속 문서**인데 #16 의 제외 목록이 그 셋을 몰랐다 |

🔑 ***「같은 DB 스냅샷에서 byte 동일」(동결 §2)의 전제가 깨진 것이지, 산출물이 오염된 것이 아니다.*** 탐색 산출물 자신이 §8 #5 에 *「창 종료일을 「실행 시 스냅샷」으로 동결한 `WRC-D7` 은 「매 실행마다 값이 달라진다」를 «의미»한다」* 라고 이미 적어 두었다.
🔴 **B 는 «자기 자신을 무는» 가드다** — #16 은 *「전체 건수는 실행 차수에 따라 달라지므로 적지 않는다(산출물이 생기면 스스로 잡힌다 ⇒ 재현 게이트가 깨진다)」*라고 그 위험을 알고 제외 목록을 두었는데, **동결본·인테이크·결정문처럼 «나중에 생기는 같은 축의 문서»는 그 목록에 없었다.** 이 회차가 만든 `run_wrc_post6.py`·`RESULTS_WRC_POST6*.md`·`wrc_post6/*` 도 같은 이유로 앞으로 잡힌다.

### 1-4. 그래서 **재실행본은 폐기하고 커밋본으로 되돌렸다**

- 복원 후 md5 **18/18 OK**(`RESULTS_WRC_EXPLORE.md` `1280c0ee…` + `wrc_explore/` 17개 전부)
- `git diff --quiet HEAD -- RESULTS_WRC_EXPLORE.md wrc_explore` = ✅ **커밋과 byte 동일**(동결 md5 표에 «없는» 건별 JSON 11개까지 한 번에 덮는 검사)
- 판정 스크립트가 매 실행마다 이 두 검사를 **다시 찍는다**(`RESULTS_WRC_POST6_NUMBERS.md` §0-1 · `wrc_post6/explore_freeze_md5.json`)

### 1-5. 동결 md5 표의 불일치 3건 — **맞추지 않고 «누가 바꿨나»로 갈랐다**

| 파일 | 부류 | 원인 |
|---|---|---|
| `run_wrc_explore.py` | ① **이 회차가 바꿨다(선언됨)** | §1-1 의 post6 제외 필터 |
| `regen_gate.py` | ① **선언됨 — 이 축 «밖»의 작업** | post6 **최종 레인의 `C-23` 수리**(`PAIRS` 인자화 · `FROZEN_STALE` 스킵 · `ART_DIRS` 스냅샷). 🔴 이 회차는 그 파일의 **수정 금지** 대상이라 손대지 않았고, 그 파일은 이 축의 판정 수치를 **입력으로 쓰지 않는다**(산출물 등재·재현 게이트 전용) |
| 나머지 11개 | 🟢 불변 | — |

⇒ 🔴 **② 「이 축 밖 커밋」 = 0건 · ③ 「설명되지 않는 로컬 변경」 = 0건.**
🔑 ***동결 md5 표는 그 시점의 스냅샷이라 뒤 작업이 같은 파일을 건드리면 «정상적으로» 어긋난다 — 어긋남은 «값»이 아니라 «누가 바꿨나»로 갈라야 한다.***

### 1-6. 🔴 원장은 «공유 파일» — **통째 md5 를 박지 않는다**(`run_ranking.py` §10 관용 승계)

**실측 사유**: post6 최종 레인이 한라캐스트 `narrative` **1줄**을 정정하자 `ledger_trades.csv` 의 통째 md5 가 **`624f5a97…` → `39cd83db…`** 로 움직였다. 🔴 **이 축은 `narrative` 를 읽지 않으므로 판정 수치는 한 자리도 안 움직였다** — 통째 md5 를 박았으면 *「남의 «판정과 무관한» 편집 때문에 내 산출물이 재현 불가가 된다」*가 그대로 일어난다. ⇒ **박는 것을 «파일»에서 «의존»으로 바꿨다**:

| 지문 | 정의 | 값 |
|---|---|---|
| `ledger_trades.csv` **prefix 51줄** | 헤더 + post6 «전» 50행의 바이트 prefix | **`5d603de7…`** = 동결값 ✅ (과거 행 불변) |
| `ledger_legs.csv` **prefix 170줄** | 헤더 + post6 «전» 169레그 | **`f195e260…`** = 동결값 ✅ |
| `ledger_trades.csv` **판독 필드 지문**(post6 12행 · `RNK_KEY` 7열) | `run_ranking.py:1338` 과 **같은 컬럼·같은 순서** | `wrc_post6/explore_freeze_md5.json` |
| 〃 (**`WRC_KEY` 11열**) | 🔴 이 축의 **실제 의존** — `RNK_KEY` 에 없는 **`fill_n`·`open_ended`·`fill_level`·`preset`·`prog_ver`** 포함 | 〃 |
| `ledger_legs.csv` **판독 필드 지문**(post6 47레그 · `LEG_KEY` 4열) | `read_ledger` 가 읽는 **전부** | 〃 |

- 🔴 **`narrative` 는 어느 지문에도 없다** ⇒ 그 열의 정정으로 이 표는 **움직이지 않는다**.
- ⚠️ **`RNK_KEY` 하나만 박으면 이 축의 가드가 약해진다** — `fill_n`(분모 정의)·`open_ended`(민감도 축)가 그 7열에 «없다». 그래서 **두 지문을 다 박았다**. 🔑 ***남의 지문 정의를 그대로 쓰는 것과 «내 의존»을 덮는 것은 다른 일이다 — 둘 다 해야 한다.***
- 🟢 **prefix 가 «양방향» 가드**: 과거 행이 움직이면 prefix 가 깨지고, post6 행이 움직이면 판독 필드 지문이 깨진다. **통째 md5 는 둘을 구분하지 못한다.**
- 🔑 계열 규칙 후보: ***여러 레인이 같은 파일을 동시에 쓰는 동안에는 「파일 md5」가 재현성 장치가 아니라 «재현성 파괴 장치»다.***

---

## §2. 판정 분모 — 인테이크 «예측»과 **일치**

**분모 정의(동결 §6-1 `WRC-R11`)** = `reg_date_precision = exact` ∧ `fill_n ≥ 2` ∧ **서로 «다른» 값의 레그 ≥ 3**.

| 종목 | 코드 | 등록일 | `fill_n` | 레그 | 서로 다른 값 | `open_ended` | 창 봉수(등록일 «포함») |
|---|---|---|---|---|---|---|---|
| 한라캐스트 | 125490 | 2026-08-21 | 5 | 4 | 4 | **1** | 11 |
| 원익 | 032940 | 2026-08-31 | 3 | 7 | 7 | 0 | **5** |
| 지투파워 | 388050 | 2026-08-26 | 3 | 4 | 4 | **1** | 8 |
| 우리기술투자 | 041190 | 2026-08-25 | 4 | 5 | 5 | **1** | 9 |
| 비에이치 | 090460 | 2026-09-01 | 4 | 7 | 7 | 0 | **4** |

**분모 밖 7건과 사유**: 광전자·삼양바이오팜 = `prec ≠ exact`(후속 건 · PD-2) · 헥토파이낸셜·아난티·쿠콘 = `fill_n = 1` · 아이티센글로벌·현대약품 = **서로 다른 값 레그 2 < 3**.

- 🟢 **인테이크 §5 의 «계산 전» 예측 5건과 정확히 일치**(한라캐스트·원익·지투파워·우리기술투자·비에이치) ⇒ ***분모가 값을 보고 정해지지 않았다는 증거***다.
- 게이트 통과율(post6 `exact` 대비) = **5/10 = 50.0%**(탐색 회차 10/18 = 55.6%)
- **최소 표본 3건**(`PREREG_SELECTION.md` §7) ⇒ 🟢 게이트 열림 · 누적 규약(`PREREG_RANKING.md` §2-6 승계)대로 **이 글에서 판정한다**.
- `open_ended = 1` 이 **3/5** · 재진입은 **지투파워 1건**(`P6-PRIOR_CYCLE_IN_WINDOW = 1`) — 🔴 **현대약품(flag 0)은 분모 «밖»**이라 이 축에 들어오지 않는다.
- 🔴 **창이 짧다** — 분모 5건의 창 봉수가 **4~11봉**이다(탐색 표본은 10~20봉). 창이 짧으면 매도가 후보 격자가 줄어 feasible 이 **비기 쉬운 방향**이다. 이건 값을 본 뒤의 해석이 아니라 `WRC-D7`(창 = `[등록일, DB 최대일]`)과 «등록일이 8월 하순~9월 초」라는 사실의 산술적 귀결이다.

---

## §3. 🔴 `WRC-G1` — **가장 먼저 계산했다**(§7-B #24) · **발동**

**분자 = ① DB 종목코드 부재 + ④ 현행(`WRC-A0`) 해 0개** · 분모 = 판정 분모. ②(`fill_n` 빈칸)·③(레그 <3)은 **분모 정의의 일부라 분자가 될 수 없다**(§4-6 정정).

| 글 | 판정 분모 | ① DB부재 | ④ 해 0개 | **`WRC-G1`** | 게이트(≥ 1/3) |
|---|---|---|---|---|---|
| **post6(판정)** | 5 | **0** | **4** (한라캐스트·원익·우리기술투자·비에이치) | **4/5 = 80.0%** | 🔴 **발동** |
| 🔬 (탐색 인용 · 판정 아님) post4+post5 | 10 | 1 | 5 | 60.0% | 🔴 발동 |

- 🟢 **①이 0건**이다 — post6 은 **12/12 DB 존재**(PD-9)라 레메디형 결손이 없다. ⇒ **발동 원인은 전적으로 ④(현행 해 0개)**다.
- 🔴 **편향 방향 신고**: ④는 무작위가 아니다 — **레그 간격이 촘촘한 건**에 몰린다(§4-6 승계). ⇒ ***측정 불가가 무작위가 아니다.***
- 🔴 **④는 「값을 보고 뺀 것」이 아니다** — §2-4 의 산술 정리로 `A0 = ∅ ⇒ A1 = ∅` 가 **계산 «전»에 확정**되는 부류다. 그래도 분자에 넣어 셌다(빼면 커버리지가 좋아 보인다).

### 3-1. 🔴 문언 대조 — 자기신고

§4-6·§7-B #24 의 문언은 *「1/3 이상이면 나머지 계산을 «돌리지 않는다»」* 다. **이 실행은 관리자 지시로 나머지를 «계산하되 판정 표기를 ⛔»** 로 두었다.
⇒ ***§4 이하의 모든 값은 «관측»이며 어떤 지지·불성립도 «선언하지 않는다».*** 문턱 `1/3` 은 올리지 않았다.

---

## §4. 건별 `WRC-A0`·`A1`·`A3` 와 밴드 `bₖ`

| 종목 | `N` | `G_win` | 격자 | **`m(A0)`** | **`m(A1)`** | **`m(A3)`** | `A1` 성립? | `A1` 해 개수 | `B1` 축소율 |
|---|---|---|---|---|---|---|---|---|---|
| 한라캐스트 | 5 | 210 | 연속 | 0.00 | 0.00 | 0.00 | ⛔ (`A0` 가 ∅) | 0 | ⛔ 정의 불가 |
| 원익 | 3 | 485 | 연속 | 0.00 | 0.00 | 0.00 | ⛔ (`A0` 가 ∅) | 0 | ⛔ 정의 불가 |
| **지투파워** | 3 | 182 | 연속 | **0.89원** | 0.00원 | 0.89원 | 🟢 **성립** | **1** | 1.0000 |
| 우리기술투자 | 4 | 160 | 연속 | 0.00 | 0.00 | 0.00 | ⛔ (`A0` 가 ∅) | 0 | ⛔ 정의 불가 |
| 비에이치 | 4 | 106 | **불연속** | 0.00 | 0.00 | 0.00 | ⛔ (`A0` 가 ∅) | 0 | ⛔ 정의 불가 |

- 🟢 **`A0` 는 있는데 `A1` 이 «비는» 건 = 0건** — 탐색 회차의 최대 발견(§8 #0 「격자 정합이 해를 지운다」)이 **이번 표본에서는 재현되지 않았다**. 단 분모가 «해 있는 건 1건»뿐이라 **재현 여부를 물을 표본이 사실상 없다**(반증도 확증도 아니다).
- 🔴 **`m(A1) = 0` 은 정의의 귀결**(격자값 `N` 개의 산술평균 ⇒ 유한 점집합 ⇒ 르베그 측도 0) ⇒ `WRC-B1` 축소율은 **항등적으로 1.0000**. 🔑 ***항등식은 어떤 가설도 시험하지 않는다*** — 정리를 실제로 시험한 것은 §7 의 `WRC-X1` 1·2번이다.
- 지투파워는 격자 «연속»이라 **닫힌 형태**로 풀었고 **비트셋 DP 와 교차검증 1/1 일치**(§7).

### 4-1. 밴드 원표 — **`A1` 해가 나온 1건**

| 종목 | `N` | `H`(등록일 고가) | 차수 | `Pₖ` 범위 | **`bₖ` 밴드** | **폭(%p)** | 폭(호가 칸) |
|---|---|---|---|---|---|---|---|
| 지투파워 | 3 | 10,780 | 1차 | 9,710~10,780 | **+0.00% ~ +9.93%** | **9.93%p** | 107.0칸 |
|  |  |  | 2차 | 9,160~10,050 | **+6.77% ~ +15.03%** | **8.26%p** | 89.0칸 |
|  |  |  | 3차 | 8,970~9,680 | **+10.20% ~ +16.79%** | **6.59%p** | 71.0칸 |

1. 🔴 **「라이브 채택 금지 — 이 표의 어떤 숫자도 매매 규칙으로 옮기지 않는다」**(`PREREG.md` §0-2).
2. 🔴 **「검정 안 된 «후보»다」** → 🔴 **이 회차는 `WRC-G1` 발동(측정 불가 ≥ 1/3)으로 «판정 표기 ⛔»**이다.
3. 🔴 **「`P` 의 불확실성을 그대로 물려받는다 — 「존재한다」류 주장조차 약하다」**(`RESULTS_RECONSTRUCT_POST5.md` §10 승계).
4. 🔴 **「이 표는 «단일 평단 전제가 성립한 건»만 담는다 — 그 배제는 무작위가 아니다」**(§5-3 2번 · 두 문장 묶음).

### 4-2. `WRC-P1` 대조

- 폭 중앙값 = **8.26%p**(3차수 · 홀수 ⇒ 가운데 값) · 문턱 **< 3%p** ⇒ **위반**
- 측정 가능 **1건** 중 성립 **0건** · 「과반」 = `⌈(n+1)/2⌉` = **1건**
- ⛔ **`WRC-P1` = 판정 불가**(사유 = `WRC-G1` 발동). 「못 좁힌다」가 아니라 **「못 쟀다」**.
- 🔴 **죽은 가드 실측 점검(§7-B #14)**: 관측된 전 차수 폭 **6.59 ~ 9.93%p** ⇒ 3%p 는 이 표본에서도 **달성되지 않았다**(탐색 회차 최소 6.83%p 와 같은 방향). 🔴 **문턱을 3%p 에서 올리는 것은 어느 경우에도 금지**다(§4-1).
- `WRC-A3` 무정보 상한 대비 `A1`/`A3` = **0.598 / 0.497 / 0.397**(차수별) — 🔴 **지지로 인용 금지**(§2-6 `WRC-R4`), `WRC-P1` 의 분자·분모에 넣지 않았다.

---

## §5. 대조군 `WRC-A4` — `WRC-N1`·`WRC-N2` **둘 다 미발동**

| 갈래 | 분모 | `A0` 성립률 | **`A1` 성립률(=`N1`)** | `N2`(좁은밴드율) | 판정 |
|---|---|---|---|---|---|
| **(가) 무작위 종목** | 100,000(5건 × 20,000회) | 2.4% | **1.0%** (1,003) | **22.9%** (230/1,003) | 🟢 **둘 다 미발동**(< 50%) |
| (나) 셔플 레그 | ⛔ | ⛔ | ⛔ **무정보** | ⛔ | ⛔ **대조군 자격 없음 — 돌리지 않았다** |
| (참고) 관측 | 5 | 20.0% | **20.0%** | 0.0% (0/1) | — |

- 🔴 **(나) 를 돌리지 않은 근거**(`FREEZE_WRC_2026-09-02.md` §3 확정): `feasible_exact` 는 레그별 구간의 **교집합**이고 교집합은 **순서 불변**이며, `WRC-A1` 의 (가)(나)(다)(라)는 «매수» 사다리에만 걸려 **레그 순서를 참조하지 않는다** ⇒ 통계량이 셔플에 **항등적으로 불변**. 재명세는 **별도 사전등록 개정**이며 이 문서 안에서 새 대조군을 정의하지 않았다.
- 🟢 **`WRC-X1` 3번(상수 여부)**: 건별로 «서로 다른» 종목 **2,762~2,766가지**가 뽑혔고 **결과가 갈리는 건 2/5**. 상수 3건(한라캐스트·원익·비에이치)은 «무작위화 실패»가 아니라 ***그 레그 열이 어떤 종목에서도 `A0` 조차 못 만든다***는 뜻이다(전 반복에서 성립 **0회**).
- 🔴 **`N1` 미발동을 「판별력이 있다」로 읽지 않는다** — 관측 쪽 분모가 **5건(성립 1건)**이라 비율의 불확실성이 지배적이고, 애초에 이 회차는 `WRC-G1` 로 **⛔ 판정 불가**다.

---

## §6. `REC-Z3` 앵커와 `WRC-V1` 민감도 — **여기가 이번 회차의 갈림**

### 6-1. 앵커 `H`(등록일 고가) — 판정 분모 «전건»

| 종목 | `H` | 창 최저 저가 | 창 최고가 | **`H < 창 최고가`?** | `H`/창최고 |
|---|---|---|---|---|---|
| 한라캐스트 | 11,910 | 9,820 | 11,910 | 아니오 | 100.0% |
| **원익** | 8,440 | 6,220 | **11,060** | 🔴 **예** | **76.3%** |
| 지투파워 | 10,780 | 8,970 | 10,780 | 아니오 | 100.0% |
| 우리기술투자 | 7,200 | 5,610 | 7,200 | 아니오 | 100.0% |
| 비에이치 | 22,650 | 19,480 | 22,650 | 아니오 | 100.0% |

- **실측 1/5 = 20.0% < 1/2** ⇒ 🟢 **미발동** — ***이 축의 분모 기준으로는 「앵커 재설계 사전등록 의무」가 «발생하지 않았다».***
- ⚠️ **분모 고지**: 이 비율의 분모는 **`WRC-` 판정 분모**다. `REC-` 축(`run_reconstruct_post6.py`)은 **다른 분모**(레그 ≥4 신규 건 등)로 같은 `REC-Z3` 를 잰다 ⇒ 🔴 **두 수를 한 수로 합치지 말 것.** *「6번째 글에서 한 건이라도 더 나오면 앵커 재설계 사전등록이 의무 발생한다」*(`PREREG_POST6.md` §2-6)의 판정은 **그 축이 자기 분모로 따로 낸다.** 이 문서는 이 축의 분모에서 «미발동»이라는 사실만 적는다.
- ⚠️ 탐색 회차 **7/9 = 77.8%**(창 `~2026-09-02`)와 **직접 비교 불가** — 창이 다르다. post6 의 창은 종목마다 4~11봉으로 **더 짧아** 창 최고가가 낮고, 이 비율은 **내려가는 방향**이다.

### 6-2. 🔴🔴 `WRC-V1` — 4축 중 **3축이 갈린다**

| 축 | 갈렸나 | 실측 |
|---|---|---|
| ① 잔차 문턱 `0.022`(판정) ↔ `0.020`(민감도) | 아니오 | 두 문턱에서 분류가 갈리는 건 **0건** |
| ② 수수료 `gross`(판정) ↔ `net`(민감도) | 🔴 **예** | 해 0개 집합은 같은데 **측정 가능 분모 1 → 0** 으로 이동 |
| ③ 재진입·미완결 **포함**(판정) ↔ **제외**(민감도) | 🔴 **예 — `REC-Z3` 판정이 뒤집힌다** | 아래 표 |
| ④ 모델 `A1` ↔ `A3` | 🔴 **항상 갈린다**(설계) | `A3` 는 무정보 상한이라 «비교 대상이 아니다» |

| 갈래 | 분모 | `WRC-G1` | 측정 가능 | `REC-Z3` |
|---|---|---|---|---|
| **포함(판정)** | 5 | 4/5 = 80.0% 🔴 | 1 | **1/5 = 20.0%** 🟢 |
| 제외 — `open_ended` 뺌 | 2 | 2/2 = 100% 🔴 | 0 | 🔴 **1/2 = 50.0%** |
| 제외 — 재진입 뺌 | 4 | 4/4 = 100% 🔴 | 0 | 1/4 = 25.0% 🟢 |
| 제외 — 둘 다 뺌 | 2 | 2/2 = 100% 🔴 | 0 | 🔴 **1/2 = 50.0%** |

- 🟢 **`WRC-G1` 은 전 갈래에서 «발동»** — 뒤집히는 갈래 0개. ⇒ ***「축을 여는가」는 갈래 선택에 종속되지 않는다***(탐색 회차와 반대 방향의 관측이다).
- 🔴🔴 **`REC-Z3` 는 2개 갈래에서 뒤집힌다**(20.0% → 50.0%) ⇒ ***「앵커 재설계 의무가 발생하는가」가 민감도 갈래 선택에 종속된다.***
- 🔴 **§2-7 `WRC-R5` 의 동결 선택(«포함»이 판정 · 제외는 민감도)을 그대로 둔다.** 제외 갈래로 의무를 **발생시키지도 면제하지도 않는다** — 그건 ***가드를 통과하는 부분집합을 «골라» 결론을 만드는 동작***이다. `WRC-V1` 문언대로 **어느 쪽도 선언하지 않고** 두 값을 나란히 남긴다.
- 🔴 `open_ended` 의 방향: 제약이 실제보다 «적다» ⇒ feasible 을 **넓히는** 쪽이라 「해가 나왔다」의 증거력이 그만큼 약하다.

### 6-3. `REC-Z5` 잔차 — 해 0개 4건의 진단

| 종목 | 최소잔차 적합 `P` | 최대 오차 | @0.020 | @0.022 | 갈리나 |
|---|---|---|---|---|---|
| 한라캐스트 | 10,575 | **0.010125%p** | 반올림으로 설명 가능 | 반올림으로 설명 가능 | 아니오 |
| 원익 | 6,147 | **0.018846%p** | 반올림으로 설명 가능 | 반올림으로 설명 가능 | 아니오 |
| 우리기술투자 | 6,018 | **0.025235%p** | 🔴 모델이 틀렸다 | 🔴 모델이 틀렸다 | 아니오 |
| 비에이치 | 19,569 | **0.046553%p** | 🔴 모델이 틀렸다 | 🔴 모델이 틀렸다 | 아니오 |

- 🔴 **관측(판정 아님)**: 한라캐스트·원익은 **잔차가 두 문턱 «아래»인데도 정확 구간법에서 해가 0개**다 — 즉 *「하나의 실수 `P` 로 전 레그를 설명할 수 있는데 그 `P` 를 «격자 위 매도가»로는 실현할 수 없다」* 부류다. 이건 `REC-Z1`·`Z2` 가 재려던 바로 그 구분이며 **그 판정은 `REC-` 축 소관**이다. 이 문서는 값만 넘긴다.
- 🔴 이 축은 `WRC-` 통계량을 **움직이지 않는다** — `WRC-G1`·`B1`·밴드 폭은 잔차 문턱을 입력으로 쓰지 않는다.

---

## §7. `WRC-O1`(탐색 ↔ 판정 병기)와 `WRC-X1`(구현)

### 7-1. 탐색과 판정을 «항상» 나란히

| 통계량 | 🔬 탐색 — 동결 인용(창 `~09-02`) | 🔬 탐색 — **같은 창 재측정**(`~09-04`) | **판정 — post6**(`~09-04`) |
|---|---|---|---|
| 판정 분모 | 10건(통과율 55.6%) | 10건(DB 봉 9) | **5건**(통과율 50.0%) |
| `WRC-G1` | 6/10 = 60.0% 🔴 | — | **4/5 = 80.0%** 🔴 |
| `A0` 해 있는 건 | 4건 | **4/9** | **1/5** |
| `A1` 밴드 | 2건 | **2/9** | **1건** |
| 밴드 폭 | 전 차수 최소 **6.83%p** · 건별 중앙 11.36 / 7.38%p | 건별 중앙 **13.14 / 7.38%p**(이노테크 / 지투파워) | 전 차수 **6.59~9.93%p** · 건별 중앙 **8.26%p** |
| `WRC-P1` | 0/2 | — | **0/1** ⛔ |
| `WRC-N1` (가) | 7.0% 미발동 | — | **1.0%** 미발동 |
| `WRC-N2` (가) | 10.8% 미발동 | — | **22.9%** 미발동 |
| `REC-Z3` | 7/9 = 77.8% | **7/9 = 77.8%** | **1/5 = 20.0%** |

- 🟢 **`WRC-` 자체 통계량(`G1`·`P1`·`N1`·`N2`)은 부호가 갈리지 않는다** — 둘 다 `G1` 발동 · 둘 다 `P1` 미달 · 둘 다 `N1`·`N2` 미발동. ⇒ **「탐색 편향 의심」 인쇄 조건에 해당하지 않는다.**
- 🔴 **다만 `REC-Z3` 은 방향이 다르다**(탐색 77.8% ≥ 1/2 ↔ 판정 20.0% < 1/2). 🔴 **이걸 「탐색 편향」으로 읽지 않는다** — 두 수는 **창 길이가 다르고**(탐색 표본은 등록일이 8월 초·중순이라 창이 길다) 그 방향이 **구조적으로 정해져 있다**(창이 길수록 창 최고가는 오르기만 하므로 비율은 커진다). 🔑 *같은 이름의 두 수가 다르면 「표본이 달라서인가 잣대가 달라서인가」를 먼저 가른다 — 여기서는 잣대(창)다.* 그리고 탐색 열은 애초에 **판정이 아니다**(`WRC-O1`).
- 🔴 **탐색 성적은 판정 분모에 넣지 않았다**(`WRC-O1`). 판정은 post6 **5건**에서만 나온다.
- 🔴 **모호 지점 자기신고**: 「탐색값을 어느 창에서 인용할 것인가」의 동결 문언이 **없다**(`PREREG_POST6.md` §5-1-5 *「과거 산출물을 다시 재지 않는다」* ↔ `run_selection_post6.py` 머리말 *「직전 문서 숫자를 그대로 옮겨 비교하지 않는다」* 가 **서로 다른 방향**을 지시한다). ⇒ **양쪽을 인쇄**하고 어느 하나를 판정 근거로 고르지 않았다. 🟢 이번엔 두 인용이 `REC-Z3` 에서 **같은 값(7/9)**이라 결론이 갈리지 않는다.

### 7-2. `WRC-X1` — 5조건 **위반 0건** ⇒ 산출물 «유효»

1. `feasible(A1) ⊄ feasible(A0)`: **0건**(산술 hull + `iv_has` 로 «집합»에 대고 확인 ⇒ 1/1 통과) · 부수 점검(조립 경로 ↔ `feasible_exact` 항등 · `A3` 다른 경로 재현) **1/1 일치**
2. `A0 = ∅` 인데 `A1 ≠ ∅`: **0건**
3. 대조군 상수: (가) 갈림 2/5 ⇒ 상수 아님 · (나)는 동결 §3 으로 **돌리지 않음**
4. `tick`/`grid_prices` **import**(표 재작성 0건) · 5. `adj_factor` 산술 **0회**
- 🟢 닫힌 형태 ↔ 비트셋 DP **교차검증 1/1 일치**

---

## §8. 모호 지점·미해소 (판정 아님)

| # | 항목 | 처리 |
|---|---|---|
| 1 | 🔴 **`WRC-G1` 발동 시 「나머지를 돌리지 않는다」 ↔ 「계산하되 판정 표기 ⛔」** — 사전등록 문언은 전자, 이 실행은 후자(관리자 지시) | 값 전부를 **관측**으로만 인쇄 · 지지/불성립 **선언 0건** · 문턱 상향 0 |
| 2 | 탐색값 인용 창(동결 `~09-02` ↔ 이 창 `~09-04`) | **양쪽 인쇄**(§7-1) · 이번엔 결론이 갈리지 않음 |
| 3 | `REC-Z3` 분모가 축마다 다르다 | 이 축 분모에서 **20.0% 미발동**만 적고 의무 발생 판정은 `REC-` 축에 넘김 |
| 4 | 🔴 `WRC-V1` ③에서 **`REC-Z3` 판정이 뒤집힌다** | 동결 갈래(포함) 유지 · **어느 쪽도 선언 안 함** |
| 5 | 🔴 **`regen_gate.py` 미등재** — `PAIRS` 에 `RESULTS_WRC_POST6_NUMBERS.md`, `MANUAL_DOCS` 에 `RESULTS_WRC_POST6.md` 가 **없다** | 이 회차는 `regen_gate.py` **수정 금지 대상**이라 등재하지 않았다 ⇒ 🔴 **최종 레인이 등재해야 한다**(안 하면 §5-3 이 지적한 「어디에도 등재돼 있지 않다」 재발) |
| 6 | 🔴 `run_wrc_explore.py` md5 가 바뀌었다(§1-1 필터) | `RESULTS_WRC_EXPLORE.md` ↔ `run_wrc_explore.py` 쌍의 **재기준선 판단**은 이 축이 하지 않는다 — 최종 레인 |
| 7 | 🟡 §7 #16(`WRC-` grep) 가드가 **자기 축의 후속 문서에 스스로 걸린다** | §1-3 B 에 규명 · 제외 목록 갱신 여부는 사전등록 개정 사항 |
| 8 | 🟡 `tests/test_s5_fixes.py` **2건 실패**(`test_c21_ledger_matches_the_prereg_simulation_table` · `test_c21_gate_e_passes_on_the_current_ledger`) | **원장 append 로 50 → 62행**이 되며 post5 시점 하드코딩과 어긋난 것 · `WRC-` 축과 **무관**하고 `P6-G-E` 게이트 자체는 통과(`VP.failures` 비어 있음) ⇒ **원장 레인 소관**으로 넘김 |
| 9 | 🔴 **`WRC-G1` 사유 ④ 의 분모가 `REC-Y3` 와 다르다** — ④ 4건(한라캐스트·원익·우리기술투자·비에이치) = **`REC-Y3` 해 0개 5건 ∩ `WRC-` 판정 분모 5건**이며, 빠지는 1건은 **헥토파이낸셜**(`fill_n = 1` 이라 다차수가 아니어서 `WRC-` 분모 «밖») | 🔴 **`4/5`(이 축)와 `5/10`(`REC-Y3` · 신규 10건 분모)을 «직접» 비교하지 않는다** — 분모가 다르다. 각 축이 자기 분모로 자기 가드를 판정한다(#3 의 `REC-Z3` 고지와 같은 형식) |

---

## §9. 한계 (§9 승계 · 값과 무관하게 미리 적어 둔 것)

- 🔴 이 축은 **「해 0개」를 원리적으로 풀지 못한다**(§2-4 산술 정리 — `A1` ⊆ `A3` ⊆ `A0`). 이번 4건도 그 정리대로 `A1` 에서 «반드시» 비었다.
- 🔴 **`WRC-A1`(균등 `1/N`)이 참이라는 근거가 없다** — 저자는 차수별 비중을 적지 않았다(§2-2). ⇒ 밴드가 좁아도 「저자가 균등 분할한다」가 아니라 「균등 가정에서 밴드가 좁게 결정된다」이다.
- 🔴 판정 대상이 «해가 있는» 건뿐이라 **선택편향**이며, 그 배제는 무작위가 아니다 — `REC-Y3` 우회 방식과 편향 방식은 **하나의 사실**이다(§5-3 2번).
- 🔴 표본이 저자가 «올리기로 고른» 매매다 ⇒ 나온 `bₖ` 는 「저자의 주문 밴드」가 아니라 **「저자가 공개한 매매들의 밴드」**다.
- 🔴 차용 문턱 넷(**3%p · 50% · 1/3 · 레그 ≥3**)은 전부 «다른 것을 재던» 값이다. 🔴 **이번에 판정을 가른 것은 `1/3`(`WRC-G1`)이며, 그건 「차용 문턱에 걸렸다」로 적는다.**
- 🔴 `prog_ver` **1.0.40**(post6 전건) — 공변량 기록. 부호가 버전별로 갈리면 통합 결론을 내지 않는다.
- 이 분석은 **라이브 채택 대상이 아니다**(`PREREG.md` §0-2).

---

## §10. 재현

```bash
cd RoboTrader_template/backtest/tasso_program_journal
python run_wrc_post6.py            # → RESULTS_WRC_POST6_NUMBERS.md + wrc_post6/
python -m pytest test_post6_wrc.py -q    # 8 passed — 분모·필터·탐색본 불변 가드
```

- 🟢 **두 번 실행 byte 동일** 실측(`RESULTS_WRC_POST6_NUMBERS.md` · `wrc_post6/` 전 파일) — 시드 `20260815` 고정 · DB 는 SELECT 만 · 벽시계·HEAD 해시는 **stdout 전용**.
- 🔴 **HEAD 해시를 본문에 적지 않는다** — 커밋마다 바뀌는 값을 산출물에 적으면 그 산출물은 자기 자신을 재현할 수 없게 된다(동결 §2).
- 🔴 **DB 스냅샷 `2026-09-04`** 에 종속된다. 스냅샷이 움직이면 값이 움직인다(`WRC-D7` 의 정의된 귀결).

[[PREREG_WEIGHTED_RECON]] · [[FREEZE_WRC_2026-09-02]] · [[RESULTS_WRC_EXPLORE]] · [[RESULTS_WRC_POST6_NUMBERS]] · [[PREREG_POST6]] · [[INTAKE_2026-09-04_post6]] · [[PREDECISION_2026-09-04_post6]] · [[LABELS_2026-09-04_post6]]
