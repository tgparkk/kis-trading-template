"""연구 ② LLM 전 종목 채점 shadow «전향 관측» — 사전등록 `docs/prereg_2026-09-26_llm_candidate_shadow.md`(동결 `64f4d4a`).

> 🔒 봇은 이 패키지를 import 하지 않는다(라이브 변경 0줄). 결과는 룰 변경 근거가 아니다(§1).
> 🔒 개봉(`run_unseal.py` · §6~§8)은 이 커밋에 없다 — 2026-12-22 개봉-1 «전» 별도 커밋.
> 🔒 모델 출력은 `shadow.output_json`·`batch.raw_result`·`rep`·`search` 에만 저장한다(§4 봉인) — 로그·출력 금지.
"""
