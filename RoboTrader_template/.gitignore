# 민감한 설정 파일
config/key.ini
token_info.json
holiday_kis_cache.json

# AI/IDE tool configs
.claude/
.cursor/

# 종목 데이터 (환경별 상이, stock_list.json.example 참고)
stock_list.json

# Python
__pycache__/
*.py[cod]
*.pyd
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# lib/ lib64/ 제거(2026-07-10): Python 표준 gitignore 의 venv/setuptools 패턴이었으나
# 프로젝트 자체 패키지 RoboTrader_template/lib/ (pit_helpers, signals/*) 를 잡아먹어
# git 미추적 상태였다 → tests/test_no_lookahead.py 등 7개 테스트가 clean checkout·
# 워크트리·CI 에서 수집 불가했음. venv 는 아래 `venv/` 로, 빌드산출물은 `build/` 로 이미 무시된다.
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
Pipfile.lock

# PEP 582
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# 로그 파일
logs/
*.log

# 데이터 파일
data/
# 예외: multiverse/data/ 는 데이터 디렉토리가 아니라 프로젝트 자체 Python 패키지다.
# 위 data/ 패턴이 이 패키지를 삼켜 소스가 git 미추적이 되던 것을 해소(2026-07-16).
# git 규칙상 부모 디렉토리가 제외되면 내부 파일을 재포함할 수 없으므로, 디렉토리 자체를 먼저 un-ignore 한다.
# (2026-07-10 lib/ 사건 726febf 과 동일 유형)
!multiverse/data/
*.csv
*.xlsx
*.json.bak

# IDE 설정
.vscode/
.idea/
*.swp
*.swo

# OS 파일
.DS_Store
Thumbs.db

# 백업 파일
*.bak
*.backup
*.old
/realtime_data

# Cache data files
cache/**/*.pkl
council/sessions/*

# TickTracer JSONL 로그 (on_tick 진단용)
logs/tick_trace/*.jsonl

# Feature Edge / Timing Lab 대용량 생성 산출물 (재생성 가능, GitHub 100MB 초과)
reports/discovery/feature_edge/*.parquet
reports/discovery/timing_lab/*.parquet

# venv 백업·임시 작업 디렉토리 (git add -A 가드)
venv_broken_quantcopy/
scratchpad/
