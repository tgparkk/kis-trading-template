# ORB v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** OrbV2Strategy를 신규 구현하여 거래량 필터(당일누적/전일일봉 비율) + KOSPI 시장환경 필터(전일종가 위)를 ORB v1에 추가하고, 96 시나리오 IS/OOS 토너먼트로 3중 게이트(IS합격선 + OOS합격선 + 일관성 30% 이내) 통과 시나리오를 찾는다.

**Architecture:** `strategies/intraday/orb_v2/` 신규 패키지가 IntradayBaseStrategy를 상속한다. 외부 일별 데이터(전일 일봉 거래량 dict, KOSPI 분봉 시리즈)는 `engine.run_minute()`이 시뮬 일자 루프에서 1회 호출하는 신규 `set_daily_context(date, ctx)` 훅으로 전략에 주입한다. 데이터 결손 시 fallback으로 통과시켜 zero-trade 회피. 기존 토너먼트 러너에 `orb_v2` registry 1줄 추가로 96 시나리오 평가.

**Tech Stack:** Python 3.8+, pandas, pytest, PostgreSQL 16 + TimescaleDB (minute_candles · daily_candles), 기존 `backtest/engine.py` / `tournament_metrics.py` / `scripts/run_intraday_tournament.py`.

**Spec reference:** [spec-2026-05-18-orb-v2.md](spec-2026-05-18-orb-v2.md)

---

## File Structure

**Create:**
- `RoboTrader_template/strategies/intraday/orb_v2/__init__.py` — package marker
- `RoboTrader_template/strategies/intraday/orb_v2/config.yaml` — default parameters
- `RoboTrader_template/strategies/intraday/orb_v2/strategy.py` — `OrbV2Strategy` class
- `RoboTrader_template/tests/test_intraday_strategies_part_c.py` — OrbV2Strategy 단위 테스트
- `RoboTrader_template/scripts/check_kospi_minute_source.py` — KOSPI 데이터 소스 1회성 점검 헬퍼
- `RoboTrader_template/scripts/analyze_orb_v2_results.py` — 3중 게이트 분석 + IS/OOS 분할 스크립트

**Modify:**
- `RoboTrader_template/strategies/intraday/_base_intraday.py` — `set_daily_context()` 메서드 추가 (디폴트 no-op)
- `RoboTrader_template/utils/intraday_indicators.py` — `cumulative_volume_ratio()` 헬퍼 추가
- `RoboTrader_template/backtest/engine.py:run_minute()` — 일자 루프에서 `set_daily_context()` 호출 + 시나리오 시작 시 prev_day_volume / KOSPI 사전 로드
- `RoboTrader_template/scripts/run_intraday_tournament.py:STRATEGY_REGISTRY` — `orb_v2` 등록
- `RoboTrader_template/tests/test_intraday_indicators.py` — `cumulative_volume_ratio` 테스트 추가
- `RoboTrader_template/tests/test_backtest_engine_minute.py` — `set_daily_context` 호출 검증 + ctx 결손 fallback 테스트

---

## Tasks

### Task 1: KOSPI 분봉 데이터 소스 점검

목표: `minute_candles`에 KOSPI 지수 분봉이 어떤 stock_code로 저장돼 있는지 확인. 미존재 시 KOSPI200 ETF `069500` 대체 후보 확정.

**Files:**
- Create: `RoboTrader_template/scripts/check_kospi_minute_source.py`

- [ ] **Step 1: 점검 스크립트 작성**

```python
"""KOSPI 분봉 데이터 소스 1회성 점검."""
import sys
from pathlib import Path

_PROJECT_ROOT = Path(__file__).resolve().parent.parent
if str(_PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(_PROJECT_ROOT))

from db.connection import DatabaseConnection


CANDIDATES = ["0001", "KS11", "KOSPI", "001", "U001", "069500"]


def main() -> None:
    with DatabaseConnection.get_connection() as conn:
        cursor = conn.cursor()
        for code in CANDIDATES:
            cursor.execute(
                "SELECT COUNT(*), MIN(datetime), MAX(datetime) "
                "FROM minute_candles WHERE stock_code = %s",
                (code,),
            )
            cnt, dt_min, dt_max = cursor.fetchone()
            print(f"  code={code!s:8s}  rows={cnt:>10,}  range={dt_min} .. {dt_max}")

        cursor.execute(
            "SELECT DISTINCT stock_code FROM minute_candles "
            "WHERE stock_code IN ('0001','KS11','KOSPI','001','U001','069500')"
        )
        present = [r[0] for r in cursor.fetchall()]
        print(f"\nFound in minute_candles: {present}")
        cursor.close()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행해서 KOSPI 코드 확정**

Run: `python RoboTrader_template/scripts/check_kospi_minute_source.py`

Expected output 형식 (값은 실제 DB에 따라 다름):
```
  code=0001      rows=     0  range=None .. None
  code=KS11      rows=     0  range=None .. None
  code=069500    rows=    65,000  range=2025-09-01 09:00:00 .. 2026-05-15 15:30:00

Found in minute_candles: ['069500']
```

- [ ] **Step 3: 결과를 spec에 반영**

Edit `RoboTrader_template/memory/spec-2026-05-18-orb-v2.md` §1 "외부 데이터 의존성" 항목 2번에 확정된 코드 명시 (예: "최종 결정: stock_code='069500' (KOSPI200 ETF, KOSPI와 0.99 상관)").

- [ ] **Step 4: 커밋**

```bash
git add RoboTrader_template/scripts/check_kospi_minute_source.py RoboTrader_template/memory/spec-2026-05-18-orb-v2.md
git commit -m "chore(orb_v2): KOSPI 분봉 데이터 소스 코드 확정 점검 스크립트"
```

---

### Task 2: `cumulative_volume_ratio()` 헬퍼 (TDD)

목표: 분봉 DataFrame과 전일 일봉 거래량을 받아 당일 누적/전일 비율 Series를 반환. NaN/0 안전.

**Files:**
- Modify: `RoboTrader_template/utils/intraday_indicators.py` (파일 끝에 추가)
- Modify: `RoboTrader_template/tests/test_intraday_indicators.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성**

Append to `RoboTrader_template/tests/test_intraday_indicators.py`:

```python
import numpy as np
import pandas as pd
import pytest

from utils.intraday_indicators import cumulative_volume_ratio


class TestCumulativeVolumeRatio:
    def _make_df(self, volumes):
        return pd.DataFrame({
            "datetime": pd.date_range("2026-04-01 09:00", periods=len(volumes), freq="1min"),
            "volume": volumes,
        })

    def test_returns_running_ratio(self):
        df = self._make_df([10, 20, 30, 40])
        result = cumulative_volume_ratio(df, prev_day_volume=100.0)
        # 누적 [10, 30, 60, 100] / 100 = [0.1, 0.3, 0.6, 1.0]
        assert list(result.round(2)) == [0.10, 0.30, 0.60, 1.00]

    def test_prev_zero_returns_nan_series(self):
        df = self._make_df([10, 20, 30])
        result = cumulative_volume_ratio(df, prev_day_volume=0.0)
        assert result.isna().all()

    def test_prev_none_returns_nan_series(self):
        df = self._make_df([10, 20, 30])
        result = cumulative_volume_ratio(df, prev_day_volume=None)
        assert result.isna().all()

    def test_empty_df_returns_empty_series(self):
        df = pd.DataFrame({"datetime": [], "volume": []})
        result = cumulative_volume_ratio(df, prev_day_volume=100.0)
        assert len(result) == 0

    def test_nan_volumes_treated_as_zero(self):
        df = self._make_df([10, np.nan, 30])
        result = cumulative_volume_ratio(df, prev_day_volume=100.0)
        # 누적 [10, 10, 40] / 100
        assert list(result.round(2)) == [0.10, 0.10, 0.40]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest RoboTrader_template/tests/test_intraday_indicators.py::TestCumulativeVolumeRatio -v`

Expected: FAIL with `ImportError: cannot import name 'cumulative_volume_ratio'`

- [ ] **Step 3: 함수 구현**

Append to `RoboTrader_template/utils/intraday_indicators.py`:

```python
# ---------------------------------------------------------------------------
# 11. Cumulative Volume Ratio (당일 누적 / 전일 일봉)
# ---------------------------------------------------------------------------

def cumulative_volume_ratio(
    df: pd.DataFrame, prev_day_volume: Optional[float]
) -> pd.Series:
    """당일 누적 거래량 / 전일 일봉 거래량 비율 Series.

    prev_day_volume이 None/0이면 전 구간 NaN Series 반환 (필터 미적용 의도).
    NaN 거래량은 0으로 취급.
    """
    if df.empty:
        return pd.Series(dtype=float, index=df.index)
    if prev_day_volume is None or prev_day_volume <= 0:
        return pd.Series(np.nan, index=df.index, dtype=float)

    vol = df["volume"].astype(float).fillna(0.0)
    cum = vol.cumsum()
    return cum / float(prev_day_volume)
```

또한 파일 상단 import 절에 `Optional`이 없으면 추가:
```python
from typing import Optional, Union
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest RoboTrader_template/tests/test_intraday_indicators.py::TestCumulativeVolumeRatio -v`

Expected: 5 PASSED

- [ ] **Step 5: 커밋**

```bash
git add RoboTrader_template/utils/intraday_indicators.py RoboTrader_template/tests/test_intraday_indicators.py
git commit -m "feat(indicators): cumulative_volume_ratio 헬퍼 + 단위 테스트"
```

---

### Task 3: `set_daily_context()` 추가 (TDD)

목표: `IntradayBaseStrategy`에 일별 외부 데이터 주입용 훅. 기본은 no-op + dict 저장.

**Files:**
- Modify: `RoboTrader_template/strategies/intraday/_base_intraday.py`
- Create: `RoboTrader_template/tests/test_intraday_strategies_part_c.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `RoboTrader_template/tests/test_intraday_strategies_part_c.py`:

```python
"""OrbV2Strategy + set_daily_context 단위 테스트."""
import pytest

from strategies.intraday._base_intraday import IntradayBaseStrategy


class _DummyIntraday(IntradayBaseStrategy):
    name = "Dummy"
    version = "0.0.0"

    def generate_signal(self, stock_code, data, timeframe="minute"):
        return None


class TestSetDailyContext:
    def test_default_is_noop_storing_dict(self):
        strat = _DummyIntraday({})
        strat.set_daily_context("20260401", {"prev_day_volume": {"005930": 1000.0}})
        assert strat._daily_ctx == {"prev_day_volume": {"005930": 1000.0}}
        assert strat._daily_ctx_date == "20260401"

    def test_overwrites_on_each_call(self):
        strat = _DummyIntraday({})
        strat.set_daily_context("20260401", {"a": 1})
        strat.set_daily_context("20260402", {"b": 2})
        assert strat._daily_ctx == {"b": 2}
        assert strat._daily_ctx_date == "20260402"

    def test_none_ctx_resets_to_empty_dict(self):
        strat = _DummyIntraday({})
        strat.set_daily_context("20260401", None)
        assert strat._daily_ctx == {}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest RoboTrader_template/tests/test_intraday_strategies_part_c.py::TestSetDailyContext -v`

Expected: FAIL with `AttributeError: 'IntradayBaseStrategy' object has no attribute 'set_daily_context'`

- [ ] **Step 3: 메서드 구현**

Edit `RoboTrader_template/strategies/intraday/_base_intraday.py` — `__init__` 끝에 두 속성 초기화 추가 + 클래스 메서드 1개 추가:

```python
    def __init__(self, config: Dict[str, Any] = None):
        super().__init__(config)
        cfg = self.config if isinstance(self.config, dict) else {}
        risk = cfg.get("risk_management", {})
        self.stop_loss_pct = float(risk.get("stop_loss_pct", 0.01))
        self.take_profit_pct = float(risk.get("take_profit_pct", 0.02))
        self.max_holding_minutes = risk.get("max_holding_minutes")
        self.eod_cutoff_buy = risk.get("eod_cutoff_buy", "15:00")
        self._daily_ctx: Dict[str, Any] = {}
        self._daily_ctx_date: Optional[str] = None

    def set_daily_context(self, date: str, ctx: Optional[Dict[str, Any]]) -> None:
        """엔진이 일자 루프 시작 시 호출. 외부 일별 데이터 주입용 훅.

        기본 구현은 dict 저장만. 서브클래스에서 추가 처리 필요 시 super() 호출.
        ctx=None이면 빈 dict로 리셋.
        """
        self._daily_ctx_date = date
        self._daily_ctx = ctx if ctx is not None else {}
```

또한 파일 상단 import 절에 `Optional`이 없으면 추가:
```python
from typing import Any, Dict, Optional
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest RoboTrader_template/tests/test_intraday_strategies_part_c.py::TestSetDailyContext -v`

Expected: 3 PASSED

- [ ] **Step 5: 커밋**

```bash
git add RoboTrader_template/strategies/intraday/_base_intraday.py RoboTrader_template/tests/test_intraday_strategies_part_c.py
git commit -m "feat(intraday): IntradayBaseStrategy.set_daily_context 훅 추가"
```

---

### Task 4: `OrbV2Strategy` 본체 (TDD)

목표: ORB v1 진입 게이트(시간 + 박스 + 돌파)에 거래량 필터 + KOSPI 필터를 AND로 추가한 새 전략.

**Files:**
- Create: `RoboTrader_template/strategies/intraday/orb_v2/__init__.py`
- Create: `RoboTrader_template/strategies/intraday/orb_v2/config.yaml`
- Create: `RoboTrader_template/strategies/intraday/orb_v2/strategy.py`
- Modify: `RoboTrader_template/tests/test_intraday_strategies_part_c.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성**

Append to `RoboTrader_template/tests/test_intraday_strategies_part_c.py`:

```python
import pandas as pd
from datetime import datetime, timedelta

from strategies.base import SignalType
from strategies.intraday.orb_v2.strategy import OrbV2Strategy


def _make_minute_df(or_high=10100.0, breakout_close=10200.0, n_box=30, volumes_box=None, volumes_after=None):
    """ORB 시뮬용 분봉 DF — 09:00부터 N+5 분봉. 마지막 봉이 돌파 봉."""
    start = pd.Timestamp("2026-04-01 09:00:00")
    rows = []
    box_vols = volumes_box if volumes_box is not None else [100] * n_box
    after_vols = volumes_after if volumes_after is not None else [100, 100, 100, 100, 100]

    # 박스 구간: high를 or_high 직전까지만
    for i, v in enumerate(box_vols):
        rows.append({
            "datetime": start + timedelta(minutes=i),
            "open": 10000.0, "high": or_high - 10, "low": 9990.0, "close": 10000.0,
            "volume": float(v),
        })
    # 박스 직후
    for i, v in enumerate(after_vols[:-1]):
        rows.append({
            "datetime": start + timedelta(minutes=n_box + i),
            "open": 10050.0, "high": 10080.0, "low": 10040.0, "close": 10050.0,
            "volume": float(v),
        })
    # 돌파 봉
    rows.append({
        "datetime": start + timedelta(minutes=n_box + len(after_vols) - 1),
        "open": 10100.0, "high": breakout_close + 50, "low": 10090.0, "close": breakout_close,
        "volume": float(after_vols[-1]),
    })
    return pd.DataFrame(rows)


class TestOrbV2Strategy:
    def _cfg(self, vol_ratio=1.0, use_market=False):
        return {
            "parameters": {
                "box_minutes": 30,
                "volume_ratio_threshold": vol_ratio,
                "use_market_filter": use_market,
            },
            "risk_management": {"stop_loss_pct": 0.03, "take_profit_pct": 0.06},
        }

    def test_volume_filter_blocks_when_below_threshold(self):
        strat = OrbV2Strategy(self._cfg(vol_ratio=1.0))
        # 누적 거래량 = 30*100 + 5*100 = 3500. 전일 = 10000. 비율 = 0.35 < 1.0
        strat.set_daily_context("20260401", {"prev_day_volume": {"005930": 10000.0}})
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is None

    def test_volume_filter_allows_when_above_threshold(self):
        strat = OrbV2Strategy(self._cfg(vol_ratio=0.3))
        # 누적 거래량 3500 / 10000 = 0.35 > 0.3
        strat.set_daily_context("20260401", {"prev_day_volume": {"005930": 10000.0}})
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is not None
        assert sig.signal_type == SignalType.BUY

    def test_volume_filter_skipped_when_prev_zero(self):
        """전일 데이터 결손 fallback — 필터 미적용, 통과."""
        strat = OrbV2Strategy(self._cfg(vol_ratio=10.0))  # 매우 높은 임계값
        strat.set_daily_context("20260401", {"prev_day_volume": {"005930": 0.0}})
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is not None  # fallback 통과

    def test_volume_filter_skipped_when_code_missing(self):
        strat = OrbV2Strategy(self._cfg(vol_ratio=10.0))
        strat.set_daily_context("20260401", {"prev_day_volume": {}})  # 005930 없음
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is not None  # fallback 통과

    def test_market_filter_blocks_when_kospi_down(self):
        """전일 KOSPI 일봉 하락(kospi_market_up=False) → 진입 차단."""
        strat = OrbV2Strategy(self._cfg(vol_ratio=0.0, use_market=True))
        strat.set_daily_context("20260401", {
            "prev_day_volume": {"005930": 10000.0},
            "kospi_market_up": False,
        })
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is None

    def test_market_filter_allows_when_kospi_up(self):
        """전일 KOSPI 일봉 상승(kospi_market_up=True) → 진입 허용."""
        strat = OrbV2Strategy(self._cfg(vol_ratio=0.0, use_market=True))
        strat.set_daily_context("20260401", {
            "prev_day_volume": {"005930": 10000.0},
            "kospi_market_up": True,
        })
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is not None

    def test_market_filter_skipped_when_kospi_missing(self):
        """KOSPI 일봉 결손(kospi_market_up 키 없음) → fallback 통과."""
        strat = OrbV2Strategy(self._cfg(vol_ratio=0.0, use_market=True))
        strat.set_daily_context("20260401", {
            "prev_day_volume": {"005930": 10000.0},
            # kospi_market_up 키 없음
        })
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is not None

    def test_market_filter_disabled_ignores_kospi(self):
        """use_market_filter=False일 때 kospi_market_up=False도 무시."""
        strat = OrbV2Strategy(self._cfg(vol_ratio=0.0, use_market=False))
        strat.set_daily_context("20260401", {
            "prev_day_volume": {"005930": 10000.0},
            "kospi_market_up": False,  # 하락이지만 무시
        })
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        assert sig is not None

    def test_no_ctx_set_uses_safe_defaults(self):
        """set_daily_context 호출 전이라도 안전하게 동작."""
        strat = OrbV2Strategy(self._cfg(vol_ratio=10.0, use_market=True))
        df = _make_minute_df()
        sig = strat.generate_signal("005930", df, "minute")
        # ctx 없으면 두 필터 모두 fallback → 통과
        assert sig is not None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest RoboTrader_template/tests/test_intraday_strategies_part_c.py::TestOrbV2Strategy -v`

Expected: FAIL with `ModuleNotFoundError: No module named 'strategies.intraday.orb_v2'`

- [ ] **Step 3: 패키지 생성**

Create `RoboTrader_template/strategies/intraday/orb_v2/__init__.py`:

```python
"""ORB v2 — 거래량 + KOSPI 시장환경 필터 추가."""
```

Create `RoboTrader_template/strategies/intraday/orb_v2/config.yaml`:

```yaml
strategy:
  name: ORB_v2
  version: 2.0.0
  description: "Opening Range Breakout + 당일누적/전일일봉 거래량 필터 + KOSPI 전일종가 위 필터"

parameters:
  box_minutes: 30
  volume_ratio_threshold: 1.0    # 당일 누적 거래량 / 전일 일봉 ≥ 이 값
  use_market_filter: true        # KOSPI 전일종가 위에 있을 때만 진입

risk_management:
  stop_loss_pct: 0.03
  take_profit_pct: 0.06
  max_holding_minutes: null
  eod_cutoff_buy: "15:00"
```

Create `RoboTrader_template/strategies/intraday/orb_v2/strategy.py`:

```python
"""ORB v2 — 거래량 + KOSPI 시장환경 필터 추가."""

from datetime import time
from typing import Optional

import pandas as pd

from strategies.intraday._base_intraday import IntradayBaseStrategy
from strategies.base import Signal
from utils.intraday_indicators import orb_levels


class OrbV2Strategy(IntradayBaseStrategy):
    """ORB v1 진입 게이트 + 거래량 필터 + KOSPI 시장환경 필터.

    필요한 일별 외부 데이터 (set_daily_context로 주입):
      ctx["prev_day_volume"]: dict[stock_code -> float] — 전일 일봉 거래량
      ctx["kospi_market_up"]: bool — 직전 거래일 KOSPI 종가 > 그 전 거래일 종가이면 True

    결손 fallback: 누락 시 해당 필터 미적용 (통과시킴).
    """

    name = "ORB_v2"
    version = "2.0.0"
    description = "ORB + 거래량/시장환경 필터"
    author = "Template"

    def __init__(self, config=None):
        super().__init__(config)
        params = self.config.get("parameters", {}) if isinstance(self.config, dict) else {}
        self._box_minutes = int(params.get("box_minutes", 30))
        self._volume_ratio_threshold = float(params.get("volume_ratio_threshold", 1.0))
        self._use_market_filter = bool(params.get("use_market_filter", True))
        cutoff_min = self._box_minutes
        cutoff_hour = 9 + cutoff_min // 60
        cutoff_min_rem = cutoff_min % 60
        self._breakout_after = time(cutoff_hour, cutoff_min_rem)
        self._warned_missing_kospi: set[str] = set()
        self._warned_missing_prev_vol: set[str] = set()

    def on_init(self, broker, data_provider, executor) -> bool:
        self._broker = broker
        self._data_provider = data_provider
        self._executor = executor
        self._is_initialized = True
        self.logger.info(
            f"{self.name} v{self.version} 초기화 완료 "
            f"(box={self._box_minutes}, vol_ratio≥{self._volume_ratio_threshold}, "
            f"market_filter={self._use_market_filter})"
        )
        return True

    def generate_signal(
        self,
        stock_code: str,
        data: pd.DataFrame,
        timeframe: str = "minute",
    ) -> Optional[Signal]:
        if timeframe != "minute":
            return None
        if not isinstance(data, pd.DataFrame) or len(data) < self._box_minutes + 5:
            return None
        if self._is_after_eod_cutoff(data["datetime"].iloc[-1]):
            return None

        last_dt = pd.to_datetime(data["datetime"].iloc[-1])
        if last_dt.time() < self._breakout_after:
            return None

        levels = orb_levels(data, window_minutes=self._box_minutes)
        or_high = levels.get("or_high")
        or_low = levels.get("or_low")
        if or_high is None or pd.isna(or_high):
            return None

        last_close = float(data["close"].iloc[-1])
        if not (last_close > float(or_high)):
            return None

        # 필터 1: 거래량
        if not self._volume_filter_passes(stock_code, data):
            return None

        # 필터 2: 시장환경
        if not self._market_filter_passes(last_dt):
            return None

        return self._make_buy_signal(
            stock_code,
            last_close,
            reason=(
                f"ORB v2 breakout: close={last_close:.0f} > or_high={or_high:.0f} "
                f"| vol_ratio≥{self._volume_ratio_threshold} | mkt={self._use_market_filter}"
            ),
            or_high=or_high,
            or_low=or_low,
            or_range=levels.get("or_range"),
        )

    # ---------- internal filters ----------

    def _volume_filter_passes(self, stock_code: str, data: pd.DataFrame) -> bool:
        prev_vol_map = self._daily_ctx.get("prev_day_volume", {}) if self._daily_ctx else {}
        prev_vol = prev_vol_map.get(stock_code, 0.0)
        if prev_vol <= 0:
            if stock_code not in self._warned_missing_prev_vol:
                self.logger.warning(
                    f"{stock_code} 전일 일봉 거래량 누락 — volume 필터 미적용"
                )
                self._warned_missing_prev_vol.add(stock_code)
            return True  # fallback

        cum_vol = float(data["volume"].fillna(0.0).sum())
        ratio = cum_vol / float(prev_vol)
        return ratio >= self._volume_ratio_threshold

    def _market_filter_passes(self, last_dt: pd.Timestamp) -> bool:
        if not self._use_market_filter:
            return True
        ctx = self._daily_ctx or {}
        flag = ctx.get("kospi_market_up")
        if flag is None:
            date_key = self._daily_ctx_date or "unknown"
            if date_key not in self._warned_missing_kospi:
                self.logger.warning(
                    f"{date_key} kospi_market_up 결손 — market 필터 미적용"
                )
                self._warned_missing_kospi.add(date_key)
            return True  # fallback (전전 거래일 KS11 결손 등)
        return bool(flag)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest RoboTrader_template/tests/test_intraday_strategies_part_c.py::TestOrbV2Strategy -v`

Expected: 9 PASSED

- [ ] **Step 5: 커밋**

```bash
git add RoboTrader_template/strategies/intraday/orb_v2/ RoboTrader_template/tests/test_intraday_strategies_part_c.py
git commit -m "feat(strategy): OrbV2Strategy 본체 + 단위 테스트 9건"
```

---

### Task 5: `engine.run_minute()` 통합 (TDD)

목표: BacktestEngine이 시나리오 시작 시 prev_day_volume / KOSPI 데이터를 한 번에 로드하고, 거래일 루프마다 strategy.set_daily_context(date, ctx)를 호출.

**Files:**
- Modify: `RoboTrader_template/backtest/engine.py:run_minute()`
- Modify: `RoboTrader_template/tests/test_backtest_engine_minute.py` (테스트 추가)

- [ ] **Step 1: 실패 테스트 작성**

Append to `RoboTrader_template/tests/test_backtest_engine_minute.py`:

```python
class TestSetDailyContextCall:
    def test_run_minute_calls_set_daily_context_per_day(self, monkeypatch):
        """run_minute이 거래일 루프마다 strategy.set_daily_context를 호출."""
        from backtest.engine import BacktestEngine
        from strategies.intraday.orb_v2.strategy import OrbV2Strategy

        strat = OrbV2Strategy({})
        called_dates = []
        original = strat.set_daily_context

        def _spy(date, ctx):
            called_dates.append(date)
            return original(date, ctx)

        strat.set_daily_context = _spy

        engine = BacktestEngine(strategy=strat, initial_capital=10_000_000, max_positions=3)

        # PriceRepository mock — 빈 데이터 반환해서 빠르게 종료
        from unittest.mock import patch, MagicMock
        with patch("db.repositories.price.PriceRepository") as mock_repo_cls:
            mock_repo = MagicMock()
            mock_repo.fetch_minute_candles.return_value = pd.DataFrame()
            mock_repo_cls.return_value = mock_repo

            engine.run_minute(
                stock_codes=["005930"],
                start_date="20260401",
                end_date="20260403",
                candidate_provider=lambda d: ["005930"],
            )

        # 거래일이 1+개라면 한 번이라도 호출됐어야 함
        assert len(called_dates) >= 1
        # YYYYMMDD 포맷 보장
        for d in called_dates:
            assert len(d) == 8 and d.isdigit()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pytest RoboTrader_template/tests/test_backtest_engine_minute.py::TestSetDailyContextCall -v`

Expected: FAIL — `called_dates`가 비어있음

- [ ] **Step 3: engine.run_minute() 수정**

`backtest/engine.py:run_minute()` 함수 내부에서 거래일 루프 시작 직전에 두 가지 추가:
1. **시나리오 시작 시 사전로드** (PriceRepository 초기화 후): 모든 후보 종목 × 거래일에 대한 prev_day_volume dict + KOSPI 분봉 dict를 1회 조회
2. **거래일 루프 진입 시**: `strategy.set_daily_context(date, ctx_for_today)` 호출

구체적 수정 위치 (executor가 `Grep`/`Read`로 정확한 라인 찾기 — 거래일 루프 시작 라인 = `for trade_date in trading_days:` 와 유사):

`RoboTrader_template/backtest/engine.py` 의 `run_minute()` 내부, `trading_days = ...` 정의 직후에 사전로드 블록 삽입:

```python
# === ORB v2 외부 데이터 사전로드 (KS11 일봉 → kospi_market_up bool 플래그) ===
# 전략이 set_daily_context를 사용하는 경우에만 의미 있음. 비용은 작음(쿼리 1번).
# minute_candles에 KOSPI 분봉이 없어 daily_candles(KS11)로 대체. 시장환경 정의:
#   당일 ctx["kospi_market_up"] = (직전 거래일 KS11 종가 > 그 전 거래일 종가)
# look-ahead 방지: 당일 KS11 일봉은 사용하지 않음.
kospi_market_up_by_date: Dict[str, bool] = {}
try:
    if trading_days:
        from db.connection import DatabaseConnection
        min_d = min(trading_days)
        max_d = max(trading_days)
        with DatabaseConnection.get_connection() as _conn:
            _cur = _conn.cursor()
            # 시작일 이전 2거래일도 포함하도록 90일 lookback (휴장일 여유)
            _cur.execute(
                """SELECT stck_bsop_date, stck_clpr FROM daily_candles
                   WHERE stock_code = %s
                     AND stck_bsop_date <= %s
                   ORDER BY stck_bsop_date ASC""",
                (KOSPI_CODE, max_d),
            )
            ks11_rows = _cur.fetchall()
            _cur.close()
        if ks11_rows:
            # (date, close_float) 정렬된 리스트
            _ks11 = []
            for _d, _c in ks11_rows:
                try:
                    _ks11.append((str(_d), float(_c)))
                except (TypeError, ValueError):
                    continue
            # 각 거래일 d에 대해, d 이전의 KS11 종가 두 개 비교
            for _trade_d in trading_days:
                # d 이전 KS11 데이터만 (look-ahead 방지)
                _prior = [(d, c) for d, c in _ks11 if d < _trade_d]
                if len(_prior) >= 2:
                    _, prev_close = _prior[-1]
                    _, prev_prev_close = _prior[-2]
                    kospi_market_up_by_date[_trade_d] = prev_close > prev_prev_close
                # else: 키 누락 → 전략에서 fallback 통과
except Exception as _exc:
    self.logger.warning(f"run_minute: KS11 일봉 사전로드 실패 — kospi_market_up 결손 ({_exc})")
```

상단 import 절에 추가:
```python
KOSPI_CODE = "KS11"  # Task 1에서 확정 — daily_candles.stock_code 값
```

거래일 루프(`for trade_date in trading_days:` 또는 유사) 진입 직후, 종목 루프 시작 전에:

```python
        # === per-day daily context for strategy ===
        if hasattr(self.strategy, "set_daily_context"):
            # 당일 후보 종목 결정 (candidate_provider 우선)
            if candidate_provider is not None:
                _today_codes = candidate_provider(trade_date)
            else:
                _today_codes = stock_codes or []

            # 전일 일봉 거래량 조회 (당일 후보만)
            _prev_vol: Dict[str, float] = {}
            try:
                if _today_codes:
                    from db.connection import DatabaseConnection
                    with DatabaseConnection.get_connection() as _conn:
                        _cur = _conn.cursor()
                        # 직전 거래일 daily_candles 조회
                        _cur.execute(
                            """SELECT stock_code, volume FROM daily_candles
                               WHERE stock_code = ANY(%s)
                                 AND date = (
                                   SELECT MAX(date) FROM daily_candles
                                   WHERE stock_code = ANY(%s) AND date < %s
                                 )""",
                            (
                                _today_codes,
                                _today_codes,
                                f"{trade_date[:4]}-{trade_date[4:6]}-{trade_date[6:8]}",
                            ),
                        )
                        for _code, _vol in _cur.fetchall():
                            _prev_vol[_code] = float(_vol or 0.0)
                        _cur.close()
            except Exception as _exc:
                self.logger.warning(f"run_minute: {trade_date} prev_day_volume 조회 실패 ({_exc})")

            ctx = {
                "prev_day_volume": _prev_vol,
            }
            if trade_date in kospi_market_up_by_date:
                ctx["kospi_market_up"] = kospi_market_up_by_date[trade_date]
            # 키 누락 시 전략은 fallback 통과
            self.strategy.set_daily_context(trade_date, ctx)
```

(주의: 정확한 라인 번호는 executor가 `Grep "for trade_date" backtest/engine.py` 로 확인. 위 두 블록은 각각 "trading_days 선언 직후" + "for 루프 진입 직후"에 들어간다.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `pytest RoboTrader_template/tests/test_backtest_engine_minute.py::TestSetDailyContextCall -v`

Expected: PASSED

또한 전체 회귀로 다른 분봉 백테스트 테스트가 깨지지 않았는지:
Run: `pytest RoboTrader_template/tests/test_backtest_engine_minute.py -v`
Expected: 모든 기존 테스트 PASS 유지

- [ ] **Step 5: 커밋**

```bash
git add RoboTrader_template/backtest/engine.py RoboTrader_template/tests/test_backtest_engine_minute.py
git commit -m "feat(engine): run_minute이 거래일별 set_daily_context 호출 + KOSPI/prev_vol 사전로드"
```

---

### Task 6: 토너먼트 registry 8종 등록 + smoke (사장님 결재: 대안 A)

**Files:**
- Modify: `RoboTrader_template/scripts/run_intraday_tournament.py:STRATEGY_REGISTRY` + `_load_strategy()`

목표: STRATEGY_REGISTRY 항목을 (class_path, params_dict) 형태로 확장해 같은 OrbV2Strategy 클래스를 8개 서로 다른 디폴트 파라미터로 등록. 호환성을 위해 기존 string 값도 계속 지원.

- [ ] **Step 1: STRATEGY_REGISTRY 확장 + _load_strategy 호환 분기**

Edit `RoboTrader_template/scripts/run_intraday_tournament.py`:

```python
# 기존 형식 (dotpath 문자열) + 신규 형식 (dict with class + params) 모두 지원
STRATEGY_REGISTRY: dict[str, object] = {
    "abcd_pattern":        "strategies.intraday.abcd_pattern.strategy.AbcdPatternStrategy",
    "bull_flag":           "strategies.intraday.bull_flag.strategy.BullFlagStrategy",
    "reversal_rsi":        "strategies.intraday.reversal_rsi.strategy.ReversalRsiStrategy",
    "reversal_vwap":       "strategies.intraday.reversal_vwap.strategy.ReversalVwapStrategy",
    "ma_trend":            "strategies.intraday.ma_trend.strategy.MaTrendStrategy",
    "vwap_trade":          "strategies.intraday.vwap_trade.strategy.VwapTradeStrategy",
    "support_resistance":  "strategies.intraday.support_resistance.strategy.SupportResistanceStrategy",
    "red_to_green":        "strategies.intraday.red_to_green.strategy.RedToGreenStrategy",
    "orb":                 "strategies.intraday.orb.strategy.OrbStrategy",
    "pullback":            "strategies.intraday.pullback.strategy.PullbackStrategy",
    # ORB v2 — 거래량 임계값(0.5/1.0/1.5/2.0) × 시장환경 필터(off/on) = 8 entries
    "orb_v2_vr05_nomkt": {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 0.5, "use_market_filter": False}}},
    "orb_v2_vr05_mkt":   {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 0.5, "use_market_filter": True}}},
    "orb_v2_vr10_nomkt": {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 1.0, "use_market_filter": False}}},
    "orb_v2_vr10_mkt":   {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 1.0, "use_market_filter": True}}},
    "orb_v2_vr15_nomkt": {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 1.5, "use_market_filter": False}}},
    "orb_v2_vr15_mkt":   {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 1.5, "use_market_filter": True}}},
    "orb_v2_vr20_nomkt": {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 2.0, "use_market_filter": False}}},
    "orb_v2_vr20_mkt":   {"class": "strategies.intraday.orb_v2.strategy.OrbV2Strategy",
                           "params": {"parameters": {"volume_ratio_threshold": 2.0, "use_market_filter": True}}},
}


def _load_strategy(key: str):
    """레지스트리 키 → BaseStrategy 인스턴스 (string 또는 dict 형식 둘 다 지원)."""
    entry = STRATEGY_REGISTRY[key]
    if isinstance(entry, str):
        dotpath = entry
        params = {}
    elif isinstance(entry, dict):
        dotpath = entry["class"]
        params = entry.get("params", {})
    else:
        raise TypeError(f"Unsupported STRATEGY_REGISTRY entry type for {key}: {type(entry)}")

    module_path, cls_name = dotpath.rsplit(".", 1)
    try:
        mod = importlib.import_module(module_path)
        cls = getattr(mod, cls_name)
        return cls(params)
    except Exception as exc:
        logger.error(f"전략 로드 실패 [{key}]: {exc}")
        raise
```

- [ ] **Step 2: 새 registry 항목들이 로드되는지 단위 테스트 추가**

Append to `RoboTrader_template/tests/test_intraday_strategies_part_c.py`:

```python
class TestOrbV2RegistryLoading:
    def test_all_8_orb_v2_keys_loadable(self):
        from scripts.run_intraday_tournament import _load_strategy
        keys = [
            "orb_v2_vr05_nomkt", "orb_v2_vr05_mkt",
            "orb_v2_vr10_nomkt", "orb_v2_vr10_mkt",
            "orb_v2_vr15_nomkt", "orb_v2_vr15_mkt",
            "orb_v2_vr20_nomkt", "orb_v2_vr20_mkt",
        ]
        for k in keys:
            strat = _load_strategy(k)
            assert strat.name == "ORB_v2"
            # params 주입 검증
            if "nomkt" in k:
                assert strat._use_market_filter is False
            else:
                assert strat._use_market_filter is True
            if "vr05" in k:
                assert strat._volume_ratio_threshold == 0.5
            elif "vr10" in k:
                assert strat._volume_ratio_threshold == 1.0
            elif "vr15" in k:
                assert strat._volume_ratio_threshold == 1.5
            elif "vr20" in k:
                assert strat._volume_ratio_threshold == 2.0

    def test_legacy_string_entries_still_work(self):
        """기존 string 형식 registry 항목이 깨지지 않는지."""
        from scripts.run_intraday_tournament import _load_strategy
        strat = _load_strategy("orb")
        assert strat.name == "ORB"
```

Run: `pytest RoboTrader_template/tests/test_intraday_strategies_part_c.py::TestOrbV2RegistryLoading -v`
Expected: PASS

- [ ] **Step 2: smoke 실행 (5영업일)**

Run:
```powershell
python RoboTrader_template/scripts/run_intraday_tournament.py `
  --start 20260401 --end 20260408 `
  --strategies orb_v2 `
  --universe dynamic --max-positions 4 `
  --sl-grid 0.03 --tp-grid 0.06 `
  --workers 4 `
  --dynamic-top-n 50 --dynamic-rank-by volatility_pct `
  --out RoboTrader_template/reports/orb_v2_smoke
```

Expected: 1 시나리오 완료 (~3~5분). 결과 디렉토리 `orb_v2_smoke/<timestamp>/` 생성 + `tournament_round1_summary.md` 출력. `trade_count > 0` 확인. ERROR 0건.

- [ ] **Step 3: smoke 로그 확인**

`tournament_round1_summary.md` 또는 stdout 확인:
- `trades > 0` (필터가 모든 거래를 막지 않음)
- `total_pnl_pct` 수치 (양수/음수 무관 — 동작 확인 목적)
- 로그에 "ORB v2 breakout: ..." 메시지 1+ 회 출현

만약 trades=0이면: volume_ratio_threshold가 너무 높을 가능성. config.yaml의 디폴트 1.0을 0.5로 낮춰 재실행.

- [ ] **Step 4: 커밋**

```bash
git add RoboTrader_template/scripts/run_intraday_tournament.py
git commit -m "feat(tournament): STRATEGY_REGISTRY에 orb_v2 등록 + smoke 통과"
```

---

### Task 7: 회귀 PASS + 단일 push

**Files:** 없음 (검증 단계)

- [ ] **Step 1: 분봉 관련 회귀 실행**

Run: `pytest RoboTrader_template/tests/test_intraday_indicators.py RoboTrader_template/tests/test_intraday_strategies_part_c.py RoboTrader_template/tests/test_backtest_engine_minute.py -v`

Expected: 모두 PASS. 신규 5+3+9+1 = 18 신규 테스트.

- [ ] **Step 2: 전체 회귀 실행**

Run: `pytest RoboTrader_template/tests/ -v --tb=short -x 2>&1 | tail -40`

Expected: 1,921+ passed, 1 pre-existing fail (test_screener_pipeline, 기존). 신규 실패 0건.

- [ ] **Step 3: 만약 실패 있으면 fix → 재실행**

신규 실패가 있을 시 executor가 root cause 분석 후 수정. fix 커밋은 별도 메시지로.

- [ ] **Step 4: origin 푸시 (사장님 확인 후)**

executor는 직접 push 금지. Claude(관리자)에게 commit hash 보고 → 사장님 결재 후 push.

---

### Task 8: 96 시나리오 토너먼트 실행 (관리자 직접)

**Owner:** Claude (executor 위임 대상 아님)

목표: ORB v2 96 시나리오 본 토너먼트를 백그라운드로 실행.

- [ ] **Step 1: 명령어 실행 (백그라운드, 대안 A 확정)**

8 strategies × 2 SL × 2 TP × 3 pos = 96 시나리오

```powershell
python RoboTrader_template/scripts/run_intraday_tournament.py `
  --start 20250901 --end 20260515 --skip 202603 `
  --capital 10000000 `
  --max-positions 3 4 5 `
  --universe dynamic `
  --strategies orb_v2_vr05_nomkt,orb_v2_vr05_mkt,orb_v2_vr10_nomkt,orb_v2_vr10_mkt,orb_v2_vr15_nomkt,orb_v2_vr15_mkt,orb_v2_vr20_nomkt,orb_v2_vr20_mkt `
  --sl-grid 0.02,0.03 --tp-grid 0.04,0.06 `
  --eod 15:20 --slip-bps 5 `
  --workers 8 `
  --dynamic-top-n 50 --dynamic-rank-by volatility_pct `
  --out RoboTrader_template/reports/tournament_orb_v2 `
  --log-level INFO `
  > RoboTrader_template/reports/tournament_orb_v2_run.log 2>&1
```

예상 시간: ~2~3h (workers=8, 168일 분봉, 96 시나리오)

- [ ] **Step 2: 로그 모니터링**

`reports/tournament_orb_v2_run.log`에 백그라운드 출력 저장. ~2~3h 후 완료 알림 대기.

- [ ] **Step 3: 결과 디렉토리 확인**

Expected: `RoboTrader_template/reports/tournament_orb_v2/<timestamp>/tournament_results.parquet` + `tournament_round1_summary.md` 생성. 시나리오 96개 또는 24개(대안 A 미적용 시).

---

### Task 9: 3중 게이트 결과 분석

**Owner:** Claude (or general-purpose / scientist agent)

목표: 토너먼트 결과 parquet에서 일자별 PnL 추출 → IS/OOS 분할 → 3중 게이트 평가 → 우승 시나리오 선정.

**Files:**
- Create: `RoboTrader_template/scripts/analyze_orb_v2_results.py`

- [ ] **Step 1: 분석 스크립트 작성**

Create `RoboTrader_template/scripts/analyze_orb_v2_results.py`:

```python
"""ORB v2 토너먼트 결과 IS/OOS 분할 + 3중 게이트 평가.

전제: tournament_results.parquet에 일자별 PnL 컬럼이 있다고 가정.
없으면 본 스크립트가 BacktestEngine을 재호출 (1+h 소요)하는 fallback 분기 추가 필요.
"""
import sys
from pathlib import Path

_PROJECT_ROOT = Path(__file__).resolve().parent.parent
if str(_PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(_PROJECT_ROOT))

import argparse
import pandas as pd


IS_RATIO = 0.70
IS_PASS_DAILY = 0.003
IS_PASS_WIN = 50.0
IS_PASS_MDD = -15.0
CONSISTENCY_THRESHOLD = 0.30


def analyze(parquet_path: Path) -> None:
    df = pd.read_parquet(parquet_path)
    print(f"시나리오 수: {len(df)}")
    print(f"컬럼: {list(df.columns)}")

    # 일자별 PnL 컬럼이 있다고 가정 — 없으면 명시적 에러
    if "daily_pnl_series" not in df.columns:
        raise NotImplementedError(
            "tournament_results.parquet에 daily_pnl_series 컬럼 없음. "
            "engine.run_minute 결과에 일자별 시계열 추가 필요."
        )

    rows = []
    for _, row in df.iterrows():
        pnl_series = row["daily_pnl_series"]  # list[float] or pd.Series
        if pnl_series is None or len(pnl_series) == 0:
            continue
        pnl = pd.Series(pnl_series)
        cutoff = int(len(pnl) * IS_RATIO)
        is_pnl = pnl.iloc[:cutoff]
        oos_pnl = pnl.iloc[cutoff:]

        is_daily = is_pnl.mean() * 100  # %
        oos_daily = oos_pnl.mean() * 100
        is_win = (is_pnl > 0).mean() * 100
        oos_win = (oos_pnl > 0).mean() * 100
        is_mdd = _max_drawdown_pct(is_pnl)
        oos_mdd = _max_drawdown_pct(oos_pnl)

        is_pass = (is_daily >= IS_PASS_DAILY) and (is_win >= IS_PASS_WIN) and (is_mdd >= IS_PASS_MDD)
        oos_pass = (oos_daily >= IS_PASS_DAILY) and (oos_win >= IS_PASS_WIN) and (oos_mdd >= IS_PASS_MDD)
        consistency = abs(is_daily - oos_daily) / abs(is_daily) if is_daily != 0 else float("inf")
        consistency_pass = consistency < CONSISTENCY_THRESHOLD

        rows.append({
            "strategy": row.get("strategy"),
            "universe": row.get("universe"),
            "max_positions": row.get("max_positions"),
            "sl_pct": row.get("sl_pct"),
            "tp_pct": row.get("tp_pct"),
            "is_daily_pct": is_daily,
            "is_win_pct": is_win,
            "is_mdd_pct": is_mdd,
            "oos_daily_pct": oos_daily,
            "oos_win_pct": oos_win,
            "oos_mdd_pct": oos_mdd,
            "consistency": consistency,
            "is_pass": is_pass,
            "oos_pass": oos_pass,
            "consistency_pass": consistency_pass,
            "triple_pass": is_pass and oos_pass and consistency_pass,
        })

    result_df = pd.DataFrame(rows)
    triple_pass_df = result_df[result_df["triple_pass"]]
    print(f"\n3중 게이트 통과: {len(triple_pass_df)} / {len(result_df)}")
    if not triple_pass_df.empty:
        print(triple_pass_df.sort_values("oos_daily_pct", ascending=False).to_string(index=False))

    out_path = parquet_path.parent / "triple_gate_analysis.csv"
    result_df.to_csv(out_path, index=False)
    print(f"\n저장: {out_path}")


def _max_drawdown_pct(pnl: pd.Series) -> float:
    """일별 PnL series → MDD %. 양수면 무이익(0%), 음수면 누적 최대 손실."""
    equity = (1 + pnl).cumprod()
    peak = equity.cummax()
    drawdown = (equity - peak) / peak * 100
    return float(drawdown.min())


def main() -> None:
    p = argparse.ArgumentParser()
    p.add_argument("--parquet", required=True, type=Path)
    args = p.parse_args()
    analyze(args.parquet)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행**

Run: `python RoboTrader_template/scripts/analyze_orb_v2_results.py --parquet RoboTrader_template/reports/tournament_orb_v2/<timestamp>/tournament_results.parquet`

Expected: 3중 게이트 통과 시나리오 수 출력. CSV 저장.

- [ ] **Step 3: 결과 해석**

- 통과 1+개: 가장 좋은 시나리오(OOS daily 1위)를 우승자로 사장님께 보고. Phase 4 페이퍼 결재 받기
- 통과 0개: ORB 폐기 + 분봉 전체 보류 결재 받기

**Note:** 만약 `daily_pnl_series` 컬럼이 parquet에 없다면, `BacktestEngine.run_minute()`이 결과 dict에 일자별 시계열을 추가 저장하도록 보강 필요. 이 경우 Task 9를 두 단계로 분리:
- 9a: engine.run_minute이 일자별 PnL 저장하도록 수정 + 단위 테스트 + 96 시나리오 재실행
- 9b: 분석 스크립트 실행

(executor가 Task 5 진행 중 daily_pnl_series 저장도 함께 구현하면 9a 생략 가능. 권장.)

---

### Task 10: Changelog 작성

**Owner:** Claude

목표: 결과를 `RoboTrader_template/memory/changelog-YYYY-MM-DD-orb-v2-results.md`에 정리, MEMORY.md 인덱스 추가.

- [ ] **Step 1: changelog 작성**

내용:
- ORB v2 96 시나리오 결과 요약 (통과 수, 우승자, IS/OOS 지표 비교 표)
- 3중 게이트 통과 시 우승 파라미터 (volume_ratio, market_filter, SL/TP, pos)
- 통과 0건 시 ORB 폐기 결론
- Phase 4 페이퍼 결재 또는 분봉 보류 결재 명시

- [ ] **Step 2: MEMORY.md 인덱스 추가**

추가:
```markdown
## ORB v2 결과 (2026-MM-DD)
- 상세: [changelog-YYYY-MM-DD-orb-v2-results.md](changelog-YYYY-MM-DD-orb-v2-results.md)
- 96 시나리오 3중 게이트 통과: N개 / 우승: ... (또는 폐기 결정)
```

- [ ] **Step 3: 커밋**

```bash
git add RoboTrader_template/memory/changelog-*-orb-v2-results.md C:\Users\sttgp\.claude\projects\D--GIT-kis-trading-template\memory\MEMORY.md
git commit -m "docs(memory): ORB v2 96 시나리오 결과 일지"
```

---

## Self-Review (Plan vs Spec)

**Spec coverage check:**
- §1 아키텍처: 디렉토리/파일 구조 → Task 4. set_daily_context → Task 3. engine 통합 → Task 5. KOSPI 데이터 점검 → Task 1 ✅
- §2 그리드 평가: 96 시나리오 실행 → Task 8 (대안 A 명시). IS/OOS 분할 + 3중 게이트 → Task 9 ✅
- §3 에러처리 fallback: prev_vol 결손 통과 → Task 4 Step 3 코드 / KOSPI 결손 forward-fill → Task 4 Step 3 코드. 단위 테스트 → Task 4 Step 1 ✅
- §4 산출물 1~8: 모두 Task 매핑 완료 ✅

**Placeholder scan:**
- Task 5 Step 3에 "정확한 라인 번호는 executor가 Grep로 확인" — executor 위임이므로 OK
- 다른 위치는 모두 구체 코드 제공 ✅

**Type consistency:**
- `self._daily_ctx` (dict), `self._daily_ctx_date` (Optional[str]), `set_daily_context(date: str, ctx: Optional[Dict[str, Any]])` 일관 ✅
- ctx 키 이름: `prev_day_volume`, `kospi_prev_close`, `kospi_minute_df` — Task 4/5 일관 ✅
- `KOSPI_CODE = "069500"` — Task 1 점검 후 확정 (placeholder OK, Task 1이 자체 확정 step 포함)

**미해결 사항:**
- Task 8 대안 A/B 결재 — 사장님 결정 필요 (plan에 명시했지만 spec에 명시되지 않은 결정사항). Task 6에서 대안 A로 가면 config 8개 추가 작업 필요.

위 사항은 본 plan을 사장님 리뷰 후 결정 — 결재 결과를 spec에 반영하고 본 plan의 Task 6, 8 갱신.
