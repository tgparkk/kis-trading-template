# ORB v2 설계서 — 거래량 + KOSPI 시장환경 필터 (2026-05-18)

## 배경
- ORB v1(SL3%/TP6%) 30일 그리드 우승(+0.442%/일) → 168일 OOS 완전 붕괴(-0.425%/일, 승률 34.6%, MDD -58%)
- Scientist 진단: (1) 2026-04 반등기 표본 편향 (2) 이론 EV +0.11%/거래가 비용 0.44%/거래에 잠식
- 결재: ORB 재설계 우선 (거래량 + 시장환경 필터 추가)
- 상세 배경: [changelog-2026-05-18-orb-oos-failure.md](changelog-2026-05-18-orb-oos-failure.md)

## 목표
168일 OOS 합격선(일0.3%/승률50%/MDD-15%) AND IS/OOS 일관성 30% 이내 통과하는 분봉 ORB v2 도출.

---

## §1. 아키텍처 & 컴포넌트

### 디렉토리 (신규)
```
RoboTrader_template/strategies/intraday/orb_v2/
  ├── __init__.py
  ├── config.yaml
  └── strategy.py  # OrbV2Strategy(IntradayBaseStrategy)
```

### 기존 파일 수정 (최소 침습)
| 파일 | 변경 |
|---|---|
| `strategies/intraday/_base_intraday.py` | `set_daily_context(date, ctx_dict)` 추가 — 기본 no-op |
| `backtest/engine.py: run_minute()` | 일자 루프에서 strategy에 set_daily_context 1회 호출 |
| `scripts/run_intraday_tournament.py: STRATEGY_REGISTRY` | `orb_v2` 1줄 추가 |
| `utils/intraday_indicators.py` | `cumulative_volume_ratio()` 1개 헬퍼 추가 |

### 외부 데이터 의존성
1. **전일 일봉 거래량** — 시나리오 시작 시 종목 리스트 × 168일 일괄 조회 → dict 캐시
   - **소스 우선순위**: `daily_candles` 테이블 (executor가 존재 확인). 미존재/결손 종목은 `minute_candles`에서 일별 sum(volume) fallback
2. **KOSPI 분봉** — `minute_candles`에서 168일 전부 1회 로드
   - **소스 우선순위**: KOSPI 지수 코드 (executor가 DB에서 사용 가능한 코드 확인 — 예: '0001', 'KS11', 'KOSPI' 후보 점검). 미존재 시 KOSPI200 ETF `069500` 분봉으로 대체 (현실적으로 KOSPI와 0.99+ 상관)
   - **최종 결정 (2026-05-19 점검)**: minute_candles에 KOSPI 지수 코드(0001/KS11/KOSPI/001/U001) 및 KOSPI200 ETF(069500) 모두 0건. use_market_filter=True 시 §3 "KOSPI 분봉 당일 완전 결손 fallback(통과)" 정책이 상시 적용됨. 그리드에서 use_market_filter=False/True 비교는 유효하나 True 시나리오는 필터 미작동 상태로 실행됨에 유의.
   - dict: `{date_str: {"prev_close": float, "minute_df": DataFrame[datetime, close]}}`

### 전략 본체 로직 (의사코드)
```python
def generate_signal(self, stock_code, data, timeframe):
    # 기존 ORB v1 게이트 (시간 + 박스 완성 + 돌파)
    if not breakout_detected(): return None

    # 필터 1: 거래량 (당일 누적 / 전일 일봉 비율)
    cum_vol_today = data["volume"][:cur_idx+1].sum()
    prev_day_vol = self._daily_ctx["prev_day_volume"].get(stock_code, 0)
    if prev_day_vol > 0:
        ratio = cum_vol_today / prev_day_vol
        if ratio < self._volume_ratio_threshold: return None
    # prev_day_vol==0 → fallback: 필터 미적용 (WARNING 로그)

    # 필터 2: 시장환경 (KOSPI 전일종가 위)
    if self._use_market_filter:
        kospi_now = self._lookup_kospi_at(last_dt)
        if kospi_now is None:  # 데이터 결손 fallback
            pass  # 통과
        elif kospi_now <= self._daily_ctx["kospi_prev_close"]:
            return None

    return self._make_buy_signal(...)
```

---

## §2. 그리드 & 평가 설계

### 기간 분할
- 전체: 2025-09-01 ~ 2026-05-15 (skip 202603)
- IS = 앞 70% ≈ 118일 (2025-09-01 ~ 2026-02-28)
- OOS = 뒤 30% ≈ 50일 (2026-04-01 ~ 2026-05-15)
- 토너먼트 결과 parquet에서 일자별 PnL 추출 → 사후 분할 (재실행 불필요)

### 파라미터 그리드 (96 시나리오)
| 파라미터 | 값 |
|---|---|
| `volume_ratio_threshold` | 0.5, 1.0, 1.5, 2.0 |
| `use_market_filter` | False, True |
| `max_positions` | 3, 4, 5 |
| SL / TP | (0.02, 0.04), (0.02, 0.06), (0.03, 0.04), (0.03, 0.06) |
| `box_minutes` | 30 (고정) |
| universe | dynamic (고정, top_n=50, volatility_pct) |

총 = 4 × 2 × 3 × 4 = **96 시나리오**, 예상 시간 ~2~3h (workers=8)

### 합격 조건 (3중 게이트)
1. **IS 합격선**: 일수익률 ≥ 0.3% AND 일승률 ≥ 50% AND MDD ≥ -15%
2. **OOS 합격선**: 같음
3. **일관성 게이트**: `|IS_daily - OOS_daily| / |IS_daily| < 0.30`

### 우승자 선정
- 3중 게이트 모두 통과한 시나리오 → OOS 종합점수 1위
  - `composite = 0.4×z(OOS_daily) + 0.3×z(OOS_win) + 0.3×z(OOS_calmar)`
- 0개 통과 → ORB 폐기 + 분봉 보류 결재 / 1+ 통과 → Phase 4 페이퍼 결재

---

## §3. 에러 처리 & 테스팅

### Fallback
| 결손 | 처리 |
|---|---|
| 전일 일봉 거래량 = 0 또는 미존재 | 해당 필터 미적용, 통과시킴, WARNING 로그 (종목 단위 1회만, 같은 종목 반복 로그 금지) |
| KOSPI 분봉 부분 결손 (특정 분봉 시각만 누락) | **forward-fill**: 그 시각 이전 가장 가까운 유효 분봉의 close 사용 |
| KOSPI 분봉 당일 완전 결손 | use_market_filter=True여도 통과 (거래 기회 0건 방지), WARNING 로그 |

### 테스트 (신규/확장)
| 파일 | 범위 |
|---|---|
| `tests/test_intraday_strategies_part_c.py` | OrbV2Strategy 단위 — 거래량 통과/실패, KOSPI 통과/실패, 조합, ctx 누락 fallback |
| `tests/test_intraday_indicators.py` (확장) | `cumulative_volume_ratio()` 단위 |
| `tests/test_backtest_engine_minute.py` (확장) | set_daily_context 호출 검증, ctx 결손 fallback |

### Smoke
```powershell
python RoboTrader_template/scripts/run_intraday_tournament.py `
  --start 20260401 --end 20260408 `
  --strategies orb_v2 `
  --universe dynamic --max-positions 4 `
  --sl-grid 0.03 --tp-grid 0.06 `
  --workers 4 `
  --out RoboTrader_template/reports/orb_v2_smoke
```
5영업일 smoke (~5분). 통과 시 본 96-시나리오 실행.

---

## §4. 산출물 체크리스트

1. **신규 코드** (executor 위임)
   - `strategies/intraday/orb_v2/{__init__.py, config.yaml, strategy.py}`
   - `_base_intraday.py: set_daily_context()` 추가
   - `utils/intraday_indicators.py: cumulative_volume_ratio()` 추가
   - `backtest/engine.py: run_minute()` 일자 루프에 set_daily_context 호출 + KOSPI/prev_vol 사전 로드
   - `scripts/run_intraday_tournament.py: STRATEGY_REGISTRY['orb_v2']` 등록
2. **테스트 추가** (executor 위임)
3. **회귀 PASS** 확인 (executor 위임)
4. **smoke 통과** 확인
5. **토너먼트 96 시나리오 실행** (Claude 직접, 백그라운드 ~2~3h)
6. **결과 분석** — 3중 게이트 통과 시나리오 추출 (scientist 또는 직접)
7. **changelog 작성** — `changelog-YYYY-MM-DD-orb-v2-results.md`
8. **결재**: 통과 시 Phase 4 페이퍼 / 미통과 시 ORB 폐기 + 분봉 보류

---

## §5. 위임 정책 (메모리 규칙)
- 관리자(Claude)는 코드 직접 수정 금지 → executor 에이전트 위임
- 작업 단위:
  - Task A (executor): 코드 구현 (1~3, 위 §4) — 1일 예상
  - Task B (Claude): smoke 검증 + 96 시나리오 토너먼트 실행
  - Task C (scientist 또는 Claude): 결과 분석 + 3중 게이트 평가
  - Task D (Claude): changelog 작성 + 사장님 결재

## §6. 관련 문서
- [changelog-2026-05-18-orb-oos-failure.md](changelog-2026-05-18-orb-oos-failure.md) — 본 설계의 동기
- [changelog-2026-05-17-tournament-grid-orb-winner.md](changelog-2026-05-17-tournament-grid-orb-winner.md) — ORB v1 30일 우승
- [changelog-2026-05-16-phase1-infra.md](changelog-2026-05-16-phase1-infra.md) — 분봉 인프라
