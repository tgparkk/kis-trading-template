---
name: todo-2026-08-27
description: 2026-08-27 장 마감 시점 TODO 통합 목록 — ① 매매 동작 오류 ② 8전략 고도화 ③ 데이터·인프라. 각 항목 = 무엇이 문제인지(예시) · 지금 상태 · 다음 한 걸음. 사장님 「쉽고 자세히」 요청으로 작성.
metadata:
  type: project
---

# TODO 통합 목록 (2026-08-27 장 마감 기준)

> 읽는 법: **🔴 = 돈이 새거나 잘못 사는 것** · 🟡 = 실탄 전환 전 반드시 · ⚪ = 연구/정리.
> 「상태」의 **코드 0줄** = 아직 아무것도 안 고쳤고 조사만 끝났다는 뜻.

---

## ① 매매 동작 오류 (봇이 «틀리게» 사고파는 것)

### 1. 🔴 rs_leader 매수·매도 룰 «동시 참» → 0초 왕복
- **쉽게**: 어제 종가 하나를 보고 「사라」와 「팔아라」가 같이 나온다. 그래서 사자마자 같은 초에 판다(`049080` 3일 연속, 오늘 `270520`). 실손익 0 이지만 **그 자리에 다른 종목을 살 기회를 버린다**.
- **상태**: 원인 확정. 수정 브랜치 `fix/rs-leader-entry-exit-overlap`(`16d97b8`) 만들어 두고 **미머지**(반증예측 측정용으로 일부러 둠). → **✅ 완료: `45c5a5d`(2026-08-27 16:35) 로 main 머지** — 진입에 `종가 > MA20` 추가(`rule.py`·`strategy.py`·README·config).
- **다음**: 측정은 충분(4회 적중). ①안(안 사는 게 아니라 **다른 종목**을 삼) vs 현행 비교 한 번 → 사장님 머지 결정. → 종결(머지됨).

### 2. 🔴 rs_leader 「RS 리더」 = 사실은 «주식병합 배율 랭킹» (N-1)
- **쉽게**: 10주→1주 병합한 종목은 데이터상 주가가 10배 뛴 것처럼 보인다. 봇은 이걸 「강한 종목」으로 착각해 산다. rs_leader 매수 71건 중 53건(74.6%)이 이런 종목, 손실 −1,658,007 vs 정상 종목 −69,700.
- **상태**: 조사 M1·M2·M3 완료(코드 0줄). `adj_factor` 는 고장이 아니라 **증분 writer 가 원래 안 쓰는 컬럼**. 가격 규약이 2026-04-27 전후로 다름.
- **다음**: M2 재분석 verifier(읽기 전용) → 사장님 승인 → **보정 사전등록 ②** → 그 뒤에 고침. 🔒사장님 결정: rs_leader 끄지 않고 데이터 고쳐서 컨셉대로.

### 3. 🔴 포지션 사이징 = 「남은 현금 전부」
- **쉽게**: 한 종목에 423원 사기도, 636만원 사기도 한다. 룰이 종목당 얼마인지 안 정해서 **이익 나는 전략도 금액가중으로는 손실**(ma20: 건당 +0.29% → 금액가중 −1.6%).
- **상태**: 결함 확정, 코드 0줄. `max_capital_pct` 상한도 **아무것도 안 함**(provider 미주입). → **일부 완료: `fe02983`(08-27) 로 `max_per_stock_amount` 캡 결선**(`get_max_quantity` min() 3항 · 8전략 yaml 전부 선언). 현행 페이퍼 사이징 = 자본/K + 재기동 복리 재산정 + 캡(→ `docs/PAPER_STRATEGIES.md` §0.4). `max_capital_pct` 미배선은 **그대로**(09-17 확인 — `main.py` 가 `FundManager(max_daily_loss_ratio=…)` 로만 생성).
- **다음**: 전문가 자문 순서상 「안전장치」 단계. 사이징 룰 1개(예: 총자금÷K) 사전등록 → 구현. → 페이퍼 쪽은 위 캡 결선으로 닫힘 · 실탄 경로는 별건.

### 4. 🔴 손절 단일 경로 타임아웃 + 09:00~09:05 손절 공백
- **쉽게**: 손절은 `position_monitor` 한 곳에서만 본다. 그게 멈추면 아무도 안 판다. 8/25 sl/tp 위임 후 장 시작 5분간은 손절이 안 걸린다(54일 중 2일 해당, 수용함).
- **상태**: 전문가 3인이 「안전장치 2순위」로 지목. 코드 0줄.
- **다음**: 타임아웃/감시 로그 1줄 추가 설계(계기 먼저).

### 5. 🟡 `max_daily_trades` 가 매도도 센다 (3전략)
- **쉽게**: 「하루 5번만 사라」인데 파는 것도 세서 실제론 2~3번밖에 못 산다.
- **상태**: 08-24 검수로 확정(N-2), 코드 0줄. **다음**: 한 줄 수정 + 테스트, 사장님 승인 후.

### 6. 🟡 거래정지 «패딩봉»이 거래량 룰 분모를 채워 실매수 발화
- **쉽게**: 정지된 날을 가짜 봉으로 메워 넣은 게 「거래량 급감」처럼 보여 샀다(`090150`).
- **상태**: 원인 확정, 보정 도구 `--apply` 막힘(volume 게이트 10/10 위반). → 항목 ③-1 과 같은 데이터 층.

### 7. 🟡 실탄 전환 전 P1 묶음 (지금은 페이퍼라 무해)
- 실주문 경로 라이브 미검증(휴장일에만 가능, 다음 창 **9/24~28 추석**) · 텔레그램 `enabled=false`(경보가 아무 데도 안 감) · 상한 없음(#3) · 「주문 가능 금액」은 VTM(현금주의) 정의로.
- **다음**: 사장님 입력 4개(총자금 cap · 전략 2개 · 계좌/앱키 · 텔레그램) 오면 착수. 🟢안 하면 페이퍼 그대로.

### 8. ⚪ 정합 보정 −1,44x / +2,025 / −774 원인
- **쉽게**: 매일 아침 가용자금이 어제 EOD 와 몇백~천 원 다르다.
- **상태**: 🆕오늘 **「부팅 가용 = 전일 `paper_trading_state` 잔고」** 가설 세움(−774 정확 일치). **다음**: 내일 부팅 19,565,968 이면 확정 → 8/24·25 소급 → 종결.

---

## ② 가상매매 8전략 고도화 (룰을 «더 잘» 사고팔게)

> 🔑 대원칙(전문가 3인 합의): **계기(로그·벤치마크) → 안전장치 → 데이터(N-1) → 룰은 맨 마지막**. 룰부터 손대지 말 것.
> 🔑 지금까지 검정 결과: 「정보다」로 살아남은 건 **minervini TT·재무** 둘뿐. 랭킹·dryup·ma5 방향·청산 방식은 **정보 아님**.

### 0. ⚪ 계기 먼저 — 벤치마크·거절 로그·텔레그램
- **쉽게**: 페이퍼 기간 KOSPI −23.6% 인데 우린 −19.5%. 시장 대비 얼마나 잘/못했는지 매일 한 줄이 없다. 매수 거절(밴드·한도)도 로그에 안 남아 «왜 안 샀는지» 모른다.
- **다음**: EOD 리포트에 벤치마크 1줄 + 거절 INFO 로그. 코드 소량, 매매 무영향.

### 1. 🟢 minervini — TT 게이트 on 발효(8/26~), 관측 중
- **상태**: 후보 2→5. 판정은 20거래일/20건 후. **TT 반사실 원장**(`run_ledger.py`, 사전등록 `02498ad`) 작성만 하고 **미실행**.
- **다음**: `--dry-run` → 재현성 게이트 → 8/18~ 소급 → 매일 1줄을 EOD 점검표에.
- 남은 축(SSOT `minervini-remaining-worklist.md`): **A-1 매수 방아쇠 방향 반대**(가장 무거움) · VCP 베이스 과검출(leg 6~7) · 돌파 방아쇠 사전등록 `dad42f7` 미실행 · 재무 갱신 수집기 없음(꽂는 자리 `match()`).

### 2. ⚪ rs_leader — #①-1·2 고친 뒤에야 컨셉 평가 가능
- README 「트레일이 주청산」인데 실측 손절 50·익절 22·트레일 3. 당일 사망률(보유 216h→1.4h) 추적 중. 룰 변경은 데이터 고친 후.

### 3. ⚪ ma20 / ma5 (`book_pullback_*`)
- `_recent_surge` 방향 없음(하락형 55/102) + `rule_ma5_pullback` 이봉 분리(15/102) · **함수가 두 벌**(`haru_silijeon:52` / `trading_legends:49`). 사전등록 표적. ma5 B반 「열지 말 것」 권고 — 사장님 결정 대기.
- 섹터 축은 `stock_industry`(KSIC) 쓰기로.

### 4. ⚪ daytrading — 고유 청산 굶김(0/106) · 진입 밴드 코드에만 존재
- 룰이 있어도 실행된 적 없는 코드가 많았다(8/25 위임으로 정리). 개념축 ④ 미착수.

### 5. ⚪ elder / envelope / 나머지
- elder score = 종가(901/901, 10건 컷 legacy) · envelope 가드 순서 «뒤»(N-4). 검수 결정 13건(`docs/audit_2026-08-23·24/SUMMARY.md` §5·§6) **사장님 결정 대기**.

### 6. ⚪ 개념축 프로그램 (`backtest/concept_axes/REGISTRY.md`)
- ①minervini ✅ ②ma5 ✅ ③ma20 ⬜ ④daytrading ⬜. 귀무 재설계(크기 정합·시드 100) 반영 필요.

---

## ③ 데이터·인프라 (매매에 간접 영향)

1. 🔴 **N-1 보정 사전등록 ②** — ①-2 와 동일. 가격 규약 혼재 → 4/27 전/후 축.
2. 🔴 **불가능봉·패딩 보정 도구** `--apply` 막힘 · `adj_factor≠1` 99종목 규약 전수 측정 먼저.
3. 🟡 **`robotrader` DB 폐기 4단계** — 2주 관측(8/17~) → `GRANT SELECT` → `pg_dump` → DROP. ⚠️470만 행 「0행 오판」 재발 방지.
4. 🟡 일봉 결손 49,252행 복구(원인은 고침) · 팬텀 세션 293,093+167,951행 relabel · `corp_action_watch` 큐 234건 pending.
5. ⚪ 재무 수집기 C안(63스텝) 미착수 · 재무 PIT 필터 P2B · 시총 백필 v3 사전등록 · 급락게이트 임계값 논의 · 뉴스 타임아웃.
6. ⚪ untracked 문서 23편(audit 18 + docs 5) 커밋 여부 · 🔴**메모리 폴더 백업**(git 밖, 단일 디스크).

### 🆕 7. ⚪ 오버행 데이터 수집 과제 (착수 «보류» · 2026-09-14 밤 결정 C-7 로 큐 등재만)

- **쉽게**: 「지금 전환가 아래에 묶여 있는 CB/BW 물량이 얼마나 되나」 · 「보호예수가 언제 풀리나」를
  **DB 로는 물을 수 없다.** 컬럼이 아예 없다. ⇒ 이건 **검정 과제가 아니라 «수집» 과제**다.
- **상태**: 코드 0줄 · **착수 보류**(재무 수집기 8과제·63스텝이 아직 미착수다 — **줄을 세워야지 끼워 넣을 자리가 아니다**).

**필요 필드 4** (이게 없어서 못 묻는 것)

| # | 필드 | 지금 상태(실측) |
|---|---|---|
| 1 | **CB/BW 미전환 «잔량»** | 🔴 없다 — `information_schema` 전수(`convert\|cb\|bw\|warrant\|bond\|dilut\|lockup`) **히트 0건** |
| 2 | **전환가(행사가) vs 현재가** | 🔴 없다 — `news` 제목 `전환가액\|행사가액` **501건**이 전부이고 **2025-12-17 이후**뿐 |
| 3 | **유증 신주 «상장일»** | 🔴 없다 — 제목 `신주상장\|추가상장` **3건** · `corp_events.meta` 키는 `rcept_dt·rcept_no·report_nm·source` **4개뿐** |
| 4 | **보호예수 해제일** | 🔴 없다 — 제목 `보호예수\|의무보유` **33건 / 21만 건 중** = 사실상 0 |

**소스 후보**(이름만 · 오늘 착수 권고 아님)
- **DART OpenAPI 주요사항보고서 «원문»** — `전환사채권발행결정`·`신주인수권부사채권발행결정`
  (**전환가액·발행총액·전환청구 개시일이 본문에 있다**) · 정기보고서의 **증자(감자) 현황**
- **KRX KIND 의무보유(보호예수) 등록·해제** · KRX **추가상장(유상증자 신주) 일정**

🔴 **인계 3줄 (받는 사람이 반드시 같이 읽을 것 · C-7 필수 문면)**
1. **`corp_events` 는 `news` 유상증자의 «59%»만 덮는다** — 2026 동일 창 실측: 종목 **258 vs 435** ·
   news 에만 **182종목** · corp_events 에만 5 · 날짜 일치 85.8%.
   ⇒ ***「5.5년을 덮는 유일한 원장」과 「그 기간을 «다» 덮는 원장」은 다른 말이다.***
2. **A3 «사건» 절반은 이미 답할 수 있다** — 「이번 달에 CB 발행결정이 났나」는
   `NW2`(`docs/prereg_2026-09-14_news_event_curve.md`)의 **D6 발행결정**이 묻는다.
   🆕🔴 **2026-09-15 정정 (비판 리뷰 #8)**: 그 건수는 **801(미연결 포함) / «761»(연결분) · 주검정 = 761** 이다.
   ~~「801건」~~ 은 **`stock_code` 가 안 붙은 40건을 포함**한 원시행 수였다(SQL 재현 `backtest/concept_axes/_defs/nw2_counts.sql`).
   ⇒ ***A3 를 통째로 「기각」이라 인계하면 과잉기각이다.*** 🔴 **그러나 「801」을 단독으로 인용하면 과대계상이다** — 병기할 것.
3. **정말 없는 것은 «잔량 · 전환가 · 해제일» 셋뿐이다** — 이 수집 과제의 범위는 딱 그 셋이다.
   🔑 ***커버리지를 2배로 고쳐도 «없는 컬럼»은 생기지 않는다*** — 그래서 이건 커버리지 문제가 아니라 수집 문제다.

⚠️ **부산물(인쇄만 · 오늘 검정 금지)**: `rights_issue` D-60 적중이 **rs_leader 만 혼자 높다**.
🔴 인용은 **긴 창(2026-06-05~09-11) 값으로** — rs_leader **13.07%** vs ma20 3.63%(= 3.6배).
짧은 창(12거래일)의 「20.0% = 3~4배」는 **과장**이다(CRITIC §1-4).

---

## 🎯 관리자 추천 순서 (다음 1~2주)
1. **계기**(②-0) — 벤치마크 1줄·거절 로그 → 매매 무영향, 이후 모든 판단의 눈.
2. **rs_leader 겹침 머지 결정**(①-1) — 측정 끝. 사장님 한 마디면 됨. → ✅ `45c5a5d`(08-27) 머지 완료.
3. **N-1 사전등록 ②**(①-2/③-1) — M2 verifier 부터.
4. **사이징 룰**(①-3) — 실탄 전 필수. → 페이퍼 캡 결선 `fe02983`(08-27) 완료 · 실탄 경로는 별건.
5. minervini TT 반사실 원장 dry-run — 병렬로 언제든.
