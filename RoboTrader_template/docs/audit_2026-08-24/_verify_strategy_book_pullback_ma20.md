# 검증 — `strategy_book_pullback_ma20.md` (2026-08-24)

> 검증 직원 · **읽기 전용**(코드 수정 0 · git 쓰기 0 · 라이브 트리 python/pytest 0). 판정 = 코드 독해 + DB SELECT + 로그 grep, 전부 **독립 재도출**(보고서의 `scratchpad/ma20/q*.sql` 은 참고만 하고 신뢰하지 않음 — 재현 SQL 은 별도 파일 `scratchpad/v_ma20_1.sql` ~ `v_ma20_5.sql`, 실행은 `PGCLIENTENCODING=UTF8 psql -h 127.0.0.1 -p 5433 -U robotrader -d kis_template -X -A -F'|' -f <file>`).
> 판정 등급: **SUPPORTED**(독립 재현 일치) / **REFUTED**(재현 불일치 또는 논리 오류) / **UNVERIFIABLE**(DB·로그·코드로 판별 불가). 부분 판정은 `SUPPORTED(부분)` 로 쓰고 무엇이 틀렸는지 적는다.
> ⚠️ `daily_prices` 는 살아 있는 표 — 아래 SQL 수치는 2026-08-24 아침 시점.

---

## 0. 판정 요약표

| # | 검증 항목 | 판정 | 핵심 근거 (재현 수치) |
|---|---|---|---|
| 1 | M20-1 `_recent_surge` 위치·별도 사본·import · 55/102 하락형 | **SUPPORTED** | `diff` 본문 10줄 동일·정의 2개 · `strategy.py:42`·`screener.py:9` 둘 다 `haru_silijeon` · SQL 102건 중 down_first **55** / up_first **47** / 동일일 0 |
| 2 | M20-2 `078130` +949%(패딩 제외 +68%) · `046390` +174% · 패딩 vs 실제 변동 | **SUPPORTED(부분) — 영향 등급 과대** | 폭 949.3/67.9 · 174.0/94.6 재현 ✅. 그러나 **패딩 행 수는 5(05-13~05-19)·9(05-12~05-22)** 이지 8·11 이 아님. 🔑**패딩 제외 폭이 4/4 매수·14/14 스냅샷 전부 ≥25% ⇒ 패딩이 «결정»을 바꾼 사례 0**. +791% 의 정체(감자)는 `corp_events` 0행이라 **UNVERIFIABLE** |
| 3 | M20-4 고유 청산 7/97 · 06-27 이후 0/66 · 7건 = 분봉 잔재 | **SUPPORTED** | 손절 51 / 익절 36 / MA20 trailing 7 / 공통 trailing 3 · 06-27 이후 stop 33 + tp 33 = 66, 고유 0 · 7건 전부 매수 당일 09:02~09:36 매도, 06-12~06-16 = `1b73ca5`(06-18) 이전 |
| 4 | M20-3 variant A/B 라인 인용 · README 0.44 = B | **SUPPORTED** | `run_haru_silijeon_daily.py:45-50,53-55,182-185` 내용 일치(라이브 트리 파일 실재) · `report.md:46-51` Variant B 표 `daily_ma20_pullback … 0.44` · `config.yaml:31-34` mh 50 / trail 20 |
| 5 | M20-5 조건 2 함의 · 0/102 | **SUPPORTED** | 논리: `touched ⇒ low ≥ 0.98·ma20`, `close ≥ low` ⇒ `above`. SQL `close < ma20` **0/102**, `above` 102/102 |
| 6 | M20-9 카운터가 매도도 셈 | **SUPPORTED(독해+로그 보강)** | `strategy.py:148` 은 `is_buy` 분기 «앞». 08-21 로그에 `📤 [PAPER] 매도 체결` 2건(:165 는 `on_order_filled` 의 `elif` 안) ⇒ 매도 체결이 콜백에 도달함이 실측됨. 실제 «차단» 사례는 원리적으로 로그 없음 |
| 7 | §6 정정 6건(#1·#5·#13·#14·#15·#17) | **SUPPORTED 6/6** (#5 는 표현 보강 필요) | 아래 §7 |
| 8 | 손계산 `006340` MA20 13,926.00 | **SUPPORTED** | 20봉(07-23~08-20) 평균 **13,926.0000** · low 13,940 / close 14,530 / open 14,280 · 창 고 16,370(08-05)/저 9,210(07-09) = +77.7% · score 14,136,889.20 vs 스냅샷 14,130,942.25 |

부수: 인용 라인 드리프트 5건(§9) — 판정에 영향 없음.

---

## 1. M20-1 — `_recent_surge` 위치·사본·방향 없음

**(a) 별도 사본인가** — SUPPORTED.
```
diff <(sed -n '49,58p' strategies/books/trading_legends/rules_daily.py) <(sed -n '52,61p' strategies/books/haru_silijeon/rules_daily.py)   # 출력 없음 = 본문 동일
grep -rn "_recent_surge" strategies/ --include=*.py
#  haru_silijeon/rules_daily.py:52 def · :108/:157/:330 호출
#  trading_legends/rules_daily.py:49 def · :286 호출
```
두 정의는 본문 10줄이 글자까지 같고 **서로 import 하지 않는다**. ⇒ 「공유」가 아니라 「두 벌」 — 보고서 정정 옳음. 덧붙임: haru 사본은 ma20 외에 haru 일봉 룰 2개(`:157`,`:330`)도 쓰므로 haru 쪽을 고치면 그 룰들도 같이 움직인다(라이브엔 ma20 만 실림).

**(b) 누가 import 하나** — SUPPORTED. `strategy.py:42` `from strategies.books.haru_silijeon.rules_daily import _ma, rule_daily_ma20_pullback` · `screener.py:9` 동일 모듈. `book_pullback_ma5/strategy.py:42` 는 `trading_legends`.

**(c) 매수 102건 정의** — SUPPORTED. `virtual_trading_records` `strategy='book_pullback_ma20'`: BUY **102**(06-10~08-21) · SELL **97** · 미청산 **5**(`v_ma20_1.sql` 앞부분 별도 실행).

**(d) 하락형 55/102 재현** — SUPPORTED. `v_ma20_2.sql` V6: 신호봉 = 매수일(KST) 직전 일봉(rn=1), 창 = rn 2..31(30봉). 창 최저가 날짜 vs 최고가 날짜:
`up_first 47 · down_first 55 · same_day 0 · win_full 102 · avg_range 85.5%`. 보고서 숫자와 전건 일치. 판정 방법(최저·최고 «날짜» 순서, 동가 시 앞 날짜)도 보고서와 동일하게 재구성됐다.

부수 확인: `ma20_match 102 · bar_match 102 · touched 102 · above 102 · bullish 102 · surged 102` — 「102/102 코드대로 돌았다」 재현.

---

## 2. M20-2 — 정지 패딩 + 미조정 상승봉

**(a) 수치 재현** — `v_ma20_2.sql` V7 / `v_ma20_4.sql` V20:

| 종목 | 매수일 | 창 | 패딩 행 (보고서) | 패딩 행 (**재현**) | 폭 전체 | 폭 패딩 제외 |
|---|---|---|---|---|---:|---:|
| `078130` | 06-29 | 05-13~06-25 | 8행(05-08~05-19) | **5행(05-13~05-19)** | **949.3%** ✅ | **67.9%** ✅ |
| `046390` | 06-26 | 05-12~06-24 | 11행(05-08~05-22) | **9행(05-12~05-22)** | **174.0%** ✅ | **94.6%** ✅ |
| `115610` | 06-15 | | 3행 | 3행 ✅ | 159.9 ✅ | 112.2 ✅ |
| `065170` | 07-20 | | 1행 | 1행 ✅ | 249.8 ✅ | 249.8 ✅ |

폭·저점·고점은 전부 일치하나 **패딩 행 수 2건이 틀렸다**(05-08~05-12 는 30봉 창 밖이다 — V20 이 31행을 날짜까지 인쇄). 판정엔 영향 없음, 표만 정정.

**(b) 패딩 아티팩트인가, 실제 변동인가** — 구분해서 판정:
- `078130`: 04-28~05-19 **15거래일** OHLC=436·volume=0(정지 패딩, `adj_factor=1`), 05-20 시가 4,300(+791%, `adj_factor` 전 이력 1 = 미조정). 비율 ≈ ×9.9 ⇒ 10:1 감자·거래정지 패턴과 정합하나 **`corp_events` 에 두 종목 다 0행**(`v_ma20_4.sql` V21) ⇒ 「감자」는 **추론**이지 DB 증거가 아니다. 보고서의 「미조정 감자 해제 봉」은 **UNVERIFIABLE** — 「미조정 점프」까지만 말할 수 있다.
- `046390`: 04-30~05-22 **17거래일** 991 고정, 05-26 시가 2,195(+111.9%). ×2.2 는 정수 감자비와 맞지 않는다 — 정체 불명(UNVERIFIABLE).
- 창 안에서 «최저가»를 만든 것은 두 종목 다 **패딩 행**(436·991)이고 «최고가»는 **해제 첫 봉**(4,575·2,715)이다 ⇒ 「패딩이 분모, 미조정 점프가 분자」 메커니즘 서술은 SUPPORTED.

**(c) 🔑 결정 영향 — 보고서가 안 잰 것** — `v_ma20_5.sql` V28: 스냅샷 812행 중 31봉 창에 패딩이 든 **14행/12종목**(재현 일치) 전부에 대해 「`volume=0` 행을 창에서 빼고 다시 잰 폭」을 계산:
```
078130 2026-06-26  pad 5   949.3% → 67.9%   pass_nonpad = t
046390 2026-06-25  pad 9   174.0% → 94.6%   t
097800 2026-06-11  pad 14  505.9% → 67.0%   t
900300 2026-07-16  pad 14  6394.5% → 228.3% t
... (14행 전부 pass_all = t, pass_nonpad = t)
```
**14/14 이 패딩을 빼도 ≥25% 를 통과**하고, 매수 4/4 도 마찬가지(67.9 / 94.6 / 112.2 / 249.8). ⇒ 이 표본에서 **패딩이 후보 선정이나 매수 «결정»을 바꾼 사례는 0건**이다. 보고서 §2-5 「급등 이력을 위조」·§0 「「+949% 급등」을 위조했으며(`078130` 실매수)」는 «폭의 크기»에 대해선 참이지만 «매수가 패딩 때문에 일어났다»로 읽히게 쓰여 있고, 실제로는 패딩 없이도 같은 매수가 났다. 「위생 가드가 상승을 못 잡는다」(`data_sanity.py:81-82` `ret < threshold` ✅ 독해 일치)는 구조 지적으로 유효.

⇒ **등급 제안: M20-2 🔴🔴 → 🔴** (메커니즘 실재·가드 방향 결함 실재, 단 이 전략에서의 결정 영향 0/102·0/14 실측. 08-23 D3 의 daytrading 「실매수 발화」와 같은 급이 아니다).

---

## 3. M20-4 — 고유 청산 굶김

`v_ma20_1.sql` V1~V5 재현:

| 사유 | n | 평균 | 합계 | 기간 |
|---|---:|---:|---:|---|
| 손절 실행(범용) | **51** | −8.76% | −6,790,607 | 06-10~08-21 |
| 목표 익절 도달(범용) | **36** | +12.49% | +4,468,536 | 06-10~08-20 |
| MA20 trailing 이탈(전략) | **7** | +1.57% | +192,585 | 06-12~06-16 |
| 트레일링 스톱(공통) | 3 | +4.75% | +34,650 | 06-12~06-18 |

06-27 이후: tp **33** + stop **33** = 66, 기타 **0** ✅. 최대 보유 **22 달력일**(max_hold 50 미도달) ✅.

**7건 = 분봉 잔재 근거** — SUPPORTED:
- 7건 전부 `buy_ts` 와 같은 날(09:02:46→09:07:02 등, V1 세 번째 표 = `q1` 세 번째 쿼리와 동일 결과). 사유 문자열 「종가 10150 < MA20 10184」는 일봉 종가라면 매수 5분 뒤에 생길 수 없다 — 분봉 close/MA20.
- `git log`: `1b73ca5` **2026-06-18** `exit_timeframe='daily'`(ma20 strategy.py 1줄 추가) · `a736065` **2026-06-24** timeframe 가드(`strategy.py:133-134` 현행 확인). 7건 모두 06-16 이전 ⇒ 두 수정 «전». 보고서 「06-24 이전」은 참이나 더 정확히는 **06-18 이전**.
- 현행 경로: `core/trading/position_monitor.py:339-361` 이 `generate_signal(..., timeframe='intraday')` 로만 호출 ✅ → `strategy.py:133-134` 가 `None` ✅ → 고유 청산은 `base.py:691-713` 일봉 루프뿐 ✅(`exit_timeframe=='daily'` 분기 `:694-696`). `position_monitor.py:206` 실시간가 · tp `:314-323` · sl `:325-335` ✅.

06월 21건 sl/tp 문턱 비표준(M20-12): V4 `after_0627=f → 21/21 nonstd`, `t → 0/66` ✅ 재현.

---

## 4. M20-3 — 청산 파라미터 vs 백테스트 variant

라이브 트리 `RoboTrader_template/scripts/run_haru_silijeon_daily.py` 실재(8/3 mtime, 최종 커밋 `00174ce`). 인용 라인 대조:
- `:45-50` `VARIANT_PARAMS` — A: sl 0.08 / tp 0.99 / mh **100** / use_trail True · B: sl 0.08 / tp 0.12 / mh **20** / use_trail False ✅ (보고서 「45-49」는 닫는 괄호 한 줄 차이)
- `:53-55` `RULE_TP_OVERRIDE = {"daily_ma20_pullback": 0.10}` ✅ · `:59-60` trail 20 ✅
- `:182-185` `elif trail_ma is not None and i + 1 >= trail_ma: … if cur_close < ma: exit_reason = "trail_ma"` — **`ret > 0` 조건 없음(무조건)** ✅
- `config.yaml:31-34` tp 0.10 / sl 0.08 / mh **50** / trail 20 ✅ · `:3` 「백테스트 검증값 1:1 반영」 ✅ · `strategy.py:241` `trail_ma is not None and ret > 0` ✅
- `report.md:44` 헤더 「Variant A(trail MA, sl 8% / mh 100) vs Variant B(sl 8% / tp 12%(ma20는 10%) / mh 20)」 · `:46-51` Variant B 표에서 `daily_ma20_pullback (tp 10%) | 695 | +16.00% | 0.44` ✅ · `:68` A 는 +5.75%/0.23 ✅ · `:117` 통합표 「ma20_pullback B … 0.44」 ✅ · `README.md:30` 「Sharpe 0.44 / +16%」 ✅

⇒ 「README 0.44 = variant B(trail 없음·mh 20)」 SUPPORTED. 「라이브 mh 50·조건부 trail 은 어느 variant 도 아님」 SUPPORTED. 등급 🔴 유지.

---

## 5. M20-5 — 조건 2 함의

`rules_daily.py:94-95` `touch_tol = below_tol = 0.02` ✅ · `:109` `above = last_close >= ma20*(1-below_tol)` · `:110` `touched = _touch_ma(last_low, ma20, touch_tol)` = `|low−ma20|/ma20 ≤ 0.02` ✅.
논리: `touched ⇒ low ≥ 0.98·ma20`; 유효 OHLC 에서 `close ≥ low`; ⇒ `close ≥ 0.98·ma20 = above`. **함의 성립**(반례는 `close < low` 인 손상 봉뿐 — 패딩 행은 4값 동일이라 반례 아님).
SQL(V6): `close_below_ma20` **0/102** · `above` 102/102 ✅. 등급 🔴(문서) 유지.

---

## 6. M20-9 — `max_daily_trades` 카운터

`strategy.py:147-148`:
```python
def on_order_filled(self, order: OrderInfo) -> None:
    self.daily_trades += 1          # ← is_buy 분기(:149) 앞
```
`:140` `if self.daily_trades >= self._max_daily_trades: return None` (매수 경로만, 매도는 `:137-138` 로 먼저 빠짐) ✅.
**콜백이 매도에도 오는가** — 로그 08-21: `📤 [PAPER] 매도 체결: 004310 @ 5,920 (수익률 -8.9%)`(09:05:01) · `475150`(09:05:37) — 이 문자열은 `strategy.py:165` 이고 `on_order_filled` 의 `elif` 안에서만 인쇄된다 ⇒ **매도 체결 시 카운터 증가 실측**(범용 손절이라도 소유 전략으로 라우팅됨, `order_completion_handler.py:197-217` · `trading_decision_engine.py:703,935`).
SQL(`v_ma20_3.sql` V15): 매매활동일 47 · 체결 ≥5 인 날 **22** · 매도 ≥5 인 날 3 · 07-29 매도 7/매수 3 ✅.
실제 차단 사례: `:140` 은 로그 없이 `None` ⇒ 로그로 판별 불가 — 보고서 자인과 같음. 🟡 유지, 단 「독해뿐」 → 「독해 + 콜백 도달 실측」으로 격상 가능.

---

## 7. §6 「정정」 6건 대조

| # | 원문 | 보고서 정정 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | 브리프: `trading_legends/rules_daily.py:49`, ma5 와 공유 | haru `:52` 별도 사본 | **SUPPORTED** | §1(a)(b) |
| 5 | 3축 `:243-246` 「MA20 이탈 7건 당일 왕복 … `rs_leader` 겹침과 같은 형태」(단 `:246` 「무조건 겹치는지 확인 안 했다」 자기단서 있음) | 분봉 잔재 · 일봉 겹침 구간 0/102 · `ret>0` | **SUPPORTED(표현 보강)** | §3. 단 「rs_leader 형태 아님」의 뜻을 명확히: 일봉 매수 조건(`close ≥ 0.98·ma20`)과 매도 조건(`close < ma20`)은 **[0.98·ma20, ma20) 구간에서 «겹칠 수 있다»**(구조적 불가능이 아님) — 실측 0/102 이고 rs_leader 처럼 «무조건» 겹치지 않는다는 뜻. 원문도 이미 그 단서를 달고 있었으므로 「정정」보다 「원문 단서의 확정」에 가깝다 |
| 13 | `README.md:24` 「K=5 / 종목당 200만」 | 200만 = 1,000만/5 · config 300만 미배선 · 실제 718원~636만 | **SUPPORTED** | `constants.py:124` `VIRTUAL_CAPITAL_PER_STRATEGY = 10_000_000` · `initializer.py:487-488` `allocate_strategy_capital(key, …, max_positions=k)` · `virtual_trading_manager.py:243-245` `per_stock = amount/max_positions` · `grep recommended_qty core/ bot/` **0건**(metadata 소비처 없음) · SQL V16 min 718 / max 6,360,000 / 먼지 21 / 1주 15 |
| 14 | 브리프 「섹터 축 미구현」 | 책 A-07 에 섹터 조건 없음 ⇒ 연구 축 | **SUPPORTED** | `reading_notes.md:75-85` 전문 확인 — 대상·지표·규칙·사례·목표수익률에 섹터·대장주 언급 없음 |
| 15 | 자문종합 `:87-88` 「라운드로빈 재시도(`main.py:422-424`, 전략당 ~72초)」 | 재시도 참 · 간격 ≈5분(77회/일) | **SUPPORTED** | 로그 08-21 `BookPullbackMa20Strategy … [on_tick] 매수검토` **77줄**, 타임스탬프 09:03:35 → 09:08:32 → 09:13:32 → 09:18:39 → 09:23:54(≈5분) · `003280` 09:08:30 쿨다운 스킵 → 09:13:32 체결 ✅. `main.py:422-424` 는 3초×3=9초 주기를 선언하므로 원문 72초(=8전략×9초)는 «선언값», 5분은 «실측» — 둘 다 적는 게 정확 |
| 17 | `README.md:28-30`·`config.yaml:3` | 0.44 = variant B · 라이브는 어느 variant 도 아님 | **SUPPORTED** | §4 |

「참」으로 판정한 12건 중 표본 확인: #2 47/55 ✅ · #3 0/102 ✅ · #4 `119830` 창 05-12~06-24 고 8,900 / 저 5,750 ✅(V24) · #7 5,944/1,224 = **20.6%** ✅(보고서 `q7_band.sql` 재실행, 시총 부분집합 1,483/438 = 29.5% ✅) · #8 51/36/max 22일 ✅ · #9 KOSPI bear 0 / bull 40 / 57일 ✅(V25) · #10 최대 동시보유 **10** · 일중 >5 **28/47** · EOD >5 **23/47**(V23; 사이징 문서 `:55` 의 25/55 는 분모가 «거래일 55»라 세 정의가 다르다 — 병기 필요) · #11 gross −2,094,836 / 금액가중 **−1.599%** / 단순평균 +0.29% / equity 08-21 7,477,191.43 ✅ · #12 score 항등 07-15 **20/20** · 08-20 **19/20** ✅(V22).

---

## 8. 손계산 `006340` (2026-08-20 신호봉 → 08-21 09:08:30 체결)

`v_ma20_2.sql` V8·V9:
- MA20 = 20봉(07-23~08-20) 종가 평균 = **13,926.0000** (종가 20개 인쇄: 14200 13730 13980 13600 11050 10320 13000 13110 13790 15450 14400 13790 14950 14520 16030 15110 14710 14110 14140 14530 → 합 278,520 / 20 = 13,926.0) ✅ 로그 `ma20=13926.00`
- 신호봉 low 13,940 / close 14,530 / open 14,280 ✅ (`touched` |13940−13926|/13926 = 0.10% · 양봉 +1.75%)
- 급등 창(rn 2..31): 고 **16,370**(08-05) / 저 **9,210**(07-09) → +77.7%, 저점 먼저 ✅
- score: 20봉 평균 조정거래량 **14,136,889.20** vs `screener_snapshots` 08-20 rank 1 **14,130,942.25**(차 0.042%) ✅
- 체결: `virtual_trading_records` id 2152 09:08:30 **114주 @13,800** ✅ · 13,800/14,530 −1 = **−5.02%** ✅ 로그 `가상매수: 006340 114주 @13,800 (익절:10.0% 손절:8.0%)`

---

## 9. 부수 발견 — 인용 드리프트·표현 (판정 불변, 정정 권고)

1. §2-5 표 패딩 행 수: `078130` 8→**5**, `046390` 11→**9**(§2).
2. §2-5 「스크리너가 06-26·07-16 두 번 후보로 올렸고」 — `screener_snapshots` `078130` 06-26 **rank 3** · 07-16 **rank 11**(V13). 07-16 은 20건 스냅샷엔 있으나 **10건 컷 밖** ⇒ 「후보(라이브 10)」는 한 번.
3. §3 표 A 행: 「`db/quant_daily_reader.py:161-190` … `:177-183` `describe_impossible_drop`」 — 위생 가드는 **`strategies/_rule_screener_base.py:177-178`**(`sane_view = df if self.sanity_window is None …; bad = describe_impossible_drop(sane_view)`). `quant_daily_reader.py:177` 은 SQL execute 줄.
4. §1 ④·§2-4 「`reading_notes.md:85`」 10% 익절 문구는 **`:84`**(`:85` 는 「외부데이터 의존 없음」).
5. §2-8 「매도검토 31~36종목」 — 08-21 실측 분포 31·32·35(17회)·36(37회)·37·38(18회)·**39** ⇒ 31~**39**.
6. §1 ③ 「실측 중앙 09:07」 — 매수 102건 체결시각 중앙값(percentile_cont) **09:08:31**(V27). 사소.
7. §2-4(b) 「06-24 이전의 분봉 MA20 경로 잔재」 — 7건 전부 **06-16 이전**이고 분봉 청산을 끊은 첫 수정은 `1b73ca5` **06-18** ⇒ 「06-18 이전」이 더 정확(06-24 `a736065` 는 2차 가드).
8. §1 ④ `position_monitor.py:310-330` — 현행 tp `:314-323` · sl `:325-335` · 공통 트레일링 `:287-308` · 전략 신호 `:339-365`(보고서 `:288-303`, `:334-362` 는 수 줄 어긋남).

---

## 10. 등급 변경 제안

| ID | 보고서 등급 | 제안 | 이유 |
|---|---|---|---|
| M20-1 | 🔴🔴 | **유지** | 55/102·두 벌·import 전부 재현. 단 보고서 자인대로 「고치면 좋아진다」는 미측정 — 등급은 «개념 위반의 크기»로 읽을 것 |
| **M20-2** | 🔴🔴 | **🔴 로 하향** | 메커니즘(패딩 분모·가드 하락만)은 실재하나 **패딩 제외 시에도 4/4 매수·14/14 스냅샷이 ≥25% 통과 = 결정 영향 0**(§2(c)). 08-23 D3 daytrading(정지 패딩이 실매수를 «발화»)과 동급으로 두면 과대. 「감자」는 UNVERIFIABLE 이므로 「미조정 점프」로 표기 |
| M20-3 | 🔴 | 유지 | 전 인용 라인·숫자 일치 |
| M20-4 | 🔴 | 유지 | 7/97 · 0/66 · 경로 독해 전부 일치. 「06-24 이전」→「06-18 이전」 |
| M20-5 | 🔴(문서) | 유지 | 함의 논리 성립 · 0/102 |
| M20-9 | 🟡 | 유지(근거 격상) | 「독해뿐」 → 매도 체결이 `on_order_filled` 에 도달함은 08-21 로그로 실측. 차단 사례는 원리적 관측 불가 |
| M20-12 | 🟡(보고) | 유지 | 21/21 비표준 · 0/66 재현 |

**총평**: 보고서의 수치·인용은 8개 검증 축 중 7개가 독립 재현으로 그대로 성립하고, 1개(M20-2)는 «크기»는 맞으나 «결정 영향»을 안 재서 등급이 한 단계 높다. 인용 드리프트 8건은 판정에 영향 없음.
