# `deep_mr_dev20` — 개념 ↔ 코드 3단 비교 검수 (2026-08-24)

> **읽기 전용 검수. 코드 0줄 수정 · git 쓰기 0회 · 라이브 트리 python/pytest 실행 0회.** 판단은 코드 독해 + `kis_template` SQL(SELECT) + 로그 grep. 산술 재현 1건(RSI)은 scratchpad 에서 순수 pandas 로만 했다(프로젝트 모듈 import 없음).
> 정본 3층: ①원저작 = **발굴 파이프라인 배치3**(책 아님) — `reports/discovery/gate_deep_mr_dev20.md` · `reports/discovery/CANDIDATES.md:55-100` · `reports/discovery/dev20_sizing_scenarios.tsv` · 백테스트 드라이버 `scripts/strategy_gate.py:220-227` + `scripts/exit_multiverse/portfolio_sim.py` + `scripts/discovery/exit_adapters.py:47-67` ②선언 = `strategies/deep_mr_dev20/README.md` · `config.yaml` · `docs/PAPER_STRATEGIES.md:115,147` ③라이브 = `strategy.py` · `screener.py` · `rule.py` + 공통 경로(`_rule_screener_base.py` · `core/trading/position_monitor.py` · `core/virtual_trading_manager.py`).
> 표기: **[독해]** = 코드 읽기만 · **[SQL]** = DB 실측 · **[로그]** = 로그 grep · **[산술]** = scratchpad 재계산.

---

## §0 한 줄 결론

**진입 룰(MA20 −20% + RSI14<30)은 원저작·README·코드가 «한 글자까지» 같고 47/47 매수가 SQL 로 재현되지만, 그 룰 «바깥»이 전부 다르다** — 원저작은 «정적 top300·다음날 시가·종가 판정 청산»인데 라이브는 «D-1 거래대금≥100억·장중 첫 틱·장중가 손절»이고, 그 결과 매도 46건 중 **전략 고유 청산은 0건(정상 경로 기준)·손절 33건(71.7%)·≤1거래일 청산 33건(71.7%)·손절 실현 평균 33건 −9.31%(6월 선언값 혼재 12종) · 선언 −7% 19건 −10.41%** 이다. 정지 패딩 행이 룰 창에 있던 매수는 **1/47(`025560`, 직전 20봉 중 13봉 volume=0)** 로 «측정»됐으나 그 매수의 원인은 패딩이 아니라 **미조정 재개 절벽(−75.5%)** 이고(패딩을 빼도 이탈 −83.0%·RSI 28.2 로 똑같이 매수), 그 절벽은 8/15 위생 가드가 이미 잡는다(가드 이후 매수 1건 0건). 사이징은 이 전략만 `paper_investment_per_stock` 이 실효(0/47 이 200만 초과)하나 **07-29 복리 재산정으로 지금은 128만원**(README 「200만」 불일치).

> 🔎 **독립 검증(2026-08-24, 별도 verifier — `_verify_strategy_deep_mr_dev20.md`)**: 항목 **34건** 재도출 — SUPPORTED 24 · 부분(숫자 맞고 라벨/해석 틀림) 8 · **REFUTED 1**(DM-2 「패딩이 MA20 을 부풀린다」 메커니즘) · 해소 1(§7-4 `sync_positions` 미확인 → 확인됨). 등급 변경 **2건**: **DM-2 🔴→🟡(재정의)** · **DM-3 🔴→🟡(K 부분은 08-12 `8238f91` 로 해소, `daily_trades` 만 잔존)**. 정정 전부 본문에 반영(§0 손절 라벨 · 유니버스 46/47 · 하락 42/음봉 39 · RSI 라벨 · ×0.99 · sl 0.030 5+1 · 사이징 첫 값 · 보유일 0일 4/1일 29 · 80봉 3/47 · 라인 3곳).

---

## §1 6축 × 2접합 표

분류: `의도된(문서화됨)` / `문서화 안 된` / `결함(개념을 망가뜨림)`

| 축 | 원저작(발굴 배치3) → README 이탈 | 분류 | README → 코드 이탈 | 분류 |
|---|---|---|---|---|
| **① 후보 유니버스** | 게이트 = **기간 전체 `SUM(close×volume)` 상위 300 정적 집합**(`scripts/book_param_multiverse.py:196-209`, `strategy_gate.py:205 top_n=300`; 룩어헤드). README = 「거래대금 ≥ 100억 · 폭락깊이순」 + 🔴「top300 근사 불성립」을 README 자신이 명시(`README.md:21-23`) | 의도된(문서화됨) — 단 README `:29` 가 「검증 러너 유니버스 `top_volume:50`」이라 적은 것은 **이 전략엔 틀렸다**(게이트는 top300, `gate_deep_mr_dev20.md:4`) → `문서화 오류` | 코드 = `trading_value ≥ 1e10`(`screener.py:27,34`) 인데 그 값은 **D-1 «하루»의 `close×volume×adj_factor`**(`quant_daily_reader.py:90-92`) + **가격 1,000~500,000 컷**(`screener.py:28,39`) 이 README 에 없다. [SQL] 저장 컬럼 `trading_value` 기준 47/47 ≥ 100억(최소 `001430` 100.2억) — 단 **현행 식(08-16 `ac69084` 이후 `close×volume×adj_factor`) 으로 재평가하면 46/47**(`274090` 06-23 = **96.7억** < 100억, 그날 스냅샷 rank 2 실재 = 당시 리더는 저장 컬럼을 썼다; `001430` 은 100.1억) · 47/47 가격 컷 통과 · 47/47 D-1 스냅샷 실재 | 가격 컷만 `문서화 안 된`(경미) |
| ① (후보 순위) | 게이트 드라이버는 같은 날 신호가 여럿이면 **기간 전체 거래대금 내림차순**으로 K 슬롯을 채운다(`portfolio_sim.py:152`). README = 「폭락 깊이순」 | **문서화 안 된** — 백테스트와 라이브의 «누구를 먼저 사나» 축이 다르다 | 코드 = `score=|이탈깊이|` 내림차순 top-10(`screener.py:52-54`, `_rule_screener_base.py:147-148`) ✅ README 대로. [SQL] 매수 47건의 D-1 스냅샷 rank: 1위 12 · 2위 11 · 3위 7 · 4위 8 · 5위 4 · 6위 3 · 9위 1 · **20위 1**(`006800` 06-24 13:39, §2-5) | 이탈 없음(1건 예외) |
| **② 진입 셋업** | `(close−MA20)/MA20 ≤ −20%` ∧ `RSI14 < 30` — 게이트(`strategy_gate.py:222`)·README(`:14-15`)·config(`:32-34`) 동일 | 이탈 없음 | `rule.py:38-49` 동일 룰, 스크리너(`screener.py:41-47`)·라이브(`strategy.py:128-131`) 모두 같은 클래스 재사용. [SQL] 47/47 `reason` 의 이탈% = `(D-1 close − 20봉 평균)/평균` 재현(소수 첫째 자리 일치) · [산술] `214150` 08-19 이탈 **−20.99%** / RSI14 **26.22**(코드식 pandas EWM, 고전 Wilder 는 27.10) ⇒ BUY ✓(§3-4) | 이탈 없음 |
| **③ 진입 방아쇠 / 타이밍** | 원저작에도 방아쇠가 **없다** — 셋업 성립 = 다음날 시가 시장가 체결(`portfolio_sim.py:161-164`, `entry_mechanism="market"`, 슬리피지 +0.1%). 반등 확인 봉·거래량 조건 없음. README 도 없음 | 의도된(문서화됨) — **개념 자체가 「떨어지는 칼을 잡는다」**: [SQL] 47건 중 **42건(89.4%)** 은 D-1 종가가 «전일 대비 하락»(음봉 close<open 은 39건)이었고 19건은 D-1 하루에 −10% 이상 | 코드 = D-1 확정 종가 기준 신호(`trading_context.py:143-175`) → **장중 첫 통과 틱**에 체결, **밴드 [−7%, +1%]**(`strategy.py:60-62`: `entry_band_up_pct=0.01`, `down=stop_loss_pct`). README·config 에 밴드 없음. [SQL] 06-16 이후 43건 중 +1% 초과 **0** · 전 기간 −7% 미만 **0**(최소 −6.94%) ⇒ 발효 확인. 거절은 `trading_analyzer.py:92` **DEBUG**(§2-3) | **문서화 안 된** (밴드) — 단 이 전략은 8/23 X-2 와 달리 **하한이 있다** |
| **④ 청산** | 게이트 = `MAReversionExitAdapter` sl7/tp12/MA20×0.9/mh7, **bar i 종가 판정 → i+1 시가 체결**(`exit_adapters.py:4,47-67`). README `:18` 동일 값 | 이탈 없음(값) — 단 `exit_adapters.py:48`·`rule.py:17` 의 「템플릿 verbatim」은 거짓: 템플릿 `strategies/mean_reversion/strategy.py:172` 의 회복은 `deviation ≥ entry_dev×0.1`(템플릿 기본 −10 → **MA20×0.99**, −20 대입 시 ×0.98)이지 ×0.9 가 아니다 → `문서화 오류`(계보) | 룰 구현 일치(`strategy.py:137-158`). 🔴 그러나 실행 경로가 둘: 전략 고유(`base.py:691-701`, D-1 일봉, ≈5분 주기) vs `position_monitor.py:203,314-335`(**장중 현재가**). [SQL] 매도 46건 = 프레임워크 손절 **33** · 익절 **7** · 트레일링 **3**(06-16~18, owner 미부착) · 전략 `ma_recovery` **3**(전부 06-12 매수 후 10초~2.5분 = 분봉 오작동 버그, `strategy.py:89-93` 주석이 그날 수정 자인) ⇒ **정상 경로의 전략 고유 청산 0/43**. 발동 시각 09시 35/46, 그중 **손절 13건이 09:05:00~06**(09:00~09:05 손절 유예 해제 직후 = 갭다운) | **결함** (선언 「일봉 청산」이 실행 안 됨 = 8/23 X-1) |
| ④ (보유일 정의) | 백테스트 `hold_bars = i − entry_idx ≥ 7`(체결봉 기준 7봉 뒤 판정, 8봉째 시가 체결) | — | 정의가 **셋**: 전략 `count_trading_days_between(entry, now) − 1 ≥ 7`(`strategy.py:191`) · 모니터 `get_days_held ≥ 7`(매수일 포함 계수, `virtual_trading_manager.py:958-971` → `position_monitor.py:281-285`, **하루 이르다**) · 백테스트 위. [SQL] 실측 최대 보유 **4거래일**이라 어느 정의도 구속한 적 없음(max_hold 0/46) | `문서화 안 된`(잠복) |
| **⑤ 보유 K · 사이징** | 게이트 측정 = K5 · **종목당 100만(S1)**(`gate_deep_mr_dev20.md:4`). README `:25` = K5 · **200만(S2)** — 사이징 시나리오 tsv 에서 S2 선택(문서화). ⚠️ 단 README `:31` 헤드라인 「MaxDD 16.2%」는 **S1** 값이고 S2 는 **27.4%**(`dev20_sizing_scenarios.tsv` S2 행) | 의도된(문서화됨) — MaxDD 병기는 `문서화 오류` | `initializer.py:489-496` → `virtual_trading_manager.py:255-268`(`paper_investment_per_stock` 이 K분할 덮어씀 — **8전략 중 유일**). [SQL] 47건 중 **>200만 0건**, 6·7월 중앙 197만 ⇒ 상한 실효. 🔴 07-29 복리 재산정(`:271-`)이 `2,000,000 × 자본/1,000만` 으로 매일 갱신 → [로그 8/20:233] **1,283,716원**, 8/20 매수 1,274,900원. README 는 200만 고정으로 읽힘. 먼지(<10만) **5/47**(최소 5,070원) | **문서화 안 된**(복리 축소) + 먼지 |
| ⑤ (K·일일 한도) | K=5 (`strategy_gate.py:201`) | — | 게이트는 `len(self.positions) ≥ 5`(`strategy.py:98`)와 `daily_trades ≥ 5`(`:96`) 둘 다 **전략 인스턴스 메모리**. [SQL] `paper_strategy_equity.n_open` 최대 **7**(06-24, 55일 중 1일 초과) · 06-24 하루 매수 **7건**(일일 한도 5 도 초과). [로그] `robotrader_template_20260624_133415.log` = **13:34 재시작** → 13:39~13:57 에 5건 연속 매수 ⇒ `on_init` 이 `sync_positions` 주입을 덮어써 두 카운터가 0 이 됐다(§2-5). K 쪽은 **08-12 `8238f91` 재주입으로 수정**(8/21 로그 `:321`), `daily_trades` 는 미복원. `max_daily_trades` 는 매도 체결도 센다(`strategy.py:103`) — README 미기재 | **결함**(재시작 시 일일한도 리셋 — K 는 해소됨) |
| **⑥ 국면 게이트** | 게이트 백테스트 = 국면 필터 없음(`_b3` 에 regime 인자 없음). README `:24` = KOSPI / **none** | 이탈 없음 | `config/trading_config.json:83-88` `regime_gate:"none"` ✅. 단 급락 필터(KOSPI ≤ −2.5%, `constants.py:154-155`)는 켜져 있다 — [로그 8/20 09:14:25] `[시장방향성필터] … 판정=허용` | 경미한 `문서화 안 된` |
| ⑥ (G2) | 게이트 보고서 판정 = **REJECT (G2)**(`gate_deep_mr_dev20.md:10,18` — 월 6.0회 < 10회). 사장님이 빈도 제약을 «수용»해 졸업(`CANDIDATES.md:78`, `config.yaml:7`). README `:7-8` 「품질 전관문(G2~G5)을 통과」는 G2 를 포함해 틀렸다 | `문서화 오류` | — | — |

---

## §2 이탈 상세 — 증거

### 2-1. 🔴🔴 「일봉 청산」 선언 vs 장중 실행 — 이 전략이 8전략 중 가장 크게 물린다

`strategy.py:27` `exit_timeframe="daily"`, `:89-93` 은 `timeframe!='daily'` 를 **포지션 분기보다 앞**에서 막는다(8/23 D5 의 daytrading·minervini 와 달리 «앞»에 있어 분봉 회복 오작동은 06-12 이후 재발 없음 — [SQL] `ma_recovery` 3건 전부 06-12). 그런데 `position_monitor.py:203` 이 API 현재가로 `:314-335` 범용 익절/손절을 먼저 발화한다 — 임계값은 전략 값(`trading_decision_engine.py:590-606` 이 `risk_management` 를 읽음, [SQL] 06-26 이후 28/28 tp=0.12/sl=0.07).

**[SQL] 매도 46건 분해** (`virtual_trading_records`, `strategy='deep_mr_dev20'`, `is_test=true`, `action='SELL'`):

| 버킷 | 판정 코드 | 건수 | 평균 수익률 | 비고 |
|---|---|---|---|---|
| 프레임워크 범용 손절 `손절 실행 (…<=…)` | `position_monitor.py:326-335` | **33** | **−9.31%** | 최저 −30.13%(`950160` 07-21), ≤−10% **9건** |
| 프레임워크 범용 익절 `목표 익절 도달` | `:314-323` | 7 | +14.16% | 최고 +26.22% |
| 프레임워크 트레일링 | `:292-308` | 3 | +3.13% | 06-16·06-18·06-18 — `strategy_for_sell is None` 일 때만 도는 경로(`:292`) ⇒ owner 미부착(8/23 minervini §2-8 과 같은 형태·같은 6월) |
| 전략 고유 `MA20×0.9 회복` | `strategy.py:151-155` | 3 | −0.66% | **전부 06-12**, 매수 후 10초·13초·2분27초 — 분봉 프레임 오작동(`strategy.py:90-92` 주석) |
| 전략 고유 손절/익절/max_hold | `strategy.py:147-150,156-157` | **0** | — | |

⇒ **정상 경로의 전략 고유 청산 = 0/43.** 왜 이 전략에서 더 심한가: 진입 대상이 «어제 −11.4%(평균) 떨어진 종목»이라(§2-4) 다음 날 갭다운이 잦고, 장중 −7% 는 **09:05 유예 해제 직후** 바로 찍힌다 — [SQL] 손절 33건 중 **13건이 09:05:00~09:05:06** 에 체결. 백테스트(`exit_adapters.py:57`)는 **종가**가 −7% 를 넘어야 판정하고 다음 시가에 판다 ⇒ 같은 갭다운이라도 백테스트는 하루를 더 버틴다. `backtest/live_universe_revalidation/RESULTS.md:52-60` 이 「deep_mr 만 국면·유니버스로 설명 안 되는 ≤1일 잔차 46%p = 실행 경로」라 지목한 것과 **정합**(인과 검정은 아님).

**손절 실현 괴리** [SQL]: 손절 33건 전체 평균 −9.31% 은 매수기록 `stop_loss_rate` 가 **12종**(0.030×4·0.031·0.044·0.047·0.047·0.050·0.060·0.066·**0.070×19**·0.072·0.076·0.077) 섞인 값이라 「선언 −7%」와 짝지으면 안 된다. 선언 7% 인 손절 **19건**의 평균 실현 **−10.41%**(7건이 −12% 이하) ⇒ 괴리 −3.4%p. `backtest/concept_fidelity_audit/RESULTS.md:157` 의 「rs_leader −0.52%p 가 최대」는 deep_mr 을 «선언값 혼재»로 못 잰 결과로 보인다(§6).

### 2-2. 🔴 원저작과 라이브는 «같은 룰·다른 전략» — 유니버스·순위·체결이 셋 다 다르다

| 축 | 게이트 백테스트 | 라이브 | 근거 |
|---|---|---|---|
| 유니버스 | 기간 전체 거래대금 상위 300 **정적**(룩어헤드) | D-1 하루 거래대금 ≥ 100억(+가격 1,000~500,000) — 일별 중앙 **276종목**(`live_universe_revalidation/RESULTS.md:144`) | `book_param_multiverse.py:196-209` · `screener.py:27-34` · `quant_daily_reader.py:90-96` |
| 경쟁 신호 우선순위 | 거래대금 내림차순 | 이탈 깊이 내림차순 top-10 | `portfolio_sim.py:152` · `screener.py:54` |
| 체결 | 다음날 **시가** ×1.001 · 밴드 없음 · 정지(시가≤0)면 스킵 | 장중 첫 통과 틱, 밴드 [−7%,+1%] | `portfolio_sim.py:161-164` · `strategy.py:60-62,173-174` |
| 손절/익절 판정 | 봉 **종가** → 다음 시가 체결 | 장중 **현재가** 즉시 | `exit_adapters.py:57-60` · `position_monitor.py:203` |
| 사이징 | 종목당 100만(S1) | 200만(S2) → 복리 재산정(8/20 128만) | `gate_deep_mr_dev20.md:4` · `config.yaml:51` · 로그 |

README 가 「top300 근사 불성립」(`:22`)과 「평판 숫자는 다른 모집단」(`:29`)을 이미 적어 두었으므로 **유니버스 축은 문서화된 이탈**이다. 문서에 없는 것은 **순위 축**(거래대금 vs 깊이)과 **체결·판정 시점**이다. 이 둘은 백테스트 재현이 아니라 «드라이버» 층에 있어 `concept_fidelity_audit/RESULTS.md:321` 이 「드라이버를 안 읽었다」고 자인한 바로 그 자리다 — 이번에 읽었다.

발사 빈도 [SQL]: 06-12~08-20 **48거래일에 매수 47건**(≈월 20회, 감사의 18.9회와 정합) vs 게이트 월 6.0회. 단 **8월은 14거래일에 1건**, 스냅샷 후보 0~5건/일([로그] `[E6] deep_mr_dev20` 8/3~8/21) — 빈도는 국면에 따라 3배 갈린다. 「3.2배 과발사」는 6·7월 값이다.

### 2-3. 🟡 진입 밴드 — 이 전략은 «양쪽» 다 있다, 그러나 거절은 안 보인다

`strategy.py:58-62`: 상한 +1%, 하한 = 손절폭 7%. 주석이 「갭업/상한가 종목을 스테일 종가로 체결하던 허수 진입 차단(2026-06-15)」이라 기원을 적어 둠. [SQL] 체결가 ÷ D-1 종가 − 1:

| 구간 | 매수 | > +1% | < −7% | 최대 | 최소 |
|---|---|---|---|---|---|
| 06-12~06-15 (밴드 전) | 4 | **3**(`082740` +5.39%, `476830` +3.25%, `010820` +1.17%) | 0 | +5.39% | 0.00% |
| 06-16~08-20 (밴드 후) | 43 | **0** | **0** | +0.74% | −6.94% |

⇒ 밴드는 06-16 부터 발효. 8/23 X-2 의 「하한 없음」은 **이 전략엔 불성립**. 그러나 거절 사유는 `bot/trading_analyzer.py:92` `logger.debug` — [로그 8/20] `214150` 매수 시그널이 09:05:03·09:09:39 두 번 났는데 체결도 거절 사유도 없고, 09:14:25 세 번째에 33,550원(+0.45%) 체결. 앞의 두 번이 밴드(+1% = 33,734원 초과)였는지 다른 가드였는지 **로그로는 알 수 없다**(원리적 관측 불가).

### 2-4. 🔴 「방아쇠」 부재는 원저작부터다 — 떨어지는 칼을 잡는 개념

[SQL] 47건 중 D-1 종가가 전일 대비 하락 **42건(89.4%)**(close<open 음봉은 **39건**) · D-1 하루 수익률 평균 **−11.38%** · −10% 이하 **19건**. 체결가가 D-1 종가보다 낮은 매수 **31/47**. 즉 «과매도 확인» 이 「반등 시작」이 아니라 「하락 진행 중」에 잡힌다. 이건 라이브 결함이 아니라 **발굴 사양 자체**(`strategy_gate.py:220-227` 에 반등 확인 조건 없음)이고 README 도 그대로 적었다 ⇒ 분류 `의도된`. 다만 이 구조가 §2-1 의 갭다운 손절과 **결합**해 손실을 만든다는 점은 어디에도 없다.

### 2-5. 🟡 K=5·일일 5건이 재시작에 «둘 다» 뚫렸다 — 06-24 실측 · K 쪽은 08-12 수정됨

[SQL] 06-24 매수 7건: 09:04:25 `100790` · 09:07:50 `274090` · **13:39:08 `006800`(rank 20) · 13:43:46 `027360` · 13:48:20 `295310` · 13:52:56 `354320` · 13:57:24 `069540`**. [로그] `robotrader_template_20260624_133415.log:18-29` = 13:34:17 봇 재기동. **메커니즘(검증에서 확정)**: `:407` 13:34:40 `[sync_positions] 2종목 (['274090','100790'])` → 같은 초 `:435-437` `초기화 완료`(= `on_init`, `strategy.py:65 self.positions = {}`) ⇒ **동기화가 주입한 포지션을 `on_init` 이 지웠다**. 증거: `:1141-1142` 13:39:09 에 이미 보유 중인 `274090`·`100790` 에 «매수» 시그널이 다시 났다. 이 결함은 **08-12 `8238f91`**(「복원 포지션 주입이 on_init 직후에 지워져 전략측 청산이 죽어 있었다」)이 `apply_pending_strategy_positions` 로 재주입해 고쳤고 [로그 8/21] `:265`(sync) → `:310`(on_init) → **`:321` `[재주입] on_init 이후 전략 포지션 재주입 완료: 8개 전략`** 으로 발효 확인(06-24 로그엔 `재주입` 0건). **남는 것은 `daily_trades`(`strategy.py:66`)** — `sync_positions`·재주입 모두 positions 만 복원하고 일일 카운터 복원 경로는 없다(독해) ⇒ 일일 한도는 재시작마다 리셋. 뒤 5건의 사이징이 1,959,900 · 1,995,760 · 805,500 · **34,300 · 5,070**원으로 급감한 것이 잔여 현금 소진(X-3) 의 직접 관측이다.

### 2-6. 🟡 [해소됨] 6월 tp/sl 표류 — 손절 3% 로 돌던 구간

[SQL] 6월 매수 29건 중 **19건**의 `target_profit_rate/stop_loss_rate` 가 선언값이 아니다(sl 최저 **0.030** 5건 + 0.031 1건 → `손절 실행 (-3.05% <= -3.00%)`). 메커니즘: `strategy.py:171-172` 의 `target/stop` 이 **D-1 종가** 기준인데 원장 비율은 **체결가** 기준으로 환산됐다 — `100790` 06-24: 20,300×1.12=22,736 → 22,736/19,750−1=**+15.1%**, 1−20,300×0.93/19,750=**4.4%**([로그 06-24 13:34:37] `DB 복원: 101주 @19,750원 (익절:15.1% 손절:4.4%)` 정확 일치). 06-26 이후 28/28 정상. minervini §2-9 와 같은 결함·같은 시기 ⇒ **6월(29건)과 7·8월(18건)을 같은 룰의 결과로 이어 붙이면 안 된다.**

### 2-7. 🟡 사이징 「200만」은 07-29 이후 사실이 아니다

`virtual_trading_manager.py:271-` `recalculate_investment_amounts`: `per_stock = base(2,000,000) × current_capital/10,000,000`. [로그 8/20:233] `2,000,000원 → 1,283,716원 (자본 6,418,579/10,000,000)` · [SQL] 8/20 매수 1,274,900원(38주×33,550). README `:25`·config `:51` 는 200만 고정. docstring(`:290-296`)이 deep_mr 을 «예시»로 들어 설계 의도는 적혀 있으나 README 엔 없다. ⚠️ S2 검증은 **고정 200만** 이었다 — 자본 연동 축소는 S1(100만)과 S2 사이의 «검증된 적 없는 제3의 사이징»이다.

### 2-8. 🟢 후보 공급 경로는 깨끗하다 (반증 확인)

[SQL] 47/47 매수가 D-1 `screener_snapshots(strategy='deep_mr_dev20')` 에 실재. `accepts_volume_fallback=False`(`strategy.py:28`) + `candidate_selector.py:1048-1140`(스냅샷 단일 경로, fail-closed). 8월 후보 0건인 날 [로그] `[E6] deep_mr_dev20: screener_snapshots 0건 … 금일 미진입`(8/19·8/21) 정상 동작. `metadata` 는 전건 NULL.

---

## §3 데이터 변형 — 창 A / B / C + 손계산

### 3-1. 세 창의 크기 — 룰은 «스스로 34봉으로 자른다»

| 창 | 경로 | 봉 수 | 근거 |
|---|---|---|---|
| A 스크리너 | `screener.py:20` `lookback_days=45` → `quant_daily_reader.py:161-185` `ORDER BY date DESC LIMIT 45` | **45행** | 행 수 |
| B 라이브 진입 | `constants.py:19` `OHLCV_LOOKBACK_DAYS=120`(달력) → `price.py:118-133` → `_drop_unconfirmed_today_bar` | **≈80봉** | [로그 8/21:1182] 데이터 수집 47(분봉) — 일봉은 elder 편 실측 80봉과 동일 경로 |
| C 청산(전략 고유) | `base.py:694-696` `get_daily_data` (B 와 동일 80봉, D-1 확정) / 프레임워크는 봉을 안 쓴다(현재가) | 80봉 / 0 | |
| 백테스트 | `_precompute_signals` `window=df.iloc[:i+1]` **전체 이력** | 전부 | `book_portfolio_multiverse.py:283` |

🔑 **`rule.py:35,38`: `need = max(20,14)+10 = 34`, `close = df["close"].iloc[-need:]`** — 룰이 입력 길이와 무관하게 **마지막 34봉만** 쓴다. MA20 은 20봉 단순평균이라 창 무관, RSI14 는 **코드식 pandas EWM**(`ewm(alpha=1/14, min_periods=14)`, 기본 `adjust=True`, `utils/indicators.py:24-25` — 고전 Wilder(SMA 시드+재귀)와 값이 다르다)이라 **시드가 슬라이스 첫 봉**에 걸리지만 슬라이스가 항상 34 로 같아 결과도 같다. [산술] `214150` 08-19 를 34/45/80 슬라이스에서 계산 → 이탈 −20.99% · RSI 26.22 **셋 다 동일**(절단 없이 45봉 27.98 · 80봉 28.50 · 전체 이력 1,379봉 28.56 — 즉 «34봉 절단»은 값을 바꾸지만 A/B/백테스트 «사이»에선 안 바뀐다; 참고 고전 Wilder 27.10 · 단순 14봉 36.83, 셋 다 <30 이라 판정 불변). ⇒ **8/23 X-6(두 창) 은 이 전략의 룰에선 불성립.** `min_daily_bars=35`(`config.yaml:36`) ≥ 34 라 가드도 정합.

**단 위생 가드 창은 다르다**: A 는 45행 전체(`_rule_screener_base.py:94,177` `sanity_window=None`), B 는 80봉(`base.py:645`), 룰은 34. `utils/data_sanity.py:33` 설계 원칙 3 「룰이 «실제로 보는» 창만 검사한다」를 호출자가 안 지킨다. [로그 8/21 09:00:45] A 가드 4건 제외 중 `476830` 의 절벽은 **06-29** — [SQL] 08-20 기준 **36봉 전**이라 룰(34봉)은 못 보는데 가드(45행)가 뺐다. 방향은 안전 쪽이나 후보 집합이 룰이 아닌 가드 창으로 정해진다.

### 3-2. 창 C — 전략 고유 청산은 D-1 종가로 «어제» 를 판정한다

`strategy.py:144-146`: `cur_close = close.iloc[-1]` = D-1 확정 종가(B 프레임). 즉 전략 고유 sl/tp/회복은 **어제 종가**로 오늘 판정 → 프레임워크가 현재가로 먼저 판정하므로(§2-1) 실측 0건. 백테스트는 «당일 종가 → 다음 시가»라 **하루 늦게 팔고**, 라이브 전략 경로는 «어제 종가 → 오늘 장중»이라 **역시 하루 늦으나 실행되지 않는다**. 결과적으로 라이브는 세 경로 중 가장 빠른 것(장중가)만 실행된다.

### 3-3. D3 — 정지 패딩 행 «측정» (매수 47건 전수)

[SQL] 매수일 «이전» 봉을 `row_number` 로 세어 volume=0(원본 컬럼) 을 셌다:

| 창 | volume=0 행이 있는 매수 | 종목 |
|---|---|---|
| 직전 **20봉** | **1 / 47 (2.1%)** | `025560` 07-29 — 20봉 중 **13봉** volume=0 |
| 직전 34봉(룰 창) | 1 / 47 | 동일 |
| 직전 80봉(창 B) | 3 / 47 매수(2종목) | + `069540`(2행, 룰 창 밖 — 06-24·06-30 두 매수 모두) |

`025560` 상세 [SQL/산술]: 07-07~07-24 **13영업일** close 41,750 고정·volume 0(정지) → 07-27 close 10,210(**−75.5%**, `adj_factor=1` 미조정; `corp_events` 에 `rights_issue` 2026-07-09 유상증자 결정 1건) → 07-28 7,150. 룰 계산: MA20 = 40,713 → 이탈 **−82.44%**, RSI14 **14.63** ⇒ BUY. 07-29 7,180원 매수 → 07-30 6,020원 **−16.16% 손절**(선언 7% 의 2.3배). 🔑 ***이 전략의 룰은 volume 을 «안 읽는다»(`rule.py:38` close 만)*** ⇒ 8/23 D3 의 「거래량 룰 분모」는 **해당없음**. 🔎 **초안의 「고정 종가가 MA20 을 부풀린다」는 검증에서 «반증»됐다**: 패딩 13봉을 빼고 volume>0 실거래봉 34개만으로 다시 계산해도 MA20 **42,090.5** · 이탈 **−83.01%** · RSI **28.15** ⇒ **똑같이 BUY**(패딩을 넣은 MA20 40,713 이 오히려 더 낮다). 즉 가짜 폭락의 원인은 패딩이 아니라 **미조정 재개 절벽 −75.5%** 이고, 그 절벽은 8/15 가드(−35%)가 정확히 잡는다([로그 8/21 `:492`] `025560: 불가능봉 1건 @2026-07-27 최대 -75.5% … 후보 제외`). 「volume=0 행 배제」 처방은 **이 사례를 못 막는다**(실측).

불가능봉(≤ −35%) 이 룰 창(34봉) 안에 있던 매수 [SQL]: **4/47** = `476830`×2(06-30 +12.09%, 07-03 −7.30%) · `356860`(07-20, −12.38%) · `025560`(07-29, −16.16%) — `utils/data_sanity.py:18-22` 의 「46건 중 4건(8.7%)」과 동일 집합. **전부 8/15 가드 이전**이고, 가드 이후 유일한 매수 `214150`(8/20)은 0건. ⚠️ 가드 문턱 −35% 라 **−20%~−35% 의 정지 해제 절벽**(하한가 −30% 한 번)은 통과한다 — 그 경우 룰 조건이 정확히 성립하므로 D3 는 «막힌 게 아니라 좁아진» 상태다(측정된 사례는 없음).

### 3-4. 손계산 대조 1건 — `214150` (2026-08-20 매수, D-1 = 08-19)

- [SQL] `daily_prices` 08-19 close **33,400** · 직전 20봉 평균 **42,275.0** → (33,400−42,275)/42,275 = **−20.99%**. 원장 `reason` = `MA20 이탈 -21.0%` ✓ · 스냅샷 `score=0.2099` rank 1 ✓ · [로그 8/20:993] `매수 시그널: 214150 @ 33,400` ✓.
- [산술] RSI14 = 코드식 pandas EWM(alpha=1/14, adjust=True, min_periods=14) over 마지막 34봉 → **26.22** < 30 ✓ (`utils/indicators.py:21-36` 을 pandas 로 그대로 재현; 프로젝트 모듈 import 없음. 고전 Wilder 로 계산하면 27.10, 단순 14봉이면 36.83 — 코드는 «Wilder» 가 아니라 pandas EWM 이다).
- 체결 33,550원 = D-1 대비 **+0.45%** (밴드 +1% 안) · 수량 38주 = `int(1,283,716/33,550)` ✓ (`get_max_quantity` `:577-580`, 재산정값 [로그 8/20:233]).
- 유니버스: D-1 거래대금 **351.5억**(현행 식 `close×volume×adj_factor`; 저장 컬럼 `trading_value` 는 344.6억) ≥ 100억 ✓ · 가격 컷 ✓.
⇒ **룰 산술·사이징·유니버스 세 층이 SQL/산술로 전부 재현된다.** 나머지 46건도 이탈%는 SQL 재현(§1 ②), RSI 는 이 1건만.

---

## §4 결함 목록

> 결정은 사장님 몫. 「고치면 무엇이 바뀌나」는 독해와 §2~3 실측에서 논리적으로 따라오는 범위만.

| ID | 결함 | 등급 | 파일:라인 · 근거 | 고치면 무엇이 바뀌나 |
|---|---|---|---|---|
| **DM-1** | **「일봉 청산」 선언이 실행되지 않는다** — 정상 경로 전략 고유 청산 0/43, 손절 33건이 장중가로 발화(13건은 09:05:00~06), 선언 −7% 손절의 실현 평균 −10.41% | 🔴🔴 | `strategy.py:27` vs `position_monitor.py:203,314-335` · [SQL] §2-1 | 백테스트(종가 판정→다음 시가)와 정합 회복, ≤1일 청산 71.7% 가 줄고 갭다운 손실이 «더 깊게» 실현될 수도 있다(하루 더 버티므로) — 부호는 미리 모른다. 8전략 공통 경로라 단독 수정 불가(8/23 X-1) |
| **DM-2** | **미조정 재개 절벽이 가짜 폭락을 만든다(정지 패딩은 부수)** — 실측 1/47 매수가 패딩 13봉 + 절벽 −75.5% 로 −82.4% 이탈·−16.16% 손실. 🔎 초안의 「패딩이 MA20 을 부풀린다」는 **REFUTED**(패딩 제외 34봉만으로도 −83.0%·RSI 28.2 ⇒ 동일 매수). 불가능봉 창 내 매수 4/47 은 전부 8/15 가드 이전, 가드 이후 0/1 · 8/21 로그 `:492` 가 이 절벽을 잡는다. **남는 구멍 = −20~−35% 절벽**(측정 사례 0) | 🟡(재정의) | `rule.py:38-40`(close 만 읽음) · `utils/data_sanity.py:53`(−35%) · [SQL/산술] §3-3 | 이미 8/15 가드가 막고 있다. 「volume=0 행 배제」는 **이 사례에 효과 없음(실측)** — 처방에서 뺀다. 남는 것은 가드 문턱(−35%)과 KRX 하한가(−30%) 사이의 절벽뿐이며, 근본은 `adj_factor` 미적재(메모리 `defect-halt-padding-impossible-bars.md`) |
| **DM-3** | **재시작이 일일 5건 카운터를 0 으로 되돌린다(K 쪽은 해소됨)** — 06-24 13:34 재기동 후 5건 연속 매수·동시보유 7 의 원인은 `on_init` 이 `sync_positions` 주입을 `positions={}` 로 덮어쓴 것 → **08-12 `8238f91` 재주입으로 수정, 8/21 로그 `:321` 발효 확인**. 잔존 = `daily_trades` 미복원(독해) | 🟡(절반 해소) | `strategy.py:65-66`(on_init 초기화) · `state_restorer.py:178-206` `apply_pending_strategy_positions` · [로그] `20260624_133415.log:407,435-437,1141-1142` · 8/21 `:265,:310,:321` · [SQL] §2-5 | K 는 이미 재시작을 견딘다. `daily_trades` 복원을 붙이면 재시작일의 매수 건수가 5 로 묶인다 — 실측 구속 사례는 06-24 하루뿐이고 그날 먼지 매매 2건이 여기서 생겼다 |
| **DM-4** | **README 「200만」 사이징이 07-29 이후 사실이 아니다** — 복리 재산정으로 8/20 128만(S1·S2 어느 쪽도 아닌 제3의 사이징) | 🟡 | `virtual_trading_manager.py:270-296` · [로그 8/20:233] · README `:25` · config `:51` | 문서만 고치면 동작 불변. 사이징을 200만 고정으로 되돌리면 S2 검증과 정합하나 자본 64% 상태에서 K5×200만 = 1,000만 > 자본 |
| **DM-5** | **원저작↔라이브의 «순위 축»이 다르다** — 백테스트는 경쟁 신호를 거래대금순, 라이브는 깊이순으로 고른다. README 미기재 | 🟡 | `portfolio_sim.py:152` vs `screener.py:54` | 문서화하거나 백테스트를 깊이순으로 재실행. 후보 집합이 얼마나 바뀌는지는 **미측정** |
| **DM-6** | **진입 밴드 [−7%, +1%] 가 코드에만 있고 거절이 DEBUG** — 8/20 시그널 3회 중 2회 미체결 사유 관측 불가 | 🟡 | `strategy.py:58-62` · `trading_analyzer.py:92` | INFO 승격 시 「몇 건 버려지는지」가 처음 보인다. 밴드 자체는 06-16 이후 43/43 발효 확인 |
| **DM-7** | **위생 가드 창(45/80)이 룰 창(34)보다 넓다** — 8/21 `476830` 이 룰이 못 보는 36봉 전 절벽으로 제외됨 | 🟡 | `_rule_screener_base.py:94,177` · `base.py:645` · `data_sanity.py:33` 원칙 3 | `sanity_window=34` 로 고정하면 후보 집합이 «룰» 로만 정해진다. 안전 방향 손실(제외 종목 감소) — 8/21 표본 4건 중 1건 |
| **DM-8** | **README·docstring 오류 4건** — ①`:8` 「G2~G5 통과」(실제 G2 FAIL·REJECT, 빈도 수용 졸업) ②`:29` 「top_volume:50」(이 전략은 top300) ③`:31` MaxDD 16.2% 는 S1 값(S2 는 27.4%) ④`exit_adapters.py:48`·`rule.py:17` 「템플릿 verbatim」(템플릿 회복은 ×0.99 — 기본 −10 기준; −20 대입 시 ×0.98) | 🟡 | `gate_deep_mr_dev20.md:10,18` · `:4` · `dev20_sizing_scenarios.tsv` · `mean_reversion/strategy.py:172` | 문서만. ③은 「라이브 사이징의 검증된 MaxDD 가 27.4%」로 읽어야 한다 |
| **DM-9** | **보유일 정의가 셋**(전략 −1 / 모니터 포함계수 / 백테스트 체결봉 기준) — 모니터가 하루 이르다 | 🟡(잠복) | `strategy.py:191` · `virtual_trading_manager.py:971` · `portfolio_sim.py:177` | 실측 최대 보유 4일이라 한 번도 구속 안 됨. DM-1 을 고쳐 보유가 길어지면 살아난다 |
| **DM-10** | **[해소됨] 6월 tp/sl 표류** — 29건 중 19건, 손절 3% 대로 돈 6건(sl 0.030 5건 + 0.031 1건) | ⚪(기록) | [SQL] §2-6 · [로그 06-24 13:34:37] · `concept_fidelity_audit/RESULTS.md:321`(선언↔실측 혼재 미분리) | 조치 불필요. 6월과 7·8월 성과를 이어 붙이지 말 것 |
| **DM-11** | **`max_daily_trades` 가 매도 체결도 센다** + README 미기재 | ⚪ | `strategy.py:103` | 「하루 5건」의 뜻이 「매수 5건」이 아니라 「체결 5건」. 실측 구속 사례 미확인 |

기존 등록 재확인: `max_capital_pct: 0.12`(`trading_config.json:85`) 이중 미결선 — 8/23 과 동일하게 코드 불변.

---

## §5 횡단 패턴 판정표

| # | 패턴 | 판정 | 실측 |
|---|---|---|---|
| **X-1** | 전략 고유 청산 굶김 | **성립 — 8전략 중 최심** | [SQL] 정상 경로 고유 청산 **0/43**(3건 `ma_recovery` 는 06-12 버그 산물) · 손절 33/46 · 09시 발화 35/46 · 09:05:00~06 손절 13건 |
| **X-2** | 진입 밴드 코드에만·거절 DEBUG·하한 없음 | **부분 성립** — 코드에만·DEBUG 는 성립, **하한은 있다**(−7%) | `strategy.py:60-62` · [SQL] 06-16 이후 +1% 초과 0/43 · −7% 미만 0/47 · [로그 8/20] 미체결 2회 사유 없음 |
| **X-3** | K·종목당 한도 사문화·먼지 | **부분 성립** — 종목당 상한은 **실효**(0/47 > 200만, 유일하게 `paper_investment_per_stock` 결선), K 는 재시작 1일 초과(최대 7), 먼지 5/47(최소 5,070원) | [SQL] §1 ⑤ · `n_open` 55일 중 1일 초과 · [로그 06-24 재기동] |
| **X-4** | 방아쇠 축 부재 | **성립 — 단 원저작부터** | [SQL] D-1 전일 대비 하락 42/47(음봉 close<open 은 39/47) · D-1 평균 −11.4% · 반등 확인 조건은 게이트 드라이버에도 없음(`strategy_gate.py:220-227`) ⇒ 의도된 |
| **X-5** | 20→10 컷 + score 정체 | **성립(컷) / 불성립(정체)** — 컷 기준이 **깊이**(README 대로, elder 의 «종가» 와 다름) | [SQL] `params_json.max_candidates=20`, 스냅샷 44일 중 20행 5일·rank>10 60행 · 매수 rank>10 **1건**(`006800` rank 20, 06-24 재기동 후) · `score=depth` 47/47 reason 과 일치 |
| **X-6** | 두 창(행 수 vs 달력일) | **룰에선 불성립 / 가드에선 성립** | `rule.py:35,38` 34봉 자기 절단 → [산술] 34/45/80 슬라이스 결과 동일 · 가드 창 45 vs 80 vs 룰 34 (`476830` 8/21 사례) |
| **D3** | 정지 패딩이 룰 분모에 | **패딩 경로 불성립 / 절벽 경로 성립** | [SQL] 20봉 내 volume=0 매수 **1/47**(`025560` 13/20) — 그러나 [산술] 패딩 13봉을 빼도 −83.0%·RSI 28.2 로 동일 매수 ⇒ 패딩은 원인이 아니다. 원인 = 미조정 재개 절벽 −75.5%(룰은 volume 미사용 · 8/15 가드가 잡음, 가드 이후 0/1 · 남는 구멍 −20~−35% 절벽 사례 0) |

---

## §6 기존 단서 재검증표

| 단서 | 출처 | 판정 | 근거 |
|---|---|---|---|
| 정지 패딩/불가능봉을 「폭락」으로 오인해 매수한 전례 | 메모리 `defect-halt-padding-impossible-bars.md`, `data_sanity.py:15-22` | **참 + 정정(메커니즘)** — 「불가능봉」 4/47 은 참. 그중 패딩(volume=0) 행이 룰 창에 있던 건 1건(`025560`)뿐이고, **그 1건도 패딩이 아니라 미조정 재개 절벽이 원인**(패딩 제외 재계산 −83.0%·RSI 28.2 ⇒ 동일 매수). 나머지 3건(`476830`·`356860`)도 패딩 없는 미조정 절벽 | [SQL/산술] §3-3 |
| `paper_investment_per_stock` 이 K분할을 덮어씀(유일) | `docs/PAPER_STRATEGIES.md:115` | **참** — 그리고 07-29 복리 재산정이 그 위에 또 곱한다(8/20 128만) | `initializer.py:489-496` · `vtm.py:255-268,270-` · [로그 8/20:233] |
| 「top300 근사 불성립」· 월 18.9회(3.2배) | README `:22-23`, `concept_fidelity_audit/RESULTS.md:168-172` | **참 + 단서** — 48거래일 47건은 참. 8월은 14거래일 1건이라 «국면 의존» | [SQL] 월별 29/17/1 · [로그] 8월 후보 0~5건/일 |
| deep_mr 만 국면·유니버스로 설명 안 되는 ≤1일 잔차 46%p ⇒ 실행 경로 | `live_universe_revalidation/RESULTS.md:52-60` | **정합(인과 검정 아님)** — 실행 경로 = 장중가 손절(§2-1). ≤1거래일 청산 **33/46 = 71.7%**(0일 4 · 1일 29 — 4번째 0일은 `445680` 06-16 09:07 매수→09:32 트레일 매도) | [SQL] 보유 거래일 분포 |
| 손절 실현 괴리 최대는 rs_leader −0.52%p | `concept_fidelity_audit/RESULTS.md:157` | **정정** — deep_mr 선언 7% 손절 19건의 실현 평균 −10.41% = **−3.4%p**(6월 선언값 혼재를 걷어내면 드러남) | [SQL] §2-1 |
| max_hold 도달 0건(7전략) | `concept_fidelity_audit/RESULTS.md:247` | **참** — 0/46, 최대 보유 4거래일 | [SQL] |
| `−20%` 임계 정상 배선 | `concept_fidelity_audit/RESULTS.md:170` | **참** | `config.yaml:32` · [SQL] 47/47 이탈 ≤ −20.99% |
| 8/23 D2 「8/21 창 A 가드 deep_mr 4회」 | `data_transformation_4strategies.md:275` | **참 + 보강** — 4건 중 1건(`476830`)은 룰 창 밖 절벽 | [로그 8/21 09:00:45] · [SQL] 36봉 |
| 포지션사이징 문서 deep_mr 행(47건·최소 5,070·최대 1,999,750·먼지 5·초과 0) | `docs/포지션사이징_결함_2026-08-23.md:21,58` | **참**(동일 값 재현) | [SQL] |

---

## §7 한계

1. **코드 실행 0회.** 룰 판정은 SQL/pandas 재현이지 라이브 코드 실행이 아니다. RSI 손계산은 `214150` **1건**뿐이고 46건은 이탈%만 재현했다.
2. **표본 = 47매수/46매도, 단일 국면(2026-06~08), 6월과 7·8월은 다른 체제**(§2-6 tp/sl 표류·§2-3 밴드 미발효가 6월에 몰림). 비율(71.7%·2.1%·89.4%)은 관측이지 검정된 명제가 아니다.
3. **밴드 거절 건수는 원리적으로 관측 불가**(DEBUG). 8/20 의 미체결 2회 원인도 미상.
4. **06-24 재기동의 K 리셋 메커니즘은 로그 순서로 확정됐다**(`sync_positions` 13:34:40 → 같은 초 `on_init` → 13:39:09 보유 종목에 매수 시그널 재발) — 단 **당시 코드 상태를 커밋에서 재현하지는 않았고**, 08-12 `8238f91` 수정의 발효는 8/21 로그 `:321` 한 날로만 확인했다. `daily_trades` 미복원은 현행 코드 독해다.
5. **「고치면 무엇이 바뀌나」는 전부 미측정.** DM-1 을 종가 판정으로 바꿨을 때 손실이 줄지 늘지는 방향조차 모른다(하루 더 버티면 갭다운이 더 깊게 실현될 수 있다).
6. **원저작 층은 «드라이버 코드»를 정본으로 삼았다**(`strategy_gate.py`·`portfolio_sim.py`·`exit_adapters.py`). 게이트 보고서가 그 코드로 생성됐음은 `gate_deep_mr_dev20.md` 의 문구가 `strategy_gate.py:385` 포맷과 일치하는 것으로만 확인했고, 당시 커밋에서 실행된 정확한 코드 상태는 재현하지 않았다.
7. **`daily_prices` 는 살아 있는 표다**(09:00 원주가 덮어쓰기). 이 문서의 SQL 은 2026-08-24 장중에 읽은 값이며, 47/47 이탈% 가 원장 `reason` 과 일치한 것이 «이 표본에선» 그 위험이 실현되지 않았다는 증거다.
8. **성과 비교를 하지 않았다.** 페이퍼 gross −3,429,855원 · 8/21 equity 6,365,187원은 참고 숫자이고, 이 문서는 「개념↔코드 충실도」 검수다.
