# rs_leader — 합병 큐(미조정 의심) 종목 매수 배제 : 설계 (2026-09-10)

> 사장님 결정(2026-09-10 저녁, 메모리 `decision-2026-09-10-index-merge-and-rsleader-exclude` §②):
> **(b) rs_leader 후보 선정에서 합병 큐(미조정 의심) 종목을 배제**.
> 근거: `scratchpad/eod_20260910/LANE_C_eprime_guards.md` §4 · `scratchpad/eod_20260910/MANAGER_rs_leader_merge_attribution.md`.
> 범위 = **rs_leader 하나**. 다른 전략 확장은 §7 「후속」에만 적는다.
> 라이브 룰 변경이므로 **롤백 스위치 상수 1개** + 발효일을 끊는 로그 계기 1줄을 함께 넣는다.
> 구현은 워크트리 · TDD · 코드리뷰까지 · **머지·발효는 별도 승인**.
> 형식은 선례 `2026-09-10-index-kis-freshness-design.md` 를 따른다.

---

## 0. 결정된 것 / 이 문서가 판정하는 것

**결정(바꾸지 않는다)**

1. 조치는 (b) 「배제」다. (a) 관측만 · (c) 큐 종목 가격 재수집 · (d) 신규 매수 중단은 선택되지 않았다.
2. 대상 전략은 **rs_leader 만**이다.
3. 롤백 스위치 상수 1개. 발효는 별도 승인.
4. **기존 보유 종목의 청산 룰은 건드리지 않는다**(§2 Q3 · §2 Q7).

**이 문서가 판정하는 것** — Q1 소스 · Q2 배선 층 · Q3 on_tick 경로 · Q4 배제 창 · Q5 위양성 · Q6 사전등록 · Q7 범위 밖 명시.

**이 변경의 정체 규정**: 이건 **데이터 위생 가드**이지 알파 주장이 아니다. 근거는 「이렇게 하면 성과가 좋아진다」가
아니라 「RS 점수의 입력값이 오염된 종목을 랭킹에 넣지 않는다」다. `utils/data_sanity.py:38-39` 의 불가능봉 가드와
**같은 계열**이고, `core/candidate_selector.py:518-520` 의 「배제 범위는 주문 가능 여부에 한정」(사장님 2026-08-09)
과는 **축이 다르다**(§2 Q2-D).

---

## 1. 현재 구조 (전부 파일:줄)

### 1-1. rs_leader 후보가 만들어지는 경로 — 하루 안에 «같은 프로세스»에서 두 단계

| # | 단계 | 코드 | 실측 시각(2026-09-10 로그) |
|---|---|---|---|
| S1 | 스크리너 스냅샷 생성 (scan_date = **D-1**) | `bot/candidate_loader.py:52-60` → `bot/liquidation_handler.py:584-636` → `runners/screener_snapshot_collector.py:82-176`(`:118` `adapter.scan`) | 줄 **474~491** (09:00:39~43, rs_leader 20건 3.0초) |
| S2 | 그 스냅샷을 라이브 후보로 소비 | `core/candidate_selector.py:1044-1142` → `_filter_unsafe_stocks(pool, limit=10)`(`:1130`) | 줄 **529** (09:00:49, `스냅샷 20건, 목표 10건, D-1=2026-09-09`) |

- 상한 20 = `config/constants.py:170` `MAX_CANDIDATES_PER_STRATEGY`(·`bot/liquidation_handler.py:613`) · 상한 10 = `bot/candidate_loader.py:139` → `core/candidate_selector.py:1013`.
- ⇒ ***「후보 20 → 선정 10」은 서로 다른 두 상한이고 둘 다 «같은 아침 09:00»에 돈다*** — EOD 생성이 아니다
  (`core/candidate_selector.py:1073` 의 「EOD 스냅샷은 D-1에 생성됨」은 낡은 주석 — §7-3).

### 1-2. 스크리너 내부 (`strategies/_rule_screener_base.py`)

`scan`(**105-159**) = `base_filter`(**108**) → `_prepare_frame`(**161-184**, 일봉 130봉 로드 + **불가능봉 가드 177-183**)
→ `match`(**136**) → **정렬 + topK**(**147-148**) → `finalize_scan`(**150**).

- rs_leader 어댑터 `strategies/rs_leader/screener.py`: `lookback_days = 130`(**19**) · `base_filter`(**29-36**, 거래대금
  10억 컷 `:24`) · `match`(**38-62**).
- 🔑 **score = 그 종목 «하나»의 120일 수익률**(`screener.py:60` `rs_ret = last / ref - 1.0`). 횡단면 백분위가 아니다
  ⇒ **모집단을 줄여도 살아남은 종목의 score 는 안 변한다**(§2 Q2 의 「RS 분모」 우려에 대한 답).
- 대조군: minervini 는 `build_context`(`strategies/minervini_volume_dryup/screener.py:97-121`)에서 RS **백분위**를
  만들고 그 분모를 「그날 `base_filter` 통과 집합」으로 못박았다(`:102-104`) ⇒ **거기서는 분모가 진짜로 바뀐다.**

### 1-3. on_tick 경로 (`strategies/base.py:593-717`)

- 매수 루프는 `ctx.get_selected_stocks()`(**612**)를 돈다. 그 함수(`core/trading_context.py:273-297`)는
  **「내 전략 소유」 + 「소유자 미지정(`not so`)」**을 돌려준다 ⇒ **스크리너를 안 거친 종목이 들어올 수 있다.**
- 불가능봉 가드가 여기에도 있다(**640-659**) — 다만 프레임이 **82봉**이라 창 밖 사건은 원리적으로 못 본다(LANE_C §2-d).
- **실측 전례**: 09-09·09-10 이틀 연속, rs_leader **자기 스크리너가 제외한 003350**(제외 줄 **477**)에
  **자기 on_tick 이 매수 시그널**을 냈다(줄 **920**·**1046**). 체결 0건. ⇒ ***스크리너만 막으면 구멍이 남는다.***
- 매도 루프(**688-712**)도 같은 `generate_signal` 을 부르지만 `RSLeaderStrategy.generate_signal`
  (`strategies/rs_leader/strategy.py:85-101`)이 `:96` 에서
  **`if stock_code in self.positions: return self._check_sell(...)`** 로 갈라 놓는다
  ⇒ **매수 전용 진입점 `_check_buy`(157) 가 이미 존재한다.**

### 1-4. 큐 (`collectors/corp_action_watch.py`) — DB 표가 아니라 JSONL 파일

- 파일 `logs/corp_action_refetch_queue.jsonl`(`:57` · 경로 `:64-70` · **env `CORP_ACTION_QUEUE_PATH` 로 통째 바뀐다** `:67`).
  2026-09-10 15:46 현재 **303줄 · 전부 `status=pending` · merge 274 / split 29 · 고유 종목 288(merge 264)**.
- 탐지 규칙 `scan_series`(**75-110**): 직전 **3봉 이상 거래량 0**(`_HALT_MIN_BARS=3`, `:39`) 다음 봉의 종가비가
  밴드 **[0.69, 1.45]**(`:45-46`) 밖.
- 생산 시점 = **EOD `collectors/daily_collector.py:108`** (2026-09-10 로그 줄 **5029**, 15:46:42).
- **append-only · 상태 갱신 없음**(`record_candidates` **157-174**) · 스캔 창은 **120 «달력»일**(`:55`).
- 라이브 매매 트리에 **큐 독자는 0건**(LANE_C §4-b 전수 grep) — 지금은 매수 적격성과 연결된 적이 없다.

### 1-5. 오늘의 사실 (SQL 근거)

- rs_leader 09-09 스냅샷 20행 중 **9행이 merge 큐**(랭크 1·2·3·6·7·8·9·10·14) · 09-10 매수 4건 **전부** 큐 종목.
- 전 기간 rs_leader 매도 149건 중 **111건(74.5%)이 「큐 종목 · 재개일 이후」이고 누적 손실의 89.3%**(MANAGER 메모).
- `SELECT * FROM paper_strategy_equity WHERE trade_date='2026-09-10' AND strategy='rs_leader';`
  → **cash 699.85 · position_value 6,603,749 · equity 6,604,448.85 · realized_pnl_cum −3,110,484 · n_open 10**.
- 보유 10종목 중 **8종목이 merge 큐**(001210·001290·002780·008040·024850·049080·060230·204840), 비큐 2(079650·159010).

### 1-6. 스크리너 재현 성공 — 이 문서의 산술 예측이 근거를 갖는 이유

`daily_prices` 만으로 rs_leader 스크리너를 SQL 로 재현했다(유니버스 `db/quant_daily_reader.py:56-108`,
거래대금 `:90-92`, 룰 `strategies/rs_leader/rule.py:38-56`, 가드 `utils/data_sanity.py:56-84`).

| 재현값 | 값 | 교차검증 |
|---|---|---|
| `base_filter` 통과 | **760** | — |
| 불가능봉 제외 | **15** | 09-10 로그 rs_leader `후보 제외` **15줄** ✅ |
| `match` 통과 | **140** | — |
| 상위 20 | 코드·순서 **전부 일치** | `screener_snapshots(rs_leader, 2026-09-09)` 20행 rank 1~20 과 **완전 일치** ✅ |

⇒ 아래 §2 Q2 의 「후보 20 → N · 선정 10 → M」은 추정이 아니라 **재현된 산술**이다.

---

## 2. 설계 질문 판정

### Q1. 배제의 소스 오브 트루스 — **채택: (ii) 런타임 파생**

| 안 | 내용 | 장점 | 단점 |
|---|---|---|---|
| (i) | `logs/corp_action_refetch_queue.jsonl` 을 매매 트리가 읽는다 | 사람이 읽는 기록과 같은 목록 · 구현 최소 | 🔴 라이브 «매수 적격성»이 로그 파일 + env 오버라이드(`corp_action_watch.py:67`)에 달린다 · 파일이 없으면 **조용히 배제 0건**(fail-open) · append-only 라 **고쳐져도 영원히 pending** · 신선도는 잘해야 D-1 15:46 |
| **(ii)** | 큐 «생산 로직»(`scan_series`)을 스크리너가 **이미 로드한 일봉 프레임**에 그대로 적용 | **추가 DB 접근 0** · 판정 대상 = 룰이 «실제로 보는» 그 프레임 ⇒ 원리적으로 stale 불가 · 데이터가 고쳐지면 **자동 해제** · 임계·밴드는 `corp_action_watch` 상수 재사용(**새 문턱 0개**) | 큐 파일 목록과 «완전히» 같지는 않다(아래 차분) · `strategies/` → `collectors/` import 엣지 1개 신설 |
| (iii) | EOD 에 큐를 DB 표(`corp_action_queue` 류)로 적재 → 스크리너가 DB 읽기 | 표라서 관측·조인 쉬움 | 새 표 + migration + writer + 소비자 = 4곳 · **문제(신선도·자동해제)를 하나도 안 푼다** · 09:00 스크리너는 여전히 D-1 데이터를 본다 |

**실측 차분** (scan_date 2026-09-09 · `base_filter` 통과 760종목 · 프레임 130봉)

| 측정 | 값 |
|---|---|
| 런타임 파생이 표시한 종목 | **27** |
| 큐(고유 288) ∩ 760 | **28** |
| 교집합 | **26** |
| 런타임에만 있음 | **1** |
| 큐에만 있음 | **2** |
| `match` 통과 140 중 — 큐 **10** / 런타임 **9** | 상위 20 에 대한 효과는 **9 로 동일** |

🔑 **자동 해제의 실증**: 두 집합이 갈리는 대표 사례가 **014990**(큐 등재 · 재개일 2026-06-26)이다. 오늘 DB 값은
정지봉 종가 2,400(거래량 0, `adj_factor` 0.2) → 해제봉 1,720(`adj_factor` 1) = 비 **0.717** 로 밴드 [0.69, 1.45]
**안**이라 런타임 파생이 표시하지 않는다. 큐는 여전히 `pending` 이다.
⇒ ***큐는 「한 번 의심하면 영원히 의심」이고, 런타임 파생은 「지금 데이터가 그런가」를 본다.***
(014990 은 ③ 1차 트랜치 혼합 상태 3종목 중 하나다 — 값이 왜 바뀌었는지 자체는 §7-1 미확인.)

**「못 본다 ≠ 없다」**: 어느 쪽도 미조정의 «부재»를 증명 못 한다. (ii) 가 나은 점은 주장의 범위가 좁다는 것뿐이다.
큐에만 있는 2종목이 (ii) 에서 통과하는 건 **설계된 거동**(창 밖이거나 값이 변했다)이고, 런타임에만 있는 1종목은
큐의 120달력일 창이 못 본 것이다. **미탐은 두 안 모두 못 잡는다** — §6 P7 에 반증 항목으로 둔다.
**부수 결정**: 매매 트리는 큐 파일을 **읽지 않는다**(큐는 사람·EOD 리포트용 기록으로 존치, `collectors/daily_collector.py:113`).
코드 재사용은 **`scan_series` 함수 하나**로 한정한다.

### Q2. 배제 층 — **채택: `RSLeaderScreenerAdapter.match()` 안, 룰 평가 «앞»**

Q1 (ii) 를 택한 순간 `base_filter()` 는 **불가능**하다 — `base_filter` 는 `{code, market_cap, trading_value}` 만
받고(`_rule_screener_base.py:186-192`) 일봉이 없다. 일봉이 있는 가장 이른 지점이 `_prepare_frame` 다음, 즉
`match` 진입 직후다.

| 층 | 후보 20 → | 선정 10 → | 평가 |
|---|---|---|---|
| **A. `match()` 앞 (채택)** | **20** (랭크 21~29 가 백필) | **10** | 정렬·topK «앞»이라 슬롯이 안 줄어든다 · 바뀌는 파일 1개(rs_leader 어댑터) |
| A′. `_prepare_frame` 에 공통 훅 | 동일 | 동일 | 로그 위치가 불가능봉 가드와 같아 예쁘지만 **8전략 공통 경로**를 건드린다 — 확장할 때 쓴다(§7-2) |
| B. 정렬 직전(`scored` 적재 후) | 동일 | 동일 | A 와 결과 동치인데 룰 평가를 낭비 |
| C. `scan` 의 topK «뒤» | **11** | **10** | 🔴 후보 11 중 10 선정 = 「거의 전부 선정」 · 질 저하 · 슬롯 소실 |
| D. `core/candidate_selector` 안전성 필터 | 스냅샷 20 불변 · 풀 11 | **10** | 🔴 **8전략 공통** 필터라 범위(rs_leader 만)를 못 지킨다 · `:518-520` 「배제 범위는 주문 가능 여부에 한정」(2026-08-09 사장님 결정)과 정면 충돌 |

**「base_filter 에 넣으면 RS 분모가 바뀐다」는 rs_leader 에는 해당하지 않는다.** score 는 그 종목 하나의 120일
수익률(`screener.py:60`)이라 모집단 축소가 살아남은 종목의 score 를 못 바꾼다. 재현으로 확인: 10종목을 뺀 뒤
남은 순서는 **원래 순서 그대로**였다. 이 경고가 진짜인 곳은 minervini(`build_context`, §1-2)이고 §7-2 에 남긴다.
그래도 층 A 를 고른 이유는 분모가 아니라 **일봉이 필요해서**다 — 두 이유를 섞지 말 것.

**산술 예측 (채택안 A · scan_date 2026-09-09)**

- `base_filter` 760 → 불가능봉 15 제외 → `match` 통과 **140** → 그중 런타임 표시 **9** 제외 → **131**.
- **후보 20 → 20** (백필 성립: 131 ≫ 20).
  - 새 상위 20 = 기존 비큐 11개(079650·159010·002990·00088K·024840·069540·020120·131290·091590·134580·198440)
    + **신규 9개**(446540·067290·006110·078930·052860·053260·228340·098120·375500).
- **선정 10 → 10**. 새 상위 10 =
  **079650 · 159010 · 002990 · 00088K · 024840 · 069540 · 020120 · 131290 · 091590 · 134580** (안전성 필터 통과 가정).
- ⇒ 사장님께 미리 보고된 「후보 20 중 11 만 남는다」는 **C 층을 골랐을 때의 숫자**다. **A 층이면 후보 수는 줄지 않는다.**

### Q3. on_tick 경로 — **채택: `RSLeaderStrategy._check_buy`(`strategies/rs_leader/strategy.py:157`) 맨 앞**

| 안 | 평가 |
|---|---|
| (a) `strategies/base.py:640-659` 82봉 가드 옆 | 🔴 8전략 공통 경로에 「rs_leader 인가?」를 묻게 된다 — 프레임워크가 전략 이름을 아는 역전 |
| **(b) `_check_buy` 맨 앞 (채택)** | `generate_signal`(**85-101**)이 `:96` 에서 **보유=매도 / 비보유=매수** 로 이미 갈라 놨다 ⇒ 매수만 막고 **`_check_sell`(182) · `evaluate_sell_conditions`(137-155) · `core/trading/position_monitor` 는 한 줄도 안 건드린다**. 바뀌는 파일 1개 |
| (c) 스크리너가 계산한 「오늘 배제 집합」을 모듈 전역에 실어 전략이 읽음 | 130봉 사정거리를 on_tick 에도 준다는 장점이 있으나, 스크리너가 안 돌면(`SCREENER_SNAPSHOT_ENABLED=false` · 훅 실패) **집합이 비어 조용히 fail-open** — 「폴백이 고장을 감춘다」와 같은 형태. **불채택** |

**청산 불변의 보장 (3중)**

1. **구조**: 배제 코드는 `_check_buy` 안에만 있다. `_check_sell` 은 다른 함수다.
2. **계약**: 「보유 종목의 프레임이 배제 대상이어도 매도 신호는 그대로 난다」를 테스트로 못박는다(§4 T6).
3. **위임 경로 불변**: sl/tp/max_hold 는 `core/trading/position_monitor.py` 가 라이브 현재가로 판정한다
   (`strategies/rs_leader/strategy.py:137-155` 주석) — 이 변경의 사정거리 밖이다.

🔴 **정직하게 적어 둘 한계 — on_tick 의 사정거리는 82봉이다.** on_tick 이 받는 프레임은 `ctx.get_daily_data` 의
82봉(09-10 실측: `일봉 N건` 869줄이 전부 82건)이라 배제 판정도 82봉 안의 사건만 본다. 오늘 배제 대상 9종목의
재개일을 거래일 거리로 보면 **024850(88거래일 전)은 82봉 창 밖**이고 나머지 8종목은 창 안이다. 003350(97거래일 전)
유형은 **이 가드로도 안 잡힌다** — 불가능봉 가드가 같은 이유로 못 잡는 것과 동일한 구조다.
⇒ **「on_tick 도 막았다」를 「전부 막았다」로 읽지 말 것.** 스크리너(130봉)가 1차, on_tick(82봉)이 2차다.

### Q4. 배제 창 — **채택: 별도 창 파라미터를 «두지 않는다»** (프레임이 곧 창)

| 안 | 평가 |
|---|---|
| (i) 영구 배제 | 큐 파일 기반에서만 가능. 「고쳐져도 영원히 못 산다」 — 014990 이 그 예 |
| (ii) 재개일 + 120거래일 | 의도는 「RS 창이 사건을 벗어날 때까지」. 별도 만료 장부·상수·테스트가 필요하다 |
| (iii) 재수집·보정으로 절벽이 고쳐지면 자동 해제 | 바람직하나 큐 기반에선 `status` 갱신 장치가 **없다** |
| **채택** | Q1 (ii) 를 쓰면 **(ii)와 (iii)이 «자동으로» 따라온다.** 판정 대상이 130봉 프레임이므로, 사건이 프레임을 벗어나면 표시가 사라지고, 값이 고쳐지면 그날부터 사라진다. **새 상수 0개, 만료 장부 0개.** |

🔑 **순서가 안전한 방향이다**: 가드 프레임 **130봉** > RS 창 **121봉**(`rs_lb=120` + 기준봉)
⇒ 사건은 **RS 점수를 오염시키지 않게 된 «뒤에» 배제가 풀린다**(여유 약 9봉). 반대 순서였다면 오염된 점수로
사는 구간이 생긴다.

**오늘 배제 대상의 예상 해제 시점** (2026-09-10 기준 거래일 거리, 005930 달력)

| 종목 | 재개일 | 재개 후 거래일 | 130봉 창 이탈까지(예상) |
|---|---|---|---|
| 024850 | 2026-05-04 | 88 | 약 42거래일 |
| 032790 | 2026-05-27 | 73 | 약 57거래일 |
| 049080 | 2026-06-29 | 51 | 약 79거래일 |
| 060230 | 2026-07-06 | 46 | 약 84거래일 |
| 001210 | 2026-07-31 | 28 | 약 102거래일 |
| 204840 | 2026-08-11 | 21 | 약 109거래일 |
| 300120 | 2026-08-20 | 15 | 약 115거래일 |
| 002780 | 2026-09-07 | 3 | 약 127거래일 |
| 001290 | 2026-09-09 | 1 | 약 129거래일 |

⚠️ 「예상」이다 — **정지런 봉이 먼저 창을 빠져나가면 `halt_run < 3` 이 되어 표시가 더 일찍 사라진다.**
이 방향은 덜 보수적이므로 §6 P5 에서 관측한다.

### Q5. 위양성 — **채택: `corp_events` 교차확인을 «걸지 않는다»**

큐/런타임 탐지는 가격 기반이라(`corp_action_watch.py:75-89`) 진짜 급등(신규상장·실적 서프라이즈)을 배제할
위험이 제기됐다. 「공시로 확인된 것만 배제」 안의 커버리지를 DB 로 측정했다.

SQL: `qmerge(code,resume) LEFT JOIN LATERAL (SELECT 1 FROM corp_events e WHERE e.stock_code=q.code AND e.event_type='split' AND e.event_date BETWEEN q.resume-120 AND q.resume+30 LIMIT 1)`.

| 교차 조건 | 커버리지 |
|---|---|
| `event_type='split'` · 창 [재개일−120d, +30d] | **78 / 264 = 29.5%** |
| `split`+`bonus_issue`+`rights_issue` · 창 [재개일−365d, +30d] | **105 / 264 = 39.8%** |
| 오늘 `match` 통과 큐 10종목 중 `split` 공시 보유 | **4 / 10** (024850·204840·300120·001290) |

🔴 **이 안을 채택하면 손실 귀속의 머리부터 되돌아온다** — 랭크 1 **049080**(비 7.82), 랭크 3 **002780**(비 9.68),
랭크 14 032790 은 창 안에 `split` 공시가 **없어서 통과**한다. 060230·001210 은 `rights_issue` 만 있다.
즉 「공시 확인된 것만 배제」는 오늘 9종목 중 **5~6종목을 다시 통과**시킨다. **불채택.**
(배경: `corp_action_watch.py:15-20` 이 이미 「탐지는 corp_events 가 아니라 가격 그 자체로 한다 — 운영 수집기는
`pblntf_ty="B"` 만 보고 있어 분할·병합 공시를 한 건도 잡은 적이 없다」고 적어 놨다. 위 78/264 가 그 문장의 실측판이다.)

**대신 위양성은 «구조»로 제한한다.**

1. 탐지는 **연속 3봉 이상 거래량 0 + 밴드 이탈**을 «둘 다» 요구한다. 매일 거래되는 진짜 급등은 정지런이 없어
   원리적으로 안 걸린다(신규상장 첫날도 직전 3봉 자체가 없다).
2. 규모: 런타임 파생이 표시한 종목은 `base_filter` 통과 760 중 **27(3.6%)** 이다.
3. 비용 비대칭: 배제의 오류는 **기회손실**(안 산다)이고, 미배제의 오류는 **오염된 랭킹으로 산다**. 이 전략의
   누적 손실 89.3% 가 후자 쪽에서 나왔다.
4. ⚠️ 그래도 **위양성 0 을 주장하지 않는다.** §6 P6 이 「배제된 종목의 그 뒤 20거래일 수익」을 인쇄해 사후 판정
   재료를 남긴다(판정은 하지 않는다 — 사후적합 방지).

### Q6. 사전등록 — §6 참조 (지표·실패조건·롤백조건을 «발효 전»에 못박는다)

### Q7. 범위 밖으로 «명시»하는 것

1. **기존 보유 8종목(001210·001290·002780·008040·024850·049080·060230·204840)의 청산 룰은 그대로다.**
   이 변경은 매수만 막는다(§2 Q3). 보유분은 기존 ma_break / max_hold(30거래일) / position_monitor 의 sl·tp 로만 빠진다.
2. 큐 종목 **가격 재수집**(옵션 c)은 하지 않는다 — 백로그 「보정 도구 `--apply` 막힘」 계층 그대로.
3. **다른 7전략은 한 줄도 안 바뀐다**(§4 T8 이 이를 시험한다).
4. 🔴 **발효 첫날 효과가 «안 보일 수 있다», 그리고 그건 배제 때문이 아니다.**
   현재 rs_leader 는 **cash 699.85원 · n_open 10 = `max_positions` 10**(`strategies/rs_leader/strategy.py:52`)이다.
   `_check_buy` 는 **배제 판정에 닿기도 전에** `len(self.positions) >= self._max_positions` 로 `None` 을 돌려준다
   (`:99-100`). ⇒ ***「매수 0건」은 과잉결정(over-determined) 관측이라 배제 효과의 지표로 쓸 수 없다.***
   §6 은 그래서 **후보·선정 «집합»**을 1차 지표로 쓴다.

---

## 3. 채택 설계

### 3-1. 데이터 흐름

```
[스크리너 09:00, scan_date=D-1]  RuleScreenerBase.scan
   base_filter(760) → _prepare_frame(일봉 130봉 + 불가능봉 가드, 기존)
     → RSLeaderScreenerAdapter.match
         ├─ (신설) corp_action 배제 판정  ← 같은 df 로 scan_series 재사용, DB 접근 0
         │     mode=live   → return None  (+ WARNING 1줄)
         │     mode=shadow → 로그만, 판정 계속
         │     mode=off    → 판정 자체를 안 한다(코드 진입 0)
         └─ 기존 절대상승추세 룰
     → 정렬 + topK(20)        ← 배제분은 여기 오기 «전»에 빠져 백필된다
   screener_snapshots 저장(20행)

[후보 소비 09:00]  candidate_selector  → 선정 10  (이 층은 «안 바뀐다»)

[장중 on_tick]  BaseStrategy.on_tick → RSLeaderStrategy.generate_signal
   ├─ 보유 종목 → _check_sell            ← «안 바뀐다»
   └─ 비보유    → _check_buy
                   └─ (신설) 같은 배제 판정 (프레임 82봉 — 사정거리 한계 §2 Q3)
```

### 3-2. 상수 — 1개 (3모드)

```python
# config/constants.py  (섹터 뉴스 부스트 `:198-224` 와 «같은 형식»)
# =============================================================================
# rs_leader — 미조정 기업행위(합병) 의심 종목 매수 배제 (2026-09-10 사장님 결정 (b))
#   spec: docs/superpowers/specs/2026-09-10-rsleader-corp-action-exclusion-design.md
#   off    : 판정 자체를 안 한다 (코드 진입 0, 로그 0) ← 롤백 위치
#   shadow : 판정·기록만, 후보는 «원래대로» 반환      ← 기본값(제안)
#   live   : 배제 발효
# 🔴 롤백은 이 값을 "off" 로 되돌리는 것 «하나»뿐이다. 롤백은 이미 체결된 매매를 되돌리지 않는다.
# =============================================================================
RS_LEADER_CORP_ACTION_MODES = ("off", "shadow", "live")
RS_LEADER_CORP_ACTION_MODE = ...   # env RS_LEADER_CORP_ACTION_MODE, 기본 "shadow"
```

**기본값을 `shadow` 로 «제안»하는 이유** (사장님 결정 사항 — §7-4)

1. 09-05 계획서 「6주간 진입·청산 룰 라이브 변경 0건」의 예외 1건이다. `shadow` 로 머지하면 **머지 시점엔 룰이
   안 바뀌고**, 발효는 상수 한 글자를 바꾸는 별도 승인으로 분리된다 — 사장님의 「머지·발효는 별도 승인」과 정확히 맞다.
2. `shadow` 기간의 로그가 곧 §6 사전등록의 **발효 전 기준선**이 된다(같은 날 「배제했다면 무엇이 빠졌나」가 인쇄된다).
3. 선례: `SECTOR_NEWS_BOOST_MODE` 기본 `shadow`(2026-09-06 결정, `config/constants.py:202`). 모르는 값은
   **`off` + WARNING**(`resolve_sector_news_mode` `:208-215` 규약).

### 3-3. 로그 문구 (태그 고정 — 발효일을 끊는 계기)

```
# 스캔당 1줄 (mode != off 일 때만) — 이 줄의 유무·mode 값이 «발효일 계기»다
[rs-corp-action] mode=live scan_date=2026-09-09 universe=760 evaluated=725 flagged=9 kept=131 codes=049080,001210,002780,204840,001290,024850,060230,300120,032790

# 종목당 1줄 (스크리너, WARNING — 불가능봉 가드 줄과 같은 형식)
[rs_leader] 001290: 미조정 병합 의심(정지 13봉 → 재개 2026-09-09, 종가비 4.455) — 후보 제외 (mode=live)

# on_tick (WARNING)
[신호없음] 001290: 미조정 병합 의심(정지 13봉 → 재개 2026-09-09, 종가비 4.455) — 진입 제외 (mode=live)
```
(shadow 는 문구 끝만 다르다: `— 후보 제외 «안 함»(mode=shadow)`)

- 🔑 `flagged` 와 `kept` 를 **한 줄에 둘 다** 찍는다. 건수만 찍으면 「배제가 0건」과 「스캔이 안 돌았다」가 구별이
  안 된다(2026-09-08 교훈: 한 규칙의 두 축은 따로 판정한다).
- `codes=` 를 찍는 이유: EOD 점검이 **건수가 아니라 집합 차분**으로 돌아가기 때문이다(메모리 운영 규칙).

### 3-4. 롤백

`config/constants.py` 의 `RS_LEADER_CORP_ACTION_MODE` 를 **`"off"`** 로 (또는 env 한 줄).
⚠️ 롤백은 **이미 산 종목을 되돌리지 않는다**. 그리고 `shadow` 로만 내리면 **판정·로그는 계속 돈다** — 관측을
잃지 않고 룰만 끄는 중간 단계다. 완전 정지가 필요하면 `off`.

### 3-5. 바꾸면 «안 되는» 것

1. `collectors/corp_action_watch.py` 의 **임계·밴드·큐 생산 동작**(`_HALT_MIN_BARS` · `_NORMAL_BAND_*` ·
   `_REFETCH_DELAY_DAYS` · `_SCAN_LOOKBACK_DAYS` · JSONL 포맷). 재사용만 한다.
2. `strategies/_rule_screener_base.py` 의 `scan`/`_prepare_frame`/불가능봉 가드 · `sanity_window` 계약.
3. `core/candidate_selector.py` 의 안전성 필터 5축과 2026-08-09 결정(`:518-520`).
4. `RSLeaderStrategy._check_sell` · `evaluate_sell_conditions` · `core/trading/position_monitor.py`.
5. `RSLeaderStrategy.evaluate_entry`(**121-135**, `@staticmethod`) — **백테스트가 부르는 순수 함수**다. 여기에
   배제를 넣으면 연구 재현이 조용히 바뀐다. 배제는 `_check_buy` 에만.
6. `screener_snapshots` 스키마 · `MAX_CANDIDATES_PER_STRATEGY`(20) · `max_per_strategy`(10).
7. 다른 7전략의 어댑터·전략 파일.

---

## 4. 테스트 계약 (TDD — 전부 «먼저» 실패해야 한다)

| # | 테스트 | 못박는 계약 |
|---|---|---|
| T1 | `mode=live` + 「3봉 거래량 0 → 4.5배 점프」 합성 프레임 → `match()` 가 `None` | 배제가 실제로 작동 |
| T2 | `mode=shadow` + 같은 프레임 → `match()` 는 **원래 튜플** 반환 + shadow 로그 1줄 | shadow 가 룰을 안 바꾼다 |
| **T3** | **회귀 게이트** — `mode=off` 로 25종목 합성 유니버스를 `scan()` → 후보 코드·순서·score 가 변경 «전»과 **완전 일치**, 그리고 `scan_series` 호출 0회 | 🔴 **스위치 off 면 후보 완전 일치** |
| T4 | `mode=live` · 25종목 중 9종목 flagged · `max_candidates=20` → `scan()` 이 **20건**을 돌려주고 그 안에 flagged 0건 | **백필**이 일어난다(11건으로 줄지 않는다) |
| T5 | `mode=live` · `_check_buy(flagged frame)` → `None` + `[진입 제외]` 로그 | on_tick 2차 방어 |
| **T6** | 같은 종목을 `self.positions` 에 넣고 **같은 flagged 프레임**으로 `generate_signal` → `_check_sell` 경로로 가서 **매도 신호가 그대로** 난다 | 🔴 **청산 룰 불변** (대칭 단언) |
| T7 | flagged 사건이 **82봉 창 «밖»**인 프레임 → `_check_buy` 가 **막지 않는다** | 알려진 사정거리 한계를 «못박아» 과대주장 차단 |
| T8 | ma20·ma5·daytrading·elder 어댑터에 같은 flagged 프레임 → 판정 **불변** | 범위 = rs_leader 만 |
| T9 | 새 코드에 `3` · `0.69` · `1.45` 리터럴이 없다(`corp_action_watch` 상수 참조) | 문턱 이중선언 금지 |
| T10 | `RS_LEADER_CORP_ACTION_MODE="banana"` → `off` + WARNING | 모르는 값 규약(`resolve_sector_news_mode` 선례) |
| T11 | `close` 에 0·NaN, `volume` 컬럼 결측 프레임 → 예외 없음 · 배제 없음 | 「판정 불가 = 통과」(`data_sanity.py:76-84` 규약) |
| T12 | `scan_series` 를 3봉 미만 정지런·밴드 «안» 비로 호출 → flag 0 | 재사용 함수의 계약 확인(기존 `tests/collectors/test_corp_action_watch.py` 와 중복 아님) |

**리포지토리 회귀 게이트**: 워크트리에서만 · **09:10 KST 이후** · 기준선 `D:/tmp/main_baseline_failures_20260907.txt`(15줄)
와 실패 **집합의 양방향 차분**(새로 생긴 것 / 사라진 것 둘 다 0). 🔴 **라이브 트리에서 테스트 실행 금지.**

---

## 5. 구현 계획 개요

- 워크트리 `D:/tmp/kis-wt-rsleader-corp` · 브랜치 `feat/rsleader-corp-action-exclude` · base = 지수 KIS 머지 «후» main.
- 커밋 단위: ① `test:` T1~T12 (red) → ② `feat:` 상수 + `resolve_*` 헬퍼 → ③ `feat:` 스크리너 `match()` 배제 + 로그
  → ④ `feat:` `_check_buy` 배제 + 로그 → ⑤ `docs:` `strategies/rs_leader/README.md` + `docs/PAPER_STRATEGIES.md` 한 줄.
- 예상 변경: `config/constants.py` · `strategies/rs_leader/screener.py` · `strategies/rs_leader/strategy.py` + 테스트 1~2파일.
  **`collectors/` · `core/` · `bot/` · `strategies/base.py` 는 안 건드린다.**
- 코드리뷰(`code-reviewer`) → 사장님 보고 → **머지·발효는 별도 승인** · 머지는 장중(09:00~15:30) «밖» · `--no-ff`.

---

## 6. 사전등록 — 발효 후 20거래일 (예측을 «미리» 적는다)

| # | 예측 | 반증 |
|---|---|---|
| P1 | rs_leader **후보 수 = 20** 이 유지된다(`screener_snapshots` count) | 20 미만이 나오면 백필 가정(§2 Q2) 실패 |
| P2 | **선정 수 = 10** 유지(`[E6] rs_leader … 10건 확보`) | 10 미만 |
| P3 | **큐/런타임 표시 종목의 rs_leader 매수 = 0건** | 1건이라도 나오면 배선 누락 |
| P4 | 발효 첫날 `[rs-corp-action]` 줄의 `flagged` = **9 ± (그 사이 신규 큐)** · `codes` 가 §2 Q2 목록과 일치 | 집합 차분이 나면 탐지가 예상과 다르다 |
| P5 | 024850 은 약 42거래일 뒤 `codes` 에서 사라진다(더 일찍 사라질 수 있다 — §2 Q4 ⚠️) | 90거래일 뒤에도 남아 있으면 창 이해가 틀렸다 |
| P6 | 배제된 종목의 배제 후 20거래일 수익률을 **인쇄만** 한다 | — (판정 안 함. 사후적합 방지) |
| P7 | 「큐에도 없고 런타임에도 안 걸린 미조정」이 발견되면 즉시 보고 | 미탐은 두 안 모두 못 잡는다 |
| P8 | 다른 7전략의 후보·선정·매수 집합 **불변** | 하나라도 바뀌면 범위 이탈 |

**성적 비교 규약**: rs_leader 실현손익은 **「발효 전 / 발효 후」로만** 나눠 본다. 🔴 **인과 주장 금지** —
반사실(「배제 안 했으면」)은 재실행 없이 말할 수 없다(MANAGER 메모 §해석의 한계 2).
그리고 §2 Q7-4 대로 **「매수 건수」는 지표로 쓰지 않는다**(cash 699.85원 · n_open 10 = `max_positions` 로 과잉결정).

**실패 조건 (보고)**: 20거래일 중 **후보 < 10 인 날이 1일이라도** 발생 · 또는 `[rs-corp-action]` 줄이 **하루라도 없음**.
**롤백 조건 (즉시 `off`)**: ① 후보 < 5 가 2거래일 연속 ② 배제가 «보유 종목의 매도»를 막은 관측 1건
③ 다른 전략의 후보 집합이 바뀐 관측 1건.

---

## 7. 미확인 · 후속

1. **014990 의 값이 왜 바뀌었나** — 큐 등재 시점과 현재 DB 의 정지해제봉 비가 다르다(현재 0.717 = 밴드 안).
   ③ 1차 트랜치 혼합 3종목(011090·014990·226340) 중 하나라는 것 외에 원인 미확인. **자동 해제의 «증거»로는 쓰되
   「보정이 성공했다」로는 읽지 말 것.**
2. **후속 — 다른 전략 확장**: ma5 4/20 · ma20 1/20 · daytrading 1/20 · elder 0/20 · minervini 0/5 (09-09 스냅샷).
   확장하려면 층 A′(`_prepare_frame` 공통 훅 + 어댑터 플래그, 기본 False)가 맞다. 🔴 **minervini 는 `base_filter`
   통과 집합이 RS 백분위의 분모**(`minervini_volume_dryup/screener.py:102-104`)라 배제가 **TT 판정을 오염시킨다**
   — **9/22 TT 판정 전에는 건드리지 말 것.**
3. **후속 — 낡은 주석 정정**: `core/candidate_selector.py:1073` 「EOD 스냅샷은 D-1에 생성됨」은 실제와 다르다
   (당일 09:00 생성 · scan_date=D-1). 별건 커밋.
4. **사장님 결정 대기**: ① 기본값 `shadow` vs 곧바로 `live`(§3-2) ② `shadow` 관측 기간(제안: 3거래일)
   ③ 발효일 확정(3전략 집중 6주의 예외 1건이므로 W-경계에 맞추는 편이 성적 비교에 깨끗하다).
5. **후속(선택) — `scan_series` 를 `utils/corp_action_sanity.py` 로 순수 이동**해 `strategies/`→`collectors/`
   import 엣지를 없앤다. 이번 변경과 **같은 커밋에 섞지 않는다**(한 번에 한 축).
6. **미확인** — 이 설계는 «미조정이 남아 있는 동안 안 산다»일 뿐 **데이터를 고치지 않는다**. 근본 트랙(불가능봉·
   보정 도구 `--apply` 막힘 · 일봉 결손 49,252행)은 그대로 남는다.

---

## 8. errata rev2 (2026-09-10 · 코드리뷰 rev1 반영 · 구현이 정본)

> 이 절은 **설계 판정을 뒤집지 않는다**. 위 §0~§7 중 구현과 어긋나거나 내부적으로
> 모순인 문장을 «구현 쪽으로» 맞춘 기록이다. 판정(§2 Q1~Q7)은 그대로 유효하다.
> 🔑 사전등록(§6)을 읽을 때는 이 절을 «먼저» 반영해서 읽을 것 — P4 의 `codes` 집합과
> 실패 조건이 여기 걸린다.

### E1. §3-1 — 배제는 `scan()` «안»에서만 돈다 (리뷰 🟡-1, HIGH)

§3-1 다이어그램은 「`RSLeaderScreenerAdapter.match` 안」이라고만 적었다. 그런데 `match()` 는
`scan()` 전용이 아니다 — **백테스트 러너 2본이 어댑터를 만들어 `match()` 를 직접 루프한다**:

- `backtest/live_universe_revalidation/run.py:52-53,190,214`
- `backtest/universe_lookahead_ladder/run.py:48,203`

거기까지 배제가 발효하면 두 러너의 rs_leader 결과가 조용히 바뀐다 —
**§3-5 5항(`evaluate_entry` 불변 = 연구 재현 오염 금지)이 막으려던 것과 «같은 종류»의 결함**이다.
기본 `shadow` 에서도 문제였다: ①`detect` 가 룰보다 비싸 감속 ②러너 프레임엔 `attrs["stock_code"]`
가 없어 `[rs_leader] ?: …` 로그 폭주 ③`_ca_flagged` 가 `finalize_scan` 에서만 리셋되는데
러너는 `scan()` 을 안 부르므로 무한 증가.

**정본**: `scan()` override 가 `_ca_active` 를 세우고 `match()` 첫 줄이
`mode == "off" or not _ca_active` 면 이전 경로로 돌아간다. 즉 **§3-5 5항의 「배제는 `_check_buy`
에만」은 「배제는 `scan()`·`_check_buy` 두 «경로»에만」으로 읽어야 한다.**
⚠️ `attrs` 유무로 가르는 대안은 **금지** — 러너가 언젠가 attrs 를 붙이면 조용히 배제가 켜진다
(폴백이 고장을 감추는 형태). 계약은 T13 이 못박는다.

### E2. §3-1 — `flagged` 계수 시점 = 「룰을 통과했을」 종목만

§3-1 은 배제를 룰 «앞»에 두라 하고, §3-3 예시·§6 P4 는 `flagged=9`·`kept=131`
(= 140 = **룰 통과분** 기준)을 쓴다. 룰 앞에서 곧바로 `return None` 하면 `flagged` 는
**27**(`base_filter` 통과 760 중 런타임 표시 전체)이 되어 P4 의 `codes` 집합과 어긋난다.

**정본**: 판정(`scan_series`)은 룰 «앞»에 두되, **계수·종목당 로그는 룰을 통과했을 종목만** 센다.
반환 결과(후보 집합·순서)는 어느 순서로 읽어도 동일하다. ⇒ **§6 P4 의 「flagged = 9」는 그대로 유효**.

### E3. §3-3 — 계기 줄에 `matched=` 추가, `kept` = 실제 반환 수 (리뷰 🟡-3)

rev1 은 shadow 에서 flagged 가 `kept` 에도 들어가 **발효일에 `kept` 의 «정의»가 바뀌었다**
(같은 34종목: shadow `kept=34` → live `kept=25`). §3-2 는 shadow 로그를 「발효 «전» 기준선」으로
쓰라고 했는데, 정의가 모드에 걸리면 그 비교가 가짜 계단을 만든다.

**정본 — 세 칸 다 모드에 안 걸린다**:

```
[rs-corp-action] mode=shadow scan_date=2026-09-09 universe=760 evaluated=725 matched=140 flagged=9 kept=140 codes=…
[rs-corp-action] mode=live   scan_date=2026-09-09 universe=760 evaluated=725 matched=140 flagged=9 kept=131 codes=…
```

- `matched` = 룰 통과 «총수» (모드 무관 — 발효 전후 비교의 축)
- `flagged` = 표시된 수 (= 룰을 통과했을 종목 중, E2)
- `kept` = **실제로 후보가 된 수** (shadow 는 안 뺀다)

### E4. §3-3 — 기동 로그 1줄 추가 (리뷰 🟢-7)

스캔당 줄은 `SCREENER_SNAPSHOT_ENABLED`(`config/constants.py` 기본 `false`)가 꺼지면 통째로
사라져 **§6 실패 조건 「`[rs-corp-action]` 줄이 하루라도 없음」이 «무관한 이유»로 발화**한다.
그래서 전략 초기화(`RSLeaderStrategy.on_init` — `main.py:220` 이 프로세스당 1회 호출) 시점에
`[rs-corp-action] mode=… (startup)` 1줄을 남긴다. ⇒ **§6 실패 조건은 「기동 줄이 있는데 스캔 줄이
없는 날」로 읽을 것** (그래야 「스위치 값」과 「스크리너가 돌았나」를 따로 판정한다).

### E5. §3-3 — `split` 방향은 「미조정 «분할» 의심」

§3-3 예시는 merge 사례라 문구가 「병합」으로 고정돼 있다. `direction='split'` 종목까지 「병합」이라
찍으면 운영 로그에 사실과 다른 말이 남으므로 방향에 따라 한 단어를 바꾼다.
태그(`[rs_leader]`·`[rs-corp-action]`·`[신호없음]`)와 접미(`— 후보 제외 (mode=…)`)는 **그대로**라
EOD grep 계약은 안 깨진다.
🔑 참고 — split 쪽 **순증 커버리지는 종가비 [0.65, 0.69) 띠뿐**이다. 더 깊은 하락은
`_prepare_frame` 의 기존 불가능봉 가드(−35%)가 **먼저** 자른다.

### E6. §4 T4 — 유니버스 25 → 34

「25종목 중 9 flagged → `scan()` 이 20건」은 25−9=16 < 20 이라 **원리적으로 성립 불가**다.
백필을 시험하려면 clean 이 20 이상이어야 한다. 주장은 그대로(후보 수가 줄지 않는다).

### E7. §4 — 테스트 3건 추가 (T13~T15)

| # | 계약 | 근거 |
|---|---|---|
| T13 | `mode=live` + `scan()` «밖» `match()` → 원래 튜플 + `scan_series` 0회 + 카운터 무오염 (그리고 «같은» 프레임이 `scan()` 안에서는 배제된다 — 대칭 단언) | E1 |
| T14 | `scan()` 중간 예외 뒤 재스캔 → 계기 줄 이중계수 없음 | 리뷰 🟡-2 |
| T15 | `matched` 는 모드 불변 · `kept` 는 실제 반환 수 | E3 |

또 §4 T8 은 rev1 에서 ma20·ma5·daytrading 이 `None == None` 이라 **공허하게** 통과했다 —
어댑터마다 「룰이 실제로 통과하는」 프레임을 쓰고 `assert base is not None` 을 먼저 단언한다.

### E8. §5 — 변경 파일 3 → 4 (`corp_action_guard.py` 신설)

판정 로직을 스크리너와 전략이 **둘 다** 써야 한다. 전략이 스크리너를 import 하면
`core.candidate_selector` 까지 라이브 전략 import 경로에 끌려오므로 rs_leader 패키지 «안»에
가드 모듈을 따로 둔다. (§7-5 의 `utils/corp_action_sanity.py` 이동은 여전히 별건이다.)
