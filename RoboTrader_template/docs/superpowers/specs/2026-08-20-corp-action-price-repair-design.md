# 기업행위 가격 보정 — 설계

> 작성 2026-08-20 · 상태 **설계(미구현)** · 라이브 0줄
> 계기: [`불가능봉` 원인 규명](../../../../memory/defect-halt-padding-impossible-bars.md) → 「큐 252건을 소비해 `adj_factor` 를 채우자」에서 출발했으나,
> 조사 결과 **요청과 처방이 달랐다**. 이 문서는 그 재설계다.

## §1. 쉬운 설명 (한 문단)

액면병합·액면분할이 일어나면 주가는 자동으로 몇 배가 된다(주식 5장을 1장으로 합치면 760원 × 5 = 3,800원).
**재산은 하나도 안 변한다 — 단위만 바뀐다.** 그래서 과거 가격을 「합친 후 기준」으로 다시 써야 그래프가 매끈해진다.
그런데 우리 DB 는 합치기 전 760원과 합친 후 3,815원을 나란히 저장해서 **「하루 만에 +402% 폭등」**으로 보인다.
***이런 가짜 급등이 2026-03 부터 301건 쌓였고 지금도 매일 는다.***

## §2. 확정된 사실 (전부 실측)

| # | 사실 | 근거 |
|---|---|---|
| 1 | KIS 는 `adj_prc="0"` 에서 **가격·거래량을 «둘 다» 정확히 조정**해 준다 | `054940` close 760→**3,800** · vol 357,326→**71,465**(정확히 ÷5) |
| 2 | 우리 수집기는 **전부 `adj_prc="1"`(원주가)** — `"0"` 을 넘기는 호출자가 **0곳** | `api/kis_market_api.py:133·170` 기본값 · `kis_api_manager.py:357` 미전달 |
| 3 | 저장 규약 = `close` **조정** · `volume` **원본** · `adj_factor` **거래량용 PIT 메타** | `collectors/adj_factors.py:11-31` · `db/quant_daily_reader.py:157` |
| 4 | 큐의 `ratio` 로는 계수를 못 정한다 | 액면비율 스냅 성공 **55/255 (21.6%)** |
| 5 | 「정수 근처」 판정은 **판별력이 없다** | `split_factor_infer.py:49-64` 실측 TP=6 **FP=6** |
| 6 | 🆕 기존 `adj_factor` 에 **틀린 값이 섞여 있다** | `004710` 에 5.0 스탬프 · KIS 양쪽 피드 어디에도 5배 사건 없음 |
| 7 | 큐는 **탐지는 정확**하다(어디가 깨졌는지) | 255건 · 246종목 · 2026-04-16~08-20 |

🔑 **2번이 뿌리다.** 옛 행이 멀쩡했던 건 사람이 돌리던 pykrx 백필이 조정값을 줬기 때문이고,
그 스크립트가 **2026-05-04 삭제**되면서 원주가가 그대로 남기 시작했다.

## §3. 목표 / 비목표

**목표**
1. 큐 255건의 가격 계단을 없앤다 — **우리가 계수를 추론하지 않는다.**
2. 같은 일이 **다시 쌓이지 않게** 수집 경로를 고친다.
3. 6번(틀린 `adj_factor`)의 **범위를 측정**한다.

**비목표 (이 문서에서 하지 않는다)**
- 읽기 계층 변경. `close ÷ adj_factor` 는 **기각** — 옛 행은 이미 조정돼 있어 이중조정이 난다(`004710`: 5,730 ÷ 5 = 1,146).
- `corp_events_collector` 의 `pblntf_ty="I"` 미수집 수정 — 별개 과제(앞으로 생길 것을 막지만 쌓인 255건은 못 고친다).
- 백로그 「일봉 결손 49,252행」 대량 복구 — 전수 재측정 선행 결정이 이미 있다.

## §4. 설계 — 추론을 «전부» 없앤다

### 4-1. 핵심 착상

계수를 **KIS 두 피드의 비**로 정확히 구한다.

```
adj_factor(date) = vol_adj(date) / vol_raw(date)
```

🔴 **정정(2026-08-20, 구현 계획 작성 중 발견)** — 초안은 `vol_raw / vol_adj` 였는데 **방향이 뒤집혔다.**
저장 규약은 `adj_close = raw_close / adj_factor` 이고 읽기 계층은 `volume × adj_factor` 를 하므로

```
adj_factor = raw_close / adj_close = vol_adj / vol_raw
```

실측 `054940` 2026-05-29: 가격 760/3,800 = **0.2** · 거래량 71,465/357,326 = **0.2** — 둘이 같고,
DB 에 실제 저장된 값도 **0.2** 다. 검산: 357,326 × 0.2 = 71,465 ✅.
***초안대로 5.0 을 썼으면 25배 틀렸다*** — §5-1 이 발견한 `025440`(24.3배 · 방향 뒤집힘)과
**같은 형태를 우리가 새로 만드는 꾴**이었다. 화이트리스트도, 정수 스냅도, 큐 `ratio` 도 필요 없다.
⇒ **255건 전부 계산된다** (21.6% 한계 소멸).

### 4-2. 종목별 처리

전체 이력을 **두 번** 받아 컬럼별로 출처를 가른다. **기존 규약을 한 글자도 안 바꾼다.**

| 컬럼 | 출처 | 근거 |
|---|---|---|
| `open`·`high`·`low`·`close` | `adj_prc="0"` | 규약 = 조정 연속 시세(§2-3) |
| `volume` | `adj_prc="1"` | 규약 = 원본 |
| `adj_factor` | **`vol_adj / vol_raw`** | 정확값 (방향 주의 — §4-1 정정) |
| `trading_value`·`market_cap` | 손대지 않는다 | 범위 밖 |

⇒ **읽기 계층 0줄 · 소비자 전원 무영향 · 두 번째 규약 없음.**

### 4-3. 계수 산출 시 주의

- `vol_adj == 0`(거래정지 패딩·휴장) 인 날은 **계수를 못 구한다** → 그 날은 건너뛰고,
  **같은 «구간»(이벤트 사이) 안에서 가장 가까운 유효 계수**를 쓴다 — 계수는 구간별 상수라 안전하다. **구간 경계를 넘어 빌리지 않는다.**
- 종목 전체에서 계수가 **하나도** 안 구해지면 그 종목은 **건드리지 않는다**(fail-closed).
- 구해진 계수들이 한 구간 안에서 **서로 다르면** 그 종목은 **건드리지 않고 ERROR 로 보고**한다.
  🔑 *「없다」와 「고장」을 같은 값으로 보고하지 않는다* — 이 저장소가 반복해서 데인 자리다.

## §5. 범위

| 대상 | 건수 | 비고 |
|---|---|---|
| 큐 `status=pending` · `eligible_after` 도래 | **243** | 12건은 권리락 +7일 미도래 → 다음 회차 |
| 큐 전체 | 255 | 246 고유 종목(9종목은 2건) |
| 🔴 틀린 `adj_factor` 교정 | **9종목** | §5-1 측정 완료(2026-08-20) |
| 🔴 **큐 밖 추가분** | **7종목** | §5-1 — 큐는 이걸 원리적으로 못 잡는다 |

### 5-1. 틀린 `adj_factor` 범위 — 측정 완료 (2026-08-20, 읽기 전용)

`adj_factor ≠ 1` 인 **99종목** 을 KIS 두 피드와 전수 대조했다(97종목 대조 성공).
판정식은 **`af_참값 = 원주가 / 수정주가`**.

| 계수 판정 | 종목 |
|---|---|
| 일치 | **73** |
| 배당 정의 차이(off 0.86~1.2배) | **15** — 오류 아니다 |
| 🔴 **진짜 틀림(off ≥ 3배)** | **9** |

```
025440  저장 5     참값 0.206  -> 24.3배 (방향까지 뒤집힘)
060900  저장 2     참값 0.087  -> 22.9배
078590  저장 0.4   참값 0.039  -> 10.2배
003620  저장 5     참값 0.934  ->  5.4배
128820  저장 5     참값 0.973  ->  5.1배
033540  저장 0.4   참값 0.080  ->  5.0배
004710  저장 5     참값 1.015  ->  4.9배
010140  저장 5     참값 1.060  ->  4.7배
042940  저장 0.1   참값 0.032  ->  3.1배
```

| 가격 판정 | 종목 |
|---|---|
| 조정됨 | 53 |
| 🔴 미조정(원주가) | **38** |
| 기타(둘 다 아님) | **6** |

#### 🎯 큐가 «못 잡는» 7종목

| | 전체 | 큐에 있음 | **큐에 없음** |
|---|---|---|---|
| 계수 진짜 틀림 | 9 | 4 | **5** (`003620 004710 010140 042940 128820`) |
| 가격 미조정 | 38 | 36 | **2** (`010120 323350`) |
| 기타 | 6 | 5 | 1 (004710 중복) |

합집합 **7종목**. 🔑 ***큐는 「정지 해제 시 가격 점프」만 보므로
«가격은 멀줦한데 계수만 틀린» 경우를 원리적으로 못 잡는다.*** `004710` 이 그 형태다.

⇒ **고칠 범위 = 큐 255건 + 7종목.**

🟢 **부수 소득 — §4-1 이 이 문제를 자동으로 피한다.**
계수를 **거래량 비**(`vol_adj/vol_raw`)로 구하는데 **거래량은 배당의 영향을 안 받는다.**
가격 비로 구했다면 위 「배당 정의 차이 15건」이 그대로 오염으로 들어왔을 것이다.

⚠️ **이 측정은 하한이다** — 종목당 대표 날짜 «하나»만 봤다.
한 종목에 계수 구간이 여러 개면 나머지 구간은 안 쌀다.

⚠️ **같은 종목에 이벤트가 2건인 9종목**은 계수가 곱해진다 — 구간별로 나눠 처리하고 **전후 검산 필수**.

## §6. 안전장치

### 6-1. 백업 (필수)

`daily_prices_preadj_backup` 테이블에 **손대는 행 전부**를 원본째 저장한다
(`stock_code, date, open, high, low, close, volume, adj_factor, backed_up_at, batch_id`).
롤백 = 백업에서 되돌리기. **백업 없이는 한 행도 안 고친다.**

### 6-2. 멱등

이미 조정된 행을 또 조정하면 망가진다. 판별은 **가격 비교로** 한다 —
`adj_prc="0"` 값과 DB 값이 이미 같으면(상대오차 1% 이내) **건너뛴다**.
🔑 *«가격이 이미 조정됐나» 를 `adj_factor` 로 판별하지 않는다* — 6번 결함 때문에 그 값을 못 믿는다.

🔴 **정정(구현 계획 작성 중)** — 단 「이 행을 고쳐야 하나」 판정에는 `adj_factor` 를
**반드시 포함**해야 한다. 가격은 멀줦한데 계수만 틀린 종목이 실측 **9건**이고(§5-1),
가격만 비교하면 그 9건을 «고칠 필요 없음» 으로 조용히 넘긴다.
⇒ **조정 여부 판별 = 가격** / **수정 필요 판별 = 가격 + 계수**. 둘을 가른다.

### 6-3. 단계적 적용

1. **1종목**(`054940`) → 전후 비교 인쇄 → 사람이 확인
2. **10종목** → 불가능봉 개수 감소 확인
3. **전체 243건**

각 단계에서 **불가능봉 개수**를 재고, **줄지 않으면 중단**한다.

### 6-4. 검증 (각 단계)

- 처리 종목의 `|일간수익률| > 30%` 봉 수가 **0 이 되는가**
- 처리 «안 한» 종목의 그 수가 **안 변하는가**(부작용 없음 대칭 확인)
- 전체 `daily_prices` 행 수 **불변**(UPSERT 만, DELETE 없음)
- 🔴 **`volume` 은 불변이어야 한다** — 기존도 원본, 새로 넣는 것도 `adj_prc="1"` 원본이다.
  바뀜다면 어떤 전제가 틀렸다는 뜻이므로 **즉시 중단**한다.
- `adj_factor` 는 **바뀌는 게 정상이다**(그게 이 작업의 목적 중 하나) —
  §4-1 계산값과 일치하는지만 확인한다.
  ⚠️ *초안은 「`volume × adj_factor` 불변」을 검산식으로 썼는데 그건 **설계대로면 반드시 실패한다*** —
  게이트가 자기가 막아야 할 것을 못 재는 계열의 오류라 사양 검토에서 정정했다.

## §7. 지속성 — 안 고치면 다시 쌓인다

두 곳이 계속 원주가를 써넣는다:

| 경로 | 창 | 조치 |
|---|---|---|
| `core/intraday/data_collector.py:68` (09:00) | ~103봉 | `adj_prc="0"` 전달 + volume 은 원본 유지 |
| `collectors/daily_collector.py:62 collect_one` (EOD) | 7봉 | 동일 |

🔴 **이건 라이브 수집 동작 변경이다.** §6 검증이 끝난 «뒤»에 별도 승인으로 진행한다.
그전까지는 **최근 이벤트 종목이 다음 09:00 에 다시 깨질 수 있다** — 주기적 재적용으로 버틴다.

## §8. 미해결 · 위험

1. ✅ **측정 완료**(§5-1) — 계수 진짜 틀림 **9종목** · 큐 밖 추가 **7종목**.
   ⚠️ 단 **종목당 대표 날짜 하나만 봤으므로 하한**이다 — 구간이 여러 개인 종목은 미측정.
   왕곡 배수가 최대 **24.3배**이고, 거래량은 `minervini` dryup 룰과 `score` 랭킹의 **직접 입력**이다.
2. API 예산 — 246종목 × 전체이력 × **2회**. `_extended` 연속조회라 종목당 여러 호출.
   EOD(16:00)·장중과 겹치지 않게 **실행 창을 정해야** 한다.
3. `adj_prc="0"` 이 **얼마나 과거까지** 조정을 반영하는지 미확인(2021년까지 정확한가).
4. 큐 소비 후 `status` 갱신 — **별도 완료 파일**(`logs/corp_action_refetch_done.jsonl`)로 결정. 원본 JSONL 은 append-only 로그라 **재작성하지 않는다**(동시 기록 중인 EOD 수집기와 경합한다).
5. `004710` 검증에서 DB(5,135)가 KIS 양쪽(4,847·4,920) **어느 쪽과도 안 맞았다** — 원인 미규명.
   pykrx 유래로 추정되나 **확인 전에는 그 종목군을 건드리지 않는다.**

## §9. 실행 순서

1. §8-1 **범위 측정**(읽기 전용) — 틀린 계수가 몇 종목인가
2. 이 설계 승인 → 구현 계획(`writing-plans`)
3. 백업 테이블 + 1종목 → 10종목 → 243건
4. 검증 통과 후 **별도 승인**으로 §7(수집 경로)
