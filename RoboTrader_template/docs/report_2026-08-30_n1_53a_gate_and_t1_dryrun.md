# N-1 (P-a) §5-3A·3B·dry-run ④⑤ 완료 보고 — 사장님 §5-4 결정 (2026-08-30 오후)

> 🆕 **저녁 갱신** — 사장님 결정 3건 접수(§4-B) · 유보 2건 조사 완료(§7, **§3 의 049470 서술에 정정 있음**) · 워크트리 브랜치 커밋·푸시 **`77fb2c8`**(귀속 tgparkk ✅).

> 동결 문서: `docs/prereg_2026-08-27_n1_correction_plan.md` (v4.1, 커밋 `04fb4f3`)
> 오늘 낮 §5-2 dry-run(KIS 196회·DB 쓰기 0)에 이어, **§5-3A(코드 1건) → §5-3B(독립 검증) → T1 dry-run 재실행(산출물 ④⑤ 본판)** 까지 끝났습니다.
> 🔴 **라이브 반영 0** — 코드는 워크트리(`D:/tmp/kis-wt-n1-53a`, 브랜치 `fix/n1-volume-gate-p3ab`)에 미커밋 상태. DB 쓰기 0 (실행 전후 `updated_at` 대조로 증명).

## 0. 한 줄 요약

`--apply` 를 물리적으로 막던 volume 게이트의 판정식을 동결 명세대로 교체했고(검증 통과), 새 게이트로 T1 7종목을 다시 재 보니 **6종목이 걸립니다**. 그런데 그중 5종목은 「마지막 거래일 하루치 거래량이 낡은 값」이라는 **가벼운 원인**이고(8/31 밤 EOD 가 스스로 고칠 가능성이 큼), 1종목(`049470`)은 **진짜 내부 불일치**입니다. 어떻게 가를지가 오늘의 결정입니다.

## 1. 무엇을 바꿨나 — 쉬운 설명

**옛 게이트**: 「보정 전후 volume 값이 한 글자라도 다르면 중단」. 그런데 이번 보정 (P-a) 는 volume 을 KIS 원주가 피드값으로 **교체하는 것 자체가 목적**이라, 이 게이트는 무조건 발화합니다(오늘 낮 dry-run 실측 7/7 발화). 전제가 거짓인 게이트였습니다.

**새 게이트** (동결 명세 §5-3A 안 (나), 사장님이 D-8 로 승인하신 안): 값이 바뀌는 건 허용하되, **의미가 보존되는지**를 잽니다.
- **P3-A 곱 불변**: 읽기 계층이 실제로 쓰는 값은 `volume × adj_factor` 입니다. 예를 들어 DB 에 `(volume=100, 계수=10)` 이던 행을 `(volume=1,000, 계수=1)` 로 바꾸면 값은 10배 바뀌었지만 읽기값은 1,000 으로 동일 — 통과. 곱이 0.5% 넘게 어긋나면 위반.
- **P3-B 거래대금 항등**: `종가 × volume × 계수` (그날 실제로 오간 돈)가 보정 전후 같아야 함. 이건 전 행에서 잽니다.
- 위반이 있으면 **백업·쓰기 전에** 배치 전체 중단(기존과 같은 자리). 「불가능봉이 늘면 중단」 게이트와 NULL 검사는 그대로 뒀습니다.

## 2. 검증 — 자기승인 없이 3자 구조

| 단계 | 담당 | 결과 |
|---|---|---|
| 구현 | 직원 A (executor) | 1파일, 자가 테스트 42/42 |
| §5-3B 독립 검증 | 직원 B (verifier) | 채점 3항목 전부 PASS — 양성대조(어긋난 행 주입 → 백업 전 중단) 6/6 · **문서만 보고 따로 짠 판정기와 행 단위 24/24 일치** · abort 식 972조합 동형 |
| 1차 판정 | 직원 B | 🔴 REQUEST_CHANGES — 이 스크립트의 기존 안전게이트 회귀 테스트 5건이 죽음(교체된 게이트를 겨냥한 mutation 테스트 소멸 = 「죽은 가드」 계열) |
| 재수정 | 직원 A | 테스트 재작성 — abort 식 세 항 각각의 「빼면 나쁜 쓰기가 통과한다」 이빨 테스트 신설, fixture 판별력 복구. **판정식은 순수 삽입 27줄 외 무변경**(diff 구조로 증명) |
| 최종 | 직원 B | 🟢 **APPROVE** — 55 passed / 1 xfailed / **0 failed** (기준선 동일) |

⚠️ **동결 범위 이탈 1건(추인 요청)**: 문서는 「변경 1파일」이라 했는데 회귀 테스트 복구로 **2파일**(스크립트 + 그 스크립트 전용 테스트 `tests/test_repair_cli.py`)이 됐습니다. 읽기 계층·기계검사·수집기는 여전히 0파일. 테스트를 안 고치면 가드 4종이 무보호(죽은 가드)로 남아서 고쳤습니다.

## 3. T1 dry-run 재실행 — 산출물 ④⑤ 본판 (KIS 392회 · DB 쓰기 0 · rows 7,121 전임과 완전 재현)

### 🔴 헤드라인: 규칙을 문자 그대로 적용하면 T1 7종목 중 6종목이 빠집니다

| 갈래 | 종목 | 위반 | 원인 |
|---|---|---|---|
| **(가) 마지막 거래일 stale** | 011090·014990·226340·900300·950220 (5종목) | **각 정확히 1행, 전부 2026-08-28** | 기업행위 아님. 금요일 15:45 EOD 수집 시점의 거래량이 KIS 확정치보다 작음. 예: `900300` DB 2,410,924 vs KIS 2,424,935 (+0.58%) |
| **(나) 진짜 내부 불일치** | **049470** (1종목) | **1,198행 전부, 정확히 −50.00%** | DB `adj_factor=2.0` 이 KIS 함의 배율과 2배 어긋남 — P3-B 가 잡으라고 만든 바로 그 사건 |
| (다) clean | 001210 | 0 | — 단 P3-A 판정 행 19 < 20 이라 「미검증」(문서 §4 보상 규칙대로 이 목록에 올림) |

**(가)가 스스로 나을 가능성** [코드 확인]: 야간 EOD 의 최근 7봉 UPSERT 가 `volume=EXCLUDED.volume` 으로 그 행을 매일 갱신합니다(`collectors/daily_writer.py:47`). 8/31(월) EOD 후에는 08-28 행 거래량이 KIS 확정치로 바뀌어 위반이 사라질 개연성이 큽니다. **apply 창이 어차피 D-5 로 「8/31 EOD 후」**이고, 문서 스스로 「이 값들은 apply 당일 다시 계산된다」(§4·§5-5A 시점 축)고 못박았으므로, **판정을 8/31 EOD 후 재측정 값으로 하는 것은 규칙 변경이 아닙니다.**

- P3-A 미검증(판정 행 < 20) 명단: `001210`(19) · `049470`(0) — §5-4 승인 목록 게재 의무 이행.
- **산출물 ⑤ (P2 잔존 점프, 본판)**: 전임의 ±30% proxy 를 [SQL-A] 밴드(0.69/1.45) 본판으로 교체해 보정 후 합성 시계열에 재적용 — **7종목 전건 1 → 0, 잔존 0** (동결 기대치와 일치).
- 부수 이득: 이번에 KIS 피드 원본을 `feeds_raw/*.json.gz` 로 저장 — **이후 재분석은 KIS 재조회 0회**로 가능(원시 산출물 소실 재발 방지).

## 4. ⬜ 사장님 결정 요청 5건

| # | 항목 | 관리자 의견 |
|---|---|---|
| **E-1** | (가) 5종목 처리 — (a) 지금 문자 적용해 제외 (b) **8/31 EOD 후 §5-5A 재측정 시점에 재판정, 그때도 남으면 제외** | **(b)** — 문서의 시점 축 규정과 일치, apply 창과 같은 시각이라 지연 0 |
| **E-2** | (나) `049470` — (a) **문자 적용 제외 + 원인 규명 별도 조사**(corp_events `adj_factor=2.0` 뿌리) (b) 포함 강행 | **(a)** — P3-B 의 존재 이유가 이 사건. 단 이 종목이 rs_leader 점프 대장주라 **제외하면 T1 목적이 반감**됩니다. 규명 후 별도 트랜치로 되넣는 경로(문서 X4 와 같은 방식)를 권합니다 |
| **E-3** | 변경 범위 1→2파일(테스트 복구) 추인 | 추인 요청 — 안 고치면 죽은 가드 5건 |
| **E-4** | 문서 공백 2건 등재(코드 무변경 유지) — ① `close NULL` 행은 P3-B 판정 불가인데 중단 안 함(volume NULL 과 비대칭 · T1 실측 0건) ② `n_a` 에서 정지봉 제외가 게이트 완화 방향(T1 실측 1행) | T2 전에 문서 개정판에 등재. 지금은 로그 집계로 «보이게»만 해뒀음(§6-5 준수) |
| **E-5** | 승인 시 커밋 — 워크트리 브랜치 2파일 커밋(+메모리 changelog) | 승인 주시면 커밋·푸시 진행 |

## 4-B. 🔒 사장님 결정 (2026-08-30 저녁 접수)

| # | 결정 |
|---|---|
| E-1 | **8/31 EOD 후 재판정** — (가) 5종목은 야간 UPSERT 갱신 뒤 게이트 재실행, 그때도 남으면 문자 적용 제외 |
| E-2 | **049470 제외 + 원인 규명 후 재편입**(X4 방식) — 규명은 §7 로 상당 부분 완료, corp_events 수정은 별도 사전등록 |
| E-3+E-5 | **2파일 확대 추인 + 커밋·푸시** — `77fb2c8` 실행 완료, 귀속 tgparkk 확인 |
| E-4 | 문서 공백 2건(close NULL 비대칭 · n_a 정지봉 제외)은 T2 전 문서 개정판에 등재(변동 없음) |

## 5. 다음 순서 (동결 §5-0 그대로)

1. **8/31(월) 아침·장중**: 슬롯 120 P1~P5 판정이 먼저 (별건 — NEXT_SESSION.md 명령표)
2. **8/31 EOD(~16:11) 후**: §5-5A straddle 재측정(의무) + E-1 재판정 → md5 재동결
3. **T1 `--apply`** (그때 남은 대상만) → P1~P6 판정 → 화요일 P7
4. ⚠️ apply 후 화요일 아침 rs_leader top-10 교체 예상 — 슬롯 판정과 별개 축으로 기록

## 6. 증빙 위치

- 산출물: `RoboTrader_template/scratchpad/n1_pa_dryrun_20260830/rerun_53a/` (④ 집계+행 단위 상세 · ⑤ 본판 · KIS 통계 · stdout 전문 · 피드 원본 · MANIFEST 갱신본, 원본은 `.bak_before_rerun53a` 보존)
- 코드: `D:/tmp/kis-wt-n1-53a` diff numstat `149/12`(스크립트) + `145/56`(테스트) · base `8ef9939` · 미커밋
- 검증 하니스(합성 데이터·DB/KIS 0): 세션 스크래치 `n1_53a_selftest/` · `n1_53b_verify/`
- ~~유보 2건(미규명·단정 안 함): `226340` 의 +8.63% 가 다른 4종목(+0.58~1.26%)보다 한 자릿수 큰 이유 · `049470` 의 「정확히 2배」 뿌리~~ → **저녁 조사로 해소, §7 참조**

## 7. 🆕 유보 2건 조사 결과 (읽기 전용 · KIS 0회 · 저장 피드만 · 산출물 세션 스크래치 `n1_probe/`)

### 7-1. 🔴 정정 — §3 의 「049470 1,198행 전부 정확히 −50.00%」는 오독이었다

산출물 `artifact_04b` 의 위반 행 목록이 **상위 50행 캡**이라 2021년 구간(−50%)만 보였습니다. 전수 재계산(dry-run 집계와 일치) 결과 실제는 **두 무리**:

| db.adj_factor | 상대오차 | 행수 | 구간 |
|---|---|---|---|
| 2.0 | **−50%** | 801 | 2021-01-12 ~ 2024-04-19 |
| 0.2 | **+400%** | 397 | 2024-08-23 ~ 2026-04-17 |

상대오차 = `1/adj_factor − 1` 항등이 1,187/1,198행 성립 — P3-B 는 계수의 순수 판독값이었습니다. 🔑 교훈: **위반 행 목록에 캡을 두면 「전부 같은 원인」으로 오독한다** — 이후 산출물엔 분포(계수×오차 교차표)를 저장.

### 7-2. 049470 판정 — DB `adj_factor` 가 틀렸고, 뿌리는 `corp_events` 2024-05-22 행

- 야간 지우개 계산 자체는 무결(재현 1,373/1,373행 일치). 틀린 것은 입력: `corp_events` 의 2024-05-22 `split 10:1(split_factor=10.0, source pykrx)` — KIS 두 피드의 함의 배율은 **1.175**(8.51배 어긋남). 같은 표의 2026-05-15 `merge 1:5(0.2)` 는 KIS 와 소수점까지 일치(양성 대조 통과) — pykrx 전체가 틀린 게 아니라 **이 행**입니다.
- 🆕 **[공시 원문 확정]** — 049470(당시 SGA)의 KIND 사업보고서 기재: 정지 기간 중 **두 사건**이 있었습니다. ① **2024-04-24 주식분할**(액면 1,000→100원, 주식수 ×10) ② **2024-04-30 무상감자**(588,622,490주 → 58,862,249주, ÷10). **두 사건이 상쇄돼 순 가격영향 ≈ 1** — 그런데 pykrx 액면 테이블 기반 `corp_events` 는 **「액면가가 변한」 ①만 잡고, 액면 불변인 ②(감자)를 구조적으로 못 잡아** `split_factor=10.0` 이 홀로 남았습니다. 잔여 1.175 는 정지 중 희석분과 정합. 🔑 이것이 N-1 결함(「corp_events 에 감자 유형 없음」)의 **개별 사례를 공시 원문으로 처음 닫은 것**이며, 같은 「분할+감자 상쇄」 서명을 가진 종목이 재검증 후보 9종에 더 있을 수 있습니다. 출처: [KIND 사업보고서](https://kind.krx.co.kr/external/2026/03/19/001683/20260319006890/11011.htm) · [5:1 병합 보도](https://www.digitaltoday.co.kr/news/articleView.html?idxno=640213).
- DB 가격은 원주가도 수정주가도 아닌 **「2026-05-15 병합 전 수정주가」가 굳은 제3계열**(진실의 0.2배) — 가짜 절벽 1개(05-15 +254%)가 dry-run 의 `impossible 1→0` 대상.
- 🔴🔴 **반사실 계산: 보정 «단독» 실행 시 악화한다** — 보정 직후엔 전 지표 1.0 이지만 그날 밤 지우개가 계수만 2.0 으로 되돌려, af=2.0 시대의 **거래대금 정합이 현재 1.9987배 → 8.5091배로 나빠집니다**. ⇒ **E-2 (제외) 가 데이터로도 옳았고, 재편입 조건 = corp_events 2024-05-22 행 수정(별도 사전등록, 사양 §7·문서 §6-8)**.
- 같은 형태 재검증 필요 후보(`split_factor≥5` 최근): `101930·058430·003350·010120·001080·004710·128820·003620·058470` — 뒤 4개는 기지 결함 목록에 이미 있음. 049470 하나로 일반화 금지.

### 7-3. 226340 판정 — 결함 아님 (E-1 지지)

- 마지막 거래일 괴리는 **전 종목 공통 구조**: EOD 15:45 스냅샷이 시간외 확정 «전»에 찍힘. 종가는 7/7 일치, `trading_value` 도 같은 스냅샷(08-28 VWAP 이 정규장 분봉과 ±0.1% 일치), 직전 3거래일은 7종목 전부 0.00%.
- +8.63% 가 큰 이유 = **이 종목의 상시 시간외 비중**(최근 15거래일 +0.9~9.7%, 08-28 은 자기 분포 3~4위 — 이상치 아님). 상한가 가설은 기각(상한가는 001210 이었는데 괴리 최소).
- **자가치유 실측**: 226340 의 08-25~27 행은 이미 KIS 확정치로 교정돼 있음 ⇒ 08-28 행도 8/31 EOD 에 자동 교정 — **E-1 「8/31 EOD 후 재판정」이 데이터로 확인된 올바른 선택**.
