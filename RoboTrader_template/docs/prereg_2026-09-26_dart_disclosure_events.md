# 사전등록 — DART 공시 «유형» 이벤트가 매수후보 선정에 정보가 있나 (연구 ① · dart_events)

- 상태: **v0.2 · critic APPROVE-WITH-FIXES 반영(필수 6 + 권고 7~13) · 사장님 C1 = ㉯ 확정 · 동결 대기(2026-09-26 밤)** · 브랜치 `research/dart-events` · 기준 HEAD `cd639cd`
- 🔒 이 문서는 **공시와 결과(수익률·손익)를 잇는 계산을 누구든 하기 «전»에 커밋으로 동결**한다. 🔒 절은 결과를 본 뒤 바꿀 수 없다.
  바꿔야 하면 개봉(§10 ⑤) «전»에 `prereg_2026-09-26_dart_disclosure_events_amendment_<날짜>.md` 를 따로 커밋한다.
- 작성 중 조회: 코드 grep/부분 read · DB **SELECT 만**(스키마·행수·날짜·KOSPI 달력·일별 유니버스 크기·적재본 **제목 분포**) · 원장 `ret_pct` 의 **전략별 표준편차만**(공시와 무관). **공시 조건부 수익률·손익 조회 0회.**
- 🔴 **동결 전 백필 완료** — 창 전체(2024-03-13~2026-09-23)를 이 문서 동결 «전»에 적재했다 · **입력 데이터만 · 결과 조인 0 · 관리자 사전 열람 = 제목 분포·건수뿐**.
  - 실제 [실측 · 1차 프로세스 PID 543252 종료 · `summary.json` 15:33:34 확인 뒤 SELECT]: 총 **165,812행**(B 21,908 · I 143,904) · rcept_dt 2024-03-13~2026-09-23 · `max(fetched_at)` **2026-09-26 15:33:34 KST** ·
    총 외부 호출 **3,400회**(`call_log.jsonl` 줄 수 = 중복 포함 · 파일럿 24 포함 · status 000 2,547 · 013 853 · 020 0) — 1차 프로세스 자체 집계 2,578회(달력일 925 · 경고 0) · 나머지 ≈ 798회가 두 번째 프로세스 몫(그중 중복 ≈ 400 = 관리자 추정).
  - **선언된 이탈**: 호출 상한 계획 5,000 → 실행 `--limit-calls 6000`.
  - 🔴 **동시 실행 사고**: 15:16~15:24 관리자 실수로 두 번째 프로세스(PID 558856)가 같은 출력 폴더에서 동시 실행 → 중복 호출 약 400회 · PK 로 **중복 행 0** · `progress.json` 일부 갱신 유실 가능.
    두 번째 프로세스는 15:24 크래시 — 원인(`full_run2.err`): `PermissionError: [WinError 5] … 'progress.json.tmp.558856' -> 'progress.json'`(두 프로세스의 `progress.json` 원자 교체 `os.replace` 충돌). ⇒ 완결 판정은 progress 가 아니라 raw 캐시 + DB 로 한다(§2).
- 표기: **[실측]** SELECT·원자료 값(적재본 = 위 완료본 165,812행) · **[추정]** 외삽 · **[문서]** OpenDART 개발가이드·기존 문서.

## 0. 사장님 말 그대로 (2026-09-26 밤 · 관리자 전달본)

> 「매수후보 선정 방법에 대해서 논의하고싶어요」 → 「3,4 를 하고싶어요」(관리자 주: 새 데이터 추가 · 접근 방식 자체) → 「아직 모르겠다 — 아이디어부터」 → 「① 공시 먼저 + ② LLM shadow」 → 「승인 — ①부터 착수」
> C1 답: **㉯ — (a) 는 인쇄만 · (b) 만 판정**(2026-09-26 밤 확정)

⇒ 이 문서 = **①** — DART 공시 «유형»(제목 `report_nm` 분류)이 매수후보 선정에 정보가 있는지 **과거 측정**. ② LLM shadow 는 별도 사전등록.
🔒 **㉯ 에서 판정이 답하는 질문은 «공시가 독립 후보원이 되는가»(b)이지 3전략 후보의 순위 층이 아니다.** 순위 층((a))은 인쇄만.
승계: 「당분간 3전략 + 태쏘만 고도화 · 재무·뉴스 데이터로 매수후보 선정 보강」(09-11).
🔒 회의론자 기본 기대(특징 연구 §0-③ 승계): 발굴 종결(07-16) 종목선택 OOS AUC 0.556 천장 · B 특징 14개 「있음」 0 ⇒ **기본 기대는 「정보 없음」** · 중간 언어(「경향」「약한 신호」) 금지.

## 1. 이 문서가 «아닌» 것

- ② LLM shadow 연구가 아니다(별도 사전등록). (a) 순위 층 검정도 아니다(㉯ · 인쇄만).
- 라이브 변경 0줄 · shadow 배선 0 · **~2026-10-16 3전략 룰 변경 0건**(계획서 v3) · 이 결과는 **3전략 룰 변경 판단 근거가 아니다**.
- DB 쓰기 = **새 테이블 `dart_disclosures` 한 곳**(INSERT … ON CONFLICT DO NOTHING)뿐 · 다른 테이블 쓰기·retention policy 0.
- 공시 «본문» 파싱(금액·흑자/적자·발행 조건)이 아니다 — 제목만. 태쏘 프로그램 재개가 아니다. 원장·청산 규칙을 다시 정의하지 않는다(`candidate_ledger/PREREG.md` §2~§8 승계).

## 2. 데이터 계약 — `dart_disclosures` 🔒 (적재 스크립트 `scripts/dart_disclosure_backfill.py` 실제 동작 기준 · 미추적·수정 중이라 함수명으로 인용)

| 열 | 형 | 규칙 |
|---|---|---|
| rcept_no | TEXT **PK** | list.json 값 그대로 · 재적재·교차유형 중복 = ON CONFLICT DO NOTHING |
| corp_code · corp_name · corp_cls | TEXT · TEXT · CHAR(1) | 🔴 corp_name·corp_cls 는 **조회 시점 값**(§9-1) — 선택·제외에 안 씀 |
| stock_code | TEXT NULL | 빈 문자열 → NULL(`item_to_record`) |
| report_nm · rcept_dt · pblntf_ty | TEXT · DATE · CHAR(1) | 원문 그대로 · `YYYYMMDD`→DATE · 요청 유형 {B, I} |
| is_correction | BOOL | 스크립트 `is_correction` = 제목에 「정정」 포함 — 🔴 **분석은 §3-2 로 다시 계산**, 불일치 건수만 인쇄 |
| rm · raw · fetched_at | TEXT · JSONB · TIMESTAMPTZ | 🔴 rm 은 조회 시점 비고(「정」「철」 = «나중» 정보) ⇒ 선택·제외 금지 |

- 호출 = `list.json`(`crtfc_key·bgn_de·end_de·pblntf_ty·page_count=100·page_no` · corp_code 없음 · `collectors/corp_events_collector.py:113-125` 와 같음) · `last_reprt_at` **미지정 = 기본 N(원·정정 모두)** — 근거 [실측]: 실행 `call_log` URL 에 `last_reprt_at` 없음 · 적재본에 정정 접두 행 32,713(기재정정 31,049 · 첨부정정 1,304 · 첨부추가 360). 수정 중인 스크립트는 `last_reprt_at` 을 명시한다 — 재수집은 **N** 이어야 한다.
- 창 = **달력일 하루**(bgn_de = end_de · 주말·휴일 포함 · `daterange`) · 날짜마다 **B → I 순서**(`run` 루프) · 운영 `_fetch_one_type`(`corp_events_collector.py:128-160` · `_MAX_PAGES=100` 절단)은 쓰지 않는다.
- 🔒 **완결 판정(날짜 × 유형)** = status 013(무자료) **또는** 1…`total_page` 전 페이지 status 000 ∧ raw 항목 수 = 1페이지 `total_count` ∧ **DB 행수(rcept_dt = 그날 ∧ pblntf_ty = 그 유형) + 중복 수 = `total_count`**.
  - **중복 수** = 그 유형 raw 의 rcept_no 중 **다른 유형으로 이미 저장된** 수(B → I 순서라 I 쪽에만 생긴다).
  - progress `rows` = **수집 수(삽입 수 아님)** · 동시 실행으로 갱신 유실 가능 ⇒ 판정에 progress 를 쓰지 않는다(raw 캐시 + DB 만). 단 status 013 응답은 raw 에 캐시되지 않는다(`Fetcher.fetch_page`) ⇒ 013 의 증거는 `call_log.jsonl` 의 그 (유형, 날짜) 줄.
  - 미완결 1건이라도 남으면 §5·§6 **실행 금지**. **재수집 절차** = 그 (유형, 날짜)의 progress 항목과 `raw/` 페이지 파일 삭제 → `--resume` 재실행 → 다시 판정(추가 호출 수 인쇄).
- 로그(`RoboTrader_template/scratchpad/dart_backfill_20260926/`): `call_log.jsonl`(호출마다 url(키 가림)·status·행수·지연 · 두 프로세스 공용) · `progress.json` · `raw/` · `full_run.log` · `full_run2.err` · `summary.json`. 완료 뒤 보관소 `D:/research-archive/dart_backfill_20260926/` 복사 + `MD5SUMS.txt`.
- 키 = `.env` 의 `OPENDART_API_KEY`(`corp_events_collector.py:88-98` · 라이브 트리 `.env` 읽기만) — 출력·복사·로그 금지.
- `run_backfill_check.py`(**커버리지만** · 가격·결과 조회 0) = 완결 판정 전수 · 일별·월별 행수 · 원/정정 · 종목코드 유무 · 유니버스 교집합 · 태그 분포 · **정정 ↔ 원공시 동반율**(정정 행 중 같은 corp_code·같은 정규화 body 의 원공시가 더 이른 rcept_dt 로 적재본에 있는 비율 · 태그별).

## 3. 태그 사전 — `dart_tags.py` 🔒 (결과 전 고정 · import 가능 · 단위 테스트)

**3-1 정규화(순서 고정)**: ① 공백류 전부 제거 ② 가운뎃점류 `ㆍ`(U+318D)·`·`(U+00B7)·`・`(U+30FB)·`･`(U+FF65)·`∙`(U+2219)·`‧`(U+2027) → `·` ③ 앞머리 `[…]` 를 반복해 떼어 `prefixes` 로 보관 → 나머지 = `body`.

**3-2 적용 순서(고정)** — `is_corr` 는 따로 매기고, 태그는 ①→②→③ 첫 해당에서 정한다.
- `is_corr` = prefixes 중 하나라도 {`기재정정`, `첨부정정`, `첨부추가`, `정정`}. [실측 접두: 기재정정 31,049 · 첨부정정 1,304 · 첨부추가 360 · 연장결정 173 · 정정명령부과 29]
- ① 정정 외 접두(`연장결정`·`정정명령부과` 등) 하나라도 있으면 → `기타`(후속 절차 · 새 사건 아님).
- ② body 에 `(자회사의주요경영사항)` 또는 `(종속회사의주요경영사항)` → `기타`(상장사 자신의 사건 아님).
- ③ 아래 정규식(body 앞머리 `^` 고정 · 표 순서 = 태그 번호 t · 첫 일치). 불일치 → `기타`.
- 🔒 **이벤트 = 원공시(`is_corr=False`)만.** 정정 행은 남기고 표시만 · 이벤트·특징에 쓰지 않는다 · 원공시가 창 밖인 정정을 원공시로 대체하지 않는다(건수 인쇄).

**3-3 태그**(표 안 `\|` = 정규식 `|`)

| t | 태그 | 정규식(body) | 제외 예(→ 기타) | 예상 부호(가설만) |
|---|---|---|---|---|
| 1 | 유상증자 | `^(주요사항보고서\()?유(무)?상증자결정` | 1차발행가액결정 · 최종발행가액확정 · 증권발행결과 · 청약결과 · 권리락 · 기타주요경영사항(…철회) | − 희석·할인 발행 |
| 2 | CB/BW/EB | `^(주요사항보고서\()?(전환사채권\|신주인수권부사채권\|교환사채권)발행결정` | 전환가액조정 · 청구권행사 · 만기전사채취득 · 자기전환사채매도 · 자본으로인정되는채무증권 | − 잠재 희석 |
| 3 | 자기주식취득 | `^(주요사항보고서\()?자기주식취득(결정\|신탁계약체결결정)` | 처분 · 신탁계약해지 · 주식소각 · `[연장결정]` | + 수급·신호 |
| 4 | 공급계약 | `^단일판매·공급계약체결` | 단일판매ㆍ공급계약해지 · 유동성공급계약 | + 매출 가시성(태쏘 「재료」) |
| 5 | 최대주주변경 | `^(최대주주변경(\(\|$)\|최대주주변경을수반하는주식양수도계약체결\|최대주주등소유주식변동신고서\(최대주주변경시\)\|경영권변경등에관한계약체결)` | …주식담보제공계약체결·해제 · 최대주주등소유주식변동신고서(일반) · **`최대주주인명목회사·조합등의최대주주변경`·`최대주주인명목회사의최대주주변경`**(간접 · 상장사 최대주주는 그대로) · `투자판단관련주요경영사항(최대주주…)` | 무부호 |
| 6 | 잠정실적 | `^((연결재무제표기준)?영업\(잠정\)실적\(공정공시\)\|매출액또는손익구조\d+%\(대규모법인은?\d+%\)이상(변동\|변경))` | **…미만변동/변경(자율공시)** · 영업실적등에대한전망 · 결산실적공시예고 | **무부호**(제목만으론 흑자/적자 불명 · §14 C2) |
| 7 | 소송·횡령(제기) | `^((주요사항보고서\()?소송등의제기\|횡령·배임(혐의발생\|사실확인))` | **후속 공시**: 소송등의판결·결정 · 횡령·배임혐의진행사항 · 조회공시·풍문 · 회생·파산 | − 거버넌스 · `경영권분쟁` 부분집합 따로 인쇄 |
| — | 기타 | 위 전부 불일치 | 감사보고서 · 주총 · 배당 · 대표이사변경 … | 검정 안 함(건수만) |

**3-4 손라벨 15제목(V2 · 테스트에 그대로)** — 기대값 (태그, is_corr):
`[기재정정]주요사항보고서(유상증자결정)`→(유상증자, T) · `주요사항보고서(전환사채권발행결정)`→(CB/BW/EB, F) · `[연장결정]주요사항보고서(자기주식취득신탁계약체결결정)`→(기타, F) ·
`단일판매ㆍ공급계약체결(자율공시)`→(공급계약, F) · `단일판매·공급계약체결`(U+00B7)→(공급계약, F) · `단일판매ㆍ공급계약체결(자회사의 주요경영사항)`→(기타, F) ·
`[첨부추가]단일판매ㆍ공급계약체결`→(공급계약, T) · `최대주주변경을수반하는주식담보제공계약체결`→(기타, F) · `최대주주인명목회사ㆍ조합등의최대주주변경`→(기타, F) ·
`연결재무제표기준영업(잠정)실적(공정공시)`→(잠정실적, F) · `매출액또는손익구조30%(대규모법인은15%)이상변경`→(잠정실적, F) · `매출액또는손익구조30%(대규모법인15%)미만변경(자율공시)`→(기타, F) ·
`소송등의제기ㆍ신청(경영권분쟁소송)`→(소송·횡령, F) · `소송등의판결ㆍ결정`→(기타, F) · `유상증자최종발행가액확정`→(기타, F).

## 4. PIT · 이벤트 정의 🔒

- 달력 = `daily_prices` `stock_code='KOSPI'`(`replayer/loader.py:32` · `:205`) · 창 **617거래일** [실측].
- 🔒 **사용 가능일 `u(r)` = 달력에서 `rcept_dt` «초과» 첫 거래일.** list.json 엔 접수 «시각»이 없다(dry-run §5-5) ⇒ **공시 r 은 u(r) 시가 진입 판단부터 쓴다**(D 접수분은 D+1 07:40 스캔 전에 공개 · 주말·휴일 접수는 다음 거래일).
- 🔒 **W5(두 검정 공통)** = 진입일 X 기준 `u(r) ∈ [X−4, X]`(거래일) ⟺ 거래일 접수면 `rcept_dt ∈ [X−5, X−1]`. (a) 는 X = D+1 이므로 **`rcept_dt ∈ [D−4, D]`** · (b) 는 X = 그 이벤트의 u.
- 종목 연결 = list.json `stock_code` 6자리 ↔ `daily_prices.stock_code` 직접 일치 · NULL · 유니버스 밖 → 버리고 건수 인쇄(§6 비대칭 규칙 대상).
- 이벤트 키 = (t, g, rcept_dt) — 같은 키 원공시 여럿은 1건(중복 수 인쇄) · 같은 날 다른 태그는 태그별로 따로.
- 워밍업: 2024-03-13 이전 공시 미적재 ⇒ **진입일 ≥ 2024-03-21**((a) scan_date ≥ 2024-03-20 · (b) u ≥ 2024-03-21) · W20 변형은 scan_date ≥ 2024-04-10 [실측 달력].

## 5. (a) 원장 후보 안 «공시 있음 vs 없음» — 🔒 **㉯: 인쇄만**(Holm·라벨·게이트 없음 · 판정 언어 금지)

- 입력: `D:/research-archive/candidate_ledger_20260924/ledger.csv` md5 **`980e58492a7ac2ed11d25526f4488dbc`**(불일치면 중단) · 표본 = 특징 연구 §2-2 `in_full`(`features.csv` md5 `12216fe9359f5c21d9831400502730eb` 의 `in_full` 과 행 단위 일치) ∩ scan_date ≥ 2024-03-20 · 결과 = 원장 `ret_pct`(gross · %).
- 특징 = W5 이진 `x_T`(§4) · 인쇄 셀 = 3전략 × 7태그 · 셀마다 **n_with · n_without · δ · SE · 원 p 만**. 계산 = 주 도구 K2(`tool_calibration/RESULTS.md:57`)를 이진에 쓴 형태(해석 기록 14 `:249` · 에피소드 첫 행 · 날짜 안 첫 행 < 3 인 날 제외 · 종목 클러스터 CR1 `:240`).
- 🔴 원 p 는 이 표본에서 교정되지 않았다(가짜 게이트 없음) — 표 머리에 이 문장을 인쇄한다. 보조(역시 인쇄만): W1 `rcept_dt = D` · W20 `rcept_dt ∈ [D−19, D]` · D 제외판 `rcept_dt ∈ [D−5, D−1]`(v0.1 정의) · 정정 포함판 · `band_ok=True` 판.

## 6. (b) 공시 = 독립 후보원: 이벤트 로트 vs 층화 무작위 대조 🔒 (**유일한 판정 검정**)

- **유니버스** `U(e)`: 기준일 `e(r)` = `rcept_dt` 이하 마지막 거래일 · `loader.build_universe`(`replayer/loader.py:254` · `market_cap IS NOT NULL` · STOCK_ONLY `:28-29` 의사티커 제외) + `universe_snapshot`(`:274`) · 양쪽 모두 `classify_exclusions`(`:81` · 우선주·외국·리츠·스팩·ETF) 제외.
- **진입** = u 시가(`daily_prices.open` 원시) · 밴드 없음 · `arm_b_qty`(`ledger8/sizing.py:67` = max(1, floor(1,000,000/시가))).
- **청산** = ma20 규칙 · `ledger8/exitsim8.py`(`ExitRules` :67 · `Pos` :75 · `simulate_lot` :175 · `BASIS_D_OPEN` :32) · tp/sl = `sellprobe8.py:45 resolve_live_tp_sl('book_pullback_ma20', …)` — **(0.10 · 0.08 · 50) 과 다르면 중단**(`strategies/book_pullback_ma20/config.yaml:31-34` · `strategy.py:84-86`) · 데이터 청산 `SellProbe`(`sellprobe8.py:67` → `strategy.py:240-252` 보유 50일 · 수익 중 종가 < MA20) · window_fn `date < day`(`candidate_ledger/run.py:218`) · 동시 터치 = 손절 우선 · 전략 파일 blob = `2274895`(`cd639cd` 와 차이 0 [실측]).
- **계산 격자** 🔒: (종목, 진입일) 전 격자(U(e) × u ∈ [2024-03-21, 2026-07-13] ≈ 2,447 × 562 ≈ **140만 로트** [추정])를 `candidate_ledger/exit_diag/run_random_entry.py:330 sim_fast` 로 미리 계산 — 먼저 **재현 가드**(원장 ma20 `in_full` 로트 전부 · `exit_reason`·`exit_date`·`ret_pct` 불일치 > 0.1% ⇒ 원 코드 `simulate_lot`+`SellProbe` 로 교체). 1개월 파일럿 외삽이 30분 초과 ⇒ 추출된 로트만 지연 계산 · 그래도 초과 ⇒ 중단·보고(부분 판정 없음).
- 🔒 **상한**: u ≤ **2026-07-13**(u 순번 + 50 ≤ 2026-09-23 순번 [실측 달력] · 원장 ma20 상한과 같은 산식).
- 🔒 **에피소드**: 같은 (t, g) 에서 직전 이벤트 u 로부터 ≤ 5거래일 안의 이벤트는 같은 묶음 → **첫 이벤트만** 로트. 이벤트 처리 순서 = **(u, g, rcept_no)** 오름차순(모든 난수 소비 순서).
- 🔒 **대조 풀** `P_i`(이벤트 i) = `U(e)` ∩ 같은 **시총 5분위**(`U(e)` 안 `market_cap` `rank(pct=True)` · 경계 0.2 오른쪽 닫힘) ∩ u 시가 > 0 ∩ W5 에 태그-t 원공시 없음 ∩ ≠ g · 종목코드 오름차순 정렬.
- 🔒 **대조 추출(1:1 × 반복 20)**: k = 1…20 마다 `rng_k = default_rng([20261004, 3, t, k])` 를 이벤트 순서대로 소비 · `P_i` 에서 균등 1개(복원).
- 🔒 **결과 의존 제외 — 대칭 규칙**(가장 중요):
  - 사유(인쇄 · 태그 × 팔): R1 유니버스 조인 실패(stock_code 가 `daily_prices` 에 없음 = **상폐 탈락** · 또는 e 에 시총 없음) · R2 `classify_exclusions`(사전) · R3 u 시가 없음/≤0 · R4 보유 경로 `impossible_bar`(`replayer/flags.py:88`) · R5 `exit_reason=open`·경로 중 봉 끊김(상한 안).
  - 이벤트가 R3~R5 로 빠지면 **그 이벤트의 대조 20개도 뺀다**. 대조가 R4·R5 면 같은 `rng_k` 로 **같은 `P_i` 에서 재추출**(최대 20회 · 실패 시 그 (i, k) 비움 · 가중 재정규화 · 건수 인쇄).
  - 🔒 **비대칭 판정 제한**: 팔별 결과 의존 제외율 = 이벤트 (R1+R3+R4+R5)/(R2 뒤 후보) · 대조 (R4+R5 첫 추출 실패)/(첫 추출). **이벤트 − 대조 > 2%p 인 태그는 「없음」·「있음(+)」 금지 — 「있음(−)」만 가능, 아니면 판별 보류**(빠진 이벤트는 상폐·정지 쪽 = 나쁜 꼬리일 공산).
  - 원장 `corp_event` 플래그(`loader.load_corp_events` :235 · bonus_issue/split/rights_issue 가 [u, exit] 안)를 (b) 로트에 붙여 **팔별 비율 인쇄**.
- 🔒 **통계량** `δ_t = mean(ret | 이벤트 첫 로트) − Σw·ret(대조)/Σw`(w = 1/20) · p = `run_random_entry.py:436 p2_binary`(이벤트·대조 합친 종목 클러스터 CR1 · 정규) 양측 `2·min(p_up, p_down)` · **가족 m = 7**(태그 · 유일한 가족) · 이벤트 첫 로트 < 30 ⇒ p = 1.
- 🔒 **가짜 이벤트 게이트**(태그마다 j = 1…400 · 개봉 «전»): `default_rng([20261004, 4, t, j])` 로 이벤트 순서대로 g → h 매핑 — g 가 처음 나온 이벤트의 **대조 풀 `P_i`**(시총 분위 포함)에서 비복원 추출 · 이후 같은 g 는 같은 h(그 이벤트 `P_i` 에 h 가 없으면 그 이벤트만 대체) · 가짜 로트의 R4·R5 는 대조와 같은 재추출 규칙.
  가짜의 대조 = **새 추출** `default_rng([20261004, 5, t, j, k])`(k = 1…20 · 제외 대상 = h). `p<0.10` 거부율 **≤ 0.13** ⇒ CR1 · 초과 ⇒ 2원 클러스터 SE(종목 × u 20거래일 블록 · CGM `V_종목 + V_블록 − V_교차` · `t(G_블록 − 1)` · 선례 `prereg_2026-09-26_exit_bracket_pair_comparison.md:103`)로 같은 게이트 · 둘 다 초과 ⇒ 「판별 보류(도구)」 p = 1.
  - 0.13 사유: 교정 문서 합격대(0.07~0.13)의 상한 · 400개 이항 SD ≈ 0.015 라 올바른 도구도 0.12 문턱에선 ≈ 10% 확률로 떨어진다.
- 보조(인쇄만): 비용 차감 net = 이벤트 로트 `mean(ret − 0.25)`(🔑 δ 는 양쪽 같은 비용이라 net 에서도 같다) · 승률 · 로트당 net(원 정수) · 정정 포함판 · 시총 분위별 δ · `경영권분쟁` 부분집합 · **변동성 정합 대조**(시총 5분위 × u 직전 20거래일 실현변동성 5분위 · 같은 시드 규약 `[20261004, 6, t, k]`) · 반창(§8).

## 7. 판정 라벨 · 검정력 🔒 ((b) 만)

| 라벨 | 조건(태그마다) |
|---|---|
| **있음(+ / −)** | Holm(m=7) 보정 p < 0.05 ∧ \|δ\| ≥ **0.4%p**(로트당) · 단 §6 비대칭 제한 태그는 (−)만 |
| **없음** | 95% CI(쓴 도구 SE · CR1 정규 / 2원 t) ⊂ (−0.4, +0.4)%p · 비대칭 제한 태그는 금지 |
| **판별 보류** | 그 밖 전부(n < 30 · 도구 탈락 포함) — 「없음」과 다른 칸 |

- 예상 부호(§3-3)는 가설 기록 · 일치 여부만 인쇄. 0.4%p = 비용 0.25% 초과 · 짝 비교 문서의 방향 라벨 가능선(`prereg_2026-09-26_exit_bracket_pair_comparison.md:143`)과 같음(감각값).
- 🔒 **MDE 는 개봉 «전»에 인쇄·커밋**(`results/sealed_report.md`): 태그마다 n(이벤트 첫 로트) · **sd = 대조(또는 가짜 이벤트) 로트 `ret` 표준편차 — 평균 인쇄 금지** · ① 소박판 `3.53 × sd × √(1/n + 1/(20n))` ② 도구판 `3.53 × SD_null`(가짜 400개 SE 평균) · 「없음 가능」 = `1.96 × SD_null < 0.4` 예/아니오 · 비대칭 제외율(팔별).
  K = **3.53**(Holm m=7 1단계 양측 z 2.69 + 0.84).
- 사전 추정 — 밀도 [실측 · 적재 완료본 원공시 · 617거래일 · 제목·건수만] · 이벤트 수 [실측 · 유니버스 조인·에피소드 규칙까지 · R2~R5 전] · MDE 는 [추정 · sd = 원장 ma20 첫 행 8.89 · 설계효과 1]:

| t | 태그 | 원공시(코드有) · 정정 비율 | 유니버스 원공시/거래일 | (b) 이벤트 / 에피소드 첫 | MDE (%p) | 없음 가능(1.96·SE) |
|---|---|---|---|---|---|---|
| 1 | 유상증자 | 2,152(1,402) · 63.0% | 1.88 | 939 / 911 | 1.07 | 아니오(0.59) |
| 2 | CB/BW/EB | 1,416(1,355) · 61.2% | 2.08 | 1,064 / 1,049 | 0.99 | 아니오(0.55) |
| 3 | 자기주식취득 | 1,475(1,445) · 11.6% | 2.24 | 1,229 / 1,229 | 0.92 | 아니오(0.51) |
| 4 | 공급계약 | 4,111(4,111) · 60.3% | 6.32 | 3,331 / 2,912 | 0.60 | 예(0.33) |
| 5 | 최대주주변경 | 875(875) · 35.0% | 1.28 | 601 / 569 | 1.35 | 아니오(0.75) |
| 6 | 잠정실적 | 9,940(9,940) · 8.3% | 15.43 | 7,952 / 7,846 | 0.36 | 예(0.20) |
| 7 | 소송·횡령(제기) | 1,394(1,381) · 20.3% | 2.01 | 1,011 / 790 | 1.14 | 아니오(0.64) |

- ⇒ 🔒 **결과 전 인정**: 「있음」은 |δ| ≳ 0.4~1.35%p 에서만 · 「없음」은 공급계약·잠정실적만 가능 · 날짜 몰림(설계효과 > 1)이면 더 나빠진다 ⇒ **사전 최빈 결과 = 대부분 태그 판별 보류**. (a) 는 셀당 n_with ≈ 수십~수백이라 인쇄조차 거칠다.

## 8. 창 설계 — 단일 창 + 자기 표본 가짜 게이트 🔒

- (b) 판정 = **창 전체 한 번**(u 2024-03-21 ~ 2026-07-13). 탐색 E(~2025-06-30) / 확인 C(2025-07-01~) 반창은 **인쇄만**(δ·SE·부호 일치 예/아니오).
- 사유: ① 태그·창·가족이 결과 전 고정 → «E 에서 후보 고르기» 단계가 없다 ② 분할하면 드문 태그 MDE ≈ 1.3~1.9%p ③ 도구 낙관은 자기 표본 가짜 400개 게이트(교정 `RESULTS.md` §10 권고)로 막는다.
- 「있음」 태그의 반창 부호가 다르면 판정은 그대로 두고 「국면 의존 가능」을 병기한다.

## 9. 한계 선언 🔒

1. **생존자 유니버스 · 상폐 탈락** — list.json 은 **상폐 법인에도 stock_code 를 준다**[실측: 현재 corp_cls=E 인데 stock_code 有 277종목 · 그중 `daily_prices` 에 있는 것 37] ⇒ 나머지는 조인에서 빠진다(R1 · §6 비대칭 규칙). **corp_cls 는 조회 시점 값**(상폐 뒤 E 로 바뀜) — 선택에 쓰면 룩어헤드.
2. **rcept_no 앞 8자리 ≠ rcept_dt** [실측 862/165,812 · B 857 · I 5 · rcept_no 쪽이 1~8일 이르다(−1일 652 · −3일 171 · −2일 18 · −4~−8일 21)] ⇒ 날짜는 `rcept_dt` 만 쓴다. 🔴 ② LLM 문서 경고: news(dart) 의 「접수일 = rcpNo 앞 8자리」(dry-run §3) 는 B 에서 최대 8일 **이르게** 잡혀 룩어헤드가 된다.
3. **빈티지(M4)** — 현재 DB 봉 · `OVERTIME_MIN_DATE` 2026-07-03(`loader.py:177`) 이전 보정 불가.
4. **정정 비율 태그별 8~63%**(§7 표) — 원공시만 쓰므로 정정으로 드러난 정보(해지·금액 변경)는 못 본다.
5. **제목만 분류** — 규모·흑자/적자·발행 조건 모름 ⇒ 태그 안 이질성(효과 희석).
6. **접수 시각 없음** — u 사용은 보수적 · D 당일 반응(갭)은 진입가에 이미 들어 있다.
7. **list.json 은 현재 상태** — 철회·삭제 공시가 목록에서 빠졌을 가능성(⚠️ 미확인).
8. **교락** — 대조는 시총 분위만 맞춘다(업종 미정합 · 변동성은 보조 인쇄). 인과 아님.
9. **날짜 몰림** — 게이트는 SE 낙관만 막고 국면 편향은 못 없앤다 · 가족 FWER 은 문서 안에서만.
10. **원장 근사 승계** — gross · 일봉 청산 · 시가 진입(원장 §8-3~5) · **동결 전 백필**(머리말 · 입력만).

## 10. 산출물 · 순서 🔒

경로 `backtest/concept_axes/candidate_ledger/dart_events/`: `dart_tags.py`(§3 · `normalize` · `tag_of(report_nm) -> (tag, is_corr, flags)`) + `tests/test_dart_tags.py` · `run_backfill_check.py`(§2) · `run_dart_events.py`(§4~§7) ·
`events.csv`(원공시 이벤트 · t · u · e · 에피소드 · **결과 열 없음**) · `results/`(`sealed_report.md` · `cells_a.csv`(인쇄만) · `cells_b.csv` · `fake_gate.json` · `grid_meta.json` · `run_meta.json` = git sha · 입력 md5 · `loader.db_fingerprint`(:317) · `dart_disclosures` 행수·max(fetched_at) · 시드) · `RESULTS_<날짜>.md`.

순서: ⓪ 백필(동결 전 완료 · 선언된 이탈) → ① critic → **동결 커밋** → ② `run_backfill_check.py` 완결(미완결이면 §2 재수집) · V1 → ③ `dart_tags.py`·테스트·`run_dart_events.py` **코드 커밋** →
④ **봉인 단계**: events.csv · (a) 특징 열 · n · (종목, 진입일) **전체 격자 계산 허용** · 대조·가짜 게이트·sd·MDE → `sealed_report.md` 커밋 — 🔴 **실제 이벤트와 격자의 조인 금지** · (a) 특징과 ret 조인 금지 → ⑤ **개봉**: (b) δ·p·Holm·라벨 + (a) 인쇄 → `RESULTS_<날짜>.md` 커밋 → ⑥ verifier.
- 라이브 트리 실행 금지 · 워크트리만 · DB 쓰기는 ⓪·② 의 `dart_disclosures` 뿐 · 나머지는 SELECT(`kis_template` · port 5433).

## 11. 검증 (verifier 1회) 🔒

- **V1 커버리지** — 완결 판정 전수 결과 + 표본 5일(3월 1 · 비시즌 3 · 연휴 직후 1 · `default_rng([20261004, 9])`)에 list.json 재호출(≤ 20회) `total_count` = DB 행수 + 중복 수 · 동시 실행 구간(15:16~15:24 에 처리된 날짜) 전수 재대조 · **정정 ↔ 원공시 동반율** 인쇄.
- **V2 태그** — `test_dart_tags.py` 통과 · §3-4 15제목 일치 · 가운뎃점 6종·공백·접두 변형 · 태그별 무작위 20제목(`default_rng([20261004, 11, t])`) 눈검사 오분류 건수 인쇄.
- **V3 룩어헤드** — (b) 전 로트 `entry_date = u(r) > rcept_dt` · 대조 진입일 = 짝 이벤트 u · 대조 풀의 W5 판정이 `u(r′) ≤ X` 만 씀 · (a) x=1 행 전부 `rcept_dt ≤ scan_date` — 위반 0.
- **V4 손계산 12로트** — (b) 이벤트 6 + 대조 6(`default_rng([20261004, 10])` · 태그 층화): 원시 OHLC SELECT 로 시가·qty·청산일·청산가·ret_pct 재계산 일치.
- **V5 MDE 선인쇄** — `sealed_report.md` 커밋이 `RESULTS` 커밋보다 앞서고 sd·MDE·게이트·비대칭 제외율이 거기 있으며 개봉 뒤 값과 같다 · 거기 이벤트 로트 평균·대조 평균이 없다.
- **V6 git 순서** — 동결 커밋 < 코드 커밋 < `sealed_report.md` 커밋 < `RESULTS` 커밋(`git log --format='%H %cI'`) · 백필 `max(fetched_at)` < 동결 커밋은 **선언된 사실**로 인쇄.
- **V7 비대칭 규칙** — 태그 × 팔 R1~R5 수기 재집계 · 2%p 제한 적용 여부 · 대조 재추출 건수 · Holm 단계 수기(p = 1 태그 포함 · m = 7) · 가짜 거부율 재집계 · 재현 가드 대조율 · §13 위반 0.

## 12. 비용 상한 🔒

- 인력 = **executor 1 + verifier 1** · 토큰 합 ≤ **60만**.
- 백필 = 완료(동결 전 · 상한 6,000 · 선언된 이탈) · 재수집은 미완결 날짜만 · 추가 ≤ 500 호출.
- 검정 실행(④+⑤) ≤ **30분**(격자 ≈ 140만 로트 + 가짜 7 × 400 + 대조 재추출) · 넘으면 §6 격자 절차 → 그래도 넘으면 중단·보고(체크포인트 = 태그 단위).

## 13. 금지 🔒

1. 결과를 본 뒤 태그 정규식·적용 순서·정정 규칙·u·W5·표본·가족 m=7·n ≥ 30·0.4%p·2%p 비대칭 제한·게이트 0.13·시드·재추출·대체 도구 절차·라벨을 바꾸는 것.
2. **(a) 를 판정·라벨·Holm 에 쓰는 것**(㉯) · 봉인 단계에서 실제 이벤트와 결과를 잇는 것 · 보조(반창·정정 포함판·변동성 정합·기타·부분집합)를 판정에 쓰는 것.
3. 판정문 중간 언어 · 「판별 보류」를 「없음」으로 쓰는 것 · 「있음」을 인과·순위 층 근거로 쓰는 것.
4. 결과를 3전략 룰 변경·라이브 배선 근거로 인용하는 것 — 「있음」은 별도 사전등록의 입력 후보일 뿐(새 표본 = 2026-09-28~ 전향분).
5. **조회 시점 필드**(`rm`·`corp_name`·`corp_cls`)나 `rcept_no` 앞 8자리로 이벤트를 고르거나 빼거나 날짜를 정하는 것.

## 14. 확인 문항 (답을 동결 커밋 메시지에 적는다)

- **C1 원장 재사용 충돌**(특징 연구 §4-6 · `docs/prereg_2026-09-24_candidate_feature_study.md:150`) — ✅ **사장님 답 ㉯**: (a) 인쇄만 · (b) 만 판정(2026-09-26 밤).
- **C2 잠정실적 부호** — 승인안의 「잠정실적(흑자) +」 는 제목만으로 불가 ⇒ **무부호**. 흑자/적자 구분은 본문 파싱(범위 밖 · ② 또는 후속).
- **C3 손익구조 편입** — 잠정실적에 `매출액또는손익구조…**이상**변동/변경`만 넣었다(미만변경(자율공시)은 기타) · 빼면 결산 시즌 표본이 크게 준다.
- **C4 횡령·배임 첫 공시** — 「제기」 대응으로 `혐의발생`·`사실확인` 둘 다 넣고 `진행사항` 은 뺐다(사실확인이 첫 공시인 경우가 있어서 · 같은 종목 5거래일 안 연쇄는 에피소드로 1건).
