# 「N-1 미조정 주식병합·감자 점프」 — 사전등록 ①: 보정 «전» 측정 3건

> 작성 2026-08-25 밤 · **코드 0줄** · 사장님 결정 「`rs_leader` 를 끄지 않고 데이터 계층을 고쳐 컨셉대로 돌린다」에 따른 첫 문서.
> 🔑 이 문서는 «보정»이 아니라 **«보정을 안전하게 만드는 측정»**을 등록한다. 보정 실행은 **별도 문서 ②**다.
> 🔎 **검증 이력** — 초안 → 독립 검증 정정 14건(★3건 psql, 08-25) → 개정 2차: M1 판정식 복원·M3 결과 → 개정 3차: M1 실행·검증 정정(08-26 09:30) → 🆕 **개정 4차: M2 실행·사후 재분석(검증 전)**.
> 근거 SQL — M1 `scratchpad/m1/s9~s18*.sql` · 검증 `D:\tmp\verif\v1~v18.sql` · M3 `scratchpad/m3/` · M2 `scratchpad/m2/out101/`(동결 판정기)·`scratchpad/m2/analysis/`(사후 재분석) · 기타 `q/*.sql`.

## 🔴 맨 위에 못 박는 것

이 계층에서 **「안 재고 고치면 엉뚱한 데를 고친다」가 다섯 번 재현됐다** — ① 08-20 `001510` volume **이중조정**(679행 ×2.002)으로 보정 도구의 「기존도 원본」 전제가 거짓이었고, ② 08-24 08-23 D3 「패딩」을 투사한 초안 **3건이 강등**됐으며, ③ 08-25 **첫 계기가 25건(10.2%)을 놓쳤고**(§0), ④ 08-26 M1 1차 실행에서 **어제의 분류 문턱 자체가 기록되지 않은 임의값**이었음이 드러났고(§4 M1), ⑤ 08-26 M2 의 **「피드오류 6」이 판정기 퇴화**였을 가능성이 나왔다(`_odd_one_out` `factor_median = 1.0` · **검증 전**, §4 M2).

⇒ ***보정 실행은 이 문서에 넣지 않는다.*** 이 문서는 **측정 3건만** 동결한다(§4).

---

## §0. 무엇이 일어났나 (실측) — 계기가 먼저 틀렸다

첫 계기는 **「volume=0 런이 점프행 «직전»에 끝난다」**를 전제했다. 그 전제가 거짓인 서브타입이 있다 — **정지가 안 풀린 채 기준가만 갱신되는 경우**(점프행 자체의 volume 이 0, 런이 점프행을 «관통»). 실사례 `065150` 2026-05-27.

| 계기 (UP · halt≥3 동일 모집단) | 검출 |
|---|---|
| 첫 계기 (volume>0 해제봉만) | **221** |
| 고친 계기 (정지 중 기준가 갱신 포함) | **246** |
| 차이 | **25건 = 10.2% 누락** |

🔑 ***이 서브타입은 이미 코드에 문서화돼 있었다*** — `collectors/corp_action_watch.py:85-88`: *「불연속 봉에 거래량이 있을 것을 요구하지 않는다. 실측 `380540`(2026-05-22, 1:2 병합)은 **정지가 안 풀린 채 기준가만 갱신**됐다(4,225 → 8,450, 거래량 0). "거래량 > 0 인 해제봉"만 보던 초판은 이 건을 놓쳤다」*
⇒ **생산 탐지기는 옳았고, 조사용 계기가 그보다 못했다.** 교차검증 — 246건 안에서 이 서브타입을 «따로» 세면 **25/246 = 10.2%**(`q/last.sql`)이고 첫 계기가 놓친 25건과 **동일 집합**이다.

🔴 도구가 재개일을 「volume>0 첫 봉」으로 정의하면 **이 25건에서 날짜를 틀린다.** ⇒ M1 과 ② 는 **`corp_action_watch.py:85-88` 과 «같은 규칙»으로 재개일을 정의**해야 한다(§4-0).

---

## §1. 메커니즘 — 모집단과 문턱

### 1-1. 문턱은 생산 코드 쪽이 맞다

| 문턱 | 값 | 33종목 재현 |
|---|---|---|
| 조사 초안 「4.5배」 | 4.5 | **23/33** — 10종목 누락 |
| 생산 탐지기 `corp_action_watch.py:39,45-46` | `_HALT_MIN_BARS=3` · `_NORMAL_BAND_LO=0.69` · `_NORMAL_BAND_HI=1.45` | **33/33** |

4.5배가 놓친 10종목 = `002680`(4.29)·`004870`(3.96)·`011090`(4.25)·`014990`(3.58)·`066910`(4.25)·`092220`(3.50)·`193250`(4.34)·`226340`(4.10)·`227950`(3.99)·`950220`(3.53). 🔑 ***배율이 큰 것만 찾으면 5:1·4:1 병합이 전부 빠진다.***

### 1-2. 모집단 (2024-01-01 이후 · 고친 계기 · 유니버스 `^[0-9]{6}$`)

**정규식 적용 기준** 밴드이탈 합계 **402** = UP **285**(halt≥3 **246건 / 240종목** + halt=0 **39**) + DOWN **117**.
우선주 6 을 포함하면 **408 / UP 291 / halt=0 45** 다. ***머리줄과 각주는 이 한 쌍으로만 인용한다.***

| halt(정지런) | 건수 | 계기 |
|---|---|---|
| 0 | **39** (우선주 6 포함 시 45 · 그 6건은 전부 「간격>31일」 버킷) | 이봉 |
| **1~8** | **0** | 이봉 2계기 |
| **3~8** | **0 — UP 한정**(DOWN 은 **3건**) | SQL 집계 · 세 번째 독립 계기 |
| ≥9 | 246 | p10 **13** · p50 **16** · p90 **41** · max **998** |

⇒ ***문턱을 3 에서 9 로 올려도 UP 검출 집합이 안 바뀐다***(§6-2 C2′ 무손실 246/246 의 근거). ⚠️ 코드 주석의 옛 실측(`corp_action_watch.py:6-10` — 중앙값 15봉·최소 9·최대 41)은 **정답지 105건 기준**이라 모집단이 다르다. 섞지 말 것.

### 1-3. 08-24 33종목과의 대조

일치 **33/33** · 누락 **0** · **추가 207종목**. ⇒ 33 은 `rs_leader` 한정 표본이었고 계층 전체는 **240종목**이다.

🔴 **08-24 산술 오류 정정** — `docs/audit_2026-08-24/strategy_rs_leader.md:72` 는 액면병합을 「**26**」이라 적었고 근거 소계도 **18+2+4+2 = 26** 인데 **같은 셀의 종목 나열은 27개**다. 총계 제약(33 = 병합 + 감자 4 + 둘 다 2)이 **27 을 지지**하므로 ***틀린 쪽은 소계이고 나열이 맞다*** ⇒ **병합 27 · 감자 4 · 둘 다 2 = 33**.

🔴 **「33/33 이 정지 패딩 직후」의 예외 정정 (★재확인)** — 이벤트 단위로는 **36/37** 이고, 예외 `320000` 2024-03-13 은 **「733일 결측 직후」가 아니다**: 직전 봉이 **2024-02-29 · 종가 2,155 · 거래량 98,994** 로 **간격 13일 · 패딩 0봉(halt=0)** 이다.

### 1-4. 계수(`adj_factor`)가 일을 안 한다 — **점프 «직전» 봉 기준**

| 집계 | 33종목 | 246건 |
|---|---|---|
| `corp_events` 에 split 있음 (매칭 창 **±30일**) | **6/33** | **52 (21.1%)** · MISS 194 |
| `adj_factor` NULL / =1 / <1 | — | 61 / 152 / 33 |
| **계수 무용(NULL 또는 1)** | **28/33** | **213/246 = 86.6%** |
| `market_cap` 배율 부풀림 (허용오차 **±0.35**) | **16/33** (08-24 는 13 — 창 정의 차이 「추정」, **미확인**) | 보존 **129** / 배율 추종 103 |
| 점프일 ≥ 2026-06-23 | — | **71** |

각주 — ⚠️ 점프 «봉» 기준이면 NULL/=1/<1 이 **62 / 184 / 0** 이다. ⚠️ 매칭 창을 **무제한**으로 열면 52 → **60**(33종목은 6 → 8). ⚠️ `market_cap` 보존은 검증자 허용오차로는 **137**. ***창·허용오차를 함께 적지 않은 숫자는 인용 금지.***
위 52건의 최근접 오프셋은 **d=0 이 28(54%) · |d| ≤ 3 이 48(92%) · 밖이 4** 다 ⇒ 귀속은 대체로 맞다. 다만 **배율 «적용 근거»로는 여전히 금지**(§4-M3 새 제약 1).

---

## §2. 판정 기준 — 「수집기가 6/23 에 고장났다」는 **기각됐다**

### 2-1. EOD 파이프라인 순서 (확인)

`collectors/daily_collector.py:95-111` — `upsert_daily_rows` → `update_returns_volatility`(`:98`) → `infer_and_stamp_split_factors`(`:101`) → `update_adj_factors`(`:102`) → `scan_and_queue`(`:109`).

### 2-2. 🔴🔴 원인은 고장이 아니라 **증분 writer 설계 결함**이다

`collectors/daily_writer.py:40-50` `DAILY_UPSERT_SQL` — `INSERT` 컬럼 목록에 **`adj_factor` 가 없고**, 컬럼에 **DEFAULT 도 없다.** ⇒ ***신규 행은 전부 `adj_factor = NULL` 로 태어난다.*** 그 뒤 `update_adj_factors`(`collectors/daily_adj.py:93-107`)는 **`corp_events` 에 split 을 가진 종목만** 손댄다(대상 한정 근거 `_adj_update_rows` 독스트링 `:79-84`, 필터 `:30`).

**★ 실측(psql)** — `created_at = '2026-06-22'` 인 행은 **전체 2,774,773행**(그중 `date` 가 2026-05 인 분이 **44,740행**)이고 **NULL 0 · =1 2,703,823 · ≠1 70,950**, 적재 범위 2021-01-12 ~ 2026-06-22. ⇒ 조사 보고의 「44,740행」은 **2026-05 한정값**이었다.
**★ 실측** — 06-23 이후 **하루당 non-NULL 이 103행**(08-05 부터 105)이고 예외는 **07-08 하루뿐(2,483행)**, 44거래일 누계 **6,940행**. (「103 뿐인데 그 중 2,483」은 성립하지 않는 서술이었다 — 103 은 **일별**, 2,483 은 **하루치**다.)

***결함이 하루 만에 다시 재현됐다*** — 08-25 → 08-26 사이 늘어난 **76행이 전부 NULL** 이다(§4 M1 프레임 재측정).
⇒ **「6/23 부터 고장」 가설 기각.** 고장난 적이 없다 — **처음부터 채우지 않는 설계**였고 일괄 적재가 그것을 가리고 있었다.

### 2-3. 읽기 계층은 **NULL 과 1 을 구별하지 못한다**

`db/quant_daily_reader.py:157-159` · `db/repositories/price.py:129` · `:291` 이 모두 `(volume * COALESCE(adj_factor, 1))` 이고 거래대금도 같다 — `quant_daily_reader.py:91`.
⇒ ***「모른다」(NULL)가 「조정 없음」(1)으로 조용히 접힌다.*** 가격에는 곱하지 않는다(`quant_daily_reader.py:148-156` · `price.py:116-121`).

### 2-4. `corp_events` — 감자는 애초에 «분류되지 않는다»

전체 1,527행 / 457종목: `rights_issue` 1,245 · `split` 205(161종목) · `bonus_issue` 75 · `administrative` 2. 병합이 `split` + `direction='merge'` 인 것은 **의도된 규약**이다(`split_factor_infer.py:20-24`).

🔴 `corp_events_collector.py:61-66` `DART_EVENT_MAP` 에는 **감자 키워드가 없다**(분할·병합·무상·유상만) ⇒ `_classify`(`:101-106`)가 `(None, None)` 을 반환한다. 결과: **감자 6종목 중 그 감자를 잡은 이벤트 0건**.

**★ 실측 — opendart split 은 들어왔는데 계수가 없다.** 2026-07 **10건 / 미보유 10** · 2026-08 **53건 / 미보유 47** ⇒ **미보유 57건**이 `daily_adj.py:30` 의 `WHERE` 에서 탈락한다(조사 보고의 「9·47 / 51」은 틀렸다). 원인 후보 둘은 **미분해** — `split_factor_infer.py:46` `_MAX_GAP_CALENDAR_DAYS=5` / `:67-68` `_RATIO_REL_TOL=0.03`. **2026-06 이벤트는 공백**이다.

---

## §3. 선택지 — 세 갈래, 지금은 고를 수 없다

🔴🔴 **가격 컬럼에 규약이 «둘» 섞여 있다.** 2024 이후 `adj_factor` 계단 **70경계**(복원 판정식 기준, §4 M1) —

| 규약 | 건수 | 성격 |
|---|---|---|
| **S1** 조정 저장(연속) | **31** | `adj_close = raw_close / adj_factor` 가 참 |
| **S2 계열** 원주가 | **39** | 그중 **상·하한가 착지 13** |
| 미설명 | **0** | 밴드 밖이 하나도 없다 |

🔴 **표기 규약(08-26)**: `S2′ ⊂ S2` 라 「26 / 13」 분해는 **판정 우선순위의 산물**이다 ⇒ ***「S2 계열 39(그중 착지 13)」로 적고 「S2 26」 단독 인용은 금지.***
숫자 이력: 초안 **42/70** → 1차 **38/70** → 복원 **39/70**. ***결론은 세 번 다 같다 — 두 규약이 혼재하고 원주가가 과반이다.*** `CLAUDE.md` 와 `adj_repair.py:3-6` 의 `adj_close = raw_close / adj_factor` 는 **31/70 에서만 참**이다.

✅ **미확인 #11 종결** — 시대별 보고값 「S1 pre-2026 28 + 2026+ 3 = **31**」·「S2계열 2026+ **39**」은 복원된 분류와 **정확히 일치**한다. ***불일치했던 쪽은 어제의 총계였다.***

| 안 | 내용 | 판정 |
|---|---|---|
| **(가)** 과거 OHLC 에 배율을 곱해 저장 | 절벽을 없앤다 | 🔴 **전면 적용 금지** — S1 구간 31 에서 **이중조정**이 된다 |
| **(나)** `adj_factor` 만 기입 | 계수를 복구 | ⚠️ 읽기 계층이 가격에 안 쓰므로 **volume 만 바뀌고 절벽은 그대로** |
| **(다)** KIS 두 피드 재수집으로 **열별 출처 못박기** | 규약을 확정 | 🔴 **M2 가 「전면 금지」로 발화**(§4 M2) — 재분석은 근거 약화 주장, **검증 전** |

**(다)의 구현 현황** — `adj_repair.build_repair_rows:57-72`(OHLC ← 조정 피드 · volume ← 원본 피드 · `adj_factor` ← `derive_factors:16-54` 의 «거래량 비»)는 완성돼 있다. 막힌 곳은 `scripts/repair_corp_action_prices.py:250-255` 의 **volume 불변 게이트**로 **10/10 이 걸려 `--apply` 가 안 돈다.** 🔑 ***게이트가 아니라 게이트의 «전제»가 거짓이다*** — `001510` 이중조정 · `001230` 0.8509 부분조정 · `000040` **KIS 피드 자체가 틀림**.

**노출 규모** `adj_factor ≠ 1` **76,318행 / 101종목**(`< 1` 이 45,675행 / 52종목). 🔴 **변경 추적 불가** — `daily_derived.py:61` 의 `updated_at = CURRENT_TIMESTAMP` 가 `update_returns_volatility`(`:68-72`)로 **매일 전 행을 재도장**한다. 계기는 백업 `batch_id` 뿐이다.
**수집기 현황** — 탐지 `corp_action_watch.scan_series:75-110` 작동 중, 큐 `logs/corp_action_refetch_queue.jsonl` **261줄 전부 pending**. 공백은 코드가 스스로 적어 뒀다(`:6-10`) — `collect_one(lookback_days=7)` 의 **7봉 창 < 정지 중앙값 16봉**.

---

## §4. 🎯 등록하는 측정 3건 (이 문서의 본체)

### 4-0. 공통 전제와 이 문서의 성격

> 🔑 **동결하는 것은 «결과 예측»이 아니라 «결정규칙»이다.** 「**어떤 값이 나오면 ② 가 어디로 가는가**」를 미리 못박아 사후에 분기를 고쳐 쓰는 것을 막는 장치다 ⇒ **적중률이 아니라 「분기표를 그대로 따랐는가」로 판정된다.**
> **재개일 정의 = `corp_action_watch.py:85-88` 과 같은 규칙**(「volume>0 첫 봉」 아님, §0). ② 에 그대로 승계된다.

측정 모집단은 **UP · halt≥3 = 246건 / 240종목** · 유니버스 `^[0-9]{6}$`(§1-2)다.

### M1. 가격 컬럼 규약 분류표 (종목 × 시대 → S1 / S2)

**왜 먼저인가** — (가) 전면 적용은 이미 금지됐다. M1 이 정하는 것은 「(가)를 «어느 구간에» 쓸 수 있는가」다.

#### 판정식 복원 (08-26 · 동결 «후» 변경 · 사장님 재승인 09:00)

08-26 1차 실행이 **실행 전 조건 게이트에서 정지**했다 — `±0.03` 으로는 70경계가 **5 / 11 / 13 / 41** 로 갈려 「28/25/13/4」를 재현하지 못했다. 역산으로 원 규칙을 복원한 뒤 조사자가 확인해 준 것은 셋이다: ① 어제 문턱은 **기록되지 않은 임의값 0.85 / 1.18**, ② `±0.03` 은 `split_factor_infer.py:68` `_RATIO_REL_TOL`(액면비율 스냅용)을 **규약 판정에 잘못 옮긴 것**, ③ 「미설명 4」는 건별 판단 없이 그 임의 부등식에서 떨어진 **낙오**.
🔑 ***게이트가 제 일을 했다*** — 사후에 문턱을 맞추는 대신 측정이 멈췄고, 그래서 임의 문턱이 드러났다. ⚠️ **결과를 보고 분기를 고친 것이 아니라 판정식의 오기재를 고친 것**이다. 분기표는 그대로다.

**판정식** (생산 상수 `corp_action_watch.py:45-46` `_NORMAL_BAND_LO/HI`) · `step` = 경계의 `adj_factor` 배수, `r` = 경계 직후 close ÷ 직전 close.

- **S1** — `r ∈ [0.69, 1.45]` (가격이 정상 밴드 안 = 이미 조정 저장)
- **S2** — `r × step ∈ [0.69, 1.45]`
- **S2′** — `r × step ∈ [1.27, 1.33] ∪ [0.67, 0.73]` (상·하한가 착지)
- **미설명** — 둘 다 밴드 밖

🔴 **실행 전 조건**: 70경계가 **31 / 26 / 13 / 0** 으로 재현돼야 한다. 실패하면 측정을 시작하지 않는다.
**방법 B** — 표본 단위는 행이 아니라 **(종목 × 시대) 구간**, 대조는 KIS 두 피드. **N₀ = 101**(M2 가 이미 두 피드를 받는 종목 ⇒ 추가 API 0) **+ M3 3건**(`003350`·`010120`·`020400`). 그 위는 ⬜ **사장님 결정**.

| M1 결과 | 보정 문서 ② 의 진행 |
|---|---|
| 한 종목 안에서 **시대별로 갈린다** | **구간별 보정만.** ② 는 구간 경계 확정부터 |
| 시대 경계가 **전 종목 공통**이다 | 경계 한 개로 일괄 분기 — ② 최단 경로 |
| 계수 무용 구간이 **전부 S2**(원주가) | 그 구간에 한해 (가) 가능 · `CLAUDE.md` 규약 문구 **정정** |
| **미설명 0 유지** | ✅ 예정대로 진행 |
| **미설명 ≥ 1** | 🔴 **중단.** 규약이 셋 이상이라는 뜻 |

#### 🆕 M1 실행 결과 (2026-08-26 · 동결 «후»)

**게이트 ✅ 31 / 26 / 13 / 0 재현.** 시대 분해 = S1 pre-2026 **28** · 2026+ **3** / S2계열 2026+ **39** · pre **0**. 월별로는 **2024-02 ~ 2026-03 전건 S1(30/30)**, **2026-04 부터 S2계열(39/42)**.

| 분기 | 판정 |
|---|---|
| ① 종목 안에서 시대별로 갈린다 | 🔴 **철회** — `049470` 은 **전역 경계를 사이에 둔 것**이라 종목별 이질성의 근거가 **아니다** |
| ② 시대 경계가 전 종목 공통 | ✅ **성립(조건부)** |
| ③ 계수 무용 구간 전부 S2 | 🔴 **판정 불가** |
| ④ 미설명 0 유지 | ✅ |

**②** 역전(last_S1 **04-23 `058430`** vs first_S2 **04-13 `010120`**)은 M3 가 **데이터오류로 판정한** `010120`·`003350` **2경계**가 만든 것이다. 손 제외(B)하면 **04-23 / 04-27 역전 0**, 사전지정 가능한 **규칙 C**(내재주식수 계단 ∧ 밴드이탈 서명 보유 **124종목** 전건 제외 · 70경계 중 **5** 탈락)로도 **04-23 / 04-29 역전 0**. `058430` 04-23 은 pykrx 10:1 분할 **동일일자** · 내재주식수 **60,000,000 불변** · 가격 연속 = **진짜 S1**.
⇒ **경계창 `2026-04-23 < t ≤ 04-27`**(규칙 C 로는 04-29). 🔴 ***단 이것은 「오염 124종목 제외 후」 진술이다.***

**③** post 계수 대리는 구조상 **post NULL 이 불가**해 **69/70 이 퇴화**하고, 목표 **3,075,128행 중 1.6%(49,813행)**만 본다 ⇒ ***방법 B 로만 답할 수 있다.***

🔴 **방법 A 의 사각 — 계수가 «불변»인데 단위가 바뀌는 경계를 원리적으로 못 본다.**
`003350`·`010120` 의 2~3월은 **분할 후 주식수(각각 22,662,722 / 30,000,000 → 150,000,000 기준 내재)를 선반영한 조정 표현**이고, **03-16 / 03-23 에 원주가로 전환**하며, **04-20 / 04-13 은 원주가 상태의 진짜 5:1 액면분할**(pykrx 동일일자)이다. 순변화가 연속으로 보이는 것(9,660 → 9,400)은 **raw/5 가 같아진 우연**이다. ⚠️ 어제의 「왕복 여행 · 방법 A 가 끝을 오독」 해석은 **철회**한다.
`020400` 은 `corp_events` 에 split 이 **없다**(`rights_issue` 08-12 **1건뿐**) — 근거는 **내재주식수 비 1.998** 하나이고, 04-10 은 **상한가 + 단위복귀가 겹친다**(r **0.6506**).

🔑 **두 번째 계기 = 내재주식수 `market_cap / close`** — 계수가 평평한 구간에서도 단위 전환을 잡는다(M3 3건 **3/3** · 방법 A **0/3**). ⚠️ 단 S1 경계에선 `mcap_ratio` 가 `r` 을 그대로 따라간다(**31 중 26**) = **파생값** ⇒ ***계단질 때만 판별력이 있다.***

**프레임 재측정(08-26)** — NULL **371,588** + `=1` **2,703,540** = **3,075,128행** / 전체 **3,151,446**(97.6%). 08-25 값(3,075,052)과의 차 **+76 은 전부 NULL** 이다(§2-2).
**방법 B 프레임 — ② 승계 제안**: 「`adj_factor ≠ 1` 101종목」은 사각의 **1.6%** 밖에 못 본다. 계단 «안» 밴드이탈 **309건 / 251종목** 중 246 과 겹치는 것이 **208**, **신규 101건**(DOWN 93 + UP halt<3 8), 101종목 **밖이 91**. B6 서명 **127건 / 124종목**. ⇒ 제안 프레임 = **결함 서명 합집합(B6 124 ∪ N-1 240 ∪ 101)** · **확장은 별도 승인.**

✅ **미확인 #12 기각 종결** — 허위 계수 스탬프 후보 3건은 pykrx·`event_date` **3/3 일치**, 액면 배율 = `split_factor` **3/3**(단 `078590` 의 `meta.ratio` 문자열 `"1:2"` 는 2.5 와 불일치). 잔여: `036630` r **1.3000** · `078590` r **1.2994** 는 S1 31건 중 **상한가 착지 유일 2건**이라 가격만으로는 구별 불가였고 가른 것은 `corp_events` 다. `market_cap` 은 여기서 **판별력 0**.

### M2. `volume` 규약 101종목 **전수** (8/20 미결 승계)

**왜** — (다)의 게이트가 막힌 원인이 「기존 volume 도 원본」 전제의 거짓이고 반례가 **3종류**다(이중조정 `001510` · 부분조정 `001230` · 피드오류 `000040`).
모집단 = `adj_factor ≠ 1` **101종목 / 76,318행** · 비용 = KIS API **~200호출** · 산출물 = 종목별 판정 ∈ {**원본 · 이중조정 · 부분조정 · 피드오류 · 판정불가**} + 배율.

| M2 결과 (문턱은 101종목 대비 %) | ② 의 진행 |
|---|---|
| 원본 **≥ 90%** | 게이트 유지 + 나머지를 **예외목록**으로 → `--apply` 재개 |
| 이중조정 **≥ 10%** | 🔴 게이트 유지 · ② 앞에 **volume 원복 단계** 삽입 |
| 부분조정 **≥ 5%** | 🔴 배율이 단일값이 아니다 ⇒ ② 에 **구간별 배율 산출** 추가 |
| 피드오류 **≥ 3종목** | 🔴 (다) 전면 적용 금지 · **종목 화이트리스트**로만 |
| 판정불가 **≥ 20%** | ⬜ 사장님 판단 — 「모른다」를 「안전」으로 접지 말 것 |

🔴 **동시 성립 우선순위(동결)**: **피드오류 > 부분조정 > 이중조정 > 판정불가 > 원본** — ***중단·금지 분기가 재개 분기를 항상 이긴다.***
🔑 배율은 **«거래량 비»로 고정**(`derive_factors:16-54`). 종가 비는 쓰지 않는다(`corp_action_watch.py:82-83`).

#### 🆕 M2 실행 결과 (2026-08-26 21:09 · 동결 «후»)

**실행 조건** — 101종목 · KIS **202호출** · `daily_prices` **쓰기 0**. ⚠️ cwd 가 **라이브 트리**였다(토큰 캐시) — 읽기·조회뿐이라 쓰기는 0 이지만 **일탈로 기록해 둔다**.

**동결 판정기 결과 (그대로 · `scratchpad/m2/out101/`)** — 원본 **23(22.8%)** · 이중조정 **0** · 부분조정 **0** · 피드오류 **6(5.9%)** · 판정불가 **72(71.3%)**.

**발화한 분기** — ① **피드오류 ≥ 3종목** ⇒ 🔴 **(다) 전면 적용 금지 · ② 는 종목 화이트리스트로만**(우선순위 최상위라 **지배**) · ② **판정불가 ≥ 20%** ⇒ ⬜ **사장님 판단**.
`020400` 은 별도 실행에서 **판정불가**(ratio 1 ↔ 1.998 혼재 · 3/16 단위전환과는 정합). `001230` 은 **101 대상 밖**이라 결과가 없다.

#### 🆕 M2 사후 재분석 (동결 «후» · **판정기 «밖»** · `scratchpad/m2/analysis/`)

🔴 ***이것은 동결 판정이 아니다.*** **verifier 미경유**이며 위 동결 결과를 **대체하지 않는다.** 08-27 아침 검증 뒤 사장님 승인이 있어야 ② 에 반영된다.

**(1) 「판정불가 72」는 잡음이 아니었다** — 71/72 가 **±2% 상수 런 2~5개**로 완전 분해된다(coverage ≥ 0.95 가 **71/72**, 예외 `012210` 0.90). 행 모델 `DB ∈ {raw, raw × 누적배율, raw × 부분배율}` 이 **96.2%** 를 설명한다.

**(2) 재분류** — A 전 구간 완전소급조정 **39**(= 이중조정) · E 기업행위일 경계 부분조정 **21** · B 전 구간 부분 **1**(`031860`) · F 제3날짜 경계 **4**(`001510`·`003350`·`010120`·`058430`) + 혼합 **1**(`131100`) · G 진짜 판정불가 **6**(`001530`·`006040`·`036630`·`053950`·`086520`·`355150` — 상수이나 배율 출처 불명).
⇒ 이 기준이면 **이중조정 38.6% ≥ 10%** 와 **부분조정 21.8% ≥ 5%** 가 **둘 다 발화**한다 ⇒ ② 앞의 **volume 원복**과 **구간별 배율 산출**이 **둘 다 필수**가 된다(두 조치는 배타가 아니라 **가산**이다).

**(3) 「피드오류 6」은 오분류다** — `004090`·`035720`·`071090`·`098460`·`263750`·`267250` 은 **전부 2021-01-04~11 창의 첫 6행**이고 DB 가 **수정주가 피드와 일치**한다(DB 6행이 조정본). 원인은 `scratchpad/m2/m2_volume_audit.py:174-181`(def `:166`) `_odd_one_out` 이 **`factor_median = 1.0` 에서 퇴화**한 것이다 — `level = 1.0` 이면 `:174` 의 `adj_volume / level` 이 조정본 그대로라 DB 와 일치하고, `:180` 이 `"raw"`(= 피드오류)를 반환한다(작성자 확인). 8/20 의 `000040`(원주가 피드 2행 오류)과는 **구조가 다르다.** ⚠️ **원자료 재대조는 미확인.**
⇒ ***「(다) 전면 금지」의 근거가 약해진다*** — 지배 분기가 (2)의 둘로 옮겨간다. 🔑 ***판정기가 스스로 만든 범주가 최상위 우선순위를 차지했다*** — 이 문서의 다섯 번째 재현(맨 위 ⑤).

**(4) 경계 48건 / 32종목** — 계수 계단일과 일치 **31(64.6%)** · M1 경계창 04-23~27 안 **1(2.1%)** · 제3날짜 **16**(그중 **2023-04-25 가 3종목 공통** — `001510`·`003350`·`058430`) · **06-22 일괄적재 가설은 지지 없음**.
🔑 ***M1 의 전환은 «봉 축»이 아니라 «종목 축»이다*** — 마지막 기업행위일이 **04-23 이전인 57종목 중 완전조정 47(82.5%)**, **04-27 이후 40종목 중 0**. Fisher **p = 3.5e-18** · 비율차 **0.825 [0.726, 0.923]**.
⇒ **4/27 이후 기업행위는 volume 소급 반영이 0/40** ⇒ ② 화이트리스트의 축 = **「기업행위일 4/27 전 / 후」**.

**(5) 「원본 23」은 규약이 아니다** — M1 과 조인하면 S2계열 **21** · S1 **0**(p = **1.6e-07**), era 2026+ **21** / pre **0**. ⇒ 정체는 **「전환 «후»에 일어난 사건이라 아직 처리되지 않은 상태」**다. `260970`·`380540` 은 **정보행 0** 이라 검정 불가인데 원본으로 집계됐다(**오집계**).

**(6) 8/20 미결과의 대조** — `001510` 의 중간 런이 **정확히 679행**이고 `1 / 0.499463 = 2.00215` ⇒ ✅ **같은 행 집합**이다. `004710` 은 **부분 소급**(2025-09-26 반영 · 2026-06-08 미반영)이고, 04-20~06-10 구간 DB < 피드 **0.3~4%** 는 **별건 미확인**.

**② 정의 변경 제안 (⬜ 결정은 사장님)** — (i) 판정을 **행 단위 모델 적합 + 구간별 배율표 산출**로(전 구간 단일 중앙값 **폐기**) · (ii) 「원본」 판정은 **정보행 ≥ 20** 에서만, 미만은 **「검정 불가」 라벨** · (iii) `_odd_one_out` 의 level 에 `factor_median` **금지**, **행별 factor** 사용.

**한계 (재분석)** — `TOL 0.02` · `MIN_RUN 3` **단일 조합**(감도 미측정) · 창(2021-01) **이전 기업행위는 불가시** · 부분조정 후보를 **접두부로 한정**(역방향 6종목 unmatched) · **적재 이력 미조회**(제3날짜 정체 미확인) · `001230` **미대조**.

### M3. `halt=0` · 간격 **≤5일** **7건**의 정체

**★ 실측 명단 (`q/m3.sql`)**

| 종목 | 점프일 | 직전 종가 → 종가 | ratio | 간격 | 직전 봉 거래량 |
|---|---|---|---|---|---|
| `003350` | 2026-03-16 | 9,660 → 45,900 | 4.7516 | 3일 | 98,304 |
| `010120` | 2026-03-23 | 177,200 → 823,000 | 4.6445 | 3일 | 777,724 |
| `005930` | 2026-01-14 | 70,500 → 140,300 | 1.9901 | 1일 | **1,000,000** |
| `020400` | 2026-03-16 | 3,313 → 6,600 | 1.9922 | 3일 | 18,734 |
| `032980` | 2026-06-23 | 19 → 32 | 1.6842 | 1일 | 22,312,657 |
| `227100` | 2026-06-23 | 18 → 50 | 2.7778 | 1일 | 8,297,545 |
| `222160` | 2026-07-03 | 94 → 140 | 1.4894 | 1일 | 2,016,658 |

**분기표 (동결)** — 다리는 `news`(DART 제목, `news_stock` 조인) + `corp_events` 둘. 🔴 「정지사유」 표는 `kis_template` 에 **없다**.

| M3 결과 | ② 의 진행 |
|---|---|
| 7/7 이 기업행위 | 조건을 **C1 ∧ (C2′ ∨ 간격≤5)** 로 확장 · 246 → 253 재정의 |
| 일부가 **실제 급등**(정리매매·신규상장 포함) | 🔴 **「실제 급등 0」 즉시 철회**하고 ② 중단 — 판별식이 없다는 뜻이다 ※ |
| 일부가 데이터 오류 | 별건 분리 · ② 는 246 유지 |
| 판정불가 | 246 유지 + 해당 건을 **보정 대상에서 명시적 제외** |

🔴 **동시 성립 시 「중단」이 이긴다.**

#### 🆕 M3 실행 결과 (2026-08-26 · 동결 «후» · 08-26 정정 반영)

| 종목 | 판정 | 근거 |
|---|---|---|
| `222160` · `230980` | **정리매매 (라벨 양성)** | DART 공시 실재 — `222160` 「상장폐지에 따른 정리매매 개시」 06-24 · `230980` 05-21 |
| `032980` · `227100` | **정리매매 (증거등급 상향)** | 동일 서명 코호트에 라벨 양성 2건이 붙었다 |
| `005930` | **데이터 오류 — 3행 · 경로 «둘»** | ① **01-11(일요일)** = 전 DB 유일 주말행 · `created_at` **07-23 17:45:56 단독** ② **01-12·13 은 06-22 일괄적재 안** · `market_cap` ↔ close ×2 불일치는 **이 두 행** · 01-12 `market_cap` **811,463,448,910,411** = **01-08 값 바이트 복사** |
| `003350`·`010120`·`020400` | **데이터 오류 = 규약 혼재** | 진짜 계단은 **04-20 / 04-13 / 04-10 DOWN** · 3월 UP 점프는 **분기 «안» 단위 전환**(§4 M1 사각) · 내재주식수 비 **5.0 / 5.0 / 2.0** |

**정리매매 코호트** — 2026-06/07 에 시계열이 끝난 **12종목** 중 동일 서명(정지런 후 **7세션** · **95%↑ 붕괴**)이 **8종목**: `032980`·`060240`·`101390`·`106520`·`203690`·`222160`·`227100`·`230980`. 🔴 어제의 **「9 중 4」는 철회**한다.

**분기표 적용** — 행2(실제 급등)와 행3(데이터 오류)이 **동시 성립** ⇒ 동결 규칙 「중단이 이긴다」에 따라 ***② 중단 · 「실제 급등 0」 철회.***

> 🔑 **관리자 주석 (문서에 없던 판단 — 사후 추가임을 명시한다).** 정리매매 건은 **봉 단위로는** 권고 판별식 **C1 ∧ C2′ 가 이미 배제**하고 있었다(전부 halt=0) ⇒ ***판별식은 유효하다.*** 분기 행2 의 「판별식이 없다는 뜻이다」(※)는 **과한 문구**였고, 정확히는 **「모집단 «밖»에 실제 급등이 있다」**이다.
> ⚠️ **단 「종목 단위」로는 참이 아니다** — `032980` 은 **246 안에 있다**(2025-09-09 병합 건). 배제는 **봉 단위 진술**로만 인용할 것.
> 「실제 급등 0」의 철회 범위는 **halt=0 영역**이고, **halt≥3 모집단(246건) 안에서는 여전히 참**이다.

**라우팅** — `005930` **만** 일봉 결손 백로그로. 나머지 3(`003350`·`010120`·`020400`)은 **M1 표본**으로.

#### ② 로 승계되는 새 제약 2건

1. 🔴 **`corp_events` 매칭을 배율 «적용 근거»로 쓰지 말 것.** 다만 「봉 귀속 미보증」은 과했다 — 52건의 최근접 오프셋은 **d=0 이 28(54%) · |d| ≤ 3 이 48(92%)** 이고 **±3일 밖은 4건뿐**이며, 오귀속 표본 2건은 **애초에 이 52 «밖»**이었다. ⇒ **「4/52 만 ±3일 밖」**으로 완화하되 **배율 적용 금지는 유지**.
2. ⚠️ **`market_cap` 내재주식수 비를 판별식으로 승격하지 말 것** — 이번 7건은 갈랐지만 `sr ≈ 1` 군에 `002070`(r = 33.38) 같은 **close 파생 케이스**가 실재하고, S1 경계에선 `r` 의 파생값이다(§4 M1).

---

## §5. 절차

1. ✅ M1 게이트·1차 분석(08-26) · ✅ M3(08-26) · ✅ **M2 동결 판정기(08-26 21:09)** → 남은 측정은 **M1 방법 B**
2. 🔴 **M2 사후 재분석은 verifier 미경유** ⇒ **08-27 아침 검증** → 사장님 승인 → 그 뒤에 **② 사전등록 착수**
3. ⬜ **사장님 결정 3건** — ① M1 의 `N`(N₀ = 101 + M3 3건 위) · ② 방법 B 프레임 확장 여부 · ③ **② 정의 변경 제안 (i)~(iii)**
4. 🔴 **현재 ② 는 두 겹으로 막혀 있다** — M3 분기의 **중단**, 그리고 M2 동결 분기의 **(다) 전면 금지 · 화이트리스트**. 재개하려면 관리자 주석의 범위 한정과 08-27 검증 결과를 사장님이 승인해야 한다
5. 워크트리에서 보정 실행 (🔴 **라이브 트리 실행 금지** · 백업 `batch_id` 필수)
6. 판정 — 🔴 **홀드아웃은 측정 «전»에 가른다**(종목 `md5(code) % 2`)

---

## §6. 오판 방지 — 판별 조건과 잔여 위험

### 6-1. 「실제 급등 0」 — **halt≥3 안에서만 참** (08-26 범위 한정)

KRX 일일 가격제한 ±30%(`utils/data_sanity.py:50`) ⇒ **기준가 재설정 없이 종가비 1.45 를 넘는 것은 불가능하다** — 전수 조사가 «발견»한 것이 아니라 밴드 상한이 함의한다.
🔴 **예외 2**: **정리매매 구간**과 **신규상장 첫날**. ***예외가 실측으로 발화했다*** — `222160`·`230980` 이 공시로 확정된 정리매매이고 `032980`·`227100` 이 같은 서명이다(§4-M3). ⇒ **halt=0 영역에서 철회**, **halt≥3 모집단 246건 안에서는 유지.**

### 6-2. 조건별 판별력

| 조건 | 246건 | 33종목 | 판정 |
|---|---|---|---|
| **C1** `ratio > 1.45` | 246 | 33 | **(모집단 정의)** — 판별력 0 |
| **C2** 정지런 ≥ 3 | 246 | 33 | **(모집단 정의)** — 판별력 0 |
| **C2′** 정지런 ≥ 9 | **246 / 246 무손실** | — | ✅ **권고** |
| C3 `market_cap` 보존 | 129 | 17 | 🔴 **사용 금지** — 103건 오분류 |
| C4 `corp_events` 보유 | 52 | 6 | 🔴 **단독 사용 금지** (+ 새 제약 1) |
| C5 `adj_factor < 1` | 33 | 5 | ⚠️ 보조 |

> **권고 판별식 = C1 ∧ C2′.** ⚠️ C1·C2 는 모집단을 «정의»한 조건이라 그것으로 재현율 100% 를 주장하면 **순환논증**이다.
> ✅ C1 ∧ C2′ 는 08-26 정리매매 건을 **봉 단위로** 이미 배제하고 있었다 — 단 **종목 단위로는 `032980` 이 246 안**이다(§4-M3 주석).

### 6-3. 잔여 위험

**halt=0 39건**(우선주 포함 45) → 35 는 결손 · 3 은 미분류 · **7 은 ✅ 08-26 판정 완료**(§4-M3) · ✅ **IPO 교란 유보 해제**(1/246) · ⚠️ `corp_action_watch.py:6-10` 의 **7봉 창**은 M1~M3 로도 안 고쳐진다(**수집기 수정은 이 문서 밖**).

---

## §7. 이 문서가 답하지 «않는» 것 · 미확인 목록

### 7-0. 안 하는 것 (명시)

| 항목 | 어디로 |
|---|---|
| **보정 실행** (`--apply` · 과거 OHLC/계수 쓰기) | **보정 문서 ②** — 현재 **두 겹으로 중단**(§5-4) |
| **M2 사후 재분석의 «검증»** | **08-27 아침** — 이 문서는 결과를 «기록»만 한다 |
| **수집기 수정** — `daily_writer.py:40-50` 에 `adj_factor` 컬럼 추가 (§2-2) | 별도. 🔑 고쳐도 **오늘 이후**가 멎을 뿐 과거는 안 고쳐진다 |
| **수집기 수정** — `corp_events_collector.py:61-66` 감자 키워드 추가 (§2-4) | 별도 |
| **판정기 수정** — `scratchpad/m2/m2_volume_audit.py:174-181`(def `:166`) `_odd_one_out` (§4 M2 재분석 iii) | 별도 · **검증 후** |
| **`rs_leader` 랭킹 재정의** (`screener.py:54-62` 의 `ret120` 축) | 별도 사전등록. 🔴 **끄지 않는다**가 사장님 결정이다 |
| `split_factor_infer` 의 **57건** 탈락 원인 분해 (§2-4) | 별도 |
| 방법 B 프레임 **확장**(B6 124 ∪ N-1 240 ∪ 101) | ② 승계 제안 · **별도 승인** |
| 일봉 결손 49,252행(+ `005930` 3행) · 팬텀 세션 | 기존 백로그 |

### 7-1. 미확인

| # | 항목 | 상태 |
|---|---|---|
| 1 | `market_cap` 배율 부풀림 **16/33** vs 08-24 의 **13** — 창 정의 차이 「추정」 | 미확인 |
| 2 | 2026-06-22 일괄 적재(**2,774,773행**) · 07-08 하루 2,483행의 **출처** | 미확인 |
| 3 | opendart split **57건**이 `split_factor` 를 못 얻은 원인 | **미분해** |
| 4 | **2026-06 `corp_events` 공백**의 원인 | 미확인 |
| 8 | 점프가 **지표 값**을 오염시키는 정도 | **미측정** |
| 9 | `000040` 의 **KIS 피드가 틀린** 원인 | 미확인 |
| 10 | 계수 무용 구간(**3,075,128행 = 97.6%**)의 가격 규약 | 미확인 → **M1 방법 B** |
| 13 | `005930` 오염 **3행 · 경로 둘** 중 **07-23 17:45:56 단독 주입 경로** | 미확인 |
| 14 | 정리매매 코호트 **8종목 중 6종목**의 공시 수집 공백 | 미확인 |
| 15 | `020400` 의 **2:1 이 액면분할인지 무상증자인지**(공시 0) | 미확인 |
| 16 | 규칙 C 의 **124종목** 중 «진짜 결함»과 «정상 기업행위»가 **미분해** | 미확인 |
| 17 | **05-04 ↔ 05-14 가격은 불변인데 시총만 점프**하는 별종 | 미확인 |
| 18 | `003350` pykrx split 의 **02-20 vs 04-20 효력일** | 미확인 |
| **19** | 🆕 M2 「피드오류 6」의 **원자료 재대조** (§4 M2 재분석 3) | 미확인 |
| **20** | 🆕 제3날짜 경계 **16건**(2023-04-25 3종목 공통)의 정체 · **적재 이력 미조회** | 미확인 |
| **21** | 🆕 `004710` 04-20~06-10 구간 **DB < 피드 0.3~4%** | 미확인 (별건) |
| **22** | 🆕 G 「진짜 판정불가」 **6종목**의 배율 출처 · `012210` coverage 0.90 | 미확인 |

✅ **종결 5건** — **#5** 미설명(복원 판정식에서 **0**) · **#6** IPO 교란(**1/246**) · **#7** halt=0 7건(**7/7 판정**) · **#11** 시대별 건수(복원 분류와 **일치**) · **#12** 허위 계수 스탬프 3건(**기각**).

### 7-2. 영향 범위 — 「130봉 이상 창 전부」는 후보 «선정»엔 **불성립**이다

8/24 스냅샷 점프군 — `rs_leader` **9/20**(top-10 중 **7**: `900300`·`226340`·`001210`·`950220`·`011090`·`014990`·`049470`) · `ma20` 2/20 · `ma5` 1/20 · `minervini`·`elder`·`envelope`(0/3)·`daytrading`(0/8) **0**.
**최근 10스캔일 누적** — `rs_leader` **108/200 = 54.0%** · `daytrading` 19/172 = 11.0% · `ma5` 13/200 · `ma20` 8/190 · `envelope` 1/28 · `elder`·`minervini` 0 · `deep_mr` 0/21.
⚠️ `deep_mr` 의 분모 21 은 **전략별 스캔 창** 기준(전역 창이면 **0/5**) — ***분모 정의를 함께 적지 않은 비율은 인용 금지.*** ⇒ ***오염은 `rs_leader` 에 집중돼 있다.*** 단 #8 대로 **지표 «값»** 오염은 미측정이다.

### 7-3. 예측 레시피 (② 에서 동결할 후보 · **여기서는 등록하지 않는다**)

**A** 반사실 랭킹(점프 «이전» close × 배율 후 `ret120` 재계산 — `screener.py:54-62` 와 **같은 순서**로) · **B** `virtual_trading_records` 점프군/비점프군 손익 대조 · **C** 창 안에 절벽이 남은 종목 수 · **D** 홀드아웃 `md5(code) % 2` — 측정 **«전»** 분리.
